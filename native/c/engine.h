// Code generated by vegoc from the Vego JSON form. DO NOT EDIT.

#ifndef REVERA_ENG_ENGINE_H
#define REVERA_ENG_ENGINE_H

#include "vg.h"

#define revera_eng_itemChar ((uint8_t)(0))
#define revera_eng_itemElem ((uint8_t)(1))
#define revera_eng_itemEquiv ((uint8_t)(2))
#define revera_eng_itemClass ((uint8_t)(3))
#define revera_eng_capWorkLimit ((int64_t)(50000000LL))
#define revera_eng_solverArenaLimit ((int64_t)(67108864LL))
#define revera_eng_contractCap ((int64_t)(4611686018427387904LL))
#define revera_eng_frameBytes ((int64_t)(256LL))
#define revera_eng_matcherStackBytes ((int64_t)(2048LL))
#define revera_eng_singleLookupFrames ((int64_t)(8LL))
#define revera_eng_multiLookupFrames ((int64_t)(16LL))
#define revera_eng_ptreeBytes ((int64_t)(48LL))
#define revera_eng_kidBytes ((int64_t)(8LL))
#define revera_eng_mapEntryBytes ((int64_t)(128LL))
#define revera_eng_matchBytes ((int64_t)(16LL))
#define revera_eng_profileRowCost ((int64_t)(13LL))
#define revera_eng_maxElemAhead ((int64_t)(8LL))
#define revera_eng_queueCompactFactor ((int64_t)(2LL))
#define revera_eng_ErrNone ((int32_t)(0))
#define revera_eng_ErrNoMatch ((int32_t)(1))
#define revera_eng_ErrBadPat ((int32_t)(2))
#define revera_eng_ErrECollate ((int32_t)(3))
#define revera_eng_ErrECType ((int32_t)(4))
#define revera_eng_ErrEEscape ((int32_t)(5))
#define revera_eng_ErrESubReg ((int32_t)(6))
#define revera_eng_ErrEBrack ((int32_t)(7))
#define revera_eng_ErrEParen ((int32_t)(8))
#define revera_eng_ErrEBrace ((int32_t)(9))
#define revera_eng_ErrBadBR ((int32_t)(10))
#define revera_eng_ErrERange ((int32_t)(11))
#define revera_eng_ErrESpace ((int32_t)(12))
#define revera_eng_ErrBadRpt ((int32_t)(13))
#define revera_eng_ErrENoSub ((int32_t)(14))
#define revera_eng_FlagICase ((uint32_t)(1U))
#define revera_eng_FlagNewline ((uint32_t)(2U))
#define revera_eng_FlagNoSub ((uint32_t)(4U))
#define revera_eng_FlagMinimal ((uint32_t)(8U))
#define revera_eng_ExecNotBOL ((uint32_t)(1U))
#define revera_eng_ExecNotEOL ((uint32_t)(2U))
#define revera_eng_dupMax ((int64_t)(255LL))
#define revera_eng_classAlnum ((uint8_t)(0))
#define revera_eng_classAlpha ((uint8_t)(1))
#define revera_eng_classBlank ((uint8_t)(2))
#define revera_eng_classCntrl ((uint8_t)(3))
#define revera_eng_classDigit ((uint8_t)(4))
#define revera_eng_classGraph ((uint8_t)(5))
#define revera_eng_classLower ((uint8_t)(6))
#define revera_eng_classPrint ((uint8_t)(7))
#define revera_eng_classPunct ((uint8_t)(8))
#define revera_eng_classSpace ((uint8_t)(9))
#define revera_eng_classUpper ((uint8_t)(10))
#define revera_eng_classXdigit ((uint8_t)(11))
#define revera_eng_numClasses ((int64_t)(12LL))
#define revera_eng_firstSequenceID ((int64_t)(1114112LL))
#define revera_eng_secCtypeStage1 ((int64_t)(0LL))
#define revera_eng_secCtypeBlocks ((int64_t)(1LL))
#define revera_eng_secCaseDefault ((int64_t)(2LL))
#define revera_eng_secCaseTurkic ((int64_t)(3LL))
#define revera_eng_secSeqCodepoints ((int64_t)(4LL))
#define revera_eng_secSequences ((int64_t)(5LL))
#define revera_eng_secRootContractions ((int64_t)(6LL))
#define revera_eng_secRootEquivalences ((int64_t)(7LL))
#define revera_eng_secCollationOverrides ((int64_t)(8LL))
#define revera_eng_secContractionAdds ((int64_t)(9LL))
#define revera_eng_secContractionRemoves ((int64_t)(10LL))
#define revera_eng_secCollationProfiles ((int64_t)(11LL))
#define revera_eng_secTypeNames ((int64_t)(12LL))
#define revera_eng_secTypeNameOffsets ((int64_t)(13LL))
#define revera_eng_secLocaleNames ((int64_t)(14LL))
#define revera_eng_secLocaleNameOffsets ((int64_t)(15LL))
#define revera_eng_secLocales ((int64_t)(16LL))
#define revera_eng_secLocaleTypes ((int64_t)(17LL))
#define revera_eng_secInvUpperDefault ((int64_t)(18LL))
#define revera_eng_secInvLowerDefault ((int64_t)(19LL))
#define revera_eng_secInvUpperTurkic ((int64_t)(20LL))
#define revera_eng_secInvLowerTurkic ((int64_t)(21LL))
#define revera_eng_secScalars ((int64_t)(22LL))
#define revera_eng_numSections ((int64_t)(23LL))
#define revera_eng_ctypeStage1Entries ((int64_t)(4352LL))
#define revera_eng_normalizedNameMax ((int64_t)(127LL))
#define revera_eng_maxPreimages ((int64_t)(16LL))
#define revera_eng_iRune ((uint8_t)(0))
#define revera_eng_iRuneFold ((uint8_t)(1))
#define revera_eng_iAny ((uint8_t)(2))
#define revera_eng_iBracket ((uint8_t)(3))
#define revera_eng_iBOL ((uint8_t)(4))
#define revera_eng_iEOL ((uint8_t)(5))
#define revera_eng_iSplit ((uint8_t)(6))
#define revera_eng_iJmp ((uint8_t)(7))
#define revera_eng_iMatch ((uint8_t)(8))
#define revera_eng_iFail ((uint8_t)(9))
#define revera_eng_failMinNone ((int64_t)(4611686018427387904LL))
#define revera_eng_maxProgram ((int64_t)(1048576LL))
#define revera_eng_maskWidth ((int64_t)(64LL))
#define revera_eng_subjectLimit ((int64_t)(2147483647LL))
#define revera_eng_lengthCap ((int64_t)(1099511627776LL))
#define revera_eng_opChar ((uint8_t)(0))
#define revera_eng_opAny ((uint8_t)(1))
#define revera_eng_opBracket ((uint8_t)(2))
#define revera_eng_opBOL ((uint8_t)(3))
#define revera_eng_opEOL ((uint8_t)(4))
#define revera_eng_opConcat ((uint8_t)(5))
#define revera_eng_opAlt ((uint8_t)(6))
#define revera_eng_opRepeat ((uint8_t)(7))
#define revera_eng_opGroup ((uint8_t)(8))
#define revera_eng_infinite ((int64_t)(((int64_t)(0ULL - (uint64_t)(1LL)))))
#define revera_eng_lenInf ((int64_t)(1073741824LL))
#define revera_eng_invalidRune ((int32_t)(((int32_t)(0ULL - (uint64_t)(1)))))

typedef struct revera_eng_runeRange revera_eng_runeRange;
typedef struct revera_eng_bracketSet revera_eng_bracketSet;
typedef struct revera_eng_bracketItem revera_eng_bracketItem;
typedef struct revera_eng_elemBuf revera_eng_elemBuf;
typedef struct revera_eng_ptree revera_eng_ptree;
typedef struct revera_eng_capSolver revera_eng_capSolver;
typedef struct revera_eng_repBest revera_eng_repBest;
typedef struct revera_eng_repWin revera_eng_repWin;
typedef struct revera_eng_BackendContract revera_eng_BackendContract;
typedef struct revera_eng_Contract revera_eng_Contract;
typedef struct revera_eng_lookupCosts revera_eng_lookupCosts;
typedef struct revera_eng_slotTable revera_eng_slotTable;
typedef struct revera_eng_engineWS revera_eng_engineWS;
typedef struct revera_eng_engineResult revera_eng_engineResult;
typedef struct revera_eng_phaseAState revera_eng_phaseAState;
typedef struct revera_eng_Error revera_eng_Error;
typedef struct revera_eng_memoKey revera_eng_memoKey;
typedef struct revera_eng_memoVal revera_eng_memoVal;
typedef struct revera_eng_memoTab revera_eng_memoTab;
typedef struct revera_eng_SecRange revera_eng_SecRange;
typedef struct revera_eng_Locale revera_eng_Locale;
typedef struct revera_eng_LocaleRow revera_eng_LocaleRow;
typedef struct revera_eng_localeRequest revera_eng_localeRequest;
typedef struct revera_eng_CasePair revera_eng_CasePair;
typedef struct revera_eng_preimageBuf revera_eng_preimageBuf;
typedef struct revera_eng_CollProfile revera_eng_CollProfile;
typedef struct revera_eng_Match revera_eng_Match;
typedef struct revera_eng_decoded revera_eng_decoded;
typedef struct revera_eng_instr revera_eng_instr;
typedef struct revera_eng_scanFilter revera_eng_scanFilter;
typedef struct revera_eng_program revera_eng_program;
typedef struct revera_eng_patchSlot revera_eng_patchSlot;
typedef struct revera_eng_frag revera_eng_frag;
typedef struct revera_eng_progBuilder revera_eng_progBuilder;
typedef struct revera_eng_Regexp revera_eng_Regexp;
typedef struct revera_eng_groupStack revera_eng_groupStack;
typedef struct revera_eng_MatchIter revera_eng_MatchIter;
typedef struct revera_eng_replPart revera_eng_replPart;
typedef struct revera_eng_node revera_eng_node;
typedef struct revera_eng_parser revera_eng_parser;
typedef struct revera_eng_interval revera_eng_interval;
typedef struct revera_eng_arr_Str_12 revera_eng_arr_Str_12;
typedef struct revera_eng_arr_i32_8 revera_eng_arr_i32_8;
typedef struct revera_eng_arr_SecRange_23 revera_eng_arr_SecRange_23;
typedef struct revera_eng_arr_i32_16 revera_eng_arr_i32_16;
typedef struct revera_eng_arr_bool_256 revera_eng_arr_bool_256;
typedef struct revera_eng_arr_i32_1 revera_eng_arr_i32_1;

typedef struct {
    revera_eng_runeRange *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_runeRange;

typedef struct {
    int32_t *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_i32;

typedef struct {
    revera_eng_slice_i32 *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_slice_i32;

typedef struct {
    int64_t *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_i64;

typedef struct {
    revera_eng_ptree *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_ptree;

typedef struct {
    uint32_t *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_u32;

typedef struct {
    revera_eng_slotTable *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_slotTable;

typedef struct {
    uint8_t *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_u8;

typedef struct {
    revera_eng_memoKey *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_memoKey;

typedef struct {
    revera_eng_memoVal *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_memoVal;

typedef struct {
    revera_eng_instr *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_instr;

typedef struct {
    revera_eng_patchSlot *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_patchSlot;

typedef struct {
    revera_eng_node *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_node;

typedef struct {
    revera_eng_bracketSet *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_bracketSet;

typedef struct {
    revera_eng_Match *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_Match;

typedef struct {
    bool *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_bool;

typedef struct {
    revera_eng_replPart *p;
    int64_t len;
    int64_t cap;
} revera_eng_slice_replPart;

struct revera_eng_runeRange {
    int32_t lo;
    int32_t hi;
};

struct revera_eng_bracketSet {
    bool negated;
    bool icase;
    bool nlMode;
    revera_eng_slice_runeRange ranges;
    uint16_t classMask;
    revera_eng_slice_slice_i32 elems;
    revera_eng_slice_slice_i32 equivs;
    uint16_t multiLens;
};

struct revera_eng_bracketItem {
    uint8_t kind;
    int32_t r;
    revera_eng_slice_i32 seq;
    uint8_t class;
};

struct revera_eng_arr_i32_8 {
    int32_t v[8];
};

struct revera_eng_elemBuf {
    revera_eng_arr_i32_8 r;
};

struct revera_eng_ptree {
    int32_t n;
    int32_t i;
    int32_t j;
    int32_t branch;
    int32_t kidsOff;
    int32_t kidsLen;
};

struct revera_eng_memoTab {
    revera_eng_slice_memoKey keys;
    revera_eng_slice_memoVal vals;
    revera_eng_slice_u8 used;
    int64_t count;
};

struct revera_eng_capSolver {
    uint32_t eflags;
    revera_eng_memoTab memo;
    revera_eng_memoTab cmemo;
    revera_eng_memoTab rmemo;
    revera_eng_slice_i64 ctrA;
    revera_eng_slice_i64 ctrB;
    int64_t work;
    bool failed;
    revera_eng_slice_ptree trees;
    revera_eng_slice_i32 kidStore;
};

struct revera_eng_repBest {
    int64_t off;
    int64_t length;
    int32_t tree;
    bool found;
};

struct revera_eng_repWin {
    int64_t off;
    int64_t length;
    bool ok;
};

struct revera_eng_BackendContract {
    int64_t HeapBytes;
    int64_t StackBytes;
    int64_t Steps;
};

struct revera_eng_Contract {
    int64_t MaxInput;
    revera_eng_BackendContract Matcher;
    revera_eng_BackendContract OnePass;
    bool HasOnePass;
    revera_eng_BackendContract Solver;
    bool HasSolver;
};

struct revera_eng_lookupCosts {
    int64_t sequenceSearch;
    int64_t contraction;
    int64_t primaryToken;
    int64_t classMask;
    int64_t casePreimages;
    int64_t caseConvert;
    int64_t preimages;
};

struct revera_eng_slotTable {
    revera_eng_slice_u32 stamp;
    revera_eng_slice_i32 starts;
    revera_eng_slice_u32 ctr;
    revera_eng_slice_u32 active;
    uint32_t gen;
};

struct revera_eng_engineWS {
    revera_eng_slice_slotTable slots;
    revera_eng_slice_u32 queue;
    revera_eng_slice_u8 onq;
    revera_eng_slice_u32 bestCtr;
    revera_eng_slice_u32 ctrBuf;
    revera_eng_slice_u32 zeros;
    revera_eng_slice_i32 ahead;
};

struct revera_eng_engineResult {
    bool matched;
    int64_t so;
    int64_t eo;
};

struct revera_eng_phaseAState {
    vg_str subject;
    uint32_t eflags;
    int64_t k;
    int64_t ring;
    bool nlMode;
    int64_t ci;
    int64_t pos;
    bool bol;
    bool eol;
    int32_t cur;
    int64_t size;
    bool matched;
    int64_t so;
    int64_t eo;
};

struct revera_eng_Error {
    int32_t Code;
    int64_t Pos;
};

struct revera_eng_memoKey {
    int32_t a;
    int32_t b;
    int32_t c;
    int32_t d;
};

struct revera_eng_memoVal {
    int32_t x;
    int32_t y;
    int32_t z;
};

struct revera_eng_SecRange {
    int64_t Off;
    int64_t End;
};

struct revera_eng_arr_SecRange_23 {
    revera_eng_SecRange v[23];
};

struct revera_eng_Locale {
    vg_str blob;
    revera_eng_arr_SecRange_23 sec;
    int64_t maxSeq;
    int64_t preimagesDefault;
    int64_t preimagesTurkic;
    uint16_t collationProfile;
    uint8_t caseProfile;
    bool posix;
    bool valid;
};

struct revera_eng_LocaleRow {
    int64_t TypeFirst;
    int64_t TypeCount;
    uint8_t CaseProfile;
    uint16_t DefaultCollation;
};

struct revera_eng_localeRequest {
    vg_str name;
    vg_str ctype;
    bool posix;
};

struct revera_eng_CasePair {
    int32_t Upper;
    int32_t Lower;
};

struct revera_eng_arr_i32_16 {
    int32_t v[16];
};

struct revera_eng_preimageBuf {
    revera_eng_arr_i32_16 r;
    int64_t n;
};

struct revera_eng_CollProfile {
    int64_t OverrideFirst;
    int64_t OverrideCount;
    int64_t AddFirst;
    int64_t AddCount;
    int64_t RemoveFirst;
    int64_t RemoveCount;
};

struct revera_eng_Match {
    int64_t So;
    int64_t Eo;
};

struct revera_eng_decoded {
    revera_eng_slice_i32 runes;
    revera_eng_slice_i64 byteAt;
    bool atSubjectStart;
    bool atSubjectEnd;
    bool prevIsNewline;
    bool nextIsNewline;
};

struct revera_eng_instr {
    uint8_t op;
    uint32_t next;
    uint32_t alt;
    uint32_t arg;
    uint64_t mask;
    revera_eng_slice_u32 extra;
};

struct revera_eng_arr_bool_256 {
    bool v[256];
};

struct revera_eng_scanFilter {
    bool enabled;
    bool single;
    uint8_t b;
    revera_eng_arr_bool_256 stop;
};

struct revera_eng_program {
    revera_eng_slice_instr ins;
    uint32_t start;
    revera_eng_slice_slice_i32 foldSets;
    bool multi;
    int64_t failMin;
    revera_eng_scanFilter scan;
};

struct revera_eng_patchSlot {
    uint32_t idx;
    bool alt;
};

struct revera_eng_frag {
    uint32_t start;
    revera_eng_slice_patchSlot out;
};

struct revera_eng_progBuilder {
    revera_eng_program prog;
    bool tooBig;
    int64_t failMin;
    int32_t errCode;
    bool icase;
};

struct revera_eng_Regexp {
    revera_eng_slice_node nodes;
    revera_eng_slice_bracketSet brackets;
    int32_t root;
    int64_t nsub;
    uint32_t flags;
    revera_eng_Locale loc;
    int64_t minSlots;
    revera_eng_slice_slice_i32 nested;
    bool multi;
    bool progOK;
    revera_eng_program prog;
    int64_t minLen;
    bool anchors;
    bool onePass;
};

struct revera_eng_groupStack {
    revera_eng_slice_i32 g;
};

struct revera_eng_MatchIter {
    int64_t pos;
    int64_t lastEnd;
    int64_t limit;
    bool done;
};

struct revera_eng_replPart {
    vg_str lit;
    int64_t group;
};

struct revera_eng_node {
    uint8_t op;
    revera_eng_slice_i32 ch;
    int32_t r;
    revera_eng_slice_i32 fold;
    int32_t br;
    int64_t min;
    int64_t max;
    bool minimal;
    int64_t vego_index;
    int64_t minL;
    int64_t maxL;
    revera_eng_slice_i64 sufMin;
    revera_eng_slice_i64 sufMax;
    revera_eng_slice_slice_i32 firsts;
};

struct revera_eng_parser {
    vg_str src;
    int64_t pos;
    uint32_t flags;
    int64_t groups;
    revera_eng_slice_node nodes;
    revera_eng_slice_bracketSet brackets;
    revera_eng_Error err;
};

struct revera_eng_interval {
    int64_t lo;
    int64_t hi;
};

struct revera_eng_arr_Str_12 {
    vg_str v[12];
};

struct revera_eng_arr_i32_1 {
    int32_t v[1];
};

typedef struct {
    vg_str r0;
    bool r1;
} revera_eng_Tup_Str_bool;

typedef struct {
    vg_str r0;
    revera_eng_Error r1;
} revera_eng_Tup_Str_t4572726f72x;

typedef struct {
    bool r0;
    revera_eng_Error r1;
} revera_eng_Tup_bool_t4572726f72x;

typedef struct {
    int32_t r0;
    int64_t r1;
} revera_eng_Tup_i32_i64;

typedef struct {
    int64_t r0;
    bool r1;
} revera_eng_Tup_i64_bool;

typedef struct {
    int64_t r0;
    int64_t r1;
} revera_eng_Tup_i64_i64;

typedef struct {
    revera_eng_slice_i32 r0;
    bool r1;
} revera_eng_Tup_si32_bool;

typedef struct {
    revera_eng_slice_replPart r0;
    revera_eng_Error r1;
} revera_eng_Tup_st7265706c50617274x_t4572726f72x;

typedef struct {
    revera_eng_CasePair r0;
    bool r1;
} revera_eng_Tup_t4361736550616972x_bool;

typedef struct {
    revera_eng_Locale r0;
    bool r1;
} revera_eng_Tup_t4c6f63616c65x_bool;

typedef struct {
    revera_eng_MatchIter r0;
    revera_eng_Error r1;
} revera_eng_Tup_t4d6174636849746572x_t4572726f72x;

typedef struct {
    revera_eng_Regexp r0;
    revera_eng_Error r1;
} revera_eng_Tup_t526567657870x_t4572726f72x;

typedef struct {
    revera_eng_bracketItem r0;
    bool r1;
} revera_eng_Tup_t627261636b65744974656dx_bool;

typedef struct {
    revera_eng_interval r0;
    bool r1;
} revera_eng_Tup_t696e74657276616cx_bool;

typedef struct {
    revera_eng_localeRequest r0;
    bool r1;
} revera_eng_Tup_t6c6f63616c6552657175657374x_bool;

typedef struct {
    revera_eng_memoVal r0;
    bool r1;
} revera_eng_Tup_t6d656d6f56616cx_bool;

typedef struct {
    uint32_t r0;
    bool r1;
} revera_eng_Tup_u32_bool;

typedef struct {
    uint8_t r0;
    bool r1;
} revera_eng_Tup_u8_bool;

extern const revera_eng_arr_Str_12 revera_eng_classNames;

static inline bool revera_eng_runeRange_eq(revera_eng_runeRange a, revera_eng_runeRange b);
static inline bool revera_eng_elemBuf_eq(revera_eng_elemBuf a, revera_eng_elemBuf b);
static inline bool revera_eng_ptree_eq(revera_eng_ptree a, revera_eng_ptree b);
static inline bool revera_eng_repBest_eq(revera_eng_repBest a, revera_eng_repBest b);
static inline bool revera_eng_repWin_eq(revera_eng_repWin a, revera_eng_repWin b);
static inline bool revera_eng_BackendContract_eq(revera_eng_BackendContract a, revera_eng_BackendContract b);
static inline bool revera_eng_Contract_eq(revera_eng_Contract a, revera_eng_Contract b);
static inline bool revera_eng_lookupCosts_eq(revera_eng_lookupCosts a, revera_eng_lookupCosts b);
static inline bool revera_eng_engineResult_eq(revera_eng_engineResult a, revera_eng_engineResult b);
static inline bool revera_eng_phaseAState_eq(revera_eng_phaseAState a, revera_eng_phaseAState b);
static inline bool revera_eng_Error_eq(revera_eng_Error a, revera_eng_Error b);
static inline bool revera_eng_memoKey_eq(revera_eng_memoKey a, revera_eng_memoKey b);
static inline bool revera_eng_memoVal_eq(revera_eng_memoVal a, revera_eng_memoVal b);
static inline bool revera_eng_SecRange_eq(revera_eng_SecRange a, revera_eng_SecRange b);
static inline bool revera_eng_Locale_eq(revera_eng_Locale a, revera_eng_Locale b);
static inline bool revera_eng_LocaleRow_eq(revera_eng_LocaleRow a, revera_eng_LocaleRow b);
static inline bool revera_eng_localeRequest_eq(revera_eng_localeRequest a, revera_eng_localeRequest b);
static inline bool revera_eng_CasePair_eq(revera_eng_CasePair a, revera_eng_CasePair b);
static inline bool revera_eng_preimageBuf_eq(revera_eng_preimageBuf a, revera_eng_preimageBuf b);
static inline bool revera_eng_CollProfile_eq(revera_eng_CollProfile a, revera_eng_CollProfile b);
static inline bool revera_eng_Match_eq(revera_eng_Match a, revera_eng_Match b);
static inline bool revera_eng_scanFilter_eq(revera_eng_scanFilter a, revera_eng_scanFilter b);
static inline bool revera_eng_patchSlot_eq(revera_eng_patchSlot a, revera_eng_patchSlot b);
static inline bool revera_eng_MatchIter_eq(revera_eng_MatchIter a, revera_eng_MatchIter b);
static inline bool revera_eng_replPart_eq(revera_eng_replPart a, revera_eng_replPart b);
static inline bool revera_eng_interval_eq(revera_eng_interval a, revera_eng_interval b);
static inline bool revera_eng_arr_Str_12_eq(revera_eng_arr_Str_12 a, revera_eng_arr_Str_12 b);
static inline bool revera_eng_arr_i32_8_eq(revera_eng_arr_i32_8 a, revera_eng_arr_i32_8 b);
static inline bool revera_eng_arr_SecRange_23_eq(revera_eng_arr_SecRange_23 a, revera_eng_arr_SecRange_23 b);
static inline bool revera_eng_arr_i32_16_eq(revera_eng_arr_i32_16 a, revera_eng_arr_i32_16 b);
static inline bool revera_eng_arr_bool_256_eq(revera_eng_arr_bool_256 a, revera_eng_arr_bool_256 b);
static inline bool revera_eng_arr_i32_1_eq(revera_eng_arr_i32_1 a, revera_eng_arr_i32_1 b);

static inline bool revera_eng_runeRange_eq(revera_eng_runeRange a, revera_eng_runeRange b) {
    return a.lo == b.lo && a.hi == b.hi;
}

static inline bool revera_eng_elemBuf_eq(revera_eng_elemBuf a, revera_eng_elemBuf b) {
    return revera_eng_arr_i32_8_eq(a.r, b.r);
}

static inline bool revera_eng_ptree_eq(revera_eng_ptree a, revera_eng_ptree b) {
    return a.n == b.n && a.i == b.i && a.j == b.j && a.branch == b.branch && a.kidsOff == b.kidsOff && a.kidsLen == b.kidsLen;
}

static inline bool revera_eng_repBest_eq(revera_eng_repBest a, revera_eng_repBest b) {
    return a.off == b.off && a.length == b.length && a.tree == b.tree && a.found == b.found;
}

static inline bool revera_eng_repWin_eq(revera_eng_repWin a, revera_eng_repWin b) {
    return a.off == b.off && a.length == b.length && a.ok == b.ok;
}

static inline bool revera_eng_BackendContract_eq(revera_eng_BackendContract a, revera_eng_BackendContract b) {
    return a.HeapBytes == b.HeapBytes && a.StackBytes == b.StackBytes && a.Steps == b.Steps;
}

static inline bool revera_eng_Contract_eq(revera_eng_Contract a, revera_eng_Contract b) {
    return a.MaxInput == b.MaxInput && revera_eng_BackendContract_eq(a.Matcher, b.Matcher) && revera_eng_BackendContract_eq(a.OnePass, b.OnePass) && a.HasOnePass == b.HasOnePass && revera_eng_BackendContract_eq(a.Solver, b.Solver) && a.HasSolver == b.HasSolver;
}

static inline bool revera_eng_lookupCosts_eq(revera_eng_lookupCosts a, revera_eng_lookupCosts b) {
    return a.sequenceSearch == b.sequenceSearch && a.contraction == b.contraction && a.primaryToken == b.primaryToken && a.classMask == b.classMask && a.casePreimages == b.casePreimages && a.caseConvert == b.caseConvert && a.preimages == b.preimages;
}

static inline bool revera_eng_engineResult_eq(revera_eng_engineResult a, revera_eng_engineResult b) {
    return a.matched == b.matched && a.so == b.so && a.eo == b.eo;
}

static inline bool revera_eng_phaseAState_eq(revera_eng_phaseAState a, revera_eng_phaseAState b) {
    return vg_streq(a.subject, b.subject) && a.eflags == b.eflags && a.k == b.k && a.ring == b.ring && a.nlMode == b.nlMode && a.ci == b.ci && a.pos == b.pos && a.bol == b.bol && a.eol == b.eol && a.cur == b.cur && a.size == b.size && a.matched == b.matched && a.so == b.so && a.eo == b.eo;
}

static inline bool revera_eng_Error_eq(revera_eng_Error a, revera_eng_Error b) {
    return a.Code == b.Code && a.Pos == b.Pos;
}

static inline bool revera_eng_memoKey_eq(revera_eng_memoKey a, revera_eng_memoKey b) {
    return a.a == b.a && a.b == b.b && a.c == b.c && a.d == b.d;
}

static inline bool revera_eng_memoVal_eq(revera_eng_memoVal a, revera_eng_memoVal b) {
    return a.x == b.x && a.y == b.y && a.z == b.z;
}

static inline bool revera_eng_SecRange_eq(revera_eng_SecRange a, revera_eng_SecRange b) {
    return a.Off == b.Off && a.End == b.End;
}

static inline bool revera_eng_Locale_eq(revera_eng_Locale a, revera_eng_Locale b) {
    return vg_streq(a.blob, b.blob) && revera_eng_arr_SecRange_23_eq(a.sec, b.sec) && a.maxSeq == b.maxSeq && a.preimagesDefault == b.preimagesDefault && a.preimagesTurkic == b.preimagesTurkic && a.collationProfile == b.collationProfile && a.caseProfile == b.caseProfile && a.posix == b.posix && a.valid == b.valid;
}

static inline bool revera_eng_LocaleRow_eq(revera_eng_LocaleRow a, revera_eng_LocaleRow b) {
    return a.TypeFirst == b.TypeFirst && a.TypeCount == b.TypeCount && a.CaseProfile == b.CaseProfile && a.DefaultCollation == b.DefaultCollation;
}

static inline bool revera_eng_localeRequest_eq(revera_eng_localeRequest a, revera_eng_localeRequest b) {
    return vg_streq(a.name, b.name) && vg_streq(a.ctype, b.ctype) && a.posix == b.posix;
}

static inline bool revera_eng_CasePair_eq(revera_eng_CasePair a, revera_eng_CasePair b) {
    return a.Upper == b.Upper && a.Lower == b.Lower;
}

static inline bool revera_eng_preimageBuf_eq(revera_eng_preimageBuf a, revera_eng_preimageBuf b) {
    return revera_eng_arr_i32_16_eq(a.r, b.r) && a.n == b.n;
}

static inline bool revera_eng_CollProfile_eq(revera_eng_CollProfile a, revera_eng_CollProfile b) {
    return a.OverrideFirst == b.OverrideFirst && a.OverrideCount == b.OverrideCount && a.AddFirst == b.AddFirst && a.AddCount == b.AddCount && a.RemoveFirst == b.RemoveFirst && a.RemoveCount == b.RemoveCount;
}

static inline bool revera_eng_Match_eq(revera_eng_Match a, revera_eng_Match b) {
    return a.So == b.So && a.Eo == b.Eo;
}

static inline bool revera_eng_scanFilter_eq(revera_eng_scanFilter a, revera_eng_scanFilter b) {
    return a.enabled == b.enabled && a.single == b.single && a.b == b.b && revera_eng_arr_bool_256_eq(a.stop, b.stop);
}

static inline bool revera_eng_patchSlot_eq(revera_eng_patchSlot a, revera_eng_patchSlot b) {
    return a.idx == b.idx && a.alt == b.alt;
}

static inline bool revera_eng_MatchIter_eq(revera_eng_MatchIter a, revera_eng_MatchIter b) {
    return a.pos == b.pos && a.lastEnd == b.lastEnd && a.limit == b.limit && a.done == b.done;
}

static inline bool revera_eng_replPart_eq(revera_eng_replPart a, revera_eng_replPart b) {
    return vg_streq(a.lit, b.lit) && a.group == b.group;
}

static inline bool revera_eng_interval_eq(revera_eng_interval a, revera_eng_interval b) {
    return a.lo == b.lo && a.hi == b.hi;
}

static inline bool revera_eng_arr_Str_12_eq(revera_eng_arr_Str_12 a, revera_eng_arr_Str_12 b) {
    for (int64_t i = 0; i < 12; i++) {
        if (!(vg_streq(a.v[i], b.v[i]))) {
            return false;
        }
    }
    return true;
}

static inline bool revera_eng_arr_i32_8_eq(revera_eng_arr_i32_8 a, revera_eng_arr_i32_8 b) {
    for (int64_t i = 0; i < 8; i++) {
        if (!(a.v[i] == b.v[i])) {
            return false;
        }
    }
    return true;
}

static inline bool revera_eng_arr_SecRange_23_eq(revera_eng_arr_SecRange_23 a, revera_eng_arr_SecRange_23 b) {
    for (int64_t i = 0; i < 23; i++) {
        if (!(revera_eng_SecRange_eq(a.v[i], b.v[i]))) {
            return false;
        }
    }
    return true;
}

static inline bool revera_eng_arr_i32_16_eq(revera_eng_arr_i32_16 a, revera_eng_arr_i32_16 b) {
    for (int64_t i = 0; i < 16; i++) {
        if (!(a.v[i] == b.v[i])) {
            return false;
        }
    }
    return true;
}

static inline bool revera_eng_arr_bool_256_eq(revera_eng_arr_bool_256 a, revera_eng_arr_bool_256 b) {
    for (int64_t i = 0; i < 256; i++) {
        if (!(a.v[i] == b.v[i])) {
            return false;
        }
    }
    return true;
}

static inline bool revera_eng_arr_i32_1_eq(revera_eng_arr_i32_1 a, revera_eng_arr_i32_1 b) {
    for (int64_t i = 0; i < 1; i++) {
        if (!(a.v[i] == b.v[i])) {
            return false;
        }
    }
    return true;
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_runeRange *p = (revera_eng_runeRange *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_runeRange));
    memset(p, 0, (size_t)c * sizeof(revera_eng_runeRange));
    return (revera_eng_slice_runeRange){p, n, c};
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_runeRange_make_cap(mem, n, n);
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_grow(vg_arena *mem, revera_eng_slice_runeRange s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_runeRange *p = (revera_eng_runeRange *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_runeRange));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_runeRange));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_runeRange));
    }
    return (revera_eng_slice_runeRange){p, s.len, newcap};
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_append(vg_arena *mem, revera_eng_slice_runeRange s, revera_eng_runeRange v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_runeRange_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_append_slice(vg_arena *mem, revera_eng_slice_runeRange s, revera_eng_slice_runeRange more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_runeRange_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_runeRange));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_sub(revera_eng_slice_runeRange s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_runeRange){0};
    }
    return (revera_eng_slice_runeRange){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_tail(revera_eng_slice_runeRange s, int64_t lo) {
    return revera_eng_slice_runeRange_sub(s, lo, s.len);
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_head(revera_eng_slice_runeRange s, int64_t hi) {
    return revera_eng_slice_runeRange_sub(s, 0, hi);
}

static inline revera_eng_runeRange *revera_eng_slice_runeRange_at(revera_eng_slice_runeRange s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_runeRange_copy(revera_eng_slice_runeRange dst, revera_eng_slice_runeRange src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_runeRange));
    }
    return n;
}

static inline revera_eng_slice_runeRange revera_eng_slice_runeRange_of(vg_arena *mem, const revera_eng_runeRange *elems, int64_t n) {
    revera_eng_slice_runeRange out = revera_eng_slice_runeRange_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_runeRange));
    }
    return out;
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    int32_t *p = (int32_t *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(int32_t));
    memset(p, 0, (size_t)c * sizeof(int32_t));
    return (revera_eng_slice_i32){p, n, c};
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_i32_make_cap(mem, n, n);
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_grow(vg_arena *mem, revera_eng_slice_i32 s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    int32_t *p = (int32_t *)vg_arena_alloc(mem, (size_t)newcap * sizeof(int32_t));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(int32_t));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(int32_t));
    }
    return (revera_eng_slice_i32){p, s.len, newcap};
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_append(vg_arena *mem, revera_eng_slice_i32 s, int32_t v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_i32_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_append_slice(vg_arena *mem, revera_eng_slice_i32 s, revera_eng_slice_i32 more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_i32_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(int32_t));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_sub(revera_eng_slice_i32 s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_i32){0};
    }
    return (revera_eng_slice_i32){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_tail(revera_eng_slice_i32 s, int64_t lo) {
    return revera_eng_slice_i32_sub(s, lo, s.len);
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_head(revera_eng_slice_i32 s, int64_t hi) {
    return revera_eng_slice_i32_sub(s, 0, hi);
}

static inline int32_t *revera_eng_slice_i32_at(revera_eng_slice_i32 s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_i32_copy(revera_eng_slice_i32 dst, revera_eng_slice_i32 src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(int32_t));
    }
    return n;
}

static inline revera_eng_slice_i32 revera_eng_slice_i32_of(vg_arena *mem, const int32_t *elems, int64_t n) {
    revera_eng_slice_i32 out = revera_eng_slice_i32_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(int32_t));
    }
    return out;
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_slice_i32 *p = (revera_eng_slice_i32 *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_slice_i32));
    memset(p, 0, (size_t)c * sizeof(revera_eng_slice_i32));
    return (revera_eng_slice_slice_i32){p, n, c};
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_slice_i32_make_cap(mem, n, n);
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_grow(vg_arena *mem, revera_eng_slice_slice_i32 s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_slice_i32 *p = (revera_eng_slice_i32 *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_slice_i32));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_slice_i32));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_slice_i32));
    }
    return (revera_eng_slice_slice_i32){p, s.len, newcap};
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_append(vg_arena *mem, revera_eng_slice_slice_i32 s, revera_eng_slice_i32 v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_slice_i32_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_append_slice(vg_arena *mem, revera_eng_slice_slice_i32 s, revera_eng_slice_slice_i32 more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_slice_i32_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_slice_i32));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_sub(revera_eng_slice_slice_i32 s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_slice_i32){0};
    }
    return (revera_eng_slice_slice_i32){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_tail(revera_eng_slice_slice_i32 s, int64_t lo) {
    return revera_eng_slice_slice_i32_sub(s, lo, s.len);
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_head(revera_eng_slice_slice_i32 s, int64_t hi) {
    return revera_eng_slice_slice_i32_sub(s, 0, hi);
}

static inline revera_eng_slice_i32 *revera_eng_slice_slice_i32_at(revera_eng_slice_slice_i32 s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_slice_i32_copy(revera_eng_slice_slice_i32 dst, revera_eng_slice_slice_i32 src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_slice_i32));
    }
    return n;
}

static inline revera_eng_slice_slice_i32 revera_eng_slice_slice_i32_of(vg_arena *mem, const revera_eng_slice_i32 *elems, int64_t n) {
    revera_eng_slice_slice_i32 out = revera_eng_slice_slice_i32_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_slice_i32));
    }
    return out;
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    int64_t *p = (int64_t *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(int64_t));
    memset(p, 0, (size_t)c * sizeof(int64_t));
    return (revera_eng_slice_i64){p, n, c};
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_i64_make_cap(mem, n, n);
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_grow(vg_arena *mem, revera_eng_slice_i64 s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    int64_t *p = (int64_t *)vg_arena_alloc(mem, (size_t)newcap * sizeof(int64_t));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(int64_t));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(int64_t));
    }
    return (revera_eng_slice_i64){p, s.len, newcap};
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_append(vg_arena *mem, revera_eng_slice_i64 s, int64_t v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_i64_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_append_slice(vg_arena *mem, revera_eng_slice_i64 s, revera_eng_slice_i64 more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_i64_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(int64_t));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_sub(revera_eng_slice_i64 s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_i64){0};
    }
    return (revera_eng_slice_i64){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_tail(revera_eng_slice_i64 s, int64_t lo) {
    return revera_eng_slice_i64_sub(s, lo, s.len);
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_head(revera_eng_slice_i64 s, int64_t hi) {
    return revera_eng_slice_i64_sub(s, 0, hi);
}

static inline int64_t *revera_eng_slice_i64_at(revera_eng_slice_i64 s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_i64_copy(revera_eng_slice_i64 dst, revera_eng_slice_i64 src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(int64_t));
    }
    return n;
}

static inline revera_eng_slice_i64 revera_eng_slice_i64_of(vg_arena *mem, const int64_t *elems, int64_t n) {
    revera_eng_slice_i64 out = revera_eng_slice_i64_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(int64_t));
    }
    return out;
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_ptree *p = (revera_eng_ptree *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_ptree));
    memset(p, 0, (size_t)c * sizeof(revera_eng_ptree));
    return (revera_eng_slice_ptree){p, n, c};
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_ptree_make_cap(mem, n, n);
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_grow(vg_arena *mem, revera_eng_slice_ptree s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_ptree *p = (revera_eng_ptree *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_ptree));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_ptree));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_ptree));
    }
    return (revera_eng_slice_ptree){p, s.len, newcap};
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_append(vg_arena *mem, revera_eng_slice_ptree s, revera_eng_ptree v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_ptree_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_append_slice(vg_arena *mem, revera_eng_slice_ptree s, revera_eng_slice_ptree more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_ptree_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_ptree));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_sub(revera_eng_slice_ptree s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_ptree){0};
    }
    return (revera_eng_slice_ptree){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_tail(revera_eng_slice_ptree s, int64_t lo) {
    return revera_eng_slice_ptree_sub(s, lo, s.len);
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_head(revera_eng_slice_ptree s, int64_t hi) {
    return revera_eng_slice_ptree_sub(s, 0, hi);
}

static inline revera_eng_ptree *revera_eng_slice_ptree_at(revera_eng_slice_ptree s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_ptree_copy(revera_eng_slice_ptree dst, revera_eng_slice_ptree src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_ptree));
    }
    return n;
}

static inline revera_eng_slice_ptree revera_eng_slice_ptree_of(vg_arena *mem, const revera_eng_ptree *elems, int64_t n) {
    revera_eng_slice_ptree out = revera_eng_slice_ptree_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_ptree));
    }
    return out;
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    uint32_t *p = (uint32_t *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(uint32_t));
    memset(p, 0, (size_t)c * sizeof(uint32_t));
    return (revera_eng_slice_u32){p, n, c};
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_u32_make_cap(mem, n, n);
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_grow(vg_arena *mem, revera_eng_slice_u32 s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    uint32_t *p = (uint32_t *)vg_arena_alloc(mem, (size_t)newcap * sizeof(uint32_t));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(uint32_t));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(uint32_t));
    }
    return (revera_eng_slice_u32){p, s.len, newcap};
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_append(vg_arena *mem, revera_eng_slice_u32 s, uint32_t v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_u32_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_append_slice(vg_arena *mem, revera_eng_slice_u32 s, revera_eng_slice_u32 more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_u32_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(uint32_t));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_sub(revera_eng_slice_u32 s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_u32){0};
    }
    return (revera_eng_slice_u32){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_tail(revera_eng_slice_u32 s, int64_t lo) {
    return revera_eng_slice_u32_sub(s, lo, s.len);
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_head(revera_eng_slice_u32 s, int64_t hi) {
    return revera_eng_slice_u32_sub(s, 0, hi);
}

static inline uint32_t *revera_eng_slice_u32_at(revera_eng_slice_u32 s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_u32_copy(revera_eng_slice_u32 dst, revera_eng_slice_u32 src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(uint32_t));
    }
    return n;
}

static inline revera_eng_slice_u32 revera_eng_slice_u32_of(vg_arena *mem, const uint32_t *elems, int64_t n) {
    revera_eng_slice_u32 out = revera_eng_slice_u32_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(uint32_t));
    }
    return out;
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_slotTable *p = (revera_eng_slotTable *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_slotTable));
    memset(p, 0, (size_t)c * sizeof(revera_eng_slotTable));
    return (revera_eng_slice_slotTable){p, n, c};
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_slotTable_make_cap(mem, n, n);
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_grow(vg_arena *mem, revera_eng_slice_slotTable s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_slotTable *p = (revera_eng_slotTable *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_slotTable));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_slotTable));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_slotTable));
    }
    return (revera_eng_slice_slotTable){p, s.len, newcap};
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_append(vg_arena *mem, revera_eng_slice_slotTable s, revera_eng_slotTable v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_slotTable_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_append_slice(vg_arena *mem, revera_eng_slice_slotTable s, revera_eng_slice_slotTable more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_slotTable_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_slotTable));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_sub(revera_eng_slice_slotTable s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_slotTable){0};
    }
    return (revera_eng_slice_slotTable){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_tail(revera_eng_slice_slotTable s, int64_t lo) {
    return revera_eng_slice_slotTable_sub(s, lo, s.len);
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_head(revera_eng_slice_slotTable s, int64_t hi) {
    return revera_eng_slice_slotTable_sub(s, 0, hi);
}

static inline revera_eng_slotTable *revera_eng_slice_slotTable_at(revera_eng_slice_slotTable s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_slotTable_copy(revera_eng_slice_slotTable dst, revera_eng_slice_slotTable src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_slotTable));
    }
    return n;
}

static inline revera_eng_slice_slotTable revera_eng_slice_slotTable_of(vg_arena *mem, const revera_eng_slotTable *elems, int64_t n) {
    revera_eng_slice_slotTable out = revera_eng_slice_slotTable_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_slotTable));
    }
    return out;
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    uint8_t *p = (uint8_t *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(uint8_t));
    memset(p, 0, (size_t)c * sizeof(uint8_t));
    return (revera_eng_slice_u8){p, n, c};
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_u8_make_cap(mem, n, n);
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_grow(vg_arena *mem, revera_eng_slice_u8 s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    uint8_t *p = (uint8_t *)vg_arena_alloc(mem, (size_t)newcap * sizeof(uint8_t));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(uint8_t));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(uint8_t));
    }
    return (revera_eng_slice_u8){p, s.len, newcap};
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_append(vg_arena *mem, revera_eng_slice_u8 s, uint8_t v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_u8_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_append_slice(vg_arena *mem, revera_eng_slice_u8 s, revera_eng_slice_u8 more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_u8_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(uint8_t));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_sub(revera_eng_slice_u8 s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_u8){0};
    }
    return (revera_eng_slice_u8){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_tail(revera_eng_slice_u8 s, int64_t lo) {
    return revera_eng_slice_u8_sub(s, lo, s.len);
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_head(revera_eng_slice_u8 s, int64_t hi) {
    return revera_eng_slice_u8_sub(s, 0, hi);
}

static inline uint8_t *revera_eng_slice_u8_at(revera_eng_slice_u8 s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_u8_copy(revera_eng_slice_u8 dst, revera_eng_slice_u8 src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(uint8_t));
    }
    return n;
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_of(vg_arena *mem, const uint8_t *elems, int64_t n) {
    revera_eng_slice_u8 out = revera_eng_slice_u8_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(uint8_t));
    }
    return out;
}

static inline revera_eng_slice_u8 revera_eng_bytes_from_str(vg_arena *mem, vg_str v) {
    revera_eng_slice_u8 out = revera_eng_slice_u8_make(mem, v.len);
    if (v.len > 0) {
        memcpy(out.p, v.p, (size_t)v.len);
    }
    return out;
}

static inline vg_str revera_eng_str_from_bytes(vg_arena *mem, revera_eng_slice_u8 b) {
    char *p = (char *)vg_arena_alloc(mem, (size_t)(b.len < 1 ? 1 : b.len));
    if (b.len > 0) {
        memcpy(p, b.p, (size_t)b.len);
    }
    return (vg_str){p, b.len};
}

static inline revera_eng_slice_u8 revera_eng_slice_u8_append_str(vg_arena *mem, revera_eng_slice_u8 s, vg_str more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_u8_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len);
    }
    s.len += more.len;
    return s;
}

static inline int64_t revera_eng_slice_u8_copy_str(revera_eng_slice_u8 dst, vg_str src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n);
    }
    return n;
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_memoKey *p = (revera_eng_memoKey *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_memoKey));
    memset(p, 0, (size_t)c * sizeof(revera_eng_memoKey));
    return (revera_eng_slice_memoKey){p, n, c};
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_memoKey_make_cap(mem, n, n);
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_grow(vg_arena *mem, revera_eng_slice_memoKey s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_memoKey *p = (revera_eng_memoKey *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_memoKey));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_memoKey));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_memoKey));
    }
    return (revera_eng_slice_memoKey){p, s.len, newcap};
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_append(vg_arena *mem, revera_eng_slice_memoKey s, revera_eng_memoKey v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_memoKey_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_append_slice(vg_arena *mem, revera_eng_slice_memoKey s, revera_eng_slice_memoKey more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_memoKey_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_memoKey));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_sub(revera_eng_slice_memoKey s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_memoKey){0};
    }
    return (revera_eng_slice_memoKey){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_tail(revera_eng_slice_memoKey s, int64_t lo) {
    return revera_eng_slice_memoKey_sub(s, lo, s.len);
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_head(revera_eng_slice_memoKey s, int64_t hi) {
    return revera_eng_slice_memoKey_sub(s, 0, hi);
}

static inline revera_eng_memoKey *revera_eng_slice_memoKey_at(revera_eng_slice_memoKey s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_memoKey_copy(revera_eng_slice_memoKey dst, revera_eng_slice_memoKey src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_memoKey));
    }
    return n;
}

static inline revera_eng_slice_memoKey revera_eng_slice_memoKey_of(vg_arena *mem, const revera_eng_memoKey *elems, int64_t n) {
    revera_eng_slice_memoKey out = revera_eng_slice_memoKey_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_memoKey));
    }
    return out;
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_memoVal *p = (revera_eng_memoVal *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_memoVal));
    memset(p, 0, (size_t)c * sizeof(revera_eng_memoVal));
    return (revera_eng_slice_memoVal){p, n, c};
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_memoVal_make_cap(mem, n, n);
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_grow(vg_arena *mem, revera_eng_slice_memoVal s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_memoVal *p = (revera_eng_memoVal *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_memoVal));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_memoVal));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_memoVal));
    }
    return (revera_eng_slice_memoVal){p, s.len, newcap};
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_append(vg_arena *mem, revera_eng_slice_memoVal s, revera_eng_memoVal v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_memoVal_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_append_slice(vg_arena *mem, revera_eng_slice_memoVal s, revera_eng_slice_memoVal more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_memoVal_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_memoVal));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_sub(revera_eng_slice_memoVal s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_memoVal){0};
    }
    return (revera_eng_slice_memoVal){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_tail(revera_eng_slice_memoVal s, int64_t lo) {
    return revera_eng_slice_memoVal_sub(s, lo, s.len);
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_head(revera_eng_slice_memoVal s, int64_t hi) {
    return revera_eng_slice_memoVal_sub(s, 0, hi);
}

static inline revera_eng_memoVal *revera_eng_slice_memoVal_at(revera_eng_slice_memoVal s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_memoVal_copy(revera_eng_slice_memoVal dst, revera_eng_slice_memoVal src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_memoVal));
    }
    return n;
}

static inline revera_eng_slice_memoVal revera_eng_slice_memoVal_of(vg_arena *mem, const revera_eng_memoVal *elems, int64_t n) {
    revera_eng_slice_memoVal out = revera_eng_slice_memoVal_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_memoVal));
    }
    return out;
}

static inline revera_eng_slice_instr revera_eng_slice_instr_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_instr *p = (revera_eng_instr *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_instr));
    memset(p, 0, (size_t)c * sizeof(revera_eng_instr));
    return (revera_eng_slice_instr){p, n, c};
}

static inline revera_eng_slice_instr revera_eng_slice_instr_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_instr_make_cap(mem, n, n);
}

static inline revera_eng_slice_instr revera_eng_slice_instr_grow(vg_arena *mem, revera_eng_slice_instr s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_instr *p = (revera_eng_instr *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_instr));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_instr));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_instr));
    }
    return (revera_eng_slice_instr){p, s.len, newcap};
}

static inline revera_eng_slice_instr revera_eng_slice_instr_append(vg_arena *mem, revera_eng_slice_instr s, revera_eng_instr v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_instr_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_instr revera_eng_slice_instr_append_slice(vg_arena *mem, revera_eng_slice_instr s, revera_eng_slice_instr more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_instr_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_instr));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_instr revera_eng_slice_instr_sub(revera_eng_slice_instr s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_instr){0};
    }
    return (revera_eng_slice_instr){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_instr revera_eng_slice_instr_tail(revera_eng_slice_instr s, int64_t lo) {
    return revera_eng_slice_instr_sub(s, lo, s.len);
}

static inline revera_eng_slice_instr revera_eng_slice_instr_head(revera_eng_slice_instr s, int64_t hi) {
    return revera_eng_slice_instr_sub(s, 0, hi);
}

static inline revera_eng_instr *revera_eng_slice_instr_at(revera_eng_slice_instr s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_instr_copy(revera_eng_slice_instr dst, revera_eng_slice_instr src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_instr));
    }
    return n;
}

static inline revera_eng_slice_instr revera_eng_slice_instr_of(vg_arena *mem, const revera_eng_instr *elems, int64_t n) {
    revera_eng_slice_instr out = revera_eng_slice_instr_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_instr));
    }
    return out;
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_patchSlot *p = (revera_eng_patchSlot *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_patchSlot));
    memset(p, 0, (size_t)c * sizeof(revera_eng_patchSlot));
    return (revera_eng_slice_patchSlot){p, n, c};
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_patchSlot_make_cap(mem, n, n);
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_grow(vg_arena *mem, revera_eng_slice_patchSlot s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_patchSlot *p = (revera_eng_patchSlot *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_patchSlot));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_patchSlot));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_patchSlot));
    }
    return (revera_eng_slice_patchSlot){p, s.len, newcap};
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_append(vg_arena *mem, revera_eng_slice_patchSlot s, revera_eng_patchSlot v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_patchSlot_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_append_slice(vg_arena *mem, revera_eng_slice_patchSlot s, revera_eng_slice_patchSlot more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_patchSlot_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_patchSlot));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_sub(revera_eng_slice_patchSlot s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_patchSlot){0};
    }
    return (revera_eng_slice_patchSlot){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_tail(revera_eng_slice_patchSlot s, int64_t lo) {
    return revera_eng_slice_patchSlot_sub(s, lo, s.len);
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_head(revera_eng_slice_patchSlot s, int64_t hi) {
    return revera_eng_slice_patchSlot_sub(s, 0, hi);
}

static inline revera_eng_patchSlot *revera_eng_slice_patchSlot_at(revera_eng_slice_patchSlot s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_patchSlot_copy(revera_eng_slice_patchSlot dst, revera_eng_slice_patchSlot src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_patchSlot));
    }
    return n;
}

static inline revera_eng_slice_patchSlot revera_eng_slice_patchSlot_of(vg_arena *mem, const revera_eng_patchSlot *elems, int64_t n) {
    revera_eng_slice_patchSlot out = revera_eng_slice_patchSlot_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_patchSlot));
    }
    return out;
}

static inline revera_eng_slice_node revera_eng_slice_node_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_node *p = (revera_eng_node *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_node));
    memset(p, 0, (size_t)c * sizeof(revera_eng_node));
    return (revera_eng_slice_node){p, n, c};
}

static inline revera_eng_slice_node revera_eng_slice_node_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_node_make_cap(mem, n, n);
}

static inline revera_eng_slice_node revera_eng_slice_node_grow(vg_arena *mem, revera_eng_slice_node s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_node *p = (revera_eng_node *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_node));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_node));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_node));
    }
    return (revera_eng_slice_node){p, s.len, newcap};
}

static inline revera_eng_slice_node revera_eng_slice_node_append(vg_arena *mem, revera_eng_slice_node s, revera_eng_node v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_node_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_node revera_eng_slice_node_append_slice(vg_arena *mem, revera_eng_slice_node s, revera_eng_slice_node more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_node_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_node));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_node revera_eng_slice_node_sub(revera_eng_slice_node s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_node){0};
    }
    return (revera_eng_slice_node){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_node revera_eng_slice_node_tail(revera_eng_slice_node s, int64_t lo) {
    return revera_eng_slice_node_sub(s, lo, s.len);
}

static inline revera_eng_slice_node revera_eng_slice_node_head(revera_eng_slice_node s, int64_t hi) {
    return revera_eng_slice_node_sub(s, 0, hi);
}

static inline revera_eng_node *revera_eng_slice_node_at(revera_eng_slice_node s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_node_copy(revera_eng_slice_node dst, revera_eng_slice_node src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_node));
    }
    return n;
}

static inline revera_eng_slice_node revera_eng_slice_node_of(vg_arena *mem, const revera_eng_node *elems, int64_t n) {
    revera_eng_slice_node out = revera_eng_slice_node_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_node));
    }
    return out;
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_bracketSet *p = (revera_eng_bracketSet *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_bracketSet));
    memset(p, 0, (size_t)c * sizeof(revera_eng_bracketSet));
    return (revera_eng_slice_bracketSet){p, n, c};
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_bracketSet_make_cap(mem, n, n);
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_grow(vg_arena *mem, revera_eng_slice_bracketSet s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_bracketSet *p = (revera_eng_bracketSet *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_bracketSet));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_bracketSet));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_bracketSet));
    }
    return (revera_eng_slice_bracketSet){p, s.len, newcap};
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_append(vg_arena *mem, revera_eng_slice_bracketSet s, revera_eng_bracketSet v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_bracketSet_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_append_slice(vg_arena *mem, revera_eng_slice_bracketSet s, revera_eng_slice_bracketSet more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_bracketSet_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_bracketSet));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_sub(revera_eng_slice_bracketSet s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_bracketSet){0};
    }
    return (revera_eng_slice_bracketSet){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_tail(revera_eng_slice_bracketSet s, int64_t lo) {
    return revera_eng_slice_bracketSet_sub(s, lo, s.len);
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_head(revera_eng_slice_bracketSet s, int64_t hi) {
    return revera_eng_slice_bracketSet_sub(s, 0, hi);
}

static inline revera_eng_bracketSet *revera_eng_slice_bracketSet_at(revera_eng_slice_bracketSet s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_bracketSet_copy(revera_eng_slice_bracketSet dst, revera_eng_slice_bracketSet src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_bracketSet));
    }
    return n;
}

static inline revera_eng_slice_bracketSet revera_eng_slice_bracketSet_of(vg_arena *mem, const revera_eng_bracketSet *elems, int64_t n) {
    revera_eng_slice_bracketSet out = revera_eng_slice_bracketSet_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_bracketSet));
    }
    return out;
}

static inline revera_eng_slice_Match revera_eng_slice_Match_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_Match *p = (revera_eng_Match *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_Match));
    memset(p, 0, (size_t)c * sizeof(revera_eng_Match));
    return (revera_eng_slice_Match){p, n, c};
}

static inline revera_eng_slice_Match revera_eng_slice_Match_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_Match_make_cap(mem, n, n);
}

static inline revera_eng_slice_Match revera_eng_slice_Match_grow(vg_arena *mem, revera_eng_slice_Match s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_Match *p = (revera_eng_Match *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_Match));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_Match));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_Match));
    }
    return (revera_eng_slice_Match){p, s.len, newcap};
}

static inline revera_eng_slice_Match revera_eng_slice_Match_append(vg_arena *mem, revera_eng_slice_Match s, revera_eng_Match v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_Match_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_Match revera_eng_slice_Match_append_slice(vg_arena *mem, revera_eng_slice_Match s, revera_eng_slice_Match more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_Match_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_Match));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_Match revera_eng_slice_Match_sub(revera_eng_slice_Match s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_Match){0};
    }
    return (revera_eng_slice_Match){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_Match revera_eng_slice_Match_tail(revera_eng_slice_Match s, int64_t lo) {
    return revera_eng_slice_Match_sub(s, lo, s.len);
}

static inline revera_eng_slice_Match revera_eng_slice_Match_head(revera_eng_slice_Match s, int64_t hi) {
    return revera_eng_slice_Match_sub(s, 0, hi);
}

static inline revera_eng_Match *revera_eng_slice_Match_at(revera_eng_slice_Match s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_Match_copy(revera_eng_slice_Match dst, revera_eng_slice_Match src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_Match));
    }
    return n;
}

static inline revera_eng_slice_Match revera_eng_slice_Match_of(vg_arena *mem, const revera_eng_Match *elems, int64_t n) {
    revera_eng_slice_Match out = revera_eng_slice_Match_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_Match));
    }
    return out;
}

static inline revera_eng_slice_bool revera_eng_slice_bool_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    bool *p = (bool *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(bool));
    memset(p, 0, (size_t)c * sizeof(bool));
    return (revera_eng_slice_bool){p, n, c};
}

static inline revera_eng_slice_bool revera_eng_slice_bool_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_bool_make_cap(mem, n, n);
}

static inline revera_eng_slice_bool revera_eng_slice_bool_grow(vg_arena *mem, revera_eng_slice_bool s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    bool *p = (bool *)vg_arena_alloc(mem, (size_t)newcap * sizeof(bool));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(bool));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(bool));
    }
    return (revera_eng_slice_bool){p, s.len, newcap};
}

static inline revera_eng_slice_bool revera_eng_slice_bool_append(vg_arena *mem, revera_eng_slice_bool s, bool v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_bool_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_bool revera_eng_slice_bool_append_slice(vg_arena *mem, revera_eng_slice_bool s, revera_eng_slice_bool more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_bool_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(bool));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_bool revera_eng_slice_bool_sub(revera_eng_slice_bool s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_bool){0};
    }
    return (revera_eng_slice_bool){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_bool revera_eng_slice_bool_tail(revera_eng_slice_bool s, int64_t lo) {
    return revera_eng_slice_bool_sub(s, lo, s.len);
}

static inline revera_eng_slice_bool revera_eng_slice_bool_head(revera_eng_slice_bool s, int64_t hi) {
    return revera_eng_slice_bool_sub(s, 0, hi);
}

static inline bool *revera_eng_slice_bool_at(revera_eng_slice_bool s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_bool_copy(revera_eng_slice_bool dst, revera_eng_slice_bool src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(bool));
    }
    return n;
}

static inline revera_eng_slice_bool revera_eng_slice_bool_of(vg_arena *mem, const bool *elems, int64_t n) {
    revera_eng_slice_bool out = revera_eng_slice_bool_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(bool));
    }
    return out;
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_make_cap(vg_arena *mem, int64_t n, int64_t c) {
    assert(0 <= n && n <= c);
    revera_eng_replPart *p = (revera_eng_replPart *)vg_arena_alloc(mem, (size_t)(c < 1 ? 1 : c) * sizeof(revera_eng_replPart));
    memset(p, 0, (size_t)c * sizeof(revera_eng_replPart));
    return (revera_eng_slice_replPart){p, n, c};
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_make(vg_arena *mem, int64_t n) {
    return revera_eng_slice_replPart_make_cap(mem, n, n);
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_grow(vg_arena *mem, revera_eng_slice_replPart s, int64_t need) {
    int64_t newcap = s.cap * 2 < 8 ? 8 : s.cap * 2;
    if (newcap < need) {
        newcap = need;
    }
    revera_eng_replPart *p = (revera_eng_replPart *)vg_arena_alloc(mem, (size_t)newcap * sizeof(revera_eng_replPart));
    memset(p + s.len, 0, (size_t)(newcap - s.len) * sizeof(revera_eng_replPart));
    if (s.p != NULL && s.len > 0) {
        memcpy(p, s.p, (size_t)s.len * sizeof(revera_eng_replPart));
    }
    return (revera_eng_slice_replPart){p, s.len, newcap};
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_append(vg_arena *mem, revera_eng_slice_replPart s, revera_eng_replPart v) {
    if (s.len == s.cap) {
        s = revera_eng_slice_replPart_grow(mem, s, s.len + 1);
    }
    s.p[s.len] = v;
    s.len++;
    return s;
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_append_slice(vg_arena *mem, revera_eng_slice_replPart s, revera_eng_slice_replPart more) {
    if (s.len + more.len > s.cap) {
        s = revera_eng_slice_replPart_grow(mem, s, s.len + more.len);
    }
    if (more.len > 0) {
        memmove(s.p + s.len, more.p, (size_t)more.len * sizeof(revera_eng_replPart));
    }
    s.len += more.len;
    return s;
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_sub(revera_eng_slice_replPart s, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= s.cap);
    if (s.p == NULL) {
        return (revera_eng_slice_replPart){0};
    }
    return (revera_eng_slice_replPart){s.p + lo, hi - lo, s.cap - lo};
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_tail(revera_eng_slice_replPart s, int64_t lo) {
    return revera_eng_slice_replPart_sub(s, lo, s.len);
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_head(revera_eng_slice_replPart s, int64_t hi) {
    return revera_eng_slice_replPart_sub(s, 0, hi);
}

static inline revera_eng_replPart *revera_eng_slice_replPart_at(revera_eng_slice_replPart s, int64_t i) {
    assert(i >= 0 && i < s.len);
    return &s.p[i];
}

static inline int64_t revera_eng_slice_replPart_copy(revera_eng_slice_replPart dst, revera_eng_slice_replPart src) {
    int64_t n = dst.len < src.len ? dst.len : src.len;
    if (n > 0) {
        memmove(dst.p, src.p, (size_t)n * sizeof(revera_eng_replPart));
    }
    return n;
}

static inline revera_eng_slice_replPart revera_eng_slice_replPart_of(vg_arena *mem, const revera_eng_replPart *elems, int64_t n) {
    revera_eng_slice_replPart out = revera_eng_slice_replPart_make(mem, n);
    if (n > 0) {
        memcpy(out.p, elems, (size_t)n * sizeof(revera_eng_replPart));
    }
    return out;
}

static inline revera_eng_slice_i32 revera_eng_arr_i32_8_slice(revera_eng_arr_i32_8 *a, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= 8);
    return (revera_eng_slice_i32){a->v + lo, hi - lo, 8 - lo};
}

static inline revera_eng_slice_i32 revera_eng_arr_i32_16_slice(revera_eng_arr_i32_16 *a, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= 16);
    return (revera_eng_slice_i32){a->v + lo, hi - lo, 16 - lo};
}

static inline revera_eng_slice_bool revera_eng_arr_bool_256_slice(revera_eng_arr_bool_256 *a, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= 256);
    return (revera_eng_slice_bool){a->v + lo, hi - lo, 256 - lo};
}

static inline revera_eng_slice_i32 revera_eng_arr_i32_1_slice(revera_eng_arr_i32_1 *a, int64_t lo, int64_t hi) {
    assert(0 <= lo && lo <= hi && hi <= 1);
    return (revera_eng_slice_i32){a->v + lo, hi - lo, 1 - lo};
}

int32_t revera_eng_parseBracket(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc);
revera_eng_Tup_t627261636b65744974656dx_bool revera_eng_parseBracketItem(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc, int64_t bracketStart);
vg_str revera_eng_runesToString(vg_arena *mem, revera_eng_slice_i32 seq);
revera_eng_Tup_si32_bool revera_eng_scanInner(vg_arena *mem, revera_eng_parser *p, vg_str closer, int32_t emptyCode);
void revera_eng_sortRanges(vg_arena *mem, revera_eng_slice_runeRange rr);
void revera_eng_finalizeBracket(vg_arena *mem, revera_eng_bracketSet *b, revera_eng_Locale *loc);
bool revera_eng_bracketInRanges(revera_eng_slice_bracketSet brs, int32_t bi, int32_t c);
bool revera_eng_bracketPositiveSingle(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc, int32_t c);
bool revera_eng_bracketMatchesOne(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc, int32_t c);
bool revera_eng_counterpartMatch(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc, int32_t t, int32_t e);
bool revera_eng_bracketHasMultiMembers(revera_eng_slice_bracketSet brs, int32_t bi);
bool revera_eng_bracketMatchesMulti(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc, revera_eng_slice_i32 t);
bool revera_eng_equivCandidate(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc, revera_eng_slice_i32 t, revera_eng_elemBuf *candidate, int64_t at);
bool revera_eng_bracketMatchesSpan(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc, revera_eng_slice_i32 runes, int64_t i, int64_t j);
int64_t revera_eng_bracketMinChars(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_Locale *loc);
bool revera_eng_capStep(revera_eng_capSolver *s);
void revera_eng_seedArenas(vg_arena *mem, revera_eng_capSolver *s);
int32_t revera_eng_newTree(vg_arena *mem, revera_eng_capSolver *s, revera_eng_ptree t);
int64_t revera_eng_kidAlloc(vg_arena *mem, revera_eng_capSolver *s, int64_t length);
int64_t revera_eng_kidAlloc1(vg_arena *mem, revera_eng_capSolver *s, int32_t t);
int64_t revera_eng_kidPrepend(vg_arena *mem, revera_eng_capSolver *s, int32_t head, int64_t tailOff, int64_t tailLen);
void revera_eng_setMatch(revera_eng_slice_Match caps, int64_t idx, int64_t so, int64_t eo);
void revera_eng_addCounters(revera_eng_capSolver *s, revera_eng_Regexp *re, int32_t t, revera_eng_slice_i64 out);
int64_t revera_eng_structCmp(revera_eng_capSolver *s, revera_eng_Regexp *re, int32_t a, int32_t b);
int64_t revera_eng_cmpCand(revera_eng_capSolver *s, revera_eng_Regexp *re, int32_t a, int32_t b);
int32_t revera_eng_bestParse(vg_arena *mem, revera_eng_capSolver *s, revera_eng_Regexp *re, revera_eng_decoded *d, int32_t ni, int64_t i, int64_t j);
revera_eng_Tup_i64_i64 revera_eng_bestConcat(vg_arena *mem, revera_eng_capSolver *s, revera_eng_Regexp *re, revera_eng_decoded *d, int32_t ni, int64_t idx, int64_t i, int64_t j);
void revera_eng_repTry(vg_arena *mem, revera_eng_capSolver *s, revera_eng_Regexp *re, int32_t ni, int64_t i, int64_t j, int64_t off, int64_t length, revera_eng_repBest *best);
revera_eng_repWin revera_eng_bestRep(vg_arena *mem, revera_eng_capSolver *s, revera_eng_Regexp *re, revera_eng_decoded *d, int32_t ni, int64_t i, int64_t j, int64_t done, bool hasEmpty);
revera_eng_Error revera_eng_solveCaptures(vg_arena *mem, revera_eng_Regexp *re, revera_eng_decoded *d, int64_t so, int64_t eo, uint32_t eflags, revera_eng_slice_Match caps);
void revera_eng_assignCaps(revera_eng_Regexp *re, revera_eng_capSolver *s, int32_t t, revera_eng_slice_Match caps);
int64_t revera_eng_cAdd(int64_t a, int64_t b);
int64_t revera_eng_cMul(int64_t a, int64_t b);
int64_t revera_eng_ContractHeapBytes(revera_eng_Contract *c);
int64_t revera_eng_ContractStackBytes(revera_eng_Contract *c);
int64_t revera_eng_ContractSteps(revera_eng_Contract *c);
revera_eng_Contract revera_eng_ContractFor(revera_eng_Regexp *re, int64_t maxInput);
revera_eng_BackendContract revera_eng_matcherContract(revera_eng_Regexp *re, int64_t length, int64_t atom);
int64_t revera_eng_captureHeap(revera_eng_Regexp *re, int64_t length);
revera_eng_BackendContract revera_eng_onePassContract(revera_eng_Regexp *re, int64_t length, int64_t atom);
revera_eng_BackendContract revera_eng_solverContract(revera_eng_Regexp *re, int64_t length, int64_t atom);
int64_t revera_eng_astSize(revera_eng_slice_node nodes, int32_t ni);
int64_t revera_eng_astHeight(revera_eng_slice_node nodes, int32_t ni);
int64_t revera_eng_atomCost(revera_eng_Regexp *re);
int64_t revera_eng_atomCostNode(revera_eng_slice_node nodes, revera_eng_slice_bracketSet brs, revera_eng_lookupCosts *lc, int32_t ni);
int64_t revera_eng_searchSteps(int64_t count);
int64_t revera_eng_u32ContainsCost(int64_t count);
int64_t revera_eng_findPairCost(int64_t count);
int64_t revera_eng_findCaseCost(int64_t count);
int64_t revera_eng_pairSourcesRunCost(int64_t count, int64_t preimages);
int64_t revera_eng_compareSequenceCost(int64_t length);
revera_eng_lookupCosts revera_eng_localeLookupCosts(revera_eng_Locale *l);
int64_t revera_eng_elementIDCost(revera_eng_lookupCosts *lc, int64_t length);
int64_t revera_eng_collatingElementIDCost(revera_eng_lookupCosts *lc, int64_t length);
int64_t revera_eng_primaryEqualCost(revera_eng_lookupCosts *lc, int64_t left, int64_t right);
int64_t revera_eng_equivsCost(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_lookupCosts *lc, int64_t length);
int64_t revera_eng_positiveSingleCost(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_lookupCosts *lc);
int64_t revera_eng_matchesOneCost(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_lookupCosts *lc);
int64_t revera_eng_candidateLeafCost(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_lookupCosts *lc, int64_t length);
int64_t revera_eng_probeCost(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_lookupCosts *lc, int64_t length);
int64_t revera_eng_bracketAtomCost(revera_eng_slice_bracketSet brs, int32_t bi, revera_eng_lookupCosts *lc);
int64_t revera_eng_solverSteps(revera_eng_slice_node nodes, int32_t ni, int64_t length);
int64_t revera_eng_repInstances(revera_eng_slice_node nodes, int32_t ni, int64_t length);
int64_t revera_eng_solverDepth(revera_eng_slice_node nodes, int32_t ni, int64_t length);
int64_t revera_eng_treeNodes(revera_eng_slice_node nodes, int32_t ni, int64_t length);
int64_t revera_eng_solverFanout(revera_eng_slice_node nodes, int32_t ni, int64_t length);
void revera_eng_prepare(vg_arena *mem, revera_eng_engineWS *ws, int64_t n, int64_t k, int64_t ring);
int64_t revera_eng_workspaceHeapBound(int64_t n, int64_t k, int64_t ring);
bool revera_eng_ctrLess(revera_eng_slice_u32 a, revera_eng_slice_u32 b);
int64_t revera_eng_trailingZeros64(uint64_t x);
revera_eng_engineResult revera_eng_runPhaseA(vg_arena *mem, revera_eng_Regexp *re, vg_str subject, uint32_t eflags);
uint32_t revera_eng_paGen(int64_t boundary);
void revera_eng_paConsider(revera_eng_phaseAState *e, revera_eng_engineWS *ws, int32_t start, revera_eng_slice_u32 ctr, int64_t end);
bool revera_eng_paPrune(revera_eng_phaseAState *e, revera_eng_engineWS *ws, int32_t start, revera_eng_slice_u32 ctr);
bool revera_eng_paStore(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws, int64_t si, uint32_t pc, int32_t start, revera_eng_slice_u32 ctr);
void revera_eng_paRelax(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws, int64_t si, uint32_t pc, int32_t start, revera_eng_slice_u32 ctr);
void revera_eng_compactQueue(revera_eng_engineWS *ws);
void revera_eng_paClosure(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws, revera_eng_Regexp *re, int64_t si);
void revera_eng_paArrive(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws, revera_eng_Regexp *re, uint32_t pc, int64_t delta, int32_t start, revera_eng_slice_u32 ctr);
void revera_eng_paConsume(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws, revera_eng_Regexp *re, int64_t si);
void revera_eng_decodeAhead(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws);
int64_t revera_eng_scanAhead(revera_eng_phaseAState *e, revera_eng_Regexp *re);
bool revera_eng_bolAt(revera_eng_phaseAState *e, int32_t prev);
uint32_t revera_eng_continuationFlags(revera_eng_Regexp *re, vg_str subject, int64_t pos, uint32_t eflags);
void revera_eng_paRun(vg_arena *mem, revera_eng_phaseAState *e, revera_eng_engineWS *ws, revera_eng_Regexp *re);
revera_eng_Error revera_eng_noError(void);
revera_eng_Error revera_eng_compileError(int32_t code, int64_t pos);
vg_str revera_eng_ErrorText(int32_t code);
uint64_t revera_eng_memoHash(revera_eng_memoKey k);
revera_eng_Tup_t6d656d6f56616cx_bool revera_eng_memoGet(revera_eng_memoTab *t, revera_eng_memoKey k);
void revera_eng_memoInsert(revera_eng_memoTab *t, revera_eng_memoKey k, revera_eng_memoVal v);
void revera_eng_memoGrow(vg_arena *mem, revera_eng_memoTab *t);
void revera_eng_memoPut(vg_arena *mem, revera_eng_memoTab *t, revera_eng_memoKey k, revera_eng_memoVal v);
revera_eng_Locale revera_eng_LocalePOSIX(void);
bool revera_eng_LocaleValid(revera_eng_Locale *l);
uint32_t revera_eng_u32Raw(vg_str blob, int64_t off);
uint16_t revera_eng_u16At(revera_eng_Locale *l, int64_t sec, int64_t vego_index);
uint32_t revera_eng_u32At(revera_eng_Locale *l, int64_t sec, int64_t vego_index);
int64_t revera_eng_sectionLen(revera_eng_Locale *l, int64_t sec);
vg_str revera_eng_byteString(revera_eng_Locale *l, int64_t sec, int64_t off);
bool revera_eng_localeLoad(revera_eng_Locale *l, vg_str blob);
bool revera_eng_localeValidate(revera_eng_Locale *l);
bool revera_eng_contractionIDsValid(revera_eng_Locale *l, int64_t sec, int64_t seqCount);
int64_t revera_eng_localeMaxElementLength(revera_eng_Locale *l);
bool revera_eng_validScalar(int32_t r);
uint8_t revera_eng_asciiLower(uint8_t c);
revera_eng_Tup_Str_bool revera_eng_normalizeName(vg_arena *mem, vg_str input);
revera_eng_Tup_Str_bool revera_eng_embeddedModifier(vg_str name);
vg_str revera_eng_longTypeAlias(vg_str name);
revera_eng_Tup_Str_bool revera_eng_normalizeType(vg_arena *mem, vg_str input);
int64_t revera_eng_findName(revera_eng_Locale *l, vg_str name, int64_t poolSec, int64_t offsetsSec, int64_t count);
int64_t revera_eng_localesCount(revera_eng_Locale *l);
revera_eng_LocaleRow revera_eng_localeRowAt(revera_eng_Locale *l, int64_t vego_index);
revera_eng_Tup_t6c6f63616c6552657175657374x_bool revera_eng_normalizeRequest(vg_arena *mem, vg_str name, vg_str collationType);
revera_eng_Tup_t4c6f63616c65x_bool revera_eng_posixSelect(vg_str ctype);
revera_eng_Tup_t4c6f63616c65x_bool revera_eng_LocaleLoad(vg_str blob);
revera_eng_Tup_t4c6f63616c65x_bool revera_eng_resolveLocale(revera_eng_Locale *data, revera_eng_localeRequest req);
revera_eng_Tup_t4c6f63616c65x_bool revera_eng_LocaleSelect(vg_arena *mem, revera_eng_Locale *data, vg_str name, vg_str collationType);
revera_eng_Tup_t4c6f63616c65x_bool revera_eng_LocaleOpen(vg_arena *mem, vg_str blob, vg_str name, vg_str collationType);
revera_eng_Tup_u8_bool revera_eng_classByName(vg_str name);
uint16_t revera_eng_posixMask(int32_t r);
uint16_t revera_eng_localeClassMask(revera_eng_Locale *l, int32_t r);
revera_eng_Tup_t4361736550616972x_bool revera_eng_findCase(revera_eng_Locale *l, int64_t sec, int32_t r);
int32_t revera_eng_caseConvert(revera_eng_Locale *l, int32_t r, bool toUpper);
void revera_eng_pairSourcesRun(revera_eng_Locale *l, revera_eng_preimageBuf *buf, int64_t sec, int32_t r);
int64_t revera_eng_maxPairRun(revera_eng_Locale *l, int64_t sec);
int64_t revera_eng_localeMaxPreimages(revera_eng_Locale *l);
void revera_eng_localeCasePreimages(revera_eng_Locale *l, revera_eng_preimageBuf *buf, int32_t r);
int32_t revera_eng_localeToUpper(revera_eng_Locale *l, int32_t r);
int32_t revera_eng_localeToLower(revera_eng_Locale *l, int32_t r);
int64_t revera_eng_compareSequence(revera_eng_Locale *l, revera_eng_slice_i32 seq, int64_t vego_index);
revera_eng_Tup_u32_bool revera_eng_elementID(revera_eng_Locale *l, revera_eng_slice_i32 seq);
bool revera_eng_u32Contains(revera_eng_Locale *l, int64_t sec, int64_t first, int64_t count, uint32_t needle);
revera_eng_CollProfile revera_eng_collationProfileRow(revera_eng_Locale *l, int64_t vego_index);
bool revera_eng_isContraction(revera_eng_Locale *l, uint32_t element);
revera_eng_Tup_u32_bool revera_eng_collatingElementID(revera_eng_Locale *l, revera_eng_slice_i32 seq);
bool revera_eng_localeIsCollatingElement(revera_eng_Locale *l, revera_eng_slice_i32 seq);
int64_t revera_eng_LocaleCollatingPrefix(revera_eng_Locale *l, revera_eng_slice_i32 seq);
revera_eng_Tup_u32_bool revera_eng_findPair(revera_eng_Locale *l, int64_t sec, int64_t first, int64_t count, uint32_t element);
uint64_t revera_eng_primaryToken(revera_eng_Locale *l, uint32_t element);
bool revera_eng_localePrimaryEqual(revera_eng_Locale *l, revera_eng_slice_i32 left, revera_eng_slice_i32 right);
int64_t revera_eng_localeMinEquivLength(revera_eng_Locale *l, revera_eng_slice_i32 seq);
int64_t revera_eng_minTokenLength(revera_eng_Locale *l, int64_t sec, int64_t first, int64_t count, uint64_t token, int64_t best);
bool revera_eng_localeSupportsRanges(revera_eng_Locale *l);
int64_t revera_eng_LocaleCount(revera_eng_Locale *l);
vg_str revera_eng_LocaleName(revera_eng_Locale *l, int64_t vego_index);
revera_eng_decoded revera_eng_decodeWindow(vg_arena *mem, vg_str s, int64_t so, int64_t eo);
int64_t revera_eng_indexOfByte(vg_str s, uint8_t c);
bool revera_eng_runesContain(revera_eng_slice_i32 runes, int32_t r);
bool revera_eng_charMatches(revera_eng_Regexp *re, int32_t ni, int32_t c);
bool revera_eng_anyMatches(revera_eng_Regexp *re, int32_t c);
bool revera_eng_atBOL(revera_eng_Regexp *re, revera_eng_decoded *d, int64_t i, uint32_t eflags);
bool revera_eng_atEOL(revera_eng_Regexp *re, revera_eng_decoded *d, int64_t i, uint32_t eflags);
void revera_eng_fillMatches(revera_eng_Regexp *re, revera_eng_decoded *d, revera_eng_slice_Match caps, revera_eng_slice_Match pmatch);
bool revera_eng_onePassAnalyze(vg_arena *mem, revera_eng_slice_node nodes, revera_eng_slice_bracketSet brs, int32_t ni);
bool revera_eng_fixedLength(revera_eng_slice_node nodes, int32_t ni);
bool revera_eng_disjointLengths(revera_eng_slice_node nodes, int32_t ni);
revera_eng_Tup_si32_bool revera_eng_firstSet(vg_arena *mem, revera_eng_slice_node nodes, int32_t ni);
bool revera_eng_disjointFirsts(vg_arena *mem, revera_eng_slice_node nodes, int32_t ni);
bool revera_eng_onePassCaps(revera_eng_Regexp *re, revera_eng_decoded *d, int32_t ni, int64_t i, int64_t j, uint32_t eflags, revera_eng_slice_Match caps);
void revera_eng_buildScanFilter(vg_arena *mem, revera_eng_program *pr, bool newlineMode);
int64_t revera_eng_instrEstimate(revera_eng_slice_node nodes, int32_t ni);
void revera_eng_compileProgram(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_slice_node nodes, int32_t root, bool multi, bool newlineMode);
uint32_t revera_eng_addInstr(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_instr ins);
void revera_eng_patch(revera_eng_progBuilder *b, revera_eng_slice_patchSlot slots, uint32_t target);
revera_eng_slice_patchSlot revera_eng_singleOut(vg_arena *mem, uint32_t idx, bool alt);
revera_eng_frag revera_eng_epsilonFrag(vg_arena *mem, revera_eng_progBuilder *b);
revera_eng_slice_u32 revera_eng_copyExtra(vg_arena *mem, revera_eng_slice_u32 extra);
revera_eng_frag revera_eng_consumeFrag(vg_arena *mem, revera_eng_progBuilder *b, uint8_t op, uint32_t arg, uint64_t mask, revera_eng_slice_u32 extra);
revera_eng_frag revera_eng_emit(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_slice_node nodes, int32_t ni, uint64_t mask, revera_eng_slice_u32 extra);
revera_eng_frag revera_eng_emitAlt(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_slice_node nodes, int32_t ni, uint64_t mask, revera_eng_slice_u32 extra);
bool revera_eng_fragAppend(revera_eng_progBuilder *b, revera_eng_frag *result, bool have, revera_eng_frag f);
revera_eng_frag revera_eng_emitRepeat(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_slice_node nodes, int32_t ni, uint64_t mask, revera_eng_slice_u32 extra);
revera_eng_frag revera_eng_emitStar(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_slice_node nodes, int32_t child, uint64_t mask, revera_eng_slice_u32 extra);
revera_eng_frag revera_eng_emitPlus(vg_arena *mem, revera_eng_progBuilder *b, revera_eng_slice_node nodes, int32_t child, uint64_t mask, revera_eng_slice_u32 extra);
void revera_eng_compileScan(revera_eng_Regexp *re, int32_t ni);
void revera_eng_collectNested(vg_arena *mem, revera_eng_Regexp *re, int32_t ni, revera_eng_groupStack *stack);
int64_t revera_eng_minMatchChars(revera_eng_slice_node nodes, revera_eng_slice_bracketSet brs, revera_eng_Locale *loc, int32_t ni);
revera_eng_Tup_t526567657870x_t4572726f72x revera_eng_Compile(vg_arena *mem, vg_str pattern, revera_eng_Locale loc, uint32_t flags);
int64_t revera_eng_NumSub(revera_eng_Regexp *re);
bool revera_eng_trivialNullMatch(revera_eng_Regexp *re, revera_eng_slice_Match pmatch);
revera_eng_Tup_bool_t4572726f72x revera_eng_Exec(vg_arena *mem, revera_eng_Regexp *re, vg_str subject, revera_eng_slice_Match pmatch, uint32_t eflags);
revera_eng_Tup_t4d6174636849746572x_t4572726f72x revera_eng_MatchIterInit(revera_eng_Regexp *re, int64_t limit);
revera_eng_Tup_bool_t4572726f72x revera_eng_MatchIterNext(vg_arena *mem, revera_eng_Regexp *re, revera_eng_MatchIter *it, vg_str subject, uint32_t eflags, revera_eng_slice_Match pmatch);
revera_eng_Tup_st7265706c50617274x_t4572726f72x revera_eng_parseReplacement(vg_arena *mem, vg_str replacement, int64_t nsub);
revera_eng_Tup_Str_t4572726f72x revera_eng_ReplaceAll(vg_arena *mem, revera_eng_Regexp *re, vg_str subject, vg_str replacement, int64_t limit, uint32_t eflags);
int32_t revera_eng_fail(revera_eng_parser *p, int32_t code, int64_t pos);
int32_t revera_eng_addNode(vg_arena *mem, revera_eng_parser *p, uint8_t op);
bool revera_eng_eof(revera_eng_parser *p);
uint8_t revera_eng_peekByte(revera_eng_parser *p);
uint8_t revera_eng_peekByteAt(revera_eng_parser *p, int64_t ahead);
int32_t revera_eng_nextRune(revera_eng_parser *p);
int32_t revera_eng_parse(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc, vg_str pattern, uint32_t flags);
int32_t revera_eng_parseAlt(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc, bool inGroup);
int32_t revera_eng_parseBranch(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc, bool inGroup);
bool revera_eng_isDupByte(uint8_t c);
int32_t revera_eng_parseExpr(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc);
int32_t revera_eng_charNode(vg_arena *mem, revera_eng_parser *p, revera_eng_Locale *loc, int32_t r);
revera_eng_slice_i32 revera_eng_appendUnique(vg_arena *mem, revera_eng_slice_i32 runes, int32_t r);
int32_t revera_eng_parseDup(vg_arena *mem, revera_eng_parser *p, int32_t operand);
revera_eng_Tup_t696e74657276616cx_bool revera_eng_parseInterval(revera_eng_parser *p);
void revera_eng_intervalError(revera_eng_parser *p, int64_t start);
revera_eng_Tup_i64_bool revera_eng_parseCount(revera_eng_parser *p);
int64_t revera_eng_satAdd(int64_t a, int64_t b);
int64_t revera_eng_satMul(int64_t a, int64_t b);
void revera_eng_computeLengths(vg_arena *mem, revera_eng_slice_node nodes, revera_eng_Locale *loc, revera_eng_slice_bracketSet brackets, int32_t ni);
bool revera_eng_utf8Cont(uint8_t c);
revera_eng_Tup_i32_i64 revera_eng_decodeRuneAt(vg_str s, int64_t at);
int64_t revera_eng_runeCount(vg_str s);
uint8_t revera_eng_utf8LeadByte(int32_t r);

#endif
