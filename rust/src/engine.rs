// Code generated by vegoc from the Vego JSON form. DO NOT EDIT.

#![forbid(unsafe_code)]
#![allow(non_snake_case, non_camel_case_types, non_upper_case_globals)]
#![allow(unused_parens, unused_mut, unused_variables, dead_code)]
#![allow(unreachable_code, unused_assignments)]

use crate::vg;

pub const itemChar: u8 = 0u8;
pub const itemElem: u8 = 1u8;
pub const itemEquiv: u8 = 2u8;
pub const itemClass: u8 = 3u8;
pub const capWorkLimit: i64 = 50000000i64;
pub const solverArenaLimit: i64 = 67108864i64;
pub const contractCap: i64 = 4611686018427387904i64;
pub const frameBytes: i64 = 256i64;
pub const matcherStackBytes: i64 = 2048i64;
pub const singleLookupFrames: i64 = 8i64;
pub const multiLookupFrames: i64 = 16i64;
pub const ptreeBytes: i64 = 48i64;
pub const kidBytes: i64 = 8i64;
pub const mapEntryBytes: i64 = 128i64;
pub const matchBytes: i64 = 16i64;
pub const profileRowCost: i64 = 13i64;
pub const maxElemAhead: i64 = 8i64;
pub const queueCompactFactor: i64 = 2i64;
pub const ErrNone: i32 = 0i32;
pub const ErrNoMatch: i32 = 1i32;
pub const ErrBadPat: i32 = 2i32;
pub const ErrECollate: i32 = 3i32;
pub const ErrECType: i32 = 4i32;
pub const ErrEEscape: i32 = 5i32;
pub const ErrESubReg: i32 = 6i32;
pub const ErrEBrack: i32 = 7i32;
pub const ErrEParen: i32 = 8i32;
pub const ErrEBrace: i32 = 9i32;
pub const ErrBadBR: i32 = 10i32;
pub const ErrERange: i32 = 11i32;
pub const ErrESpace: i32 = 12i32;
pub const ErrBadRpt: i32 = 13i32;
pub const ErrENoSub: i32 = 14i32;
pub const FlagICase: u32 = 1u32;
pub const FlagNewline: u32 = 2u32;
pub const FlagNoSub: u32 = 4u32;
pub const FlagMinimal: u32 = 8u32;
pub const ExecNotBOL: u32 = 1u32;
pub const ExecNotEOL: u32 = 2u32;
pub const dupMax: i64 = 255i64;
pub const classAlnum: u8 = 0u8;
pub const classAlpha: u8 = 1u8;
pub const classBlank: u8 = 2u8;
pub const classCntrl: u8 = 3u8;
pub const classDigit: u8 = 4u8;
pub const classGraph: u8 = 5u8;
pub const classLower: u8 = 6u8;
pub const classPrint: u8 = 7u8;
pub const classPunct: u8 = 8u8;
pub const classSpace: u8 = 9u8;
pub const classUpper: u8 = 10u8;
pub const classXdigit: u8 = 11u8;
pub const numClasses: i64 = 12i64;
pub const firstSequenceID: i64 = 1114112i64;
pub const secCtypeStage1: i64 = 0i64;
pub const secCtypeBlocks: i64 = 1i64;
pub const secCaseDefault: i64 = 2i64;
pub const secCaseTurkic: i64 = 3i64;
pub const secSeqCodepoints: i64 = 4i64;
pub const secSequences: i64 = 5i64;
pub const secRootContractions: i64 = 6i64;
pub const secRootEquivalences: i64 = 7i64;
pub const secCollationOverrides: i64 = 8i64;
pub const secContractionAdds: i64 = 9i64;
pub const secContractionRemoves: i64 = 10i64;
pub const secCollationProfiles: i64 = 11i64;
pub const secTypeNames: i64 = 12i64;
pub const secTypeNameOffsets: i64 = 13i64;
pub const secLocaleNames: i64 = 14i64;
pub const secLocaleNameOffsets: i64 = 15i64;
pub const secLocales: i64 = 16i64;
pub const secLocaleTypes: i64 = 17i64;
pub const secInvUpperDefault: i64 = 18i64;
pub const secInvLowerDefault: i64 = 19i64;
pub const secInvUpperTurkic: i64 = 20i64;
pub const secInvLowerTurkic: i64 = 21i64;
pub const secScalars: i64 = 22i64;
pub const numSections: i64 = 23i64;
pub const ctypeStage1Entries: i64 = 4352i64;
pub const normalizedNameMax: i64 = 127i64;
pub const maxPreimages: i64 = 16i64;
pub const iRune: u8 = 0u8;
pub const iRuneFold: u8 = 1u8;
pub const iAny: u8 = 2u8;
pub const iBracket: u8 = 3u8;
pub const iBOL: u8 = 4u8;
pub const iEOL: u8 = 5u8;
pub const iSplit: u8 = 6u8;
pub const iJmp: u8 = 7u8;
pub const iMatch: u8 = 8u8;
pub const iFail: u8 = 9u8;
pub const failMinNone: i64 = 4611686018427387904i64;
pub const maxProgram: i64 = 1048576i64;
pub const maskWidth: i64 = 64i64;
pub const subjectLimit: i64 = 2147483647i64;
pub const lengthCap: i64 = 1099511627776i64;
pub const opChar: u8 = 0u8;
pub const opAny: u8 = 1u8;
pub const opBracket: u8 = 2u8;
pub const opBOL: u8 = 3u8;
pub const opEOL: u8 = 4u8;
pub const opConcat: u8 = 5u8;
pub const opAlt: u8 = 6u8;
pub const opRepeat: u8 = 7u8;
pub const opGroup: u8 = 8u8;
pub const infinite: i64 = (1i64).wrapping_neg();
pub const lenInf: i64 = 1073741824i64;
pub const invalidRune: i32 = (1i32).wrapping_neg();

pub static classNames: [vg::Str; (numClasses) as usize] = [vg::lit(b"alnum"), vg::lit(b"alpha"), vg::lit(b"blank"), vg::lit(b"cntrl"), vg::lit(b"digit"), vg::lit(b"graph"), vg::lit(b"lower"), vg::lit(b"print"), vg::lit(b"punct"), vg::lit(b"space"), vg::lit(b"upper"), vg::lit(b"xdigit")];

#[derive(Clone, Copy)]
pub struct runeRange {
    pub lo: i32,
    pub hi: i32,
}

#[derive(Clone, Copy)]
pub struct bracketSet {
    pub negated: bool,
    pub icase: bool,
    pub nlMode: bool,
    pub ranges: vg::Slice<runeRange>,
    pub classMask: u16,
    pub elems: vg::Slice<vg::Slice<i32>>,
    pub equivs: vg::Slice<vg::Slice<i32>>,
    pub multiLens: u16,
}

#[derive(Clone, Copy)]
pub struct bracketItem {
    pub kind: u8,
    pub r: i32,
    pub seq: vg::Slice<i32>,
    pub class: u8,
}

#[derive(Clone, Copy)]
pub struct elemBuf {
    pub r: [i32; (maxElemAhead) as usize],
}

#[derive(Clone, Copy)]
pub struct ptree {
    pub n: i32,
    pub i: i32,
    pub j: i32,
    pub branch: i32,
    pub kidsOff: i32,
    pub kidsLen: i32,
}

#[derive(Clone, Copy)]
pub struct capSolver {
    pub eflags: u32,
    pub memo: memoTab,
    pub cmemo: memoTab,
    pub rmemo: memoTab,
    pub ctrA: vg::Slice<i64>,
    pub ctrB: vg::Slice<i64>,
    pub work: i64,
    pub failed: bool,
    pub trees: vg::Slice<ptree>,
    pub kidStore: vg::Slice<i32>,
}

#[derive(Clone, Copy)]
pub struct repBest {
    pub off: i64,
    pub length: i64,
    pub tree: i32,
    pub found: bool,
}

#[derive(Clone, Copy)]
pub struct repWin {
    pub off: i64,
    pub length: i64,
    pub ok: bool,
}

#[derive(Clone, Copy)]
pub struct BackendContract {
    pub HeapBytes: i64,
    pub StackBytes: i64,
    pub Steps: i64,
}

#[derive(Clone, Copy)]
pub struct Contract {
    pub MaxInput: i64,
    pub Matcher: BackendContract,
    pub OnePass: BackendContract,
    pub HasOnePass: bool,
    pub Solver: BackendContract,
    pub HasSolver: bool,
}

#[derive(Clone, Copy)]
pub struct lookupCosts {
    pub sequenceSearch: i64,
    pub contraction: i64,
    pub primaryToken: i64,
    pub classMask: i64,
    pub casePreimages: i64,
    pub caseConvert: i64,
    pub preimages: i64,
}

#[derive(Clone, Copy)]
pub struct slotTable {
    pub stamp: vg::Slice<u32>,
    pub starts: vg::Slice<i32>,
    pub ctr: vg::Slice<u32>,
    pub active: vg::Slice<u32>,
    pub r#gen: u32,
}

#[derive(Clone, Copy)]
pub struct engineWS {
    pub slots: vg::Slice<slotTable>,
    pub queue: vg::Slice<u32>,
    pub onq: vg::Slice<u8>,
    pub bestCtr: vg::Slice<u32>,
    pub ctrBuf: vg::Slice<u32>,
    pub zeros: vg::Slice<u32>,
    pub ahead: vg::Slice<i32>,
}

#[derive(Clone, Copy)]
pub struct engineResult {
    pub matched: bool,
    pub so: i64,
    pub eo: i64,
}

#[derive(Clone, Copy)]
pub struct phaseAState {
    pub subject: vg::Str,
    pub eflags: u32,
    pub k: i64,
    pub ring: i64,
    pub nlMode: bool,
    pub ci: i64,
    pub pos: i64,
    pub bol: bool,
    pub eol: bool,
    pub cur: i32,
    pub size: i64,
    pub matched: bool,
    pub so: i64,
    pub eo: i64,
}

#[derive(Clone, Copy)]
pub struct Error {
    pub Code: i32,
    pub Pos: i64,
}

#[derive(Clone, Copy)]
pub struct memoKey {
    pub a: i32,
    pub b: i32,
    pub c: i32,
    pub d: i32,
}

#[derive(Clone, Copy)]
pub struct memoVal {
    pub x: i32,
    pub y: i32,
    pub z: i32,
}

#[derive(Clone, Copy)]
pub struct memoTab {
    pub keys: vg::Slice<memoKey>,
    pub vals: vg::Slice<memoVal>,
    pub used: vg::Slice<u8>,
    pub count: i64,
}

#[derive(Clone, Copy)]
pub struct SecRange {
    pub Off: i64,
    pub End: i64,
}

#[derive(Clone, Copy)]
pub struct Locale {
    pub blob: vg::Str,
    pub sec: [SecRange; (numSections) as usize],
    pub maxSeq: i64,
    pub preimagesDefault: i64,
    pub preimagesTurkic: i64,
    pub collationProfile: u16,
    pub caseProfile: u8,
    pub posix: bool,
    pub valid: bool,
}

#[derive(Clone, Copy)]
pub struct LocaleRow {
    pub TypeFirst: i64,
    pub TypeCount: i64,
    pub CaseProfile: u8,
    pub DefaultCollation: u16,
}

#[derive(Clone, Copy)]
pub struct localeRequest {
    pub name: vg::Str,
    pub ctype: vg::Str,
    pub posix: bool,
}

#[derive(Clone, Copy)]
pub struct CasePair {
    pub Upper: i32,
    pub Lower: i32,
}

#[derive(Clone, Copy)]
pub struct preimageBuf {
    pub r: [i32; (maxPreimages) as usize],
    pub n: i64,
}

#[derive(Clone, Copy)]
pub struct CollProfile {
    pub OverrideFirst: i64,
    pub OverrideCount: i64,
    pub AddFirst: i64,
    pub AddCount: i64,
    pub RemoveFirst: i64,
    pub RemoveCount: i64,
}

#[derive(Clone, Copy)]
pub struct Match {
    pub So: i64,
    pub Eo: i64,
}

#[derive(Clone, Copy)]
pub struct decoded {
    pub runes: vg::Slice<i32>,
    pub byteAt: vg::Slice<i64>,
    pub atSubjectStart: bool,
    pub atSubjectEnd: bool,
    pub prevIsNewline: bool,
    pub nextIsNewline: bool,
}

#[derive(Clone, Copy)]
pub struct instr {
    pub op: u8,
    pub next: u32,
    pub alt: u32,
    pub arg: u32,
    pub mask: u64,
    pub extra: vg::Slice<u32>,
}

#[derive(Clone, Copy)]
pub struct scanFilter {
    pub enabled: bool,
    pub single: bool,
    pub b: u8,
    pub stop: [bool; (256) as usize],
}

#[derive(Clone, Copy)]
pub struct program {
    pub ins: vg::Slice<instr>,
    pub start: u32,
    pub foldSets: vg::Slice<vg::Slice<i32>>,
    pub multi: bool,
    pub failMin: i64,
    pub scan: scanFilter,
    pub depth: i64,
}

#[derive(Clone, Copy)]
pub struct patchSlot {
    pub idx: u32,
    pub alt: bool,
}

#[derive(Clone, Copy)]
pub struct frag {
    pub start: u32,
    pub out: vg::Slice<patchSlot>,
}

#[derive(Clone, Copy)]
pub struct progBuilder {
    pub prog: program,
    pub tooBig: bool,
    pub failMin: i64,
    pub errCode: i32,
    pub icase: bool,
}

#[derive(Clone, Copy)]
pub struct Regexp {
    pub nodes: vg::Slice<node>,
    pub brackets: vg::Slice<bracketSet>,
    pub root: i32,
    pub nsub: i64,
    pub flags: u32,
    pub loc: Locale,
    pub minSlots: i64,
    pub nested: vg::Slice<vg::Slice<i32>>,
    pub multi: bool,
    pub progOK: bool,
    pub prog: program,
    pub minLen: i64,
    pub anchors: bool,
    pub onePass: bool,
}

#[derive(Clone, Copy)]
pub struct groupStack {
    pub g: vg::Slice<i32>,
}

#[derive(Clone, Copy)]
pub struct MatchIter {
    pub pos: i64,
    pub lastEnd: i64,
    pub limit: i64,
    pub done: bool,
}

#[derive(Clone, Copy)]
pub struct replPart {
    pub lit: vg::Str,
    pub group: i64,
}

#[derive(Clone, Copy)]
pub struct node {
    pub op: u8,
    pub ch: vg::Slice<i32>,
    pub r: i32,
    pub fold: vg::Slice<i32>,
    pub br: i32,
    pub min: i64,
    pub max: i64,
    pub minimal: bool,
    pub index: i64,
    pub minL: i64,
    pub maxL: i64,
    pub sufMin: vg::Slice<i64>,
    pub sufMax: vg::Slice<i64>,
    pub firsts: vg::Slice<vg::Slice<i32>>,
}

#[derive(Clone, Copy)]
pub struct parser {
    pub src: vg::Str,
    pub pos: i64,
    pub flags: u32,
    pub groups: i64,
    pub nodes: vg::Slice<node>,
    pub brackets: vg::Slice<bracketSet>,
    pub err: Error,
}

#[derive(Clone, Copy)]
pub struct interval {
    pub lo: i64,
    pub hi: i64,
}

pub fn vego_eq_memoKey(a: memoKey, b: memoKey) -> bool {
    a.a == b.a && a.b == b.b && a.c == b.c && a.d == b.d
}

pub fn parseBracket(mem: &vg::Arena, p: &mut parser, loc: &mut Locale) -> i32 {
    let start: i64 = p.pos;
    p.pos += 1i64;
    let mut b: bracketSet = vg::zero();
    b.icase = ((p.flags & FlagICase) != 0u32);
    b.nlMode = ((p.flags & FlagNewline) != 0u32);
    if (peekByte(p) == 94u8) {
        b.negated = true;
        p.pos += 1i64;
    }
    let mut empty: bool = true;
    loop {
        if eof(p) {
            return { let _t1 = ErrEBrack; let _t2 = start; fail(p, _t1, _t2) };
        }
        if ((peekByte(p) == 93u8) && (!empty)) {
            p.pos += 1i64;
            finalizeBracket(mem, &mut b, loc);
            p.brackets = vg::append(mem, p.brackets, b);
            let n: i32 = { let _t3 = opBracket; addNode(mem, p, _t3) };
            p.nodes.update(((n) as i64), (((p.brackets.len - 1i64)) as i32), |mut _t5, _t4| { _t5.br = _t4; _t5 });
            return n;
        }
        let _t6 = { let _t7 = start; parseBracketItem(mem, p, loc, _t7) };
        let item: bracketItem = _t6.0;
        let ok: bool = _t6.1;
        if (!ok) {
            return (1i32).wrapping_neg();
        }
        empty = false;
        if ((((item.kind == itemChar) && (peekByte(p) == 45u8)) && ({ let _t8 = 1i64; peekByteAt(p, _t8) } != 93u8)) && ((p.pos + 1i64) < p.src.len)) {
            let rangeStart: i64 = p.pos;
            p.pos += 1i64;
            let _t9 = { let _t10 = start; parseBracketItem(mem, p, loc, _t10) };
            let end: bracketItem = _t9.0;
            let ok2: bool = _t9.1;
            if (!ok2) {
                return (1i32).wrapping_neg();
            }
            if (end.kind != itemChar) {
                return { let _t11 = ErrERange; let _t12 = rangeStart; fail(p, _t11, _t12) };
            }
            if (!localeSupportsRanges(loc)) {
                return { let _t13 = ErrERange; let _t14 = rangeStart; fail(p, _t13, _t14) };
            }
            if (item.r > end.r) {
                return { let _t15 = ErrERange; let _t16 = rangeStart; fail(p, _t15, _t16) };
            }
            let mut rr: runeRange = vg::zero();
            rr.lo = item.r;
            rr.hi = end.r;
            b.ranges = vg::append(mem, b.ranges, rr);
            if ((peekByte(p) == 45u8) && ({ let _t17 = 1i64; peekByteAt(p, _t17) } != 93u8)) {
                return { let _t18 = ErrERange; let _t19 = p.pos; fail(p, _t18, _t19) };
            }
            continue;
        }
        if ((((item.kind != itemChar) && (peekByte(p) == 45u8)) && ({ let _t20 = 1i64; peekByteAt(p, _t20) } != 93u8)) && ((p.pos + 1i64) < p.src.len)) {
            return { let _t21 = ErrERange; let _t22 = p.pos; fail(p, _t21, _t22) };
        }
        {
            let _t23 = item.kind;
            if _t23 == itemChar {
                let mut rr_2: runeRange = vg::zero();
                rr_2.lo = item.r;
                rr_2.hi = item.r;
                b.ranges = vg::append(mem, b.ranges, rr_2);
            } else if _t23 == itemElem {
                b.elems = vg::append(mem, b.elems, item.seq);
            } else if _t23 == itemEquiv {
                b.equivs = vg::append(mem, b.equivs, item.seq);
            } else if _t23 == itemClass {
                b.classMask |= (1u16 << item.class);
            }
        }
    }
    unreachable!()
}

pub fn parseBracketItem(mem: &vg::Arena, p: &mut parser, loc: &mut Locale, bracketStart: i64) -> (bracketItem, bool) {
    let mut item: bracketItem = vg::zero();
    let c: u8 = peekByte(p);
    if (c == 91u8) {
        let inner: u8 = { let _t1 = 1i64; peekByteAt(p, _t1) };
        if (inner == 46u8) {
            let _t2 = { let _t3 = vg::lit(b".]"); let _t4 = ErrECollate; scanInner(mem, p, _t3, _t4) };
            let seq: vg::Slice<i32> = _t2.0;
            let ok: bool = _t2.1;
            if (!ok) {
                return (item, false);
            }
            if (!{ let _t5 = seq; localeIsCollatingElement(loc, _t5) }) {
                let _ = { let _t6 = ErrECollate; let _t7 = bracketStart; fail(p, _t6, _t7) };
                return (item, false);
            }
            if (seq.len == 1i64) {
                item.kind = itemChar;
                item.r = seq.get(0i64);
                return (item, true);
            }
            item.kind = itemElem;
            item.seq = seq;
            return (item, true);
        }
        if (inner == 61u8) {
            let _t8 = { let _t9 = vg::lit(b"=]"); let _t10 = ErrECollate; scanInner(mem, p, _t9, _t10) };
            let seq_2: vg::Slice<i32> = _t8.0;
            let ok_2: bool = _t8.1;
            if (!ok_2) {
                return (item, false);
            }
            if (!{ let _t11 = seq_2; localeIsCollatingElement(loc, _t11) }) {
                let _ = { let _t12 = ErrECollate; let _t13 = bracketStart; fail(p, _t12, _t13) };
                return (item, false);
            }
            item.kind = itemEquiv;
            item.seq = seq_2;
            return (item, true);
        }
        if (inner == 58u8) {
            let _t14 = { let _t15 = vg::lit(b":]"); let _t16 = ErrECType; scanInner(mem, p, _t15, _t16) };
            let seq_3: vg::Slice<i32> = _t14.0;
            let ok_3: bool = _t14.1;
            if (!ok_3) {
                return (item, false);
            }
            let _t17 = classByName(runesToString(mem, seq_3));
            let class: u8 = _t17.0;
            let ok2: bool = _t17.1;
            if (!ok2) {
                let _ = { let _t18 = ErrECType; let _t19 = bracketStart; fail(p, _t18, _t19) };
                return (item, false);
            }
            item.kind = itemClass;
            item.class = class;
            return (item, true);
        }
    }
    let r: i32 = nextRune(p);
    if (r < 0i32) {
        return (item, false);
    }
    item.kind = itemChar;
    item.r = r;
    return (item, true);
}

pub fn runesToString(mem: &vg::Arena, seq: vg::Slice<i32>) -> vg::Str {
    let mut out: vg::Slice<u8> = vg::make_cap::<u8>(mem, 0i64, seq.len);
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < seq.len) {
            '_c1: {
                let r: i32 = seq.get(i);
                if (r < 128i32) {
                    out = vg::append(mem, out, ((r) as u8));
                } else {
                    if (r < 2048i32) {
                        out = vg::append(mem, vg::append(mem, out, (((192i32 | (r >> 6i64))) as u8)), (((128i32 | (r & 63i32))) as u8));
                    } else {
                        if (r < 65536i32) {
                            out = vg::append(mem, vg::append(mem, vg::append(mem, out, (((224i32 | (r >> 12i64))) as u8)), (((128i32 | ((r >> 6i64) & 63i32))) as u8)), (((128i32 | (r & 63i32))) as u8));
                        } else {
                            out = vg::append(mem, vg::append(mem, vg::append(mem, vg::append(mem, out, (((240i32 | (r >> 18i64))) as u8)), (((128i32 | ((r >> 12i64) & 63i32))) as u8)), (((128i32 | ((r >> 6i64) & 63i32))) as u8)), (((128i32 | (r & 63i32))) as u8));
                        }
                    }
                }
            }
            i += 1i64;
        }
    }
    return vg::str_from_bytes(mem, out);
}

pub fn scanInner(mem: &vg::Arena, p: &mut parser, closer: vg::Str, emptyCode: i32) -> (vg::Slice<i32>, bool) {
    let start: i64 = p.pos;
    p.pos += 2i64;
    let mut end: i64 = (1i64).wrapping_neg();
    {
        let mut i: i64 = p.pos;
        '_b1: while ((i + 1i64) < p.src.len) {
            '_c1: {
                if ((p.src.byte(i) == closer.byte(0i64)) && (p.src.byte((i + 1i64)) == closer.byte(1i64))) {
                    end = i;
                    break '_b1;
                }
            }
            i += 1i64;
        }
    }
    if (end < 0i64) {
        let _ = { let _t2 = ErrEBrack; let _t3 = start; fail(p, _t2, _t3) };
        return (vg::zero::<vg::Slice<i32>>(), false);
    }
    if (end == p.pos) {
        let _ = { let _t4 = emptyCode; let _t5 = start; fail(p, _t4, _t5) };
        return (vg::zero::<vg::Slice<i32>>(), false);
    }
    let mut content: vg::Slice<i32> = vg::make_cap::<i32>(mem, 0i64, (end - p.pos));
    let mut at: i64 = p.pos;
    while (at < end) {
        let _t6 = decodeRuneAt(p.src, at);
        let r: i32 = _t6.0;
        let size: i64 = _t6.1;
        if (r < 0i32) {
            let _ = { let _t7 = ErrBadPat; let _t8 = start; fail(p, _t7, _t8) };
            return (vg::zero::<vg::Slice<i32>>(), false);
        }
        content = vg::append(mem, content, r);
        at += size;
    }
    p.pos = (end + 2i64);
    return (content, true);
}

pub fn sortRanges(mem: &vg::Arena, rr: vg::Slice<runeRange>) {
    let n: i64 = rr.len;
    if (n < 2i64) {
        return;
    }
    let tmp: vg::Slice<runeRange> = vg::make::<runeRange>(mem, n);
    {
        let mut width: i64 = 1i64;
        '_b1: while (width < n) {
            '_c1: {
                {
                    let mut lo: i64 = 0i64;
                    '_b2: while (lo < n) {
                        '_c2: {
                            let mid: i64 = std::cmp::min((lo + width), n);
                            let hi: i64 = std::cmp::min((lo + (2i64 * width)), n);
                            let mut i: i64 = lo;
                            let mut j: i64 = mid;
                            let mut w: i64 = lo;
                            while ((i < mid) && (j < hi)) {
                                let less: bool = ((rr.get(i).lo < rr.get(j).lo) || ((rr.get(i).lo == rr.get(j).lo) && (rr.get(i).hi <= rr.get(j).hi)));
                                if less {
                                    tmp.set(w, rr.get(i));
                                    i += 1i64;
                                } else {
                                    tmp.set(w, rr.get(j));
                                    j += 1i64;
                                }
                                w += 1i64;
                            }
                            while (i < mid) {
                                tmp.set(w, rr.get(i));
                                i += 1i64;
                                w += 1i64;
                            }
                            while (j < hi) {
                                tmp.set(w, rr.get(j));
                                j += 1i64;
                                w += 1i64;
                            }
                        }
                        lo += (2i64 * width);
                    }
                }
                let _ = vg::vcopy(rr, tmp);
            }
            width *= 2i64;
        }
    }
}

pub fn finalizeBracket(mem: &vg::Arena, b: &mut bracketSet, loc: &mut Locale) {
    if ((!b.negated) && ((b.elems.len > 0i64) || (b.equivs.len > 0i64))) {
        {
            let mut i: i64 = 0i64;
            '_b1: while (i < b.elems.len) {
                '_c1: {
                    b.multiLens |= (1u16 << b.elems.get(i).len);
                }
                i += 1i64;
            }
        }
        if (b.equivs.len > 0i64) {
            {
                let mut length: i64 = 2i64;
                '_b2: while (length <= localeMaxElementLength(loc)) {
                    '_c2: {
                        b.multiLens |= (1u16 << length);
                    }
                    length += 1i64;
                }
            }
        }
    }
    if (b.ranges.len < 2i64) {
        return;
    }
    sortRanges(mem, b.ranges);
    let mut w: i64 = 0i64;
    {
        let mut i_2: i64 = 1i64;
        '_b3: while (i_2 < b.ranges.len) {
            '_c3: {
                if (b.ranges.get(i_2).lo <= (b.ranges.get(w).hi + 1i32)) {
                    if (b.ranges.get(i_2).hi > b.ranges.get(w).hi) {
                        b.ranges.update(w, b.ranges.get(i_2).hi, |mut _t5, _t4| { _t5.hi = _t4; _t5 });
                    }
                } else {
                    w += 1i64;
                    b.ranges.set(w, b.ranges.get(i_2));
                }
            }
            i_2 += 1i64;
        }
    }
    b.ranges = b.ranges.head((w + 1i64));
}

pub fn bracketInRanges(brs: vg::Slice<bracketSet>, bi: i32, c: i32) -> bool {
    let mut low: i64 = 0i64;
    let mut high: i64 = brs.get(((bi) as i64)).ranges.len;
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        if (brs.get(((bi) as i64)).ranges.get(middle).hi < c) {
            low = (middle + 1i64);
        } else {
            if (brs.get(((bi) as i64)).ranges.get(middle).lo > c) {
                high = middle;
            } else {
                return true;
            }
        }
    }
    return false;
}

pub fn bracketPositiveSingle(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale, c: i32) -> bool {
    if bracketInRanges(brs, bi, c) {
        return true;
    }
    if ((brs.get(((bi) as i64)).classMask != 0u16) && (({ let _t1 = c; localeClassMask(loc, _t1) } & brs.get(((bi) as i64)).classMask) != 0u16)) {
        return true;
    }
    let mut single: [i32; (1) as usize] = vg::zero();
    single[(0i64) as usize] = c;
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < brs.get(((bi) as i64)).equivs.len) {
            '_c2: {
                if { let _t3 = vg::arr_slice(&raw mut single, 0i64, ((1) as i64)); let _t4 = brs.get(((bi) as i64)).equivs.get(i); localePrimaryEqual(loc, _t3, _t4) } {
                    return true;
                }
            }
            i += 1i64;
        }
    }
    return false;
}

pub fn bracketMatchesOne(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale, c: i32) -> bool {
    if (c < 0i32) {
        return false;
    }
    if ((brs.get(((bi) as i64)).negated && brs.get(((bi) as i64)).nlMode) && (c == 10i32)) {
        return false;
    }
    let want: bool = (!brs.get(((bi) as i64)).negated);
    if ({ let _t1 = brs; let _t2 = bi; let _t3 = c; bracketPositiveSingle(_t1, _t2, loc, _t3) } == want) {
        return true;
    }
    if (!brs.get(((bi) as i64)).icase) {
        return false;
    }
    let mut buf: preimageBuf = vg::zero();
    { let _t4 = c; localeCasePreimages(loc, &mut buf, _t4) };
    {
        let mut i: i64 = 0i64;
        '_b5: while (i < buf.n) {
            '_c5: {
                if ({ let _t6 = brs; let _t7 = bi; let _t8 = buf.r[(i) as usize]; bracketPositiveSingle(_t6, _t7, loc, _t8) } == want) {
                    return true;
                }
            }
            i += 1i64;
        }
    }
    return false;
}

pub fn counterpartMatch(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale, t: i32, e: i32) -> bool {
    if (t == e) {
        return true;
    }
    if (!brs.get(((bi) as i64)).icase) {
        return false;
    }
    return ((t == { let _t1 = e; localeToUpper(loc, _t1) }) || (t == { let _t2 = e; localeToLower(loc, _t2) }));
}

pub fn bracketHasMultiMembers(brs: vg::Slice<bracketSet>, bi: i32) -> bool {
    return ((!brs.get(((bi) as i64)).negated) && ((brs.get(((bi) as i64)).elems.len > 0i64) || (brs.get(((bi) as i64)).equivs.len > 0i64)));
}

pub fn bracketMatchesMulti(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale, t: vg::Slice<i32>) -> bool {
    if (t.len > localeMaxElementLength(loc)) {
        return false;
    }
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < brs.get(((bi) as i64)).elems.len) {
            '_c1: {
                if (brs.get(((bi) as i64)).elems.get(i).len != t.len) {
                    break '_c1;
                }
                let mut all: bool = true;
                {
                    let mut k: i64 = 0i64;
                    '_b2: while (k < t.len) {
                        '_c2: {
                            if (!{ let _t3 = brs; let _t4 = bi; let _t5 = t.get(k); let _t6 = brs.get(((bi) as i64)).elems.get(i).get(k); counterpartMatch(_t3, _t4, loc, _t5, _t6) }) {
                                all = false;
                                break '_b2;
                            }
                        }
                        k += 1i64;
                    }
                }
                if all {
                    return true;
                }
            }
            i += 1i64;
        }
    }
    if (brs.get(((bi) as i64)).equivs.len == 0i64) {
        return false;
    }
    let mut candidate: elemBuf = vg::zero();
    return { let _t7 = brs; let _t8 = bi; let _t9 = t; let _t10 = 0i64; equivCandidate(_t7, _t8, loc, _t9, &mut candidate, _t10) };
}

pub fn equivCandidate(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale, t: vg::Slice<i32>, candidate: &mut elemBuf, at: i64) -> bool {
    if (at == t.len) {
        if (!{ let _t1 = vg::arr_slice(&raw mut candidate.r, 0i64, t.len); localeIsCollatingElement(loc, _t1) }) {
            return false;
        }
        {
            let mut i: i64 = 0i64;
            '_b2: while (i < brs.get(((bi) as i64)).equivs.len) {
                '_c2: {
                    if { let _t3 = vg::arr_slice(&raw mut candidate.r, 0i64, t.len); let _t4 = brs.get(((bi) as i64)).equivs.get(i); localePrimaryEqual(loc, _t3, _t4) } {
                        return true;
                    }
                }
                i += 1i64;
            }
        }
        return false;
    }
    candidate.r[(at) as usize] = t.get(at);
    if { let _t5 = brs; let _t6 = bi; let _t7 = t; let _t8 = (at + 1i64); equivCandidate(_t5, _t6, loc, _t7, candidate, _t8) } {
        return true;
    }
    if (!brs.get(((bi) as i64)).icase) {
        return false;
    }
    let mut buf: preimageBuf = vg::zero();
    { let _t9 = t.get(at); localeCasePreimages(loc, &mut buf, _t9) };
    {
        let mut i_2: i64 = 0i64;
        '_b10: while (i_2 < buf.n) {
            '_c10: {
                candidate.r[(at) as usize] = buf.r[(i_2) as usize];
                if { let _t11 = brs; let _t12 = bi; let _t13 = t; let _t14 = (at + 1i64); equivCandidate(_t11, _t12, loc, _t13, candidate, _t14) } {
                    return true;
                }
            }
            i_2 += 1i64;
        }
    }
    return false;
}

pub fn bracketMatchesSpan(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale, runes: vg::Slice<i32>, i: i64, j: i64) -> bool {
    let k: i64 = (j - i);
    if (k < 1i64) {
        return false;
    }
    if (k == 1i64) {
        return { let _t1 = brs; let _t2 = bi; let _t3 = runes.get(i); bracketMatchesOne(_t1, _t2, loc, _t3) };
    }
    if (!bracketHasMultiMembers(brs, bi)) {
        return false;
    }
    return { let _t4 = brs; let _t5 = bi; let _t6 = runes.sub(i, j); bracketMatchesMulti(_t4, _t5, loc, _t6) };
}

pub fn bracketMinChars(brs: vg::Slice<bracketSet>, bi: i32, loc: &mut Locale) -> i64 {
    if (((brs.get(((bi) as i64)).negated || (brs.get(((bi) as i64)).ranges.len > 0i64)) || (brs.get(((bi) as i64)).classMask != 0u16)) || ((brs.get(((bi) as i64)).elems.len == 0i64) && (brs.get(((bi) as i64)).equivs.len == 0i64))) {
        return 1i64;
    }
    let mut best: i64 = lenInf;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < brs.get(((bi) as i64)).elems.len) {
            '_c1: {
                best = std::cmp::min(best, brs.get(((bi) as i64)).elems.get(i).len);
            }
            i += 1i64;
        }
    }
    {
        let mut i_2: i64 = 0i64;
        '_b2: while (i_2 < brs.get(((bi) as i64)).equivs.len) {
            '_c2: {
                best = std::cmp::min(best, { let _t3 = brs.get(((bi) as i64)).equivs.get(i_2); localeMinEquivLength(loc, _t3) });
            }
            i_2 += 1i64;
        }
    }
    return best;
}

pub fn capStep(s: &mut capSolver) -> bool {
    s.work += 1i64;
    if (s.work > capWorkLimit) {
        s.failed = true;
    }
    return (!s.failed);
}

pub fn seedArenas(mem: &vg::Arena, s: &mut capSolver) {
    let scratch: ptree = vg::zero();
    s.trees = vg::append(mem, s.trees, scratch);
    s.kidStore = vg::append(mem, s.kidStore, (1i32).wrapping_neg());
}

pub fn newTree(mem: &vg::Arena, s: &mut capSolver, t: ptree) -> i32 {
    if (s.failed || (s.trees.len >= solverArenaLimit)) {
        s.failed = true;
        s.trees.set(0i64, t);
        return 0i32;
    }
    s.trees = vg::append(mem, s.trees, t);
    return (((s.trees.len - 1i64)) as i32);
}

pub fn kidAlloc(mem: &vg::Arena, s: &mut capSolver, length: i64) -> i64 {
    if (s.failed || ((s.kidStore.len + length) > solverArenaLimit)) {
        s.failed = true;
        while (s.kidStore.len < length) {
            s.kidStore = vg::append(mem, s.kidStore, (1i32).wrapping_neg());
        }
        return 0i64;
    }
    let off: i64 = s.kidStore.len;
    if ((off + length) <= s.kidStore.cap) {
        s.kidStore = s.kidStore.head((off + length));
        return off;
    }
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < length) {
            '_c1: {
                s.kidStore = vg::append(mem, s.kidStore, (1i32).wrapping_neg());
            }
            i += 1i64;
        }
    }
    return off;
}

pub fn kidAlloc1(mem: &vg::Arena, s: &mut capSolver, t: i32) -> i64 {
    let off: i64 = { let _t1 = 1i64; kidAlloc(mem, s, _t1) };
    s.kidStore.set(off, t);
    return off;
}

pub fn kidPrepend(mem: &vg::Arena, s: &mut capSolver, head: i32, tailOff: i64, tailLen: i64) -> i64 {
    let off: i64 = { let _t1 = (tailLen + 1i64); kidAlloc(mem, s, _t1) };
    s.kidStore.set(off, head);
    let _ = vg::vcopy(s.kidStore.sub((off + 1i64), ((off + 1i64) + tailLen)), s.kidStore.sub(tailOff, (tailOff + tailLen)));
    return off;
}

pub fn setMatch(caps: vg::Slice<Match>, idx: i64, so: i64, eo: i64) {
    caps.update(idx, so, |mut _t2, _t1| { _t2.So = _t1; _t2 });
    caps.update(idx, eo, |mut _t4, _t3| { _t4.Eo = _t3; _t4 });
}

pub fn addCounters(s: &mut capSolver, re: &mut Regexp, t: i32, out: vg::Slice<i64>) {
    let ni: i32 = s.trees.get(((t) as i64)).n;
    if ((re.nodes.get(((ni) as i64)).op == opRepeat) && re.nodes.get(((ni) as i64)).minimal) {
        out.update(re.nodes.get(((ni) as i64)).index, (((s.trees.get(((t) as i64)).j - s.trees.get(((t) as i64)).i)) as i64), |mut _t2, _t1| { _t2 += _t1; _t2 });
    }
    let off: i64 = ((s.trees.get(((t) as i64)).kidsOff) as i64);
    let count: i64 = ((s.trees.get(((t) as i64)).kidsLen) as i64);
    {
        let mut k: i64 = 0i64;
        '_b3: while (k < count) {
            '_c3: {
                { let _t4 = s.kidStore.get((off + k)); let _t5 = out; addCounters(s, re, _t4, _t5) };
            }
            k += 1i64;
        }
    }
}

pub fn structCmp(s: &mut capSolver, re: &mut Regexp, a: i32, b: i32) -> i64 {
    let spanA: i64 = (((s.trees.get(((a) as i64)).j - s.trees.get(((a) as i64)).i)) as i64);
    let spanB: i64 = (((s.trees.get(((b) as i64)).j - s.trees.get(((b) as i64)).i)) as i64);
    let ni: i32 = s.trees.get(((a) as i64)).n;
    if (spanA != spanB) {
        if ((re.nodes.get(((ni) as i64)).op == opRepeat) && re.nodes.get(((ni) as i64)).minimal) {
            return (spanA - spanB);
        }
        return (spanB - spanA);
    }
    let offA: i64 = ((s.trees.get(((a) as i64)).kidsOff) as i64);
    let offB: i64 = ((s.trees.get(((b) as i64)).kidsOff) as i64);
    let lenA: i64 = ((s.trees.get(((a) as i64)).kidsLen) as i64);
    let lenB: i64 = ((s.trees.get(((b) as i64)).kidsLen) as i64);
    {
        let _t1 = re.nodes.get(((ni) as i64)).op;
        if _t1 == opAlt {
            if (s.trees.get(((a) as i64)).branch != s.trees.get(((b) as i64)).branch) {
                return (((s.trees.get(((a) as i64)).branch - s.trees.get(((b) as i64)).branch)) as i64);
            }
            return { let _t2 = s.kidStore.get(offA); let _t3 = s.kidStore.get(offB); structCmp(s, re, _t2, _t3) };
        } else if _t1 == opConcat || _t1 == opGroup {
            {
                let mut idx: i64 = 0i64;
                '_b4: while (idx < lenA) {
                    '_c4: {
                        let c: i64 = { let _t5 = s.kidStore.get((offA + idx)); let _t6 = s.kidStore.get((offB + idx)); structCmp(s, re, _t5, _t6) };
                        if (c != 0i64) {
                            return c;
                        }
                    }
                    idx += 1i64;
                }
            }
        } else if _t1 == opRepeat {
            let limit: i64 = std::cmp::min(lenA, lenB);
            {
                let mut idx_2: i64 = 0i64;
                '_b7: while (idx_2 < limit) {
                    '_c7: {
                        let ka: i32 = s.kidStore.get((offA + idx_2));
                        let kb: i32 = s.kidStore.get((offB + idx_2));
                        let sa: i64 = (((s.trees.get(((ka) as i64)).j - s.trees.get(((ka) as i64)).i)) as i64);
                        let sb: i64 = (((s.trees.get(((kb) as i64)).j - s.trees.get(((kb) as i64)).i)) as i64);
                        if (sa != sb) {
                            if re.nodes.get(((ni) as i64)).minimal {
                                return (sa - sb);
                            }
                            return (sb - sa);
                        }
                    }
                    idx_2 += 1i64;
                }
            }
            if (lenA != lenB) {
                return (lenA - lenB);
            }
            {
                let mut idx_3: i64 = 0i64;
                '_b8: while (idx_3 < lenA) {
                    '_c8: {
                        let c_2: i64 = { let _t9 = s.kidStore.get((offA + idx_3)); let _t10 = s.kidStore.get((offB + idx_3)); structCmp(s, re, _t9, _t10) };
                        if (c_2 != 0i64) {
                            return c_2;
                        }
                    }
                    idx_3 += 1i64;
                }
            }
        }
    }
    return 0i64;
}

pub fn cmpCand(s: &mut capSolver, re: &mut Regexp, a: i32, b: i32) -> i64 {
    if s.failed {
        return 1i64;
    }
    if (re.minSlots > 0i64) {
        {
            let mut idx: i64 = 0i64;
            '_b1: while (idx < re.minSlots) {
                '_c1: {
                    s.ctrA.set(idx, 0i64);
                    s.ctrB.set(idx, 0i64);
                }
                idx += 1i64;
            }
        }
        { let _t2 = a; let _t3 = s.ctrA; addCounters(s, re, _t2, _t3) };
        { let _t4 = b; let _t5 = s.ctrB; addCounters(s, re, _t4, _t5) };
        {
            let mut idx_2: i64 = 0i64;
            '_b6: while (idx_2 < re.minSlots) {
                '_c6: {
                    if (s.ctrA.get(idx_2) != s.ctrB.get(idx_2)) {
                        return (s.ctrA.get(idx_2) - s.ctrB.get(idx_2));
                    }
                }
                idx_2 += 1i64;
            }
        }
    }
    return { let _t7 = a; let _t8 = b; structCmp(s, re, _t7, _t8) };
}

pub fn bestParse(mem: &vg::Arena, s: &mut capSolver, re: &mut Regexp, d: &mut decoded, ni: i32, i: i64, j: i64) -> i32 {
    let span: i64 = (j - i);
    if ((span < re.nodes.get(((ni) as i64)).minL) || ((re.nodes.get(((ni) as i64)).maxL < lenInf) && (span > re.nodes.get(((ni) as i64)).maxL))) {
        return (1i32).wrapping_neg();
    }
    let mut key: memoKey = vg::zero();
    key.a = ni;
    key.b = ((i) as i32);
    key.c = ((j) as i32);
    let _t1 = { let _t2 = key; memoGet(&mut s.memo, _t2) };
    let cached: memoVal = _t1.0;
    let hit: bool = _t1.1;
    if hit {
        return cached.x;
    }
    if (!capStep(s)) {
        return (1i32).wrapping_neg();
    }
    let mut best: i32 = (1i32).wrapping_neg();
    {
        let _t3 = re.nodes.get(((ni) as i64)).op;
        if _t3 == opChar {
            if ((j == (i + 1i64)) && { let _t4 = ni; let _t5 = d.runes.get(i); charMatches(re, _t4, _t5) }) {
                best = { let _t6 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), ..vg::zero() }; newTree(mem, s, _t6) };
            }
        } else if _t3 == opAny {
            if ((j == (i + 1i64)) && { let _t7 = d.runes.get(i); anyMatches(re, _t7) }) {
                best = { let _t8 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), ..vg::zero() }; newTree(mem, s, _t8) };
            }
        } else if _t3 == opBracket {
            if { let _t9 = re.brackets; let _t10 = re.nodes.get(((ni) as i64)).br; let _t11 = d.runes; let _t12 = i; let _t13 = j; bracketMatchesSpan(_t9, _t10, &mut re.loc, _t11, _t12, _t13) } {
                best = { let _t14 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), ..vg::zero() }; newTree(mem, s, _t14) };
            }
        } else if _t3 == opBOL {
            if ((j == i) && { let _t15 = i; let _t16 = s.eflags; atBOL(re, d, _t15, _t16) }) {
                best = { let _t17 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), ..vg::zero() }; newTree(mem, s, _t17) };
            }
        } else if _t3 == opEOL {
            if ((j == i) && { let _t18 = i; let _t19 = s.eflags; atEOL(re, d, _t18, _t19) }) {
                best = { let _t20 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), ..vg::zero() }; newTree(mem, s, _t20) };
            }
        } else if _t3 == opGroup {
            let sub: i32 = { let _t21 = re.nodes.get(((ni) as i64)).ch.get(0i64); let _t22 = i; let _t23 = j; bestParse(mem, s, re, d, _t21, _t22, _t23) };
            if (sub >= 0i32) {
                let off: i64 = { let _t24 = sub; kidAlloc1(mem, s, _t24) };
                best = { let _t25 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), kidsOff: ((off) as i32), kidsLen: 1i32, ..vg::zero() }; newTree(mem, s, _t25) };
            }
        } else if _t3 == opAlt {
            {
                let mut bi: i64 = 0i64;
                '_b26: while (bi < re.nodes.get(((ni) as i64)).ch.len) {
                    '_c26: {
                        let sub_2: i32 = { let _t27 = re.nodes.get(((ni) as i64)).ch.get(bi); let _t28 = i; let _t29 = j; bestParse(mem, s, re, d, _t27, _t28, _t29) };
                        if (sub_2 < 0i32) {
                            break '_c26;
                        }
                        let off_2: i64 = { let _t30 = sub_2; kidAlloc1(mem, s, _t30) };
                        let candidate: i32 = { let _t31 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), branch: ((bi) as i32), kidsOff: ((off_2) as i32), kidsLen: 1i32 }; newTree(mem, s, _t31) };
                        if ((best < 0i32) || ({ let _t32 = candidate; let _t33 = best; cmpCand(s, re, _t32, _t33) } < 0i64)) {
                            best = candidate;
                        }
                    }
                    bi += 1i64;
                }
            }
        } else if _t3 == opConcat {
            let _t34 = { let _t35 = ni; let _t36 = 0i64; let _t37 = i; let _t38 = j; bestConcat(mem, s, re, d, _t35, _t36, _t37, _t38) };
            let off_3: i64 = _t34.0;
            let count: i64 = _t34.1;
            if (off_3 >= 0i64) {
                best = { let _t39 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), kidsOff: ((off_3) as i32), kidsLen: ((count) as i32), ..vg::zero() }; newTree(mem, s, _t39) };
            }
        } else if _t3 == opRepeat {
            if ((i == j) && (re.nodes.get(((ni) as i64)).min == 0i64)) {
                let mut sub_3: i32 = (1i32).wrapping_neg();
                if (re.nodes.get(((ni) as i64)).max != 0i64) {
                    sub_3 = { let _t40 = re.nodes.get(((ni) as i64)).ch.get(0i64); let _t41 = i; let _t42 = i; bestParse(mem, s, re, d, _t40, _t41, _t42) };
                }
                if (sub_3 >= 0i32) {
                    let off_4: i64 = { let _t43 = sub_3; kidAlloc1(mem, s, _t43) };
                    best = { let _t44 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), kidsOff: ((off_4) as i32), kidsLen: 1i32, ..vg::zero() }; newTree(mem, s, _t44) };
                } else {
                    best = { let _t45 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), ..vg::zero() }; newTree(mem, s, _t45) };
                }
            } else {
                let win: repWin = { let _t46 = ni; let _t47 = i; let _t48 = j; let _t49 = 0i64; let _t50 = false; bestRep(mem, s, re, d, _t46, _t47, _t48, _t49, _t50) };
                if win.ok {
                    best = { let _t51 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), kidsOff: ((win.off) as i32), kidsLen: ((win.length) as i32), ..vg::zero() }; newTree(mem, s, _t51) };
                }
            }
        }
    }
    let mut val: memoVal = vg::zero();
    val.x = best;
    { let _t52 = key; let _t53 = val; memoPut(mem, &mut s.memo, _t52, _t53) };
    return best;
}

pub fn bestConcat(mem: &vg::Arena, s: &mut capSolver, re: &mut Regexp, d: &mut decoded, ni: i32, idx: i64, i: i64, j: i64) -> (i64, i64) {
    if (idx == (re.nodes.get(((ni) as i64)).ch.len - 1i64)) {
        let sub: i32 = { let _t1 = re.nodes.get(((ni) as i64)).ch.get(idx); let _t2 = i; let _t3 = j; bestParse(mem, s, re, d, _t1, _t2, _t3) };
        if (sub < 0i32) {
            return ((1i64).wrapping_neg(), 0i64);
        }
        return ({ let _t4 = sub; kidAlloc1(mem, s, _t4) }, 1i64);
    }
    let mut key: memoKey = vg::zero();
    key.a = ni;
    key.b = ((idx) as i32);
    key.c = ((i) as i32);
    key.d = ((j) as i32);
    let _t5 = { let _t6 = key; memoGet(&mut s.cmemo, _t6) };
    let cached: memoVal = _t5.0;
    let hit: bool = _t5.1;
    if hit {
        return (((cached.x) as i64), ((cached.y) as i64));
    }
    if (!capStep(s)) {
        return ((1i64).wrapping_neg(), 0i64);
    }
    let mut bestOff: i64 = (1i64).wrapping_neg();
    let mut bestLen: i64 = 0i64;
    let mut bestTree: i32 = (1i32).wrapping_neg();
    let head0: i32 = re.nodes.get(((ni) as i64)).ch.get(idx);
    let mut lo: i64 = (i + re.nodes.get(((head0) as i64)).minL);
    if (re.nodes.get(((ni) as i64)).sufMax.get((idx + 1i64)) < lenInf) {
        lo = std::cmp::max(lo, (j - re.nodes.get(((ni) as i64)).sufMax.get((idx + 1i64))));
    }
    let mut hi: i64 = (j - re.nodes.get(((ni) as i64)).sufMin.get((idx + 1i64)));
    if (re.nodes.get(((head0) as i64)).maxL < lenInf) {
        hi = std::cmp::min(hi, (i + re.nodes.get(((head0) as i64)).maxL));
    }
    {
        let mut m: i64 = lo;
        '_b7: while (m <= hi) {
            '_c7: {
                if (!capStep(s)) {
                    return ((1i64).wrapping_neg(), 0i64);
                }
                let head: i32 = { let _t8 = head0; let _t9 = i; let _t10 = m; bestParse(mem, s, re, d, _t8, _t9, _t10) };
                if (head < 0i32) {
                    break '_c7;
                }
                let _t11 = { let _t12 = ni; let _t13 = (idx + 1i64); let _t14 = m; let _t15 = j; bestConcat(mem, s, re, d, _t12, _t13, _t14, _t15) };
                let tailOff: i64 = _t11.0;
                let tailLen: i64 = _t11.1;
                if (tailOff < 0i64) {
                    break '_c7;
                }
                let off: i64 = { let _t16 = head; let _t17 = tailOff; let _t18 = tailLen; kidPrepend(mem, s, _t16, _t17, _t18) };
                let candidate: i32 = { let _t19 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), kidsOff: ((off) as i32), kidsLen: (((tailLen + 1i64)) as i32), ..vg::zero() }; newTree(mem, s, _t19) };
                if ((bestTree < 0i32) || ({ let _t20 = candidate; let _t21 = bestTree; cmpCand(s, re, _t20, _t21) } < 0i64)) {
                    bestTree = candidate;
                    bestOff = off;
                    bestLen = (tailLen + 1i64);
                }
            }
            m += 1i64;
        }
    }
    let mut val: memoVal = vg::zero();
    val.x = ((bestOff) as i32);
    val.y = ((bestLen) as i32);
    { let _t22 = key; let _t23 = val; memoPut(mem, &mut s.cmemo, _t22, _t23) };
    return (bestOff, bestLen);
}

pub fn repTry(mem: &vg::Arena, s: &mut capSolver, re: &mut Regexp, ni: i32, i: i64, j: i64, off: i64, length: i64, best: &mut repBest) {
    let candidate: i32 = { let _t1 = ptree { n: ni, i: ((i) as i32), j: ((j) as i32), kidsOff: ((off) as i32), kidsLen: ((length) as i32), ..vg::zero() }; newTree(mem, s, _t1) };
    if ((!best.found) || ({ let _t2 = candidate; let _t3 = best.tree; cmpCand(s, re, _t2, _t3) } < 0i64)) {
        best.off = off;
        best.length = length;
        best.tree = candidate;
        best.found = true;
    }
}

pub fn bestRep(mem: &vg::Arena, s: &mut capSolver, re: &mut Regexp, d: &mut decoded, ni: i32, i: i64, j: i64, done: i64, hasEmpty: bool) -> repWin {
    let mut done_v: i64 = done;
    if ((re.nodes.get(((ni) as i64)).max == infinite) && (done_v > re.nodes.get(((ni) as i64)).min)) {
        done_v = re.nodes.get(((ni) as i64)).min;
    }
    let mut key: memoKey = vg::zero();
    key.a = ni;
    key.b = ((i) as i32);
    key.c = ((j) as i32);
    key.d = (((done_v << 1i64)) as i32);
    if hasEmpty {
        key.d |= 1i32;
    }
    let mut win: repWin = vg::zero();
    let _t1 = { let _t2 = key; memoGet(&mut s.rmemo, _t2) };
    let cached: memoVal = _t1.0;
    let hit: bool = _t1.1;
    if hit {
        win.off = ((cached.x) as i64);
        win.length = ((cached.y) as i64);
        win.ok = (cached.z != 0i32);
        return win;
    }
    if (!capStep(s)) {
        win.off = (1i64).wrapping_neg();
        return win;
    }
    let mut best: repBest = vg::zero();
    if (((i == j) && (done_v >= re.nodes.get(((ni) as i64)).min)) && ((!hasEmpty) || (done_v == re.nodes.get(((ni) as i64)).min))) {
        { let _t3 = ni; let _t4 = i; let _t5 = j; let _t6 = 0i64; let _t7 = 0i64; repTry(mem, s, re, _t3, _t4, _t5, _t6, _t7, &mut best) };
    }
    let canTake: bool = ((re.nodes.get(((ni) as i64)).max == infinite) || (done_v < re.nodes.get(((ni) as i64)).max));
    if (canTake && (!(hasEmpty && (done_v >= re.nodes.get(((ni) as i64)).min)))) {
        let child: i32 = re.nodes.get(((ni) as i64)).ch.get(0i64);
        let tailMin: i64 = satMul(std::cmp::max(((re.nodes.get(((ni) as i64)).min - done_v) - 1i64), 0i64), re.nodes.get(((child) as i64)).minL);
        let mut tailMax: i64 = lenInf;
        if (re.nodes.get(((ni) as i64)).max != infinite) {
            tailMax = satMul(((re.nodes.get(((ni) as i64)).max - done_v) - 1i64), re.nodes.get(((child) as i64)).maxL);
        } else {
            if (re.nodes.get(((child) as i64)).maxL == 0i64) {
                tailMax = 0i64;
            }
        }
        let mut lo: i64 = (i + re.nodes.get(((child) as i64)).minL);
        if (tailMax < lenInf) {
            lo = std::cmp::max(lo, (j - tailMax));
        }
        let mut hi: i64 = (j - tailMin);
        if (re.nodes.get(((child) as i64)).maxL < lenInf) {
            hi = std::cmp::min(hi, (i + re.nodes.get(((child) as i64)).maxL));
        }
        {
            let mut m: i64 = lo;
            '_b8: while (m <= hi) {
                '_c8: {
                    if ((m == i) && (done_v >= re.nodes.get(((ni) as i64)).min)) {
                        break '_c8;
                    }
                    if (!capStep(s)) {
                        win.off = (1i64).wrapping_neg();
                        return win;
                    }
                    let head: i32 = { let _t9 = child; let _t10 = i; let _t11 = m; bestParse(mem, s, re, d, _t9, _t10, _t11) };
                    if (head < 0i32) {
                        break '_c8;
                    }
                    let tail: repWin = { let _t12 = ni; let _t13 = m; let _t14 = j; let _t15 = (done_v + 1i64); let _t16 = (hasEmpty || (m == i)); bestRep(mem, s, re, d, _t12, _t13, _t14, _t15, _t16) };
                    if (!tail.ok) {
                        break '_c8;
                    }
                    let off: i64 = { let _t17 = head; let _t18 = tail.off; let _t19 = tail.length; kidPrepend(mem, s, _t17, _t18, _t19) };
                    { let _t20 = ni; let _t21 = i; let _t22 = j; let _t23 = off; let _t24 = (tail.length + 1i64); repTry(mem, s, re, _t20, _t21, _t22, _t23, _t24, &mut best) };
                }
                m += 1i64;
            }
        }
    }
    let mut val: memoVal = vg::zero();
    val.x = ((best.off) as i32);
    val.y = ((best.length) as i32);
    if best.found {
        val.z = 1i32;
    } else {
        val.x = (1i32).wrapping_neg();
    }
    { let _t25 = key; let _t26 = val; memoPut(mem, &mut s.rmemo, _t25, _t26) };
    win.off = ((val.x) as i64);
    win.length = ((val.y) as i64);
    win.ok = best.found;
    return win;
}

pub fn solveCaptures(mem: &vg::Arena, re: &mut Regexp, d: &mut decoded, so: i64, eo: i64, eflags: u32, caps: vg::Slice<Match>) -> Error {
    if re.onePass {
        {
            let mut idx: i64 = 0i64;
            '_b1: while (idx < caps.len) {
                '_c1: {
                    setMatch(caps, idx, (1i64).wrapping_neg(), (1i64).wrapping_neg());
                }
                idx += 1i64;
            }
        }
        setMatch(caps, 0i64, so, eo);
        if { let _t2 = re.root; let _t3 = so; let _t4 = eo; let _t5 = eflags; let _t6 = caps; onePassCaps(re, d, _t2, _t3, _t4, _t5, _t6) } {
            return noError();
        }
        return compileError(ErrESpace, (1i64).wrapping_neg());
    }
    let mut s: capSolver = vg::zero();
    s.eflags = eflags;
    s.ctrA = vg::make::<i64>(mem, re.minSlots);
    s.ctrB = vg::make::<i64>(mem, re.minSlots);
    seedArenas(mem, &mut s);
    let best: i32 = { let _t7 = re.root; let _t8 = so; let _t9 = eo; bestParse(mem, &mut s, re, d, _t7, _t8, _t9) };
    if s.failed {
        return compileError(ErrESpace, (1i64).wrapping_neg());
    }
    if (best < 0i32) {
        return compileError(ErrESpace, (1i64).wrapping_neg());
    }
    {
        let mut idx_2: i64 = 0i64;
        '_b10: while (idx_2 < caps.len) {
            '_c10: {
                setMatch(caps, idx_2, (1i64).wrapping_neg(), (1i64).wrapping_neg());
            }
            idx_2 += 1i64;
        }
    }
    setMatch(caps, 0i64, so, eo);
    { let _t11 = best; let _t12 = caps; assignCaps(re, &mut s, _t11, _t12) };
    return noError();
}

pub fn assignCaps(re: &mut Regexp, s: &mut capSolver, t: i32, caps: vg::Slice<Match>) {
    let ni: i32 = s.trees.get(((t) as i64)).n;
    if (re.nodes.get(((ni) as i64)).op == opGroup) {
        let gi: i64 = re.nodes.get(((ni) as i64)).index;
        {
            let mut k: i64 = 0i64;
            '_b1: while (k < re.nested.get(gi).len) {
                '_c1: {
                    setMatch(caps, ((re.nested.get(gi).get(k)) as i64), (1i64).wrapping_neg(), (1i64).wrapping_neg());
                }
                k += 1i64;
            }
        }
        setMatch(caps, gi, ((s.trees.get(((t) as i64)).i) as i64), ((s.trees.get(((t) as i64)).j) as i64));
    }
    let off: i64 = ((s.trees.get(((t) as i64)).kidsOff) as i64);
    let count: i64 = ((s.trees.get(((t) as i64)).kidsLen) as i64);
    {
        let mut k_2: i64 = 0i64;
        '_b2: while (k_2 < count) {
            '_c2: {
                { let _t3 = s.kidStore.get((off + k_2)); let _t4 = caps; assignCaps(re, s, _t3, _t4) };
            }
            k_2 += 1i64;
        }
    }
}

pub fn cAdd(a: i64, b: i64) -> i64 {
    if (a > (contractCap - b)) {
        return contractCap;
    }
    return (a + b);
}

pub fn cMul(a: i64, b: i64) -> i64 {
    if ((a == 0i64) || (b == 0i64)) {
        return 0i64;
    }
    if (a > (contractCap).wrapping_div(b)) {
        return contractCap;
    }
    return (a * b);
}

pub fn ContractHeapBytes(c: &mut Contract) -> i64 {
    let mut capture: i64 = 0i64;
    if c.HasOnePass {
        capture = c.OnePass.HeapBytes;
    }
    if c.HasSolver {
        capture = std::cmp::max(capture, c.Solver.HeapBytes);
    }
    return cAdd(c.Matcher.HeapBytes, capture);
}

pub fn ContractStackBytes(c: &mut Contract) -> i64 {
    let mut deepest: i64 = c.Matcher.StackBytes;
    if c.HasOnePass {
        deepest = std::cmp::max(deepest, c.OnePass.StackBytes);
    }
    if c.HasSolver {
        deepest = std::cmp::max(deepest, c.Solver.StackBytes);
    }
    return deepest;
}

pub fn ContractSteps(c: &mut Contract) -> i64 {
    let mut steps: i64 = c.Matcher.Steps;
    if c.HasOnePass {
        steps = cAdd(steps, c.OnePass.Steps);
    }
    if c.HasSolver {
        steps = cAdd(steps, c.Solver.Steps);
    }
    return steps;
}

pub fn ContractFor(re: &mut Regexp, maxInput: i64) -> Contract {
    let length: i64 = ((std::cmp::min(std::cmp::max(maxInput, 0i64), subjectLimit)) as i64);
    let atom: i64 = atomCost(re);
    let mut c: Contract = vg::zero();
    c.MaxInput = ((length) as i64);
    c.Matcher = { let _t1 = length; let _t2 = atom; matcherContract(re, _t1, _t2) };
    if ((re.progOK && (re.nsub > 0i64)) && ((re.flags & FlagNoSub) == 0u32)) {
        if re.onePass {
            c.OnePass = { let _t3 = length; let _t4 = atom; onePassContract(re, _t3, _t4) };
            c.HasOnePass = true;
        } else {
            c.Solver = { let _t5 = length; let _t6 = atom; solverContract(re, _t5, _t6) };
            c.HasSolver = true;
        }
    }
    return c;
}

pub fn matcherContract(re: &mut Regexp, length: i64, atom: i64) -> BackendContract {
    let mut b: BackendContract = vg::zero();
    if (!re.progOK) {
        b.StackBytes = matcherStackBytes;
        b.Steps = cAdd(cMul(2i64, length), 2i64);
        return b;
    }
    let n: i64 = ((re.prog.ins.len) as i64);
    let k: i64 = ((re.minSlots) as i64);
    let mut ring: i64 = 2i64;
    if re.prog.multi {
        ring = 9i64;
    }
    let heap: i64 = workspaceHeapBound(n, k, ring);
    let weight: i64 = cAdd(cMul(4i64, k), 22i64);
    let perTest: i64 = cAdd(cAdd(atom, 2i64), cMul((ring - 1i64), cAdd(cMul(4i64, k), 12i64)));
    let mut perBoundary: i64 = cMul(weight, cMul((n + 1i64), (n + 1i64)));
    perBoundary = cAdd(perBoundary, cMul(n, perTest));
    perBoundary = cAdd(perBoundary, cAdd(cAdd(n, cMul(2i64, k)), cAdd(cMul(4i64, ring), 38i64)));
    let mut boundaries: i64 = cAdd(length, 1i64);
    if (re.prog.depth >= 0i64) {
        boundaries = std::cmp::min(boundaries, (((re.prog.depth) as i64) + 3i64));
    }
    let steps: i64 = cAdd(cAdd((24i64 + ring), length), cMul(boundaries, perBoundary));
    let stack: i64 = 6144i64;
    b.HeapBytes = heap;
    b.StackBytes = stack;
    b.Steps = steps;
    return b;
}

pub fn captureHeap(re: &mut Regexp, length: i64) -> i64 {
    let window: i64 = cAdd(cMul(4i64, length), cMul(8i64, cAdd(length, 1i64)));
    let spans: i64 = cMul(matchBytes, cAdd(((re.nsub) as i64), 1i64));
    let payload: i64 = cAdd(window, spans);
    let allowance: i64 = 24576i64;
    return cAdd(cAdd(payload, allowance), 64i64);
}

pub fn onePassContract(re: &mut Regexp, length: i64, atom: i64) -> BackendContract {
    let mut b: BackendContract = vg::zero();
    let perVisit: i64 = cAdd(atom, (((re.nsub) as i64) + 1i64));
    b.HeapBytes = { let _t1 = length; captureHeap(re, _t1) };
    b.StackBytes = cMul(cAdd(astHeight(re.nodes, re.root), 10i64), frameBytes);
    b.Steps = cMul(cMul(astSize(re.nodes, re.root), cAdd(length, 2i64)), perVisit);
    return b;
}

pub fn solverContract(re: &mut Regexp, length: i64, atom: i64) -> BackendContract {
    let mut b: BackendContract = vg::zero();
    let depth: i64 = solverDepth(re.nodes, re.root, length);
    let structural: i64 = std::cmp::min(solverSteps(re.nodes, re.root, length), cAdd(capWorkLimit, depth));
    let tree: i64 = treeNodes(re.nodes, re.root, length);
    let perStep: i64 = cAdd(atom, cAdd(cMul(2i64, tree), (cMul(2i64, ((re.minSlots) as i64)) + 4i64)));
    let steps: i64 = cAdd(cMul(structural, perStep), cMul(tree, (((re.nsub) as i64) + 2i64)));
    let perAlloc: i64 = cAdd(224i64, cMul(kidBytes, cAdd(solverFanout(re.nodes, re.root, length), 1i64)));
    let mut heap: i64 = cAdd(cMul(structural, perAlloc), cMul(16i64, ((re.minSlots) as i64)));
    heap = cAdd(heap, 4096i64);
    heap = cAdd(heap, { let _t1 = length; captureHeap(re, _t1) });
    let stack: i64 = cMul(cAdd(depth, 19i64), frameBytes);
    b.HeapBytes = heap;
    b.StackBytes = stack;
    b.Steps = steps;
    return b;
}

pub fn astSize(nodes: vg::Slice<node>, ni: i32) -> i64 {
    let mut total: i64 = 1i64;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < nodes.get(((ni) as i64)).ch.len) {
            '_c1: {
                total = cAdd(total, astSize(nodes, nodes.get(((ni) as i64)).ch.get(i)));
            }
            i += 1i64;
        }
    }
    return total;
}

pub fn astHeight(nodes: vg::Slice<node>, ni: i32) -> i64 {
    let mut deepest: i64 = 0i64;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < nodes.get(((ni) as i64)).ch.len) {
            '_c1: {
                deepest = std::cmp::max(deepest, astHeight(nodes, nodes.get(((ni) as i64)).ch.get(i)));
            }
            i += 1i64;
        }
    }
    return (deepest + 1i64);
}

pub fn atomCost(re: &mut Regexp) -> i64 {
    let mut lc: lookupCosts = localeLookupCosts(&mut re.loc);
    return { let _t1 = re.nodes; let _t2 = re.brackets; let _t3 = re.root; atomCostNode(_t1, _t2, &mut lc, _t3) };
}

pub fn atomCostNode(nodes: vg::Slice<node>, brs: vg::Slice<bracketSet>, lc: &mut lookupCosts, ni: i32) -> i64 {
    let mut cost: i64 = 1i64;
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opChar {
            cost = (((nodes.get(((ni) as i64)).fold.len) as i64) + 1i64);
        } else if _t1 == opBracket {
            cost = { let _t2 = brs; let _t3 = nodes.get(((ni) as i64)).br; bracketAtomCost(_t2, _t3, lc) };
        }
    }
    {
        let mut i: i64 = 0i64;
        '_b4: while (i < nodes.get(((ni) as i64)).ch.len) {
            '_c4: {
                cost = std::cmp::max(cost, { let _t5 = nodes; let _t6 = brs; let _t7 = nodes.get(((ni) as i64)).ch.get(i); atomCostNode(_t5, _t6, lc, _t7) });
            }
            i += 1i64;
        }
    }
    return cost;
}

pub fn searchSteps(count: i64) -> i64 {
    let mut count_v: i64 = count;
    let mut steps: i64 = 0i64;
    while (count_v > 0i64) {
        count_v /= 2i64;
        steps += 1i64;
    }
    return steps;
}

pub fn u32ContainsCost(count: i64) -> i64 {
    return (1i64 + (3i64 * searchSteps(count)));
}

pub fn findPairCost(count: i64) -> i64 {
    return (3i64 + (3i64 * searchSteps(count)));
}

pub fn findCaseCost(count: i64) -> i64 {
    return (6i64 + (3i64 * searchSteps(count)));
}

pub fn pairSourcesRunCost(count: i64, preimages: i64) -> i64 {
    return ((5i64 + (3i64 * searchSteps(count))) + (5i64 * preimages));
}

pub fn compareSequenceCost(length: i64) -> i64 {
    return (5i64 + (3i64 * length));
}

pub fn localeLookupCosts(l: &mut Locale) -> lookupCosts {
    let mut lc: lookupCosts = vg::zero();
    if l.posix {
        lc.contraction = 1i64;
        lc.classMask = 3i64;
        lc.casePreimages = 2i64;
        lc.caseConvert = 2i64;
        lc.preimages = 1i64;
        return lc;
    }
    lc.sequenceSearch = searchSteps(({ let _t1 = secSequences; sectionLen(l, _t1) }).wrapping_div(8i64));
    let row: CollProfile = { let _t2 = ((l.collationProfile) as i64); collationProfileRow(l, _t2) };
    lc.contraction = ((((14i64 + u32ContainsCost(row.AddCount)) + 1i64) + u32ContainsCost(({ let _t3 = secRootContractions; sectionLen(l, _t3) }).wrapping_div(4i64))) + u32ContainsCost(row.RemoveCount));
    lc.primaryToken = (((14i64 + findPairCost(row.OverrideCount)) + 1i64) + findPairCost(({ let _t4 = secRootEquivalences; sectionLen(l, _t4) }).wrapping_div(8i64)));
    lc.classMask = 4i64;
    lc.preimages = ((localeMaxPreimages(l)) as i64);
    lc.caseConvert = (2i64 + findCaseCost(({ let _t5 = secCaseDefault; sectionLen(l, _t5) }).wrapping_div(12i64)));
    let mut upper: i64 = secInvUpperDefault;
    let mut lower: i64 = secInvLowerDefault;
    if (l.caseProfile == 1u8) {
        lc.caseConvert += findCaseCost(({ let _t6 = secCaseTurkic; sectionLen(l, _t6) }).wrapping_div(12i64));
        upper = secInvUpperTurkic;
        lower = secInvLowerTurkic;
    }
    lc.casePreimages = (((2i64 + pairSourcesRunCost(({ let _t7 = upper; sectionLen(l, _t7) }).wrapping_div(8i64), lc.preimages)) + pairSourcesRunCost(({ let _t8 = lower; sectionLen(l, _t8) }).wrapping_div(8i64), lc.preimages)) + (lc.preimages * (2i64 + lc.preimages)));
    return lc;
}

pub fn elementIDCost(lc: &mut lookupCosts, length: i64) -> i64 {
    if (length == 1i64) {
        return 3i64;
    }
    return ((2i64 + (2i64 * length)) + (lc.sequenceSearch * (1i64 + compareSequenceCost(length))));
}

pub fn collatingElementIDCost(lc: &mut lookupCosts, length: i64) -> i64 {
    let mut cost: i64 = (1i64 + { let _t1 = length; elementIDCost(lc, _t1) });
    if (length > 1i64) {
        cost += lc.contraction;
    }
    return cost;
}

pub fn primaryEqualCost(lc: &mut lookupCosts, left: i64, right: i64) -> i64 {
    return (((1i64 + { let _t1 = left; collatingElementIDCost(lc, _t1) }) + { let _t2 = right; collatingElementIDCost(lc, _t2) }) + (2i64 * lc.primaryToken));
}

pub fn equivsCost(brs: vg::Slice<bracketSet>, bi: i32, lc: &mut lookupCosts, length: i64) -> i64 {
    let mut cost: i64 = 0i64;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < brs.get(((bi) as i64)).equivs.len) {
            '_c1: {
                cost = cAdd(cost, (1i64 + { let _t2 = length; let _t3 = ((brs.get(((bi) as i64)).equivs.get(i).len) as i64); primaryEqualCost(lc, _t2, _t3) }));
            }
            i += 1i64;
        }
    }
    return cost;
}

pub fn positiveSingleCost(brs: vg::Slice<bracketSet>, bi: i32, lc: &mut lookupCosts) -> i64 {
    let mut cost: i64 = (2i64 + searchSteps(brs.get(((bi) as i64)).ranges.len));
    if (brs.get(((bi) as i64)).classMask != 0u16) {
        cost += lc.classMask;
    }
    return cAdd(cost, { let _t1 = brs; let _t2 = bi; let _t3 = 1i64; equivsCost(_t1, _t2, lc, _t3) });
}

pub fn matchesOneCost(brs: vg::Slice<bracketSet>, bi: i32, lc: &mut lookupCosts) -> i64 {
    let positive: i64 = { let _t1 = brs; let _t2 = bi; positiveSingleCost(_t1, _t2, lc) };
    let mut cost: i64 = cAdd(1i64, positive);
    if brs.get(((bi) as i64)).icase {
        cost = cAdd(cost, cAdd(lc.casePreimages, cMul(lc.preimages, cAdd(1i64, positive))));
    }
    return cost;
}

pub fn candidateLeafCost(brs: vg::Slice<bracketSet>, bi: i32, lc: &mut lookupCosts, length: i64) -> i64 {
    return cAdd((1i64 + { let _t1 = length; collatingElementIDCost(lc, _t1) }), { let _t2 = brs; let _t3 = bi; let _t4 = length; equivsCost(_t2, _t3, lc, _t4) });
}

pub fn probeCost(brs: vg::Slice<bracketSet>, bi: i32, lc: &mut lookupCosts, length: i64) -> i64 {
    let mut cost: i64 = cAdd(2i64, ((brs.get(((bi) as i64)).elems.len) as i64));
    let mut counterpart: i64 = 1i64;
    if brs.get(((bi) as i64)).icase {
        counterpart = (1i64 + (2i64 * lc.caseConvert));
    }
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < brs.get(((bi) as i64)).elems.len) {
            '_c1: {
                if (((brs.get(((bi) as i64)).elems.get(i).len) as i64) == length) {
                    cost = cAdd(cost, cMul(length, counterpart));
                }
            }
            i += 1i64;
        }
    }
    if (brs.get(((bi) as i64)).equivs.len == 0i64) {
        return cost;
    }
    let leaf: i64 = { let _t2 = brs; let _t3 = bi; let _t4 = length; candidateLeafCost(_t2, _t3, lc, _t4) };
    if (!brs.get(((bi) as i64)).icase) {
        return cAdd(cost, cAdd((length + 1i64), leaf));
    }
    let mut candidates: i64 = 1i64;
    {
        let mut i_2: i64 = 0i64;
        '_b5: while (i_2 < length) {
            '_c5: {
                candidates = cMul(candidates, (lc.preimages + 1i64));
            }
            i_2 += 1i64;
        }
    }
    return cAdd(cost, cMul(candidates, cAdd((2i64 + lc.preimages), cAdd(lc.casePreimages, leaf))));
}

pub fn bracketAtomCost(brs: vg::Slice<bracketSet>, bi: i32, lc: &mut lookupCosts) -> i64 {
    let mut cost: i64 = { let _t1 = brs; let _t2 = bi; matchesOneCost(_t1, _t2, lc) };
    if (brs.get(((bi) as i64)).multiLens == 0u16) {
        return cost;
    }
    cost = cAdd(cost, 7i64);
    {
        let mut length: i64 = 2i64;
        '_b3: while (length <= maxElemAhead) {
            '_c3: {
                if ((brs.get(((bi) as i64)).multiLens & (1u16 << length)) != 0u16) {
                    cost = cAdd(cost, { let _t4 = brs; let _t5 = bi; let _t6 = ((length) as i64); probeCost(_t4, _t5, lc, _t6) });
                }
            }
            length += 1i64;
        }
    }
    return cost;
}

pub fn solverSteps(nodes: vg::Slice<node>, ni: i32, length: i64) -> i64 {
    let spans: i64 = (cMul(cAdd(length, 1i64), cAdd(length, 2i64))).wrapping_div(2i64);
    let mut total: i64 = spans;
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opConcat {
            total = cAdd(total, cMul((((nodes.get(((ni) as i64)).ch.len - 1i64)) as i64), cMul(spans, cAdd(length, 2i64))));
        } else if _t1 == opAlt {
            total = cAdd(total, cMul(((nodes.get(((ni) as i64)).ch.len) as i64), spans));
        } else if _t1 == opRepeat {
            let mut states: i64 = (((nodes.get(((ni) as i64)).min) as i64) + 1i64);
            if (nodes.get(((ni) as i64)).max != infinite) {
                states = (((nodes.get(((ni) as i64)).max) as i64) + 1i64);
            }
            total = cAdd(total, cMul(cMul(2i64, states), cMul(spans, cAdd(length, 2i64))));
        }
    }
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
            '_c2: {
                total = cAdd(total, solverSteps(nodes, nodes.get(((ni) as i64)).ch.get(i), length));
            }
            i += 1i64;
        }
    }
    return total;
}

pub fn repInstances(nodes: vg::Slice<node>, ni: i32, length: i64) -> i64 {
    let mut instances: i64 = cAdd(length, (((nodes.get(((ni) as i64)).min) as i64) + 1i64));
    if (nodes.get(((ni) as i64)).max != infinite) {
        instances = std::cmp::min(instances, ((nodes.get(((ni) as i64)).max) as i64));
    }
    return instances;
}

pub fn solverDepth(nodes: vg::Slice<node>, ni: i32, length: i64) -> i64 {
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opGroup || _t1 == opAlt || _t1 == opConcat {
            let mut deepest: i64 = 0i64;
            {
                let mut i: i64 = 0i64;
                '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c2: {
                        deepest = std::cmp::max(deepest, solverDepth(nodes, nodes.get(((ni) as i64)).ch.get(i), length));
                    }
                    i += 1i64;
                }
            }
            if (nodes.get(((ni) as i64)).op == opConcat) {
                return cAdd(deepest, (((nodes.get(((ni) as i64)).ch.len) as i64) + 1i64));
            }
            return (deepest + 1i64);
        } else if _t1 == opRepeat {
            return cAdd((repInstances(nodes, ni, length) + 1i64), solverDepth(nodes, nodes.get(((ni) as i64)).ch.get(0i64), length));
        }
    }
    return 1i64;
}

pub fn treeNodes(nodes: vg::Slice<node>, ni: i32, length: i64) -> i64 {
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opGroup || _t1 == opAlt {
            let mut widest: i64 = 0i64;
            {
                let mut i: i64 = 0i64;
                '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c2: {
                        widest = std::cmp::max(widest, treeNodes(nodes, nodes.get(((ni) as i64)).ch.get(i), length));
                    }
                    i += 1i64;
                }
            }
            return cAdd(widest, 1i64);
        } else if _t1 == opConcat {
            let mut total: i64 = 1i64;
            {
                let mut i_2: i64 = 0i64;
                '_b3: while (i_2 < nodes.get(((ni) as i64)).ch.len) {
                    '_c3: {
                        total = cAdd(total, treeNodes(nodes, nodes.get(((ni) as i64)).ch.get(i_2), length));
                    }
                    i_2 += 1i64;
                }
            }
            return total;
        } else if _t1 == opRepeat {
            return cAdd(1i64, cMul(repInstances(nodes, ni, length), treeNodes(nodes, nodes.get(((ni) as i64)).ch.get(0i64), length)));
        }
    }
    return 1i64;
}

pub fn solverFanout(nodes: vg::Slice<node>, ni: i32, length: i64) -> i64 {
    let mut widest: i64 = 1i64;
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opConcat {
            widest = ((nodes.get(((ni) as i64)).ch.len) as i64);
        } else if _t1 == opRepeat {
            widest = cAdd(repInstances(nodes, ni, length), 1i64);
        }
    }
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
            '_c2: {
                widest = std::cmp::max(widest, solverFanout(nodes, nodes.get(((ni) as i64)).ch.get(i), length));
            }
            i += 1i64;
        }
    }
    return widest;
}

pub fn prepare(mem: &vg::Arena, ws: &mut engineWS, n: i64, k: i64, ring: i64) {
    ws.slots = vg::make::<slotTable>(mem, ring);
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < ring) {
            '_c1: {
                ws.slots.update(i, vg::make::<u32>(mem, n), |mut _t3, _t2| { _t3.stamp = _t2; _t3 });
                ws.slots.update(i, vg::make::<i32>(mem, n), |mut _t5, _t4| { _t5.starts = _t4; _t5 });
                if (k > 0i64) {
                    ws.slots.update(i, vg::make::<u32>(mem, (n * k)), |mut _t7, _t6| { _t7.ctr = _t6; _t7 });
                }
            }
            i += 1i64;
        }
    }
    ws.onq = vg::make::<u8>(mem, n);
    if (k > 0i64) {
        ws.bestCtr = vg::make::<u32>(mem, k);
        ws.ctrBuf = vg::make::<u32>(mem, k);
        ws.zeros = vg::make::<u32>(mem, k);
    }
    ws.queue = vg::make_cap::<u32>(mem, 0i64, 16i64);
}

pub fn workspaceHeapBound(n: i64, k: i64, ring: i64) -> i64 {
    let mut perSlot: i64 = cMul(8i64, n);
    if (k > 0i64) {
        perSlot = cAdd(perSlot, cMul(4i64, cMul(n, k)));
    }
    let mut heap: i64 = cAdd(cMul(ring, 104i64), cMul(ring, perSlot));
    heap = cAdd(heap, n);
    if (k > 0i64) {
        heap = cAdd(heap, (12i64 * k));
    }
    heap = cAdd(heap, 64i64);
    heap = cAdd(heap, cMul(ring, cAdd(cMul(16i64, n), 64i64)));
    heap = cAdd(heap, cAdd(cMul(32i64, n), 272i64));
    return heap;
}

pub fn ctrLess(a: vg::Slice<u32>, b: vg::Slice<u32>) -> bool {
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < a.len) {
            '_c1: {
                if (a.get(i) != b.get(i)) {
                    return (a.get(i) < b.get(i));
                }
            }
            i += 1i64;
        }
    }
    return false;
}

pub fn trailingZeros64(x: u64) -> i64 {
    let mut x_v: u64 = x;
    let mut n: i64 = 0i64;
    if ((x_v & 4294967295u64) == 0u64) {
        n += 32i64;
        x_v >>= 32i64;
    }
    if ((x_v & 65535u64) == 0u64) {
        n += 16i64;
        x_v >>= 16i64;
    }
    if ((x_v & 255u64) == 0u64) {
        n += 8i64;
        x_v >>= 8i64;
    }
    if ((x_v & 15u64) == 0u64) {
        n += 4i64;
        x_v >>= 4i64;
    }
    if ((x_v & 3u64) == 0u64) {
        n += 2i64;
        x_v >>= 2i64;
    }
    if ((x_v & 1u64) == 0u64) {
        n += 1i64;
    }
    return n;
}

pub fn runPhaseA(mem: &vg::Arena, re: &mut Regexp, subject: vg::Str, eflags: u32) -> engineResult {
    let mut ws: engineWS = vg::zero();
    let mut e: phaseAState = vg::zero();
    e.subject = subject;
    e.eflags = eflags;
    e.k = re.minSlots;
    e.ring = 2i64;
    e.nlMode = ((re.flags & FlagNewline) != 0u32);
    if re.prog.multi {
        e.ring = 9i64;
    }
    { let _t1 = re.prog.ins.len; let _t2 = e.k; let _t3 = e.ring; prepare(mem, &mut ws, _t1, _t2, _t3) };
    paRun(mem, &mut e, &mut ws, re);
    let mut result: engineResult = vg::zero();
    result.matched = e.matched;
    result.so = e.so;
    result.eo = e.eo;
    return result;
}

pub fn paGen(boundary: i64) -> u32 {
    return (((boundary) as u32) + 1u32);
}

pub fn paConsider(e: &mut phaseAState, ws: &mut engineWS, start: i32, ctr: vg::Slice<u32>, end: i64) {
    if ((!e.matched) || (((start) as i64) < e.so)) {
        e.matched = true;
        e.so = ((start) as i64);
        e.eo = end;
        let _ = vg::vcopy(ws.bestCtr.head(e.k), ctr);
        return;
    }
    if (((start) as i64) > e.so) {
        return;
    }
    if (e.k > 0i64) {
        if ctrLess(ctr, ws.bestCtr.head(e.k)) {
            e.eo = end;
            let _ = vg::vcopy(ws.bestCtr.head(e.k), ctr);
            return;
        }
        if ctrLess(ws.bestCtr.head(e.k), ctr) {
            return;
        }
    }
    if (end > e.eo) {
        e.eo = end;
    }
}

pub fn paPrune(e: &mut phaseAState, ws: &mut engineWS, start: i32, ctr: vg::Slice<u32>) -> bool {
    if (!e.matched) {
        return false;
    }
    if (((start) as i64) > e.so) {
        return true;
    }
    if ((((start) as i64) < e.so) || (e.k == 0i64)) {
        return false;
    }
    return ctrLess(ws.bestCtr.head(e.k), ctr);
}

pub fn paStore(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS, si: i64, pc: u32, start: i32, ctr: vg::Slice<u32>) -> bool {
    if { let _t1 = start; let _t2 = ctr; paPrune(e, ws, _t1, _t2) } {
        return false;
    }
    if (ws.slots.get(si).stamp.get(((pc) as i64)) == ws.slots.get(si).r#gen) {
        let oldStart: i32 = ws.slots.get(si).starts.get(((pc) as i64));
        if (oldStart < start) {
            return false;
        }
        if (oldStart == start) {
            if (e.k == 0i64) {
                return false;
            }
            let base: i64 = (((pc) as i64) * e.k);
            if (!ctrLess(ctr, ws.slots.get(si).ctr.sub(base, (base + e.k)))) {
                return false;
            }
        }
    } else {
        ws.slots.get(si).stamp.set(((pc) as i64), ws.slots.get(si).r#gen);
        ws.slots.update(si, vg::append(mem, ws.slots.get(si).active, pc), |mut _t4, _t3| { _t4.active = _t3; _t4 });
    }
    ws.slots.get(si).starts.set(((pc) as i64), start);
    if (e.k > 0i64) {
        let base_2: i64 = (((pc) as i64) * e.k);
        let _ = vg::vcopy(ws.slots.get(si).ctr.sub(base_2, (base_2 + e.k)), ctr);
    }
    return true;
}

pub fn paRelax(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS, si: i64, pc: u32, start: i32, ctr: vg::Slice<u32>) {
    if { let _t1 = si; let _t2 = pc; let _t3 = start; let _t4 = ctr; paStore(mem, e, ws, _t1, _t2, _t3, _t4) } {
        ws.queue = vg::append(mem, ws.queue, pc);
    }
}

pub fn compactQueue(ws: &mut engineWS) {
    let mut w: i64 = 0i64;
    {
        let mut r: i64 = 0i64;
        '_b1: while (r < ws.queue.len) {
            '_c1: {
                let pc: u32 = ws.queue.get(r);
                if (ws.onq.get(((pc) as i64)) == 0u8) {
                    ws.onq.set(((pc) as i64), 1u8);
                    ws.queue.set(w, pc);
                    w += 1i64;
                }
            }
            r += 1i64;
        }
    }
    ws.queue = ws.queue.head(w);
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < ws.queue.len) {
            '_c2: {
                ws.onq.set(((ws.queue.get(i)) as i64), 0u8);
            }
            i += 1i64;
        }
    }
}

pub fn paClosure(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS, re: &mut Regexp, si: i64) {
    let limit: i64 = (queueCompactFactor * re.prog.ins.len);
    while (ws.queue.len > 0i64) {
        if (ws.queue.len > limit) {
            compactQueue(ws);
        }
        let pc: u32 = ws.queue.get((ws.queue.len - 1i64));
        ws.queue = ws.queue.head((ws.queue.len - 1i64));
        if (ws.slots.get(si).stamp.get(((pc) as i64)) != ws.slots.get(si).r#gen) {
            continue;
        }
        let start: i32 = ws.slots.get(si).starts.get(((pc) as i64));
        let base: i64 = (((pc) as i64) * e.k);
        let ctr: vg::Slice<u32> = ws.slots.get(si).ctr.sub(base, (base + e.k));
        {
            let _t1 = re.prog.ins.get(((pc) as i64)).op;
            if _t1 == iSplit {
                { let _t2 = si; let _t3 = re.prog.ins.get(((pc) as i64)).next; let _t4 = start; let _t5 = ctr; paRelax(mem, e, ws, _t2, _t3, _t4, _t5) };
                { let _t6 = si; let _t7 = re.prog.ins.get(((pc) as i64)).alt; let _t8 = start; let _t9 = ctr; paRelax(mem, e, ws, _t6, _t7, _t8, _t9) };
            } else if _t1 == iJmp {
                { let _t10 = si; let _t11 = re.prog.ins.get(((pc) as i64)).next; let _t12 = start; let _t13 = ctr; paRelax(mem, e, ws, _t10, _t11, _t12, _t13) };
            } else if _t1 == iBOL {
                if e.bol {
                    { let _t14 = si; let _t15 = re.prog.ins.get(((pc) as i64)).next; let _t16 = start; let _t17 = ctr; paRelax(mem, e, ws, _t14, _t15, _t16, _t17) };
                }
            } else if _t1 == iEOL {
                if e.eol {
                    { let _t18 = si; let _t19 = re.prog.ins.get(((pc) as i64)).next; let _t20 = start; let _t21 = ctr; paRelax(mem, e, ws, _t18, _t19, _t20, _t21) };
                }
            } else if _t1 == iMatch {
                { let _t22 = start; let _t23 = ctr; let _t24 = e.pos; paConsider(e, ws, _t22, _t23, _t24) };
            }
        }
    }
}

pub fn paArrive(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS, re: &mut Regexp, pc: u32, delta: i64, start: i32, ctr: vg::Slice<u32>) {
    let fi: i64 = ((e.ci + delta)).wrapping_rem(e.ring);
    let g: u32 = paGen((e.ci + delta));
    if (ws.slots.get(fi).r#gen != g) {
        ws.slots.update(fi, g, |mut _t2, _t1| { _t2.r#gen = _t1; _t2 });
        ws.slots.update(fi, ws.slots.get(fi).active.head(0i64), |mut _t4, _t3| { _t4.active = _t3; _t4 });
    }
    let mut newCtr: vg::Slice<u32> = ctr;
    let mask: u64 = re.prog.ins.get(((pc) as i64)).mask;
    let hasExtra: bool = (re.prog.ins.get(((pc) as i64)).extra.len > 0i64);
    if ((e.k > 0i64) && ((mask != 0u64) || hasExtra)) {
        let buffer: vg::Slice<u32> = ws.ctrBuf.head(e.k);
        let _ = vg::vcopy(buffer, ctr);
        {
            let mut m: u64 = mask;
            '_b5: while (m != 0u64) {
                '_c5: {
                    {
                        let _t6 = buffer.slot(trailingZeros64(m));
                        let _t7 = ((delta) as u32);
                        _t6.update(_t7, |mut _t8, _t7| { _t8 += _t7; _t8 });
                    }
                }
                m &= (m - 1u64);
            }
        }
        {
            let mut i: i64 = 0i64;
            '_b9: while (i < re.prog.ins.get(((pc) as i64)).extra.len) {
                '_c9: {
                    buffer.update(((re.prog.ins.get(((pc) as i64)).extra.get(i)) as i64), ((delta) as u32), |mut _t11, _t10| { _t11 += _t10; _t11 });
                }
                i += 1i64;
            }
        }
        newCtr = buffer;
    }
    let _ = { let _t12 = fi; let _t13 = re.prog.ins.get(((pc) as i64)).next; let _t14 = start; let _t15 = newCtr; paStore(mem, e, ws, _t12, _t13, _t14, _t15) };
}

pub fn paConsume(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS, re: &mut Regexp, si: i64) {
    let mut aheadReady: bool = false;
    {
        let mut ai: i64 = 0i64;
        '_b1: while (ai < ws.slots.get(si).active.len) {
            '_c1: {
                let pc: u32 = ws.slots.get(si).active.get(ai);
                if (ws.slots.get(si).stamp.get(((pc) as i64)) != ws.slots.get(si).r#gen) {
                    break '_c1;
                }
                let start: i32 = ws.slots.get(si).starts.get(((pc) as i64));
                let base: i64 = (((pc) as i64) * e.k);
                let ctr: vg::Slice<u32> = ws.slots.get(si).ctr.sub(base, (base + e.k));
                {
                    let _t2 = re.prog.ins.get(((pc) as i64)).op;
                    if _t2 == iRune {
                        if (e.cur == ((re.prog.ins.get(((pc) as i64)).arg) as i32)) {
                            { let _t3 = pc; let _t4 = 1i64; let _t5 = start; let _t6 = ctr; paArrive(mem, e, ws, re, _t3, _t4, _t5, _t6) };
                        }
                    } else if _t2 == iRuneFold {
                        if runesContain(re.prog.foldSets.get(((re.prog.ins.get(((pc) as i64)).arg) as i64)), e.cur) {
                            { let _t7 = pc; let _t8 = 1i64; let _t9 = start; let _t10 = ctr; paArrive(mem, e, ws, re, _t7, _t8, _t9, _t10) };
                        }
                    } else if _t2 == iAny {
                        if { let _t11 = e.cur; anyMatches(re, _t11) } {
                            { let _t12 = pc; let _t13 = 1i64; let _t14 = start; let _t15 = ctr; paArrive(mem, e, ws, re, _t12, _t13, _t14, _t15) };
                        }
                    } else if _t2 == iBracket {
                        let bi: i32 = ((re.prog.ins.get(((pc) as i64)).arg) as i32);
                        if { let _t16 = re.brackets; let _t17 = bi; let _t18 = e.cur; bracketMatchesOne(_t16, _t17, &mut re.loc, _t18) } {
                            { let _t19 = pc; let _t20 = 1i64; let _t21 = start; let _t22 = ctr; paArrive(mem, e, ws, re, _t19, _t20, _t21, _t22) };
                        }
                        if (re.brackets.get(((bi) as i64)).multiLens == 0u16) {
                            break '_c1;
                        }
                        if (!aheadReady) {
                            decodeAhead(mem, e, ws);
                            aheadReady = true;
                        }
                        {
                            let mut length: i64 = 2i64;
                            '_b23: while (length <= ws.ahead.len) {
                                '_c23: {
                                    if ((re.brackets.get(((bi) as i64)).multiLens & (1u16 << length)) == 0u16) {
                                        break '_c23;
                                    }
                                    if { let _t24 = re.brackets; let _t25 = bi; let _t26 = ws.ahead.head(length); bracketMatchesMulti(_t24, _t25, &mut re.loc, _t26) } {
                                        { let _t27 = pc; let _t28 = length; let _t29 = start; let _t30 = ctr; paArrive(mem, e, ws, re, _t27, _t28, _t29, _t30) };
                                    }
                                }
                                length += 1i64;
                            }
                        }
                    }
                }
            }
            ai += 1i64;
        }
    }
}

pub fn decodeAhead(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS) {
    ws.ahead = ws.ahead.head(0i64);
    let mut at: i64 = e.pos;
    while ((ws.ahead.len < maxElemAhead) && (at < e.subject.len)) {
        let _t1 = decodeRuneAt(e.subject, at);
        let r: i32 = _t1.0;
        let size: i64 = _t1.1;
        ws.ahead = vg::append(mem, ws.ahead, r);
        at += size;
    }
}

pub fn scanAhead(e: &mut phaseAState, re: &mut Regexp) -> i64 {
    if re.prog.scan.single {
        let idx: i64 = indexOfByte(e.subject.tail(e.pos), re.prog.scan.b);
        if (idx < 0i64) {
            return e.subject.len;
        }
        return (e.pos + idx);
    }
    {
        let mut i: i64 = e.pos;
        '_b1: while (i < e.subject.len) {
            '_c1: {
                if re.prog.scan.stop[(e.subject.byte(i)) as usize] {
                    return i;
                }
            }
            i += 1i64;
        }
    }
    return e.subject.len;
}

pub fn bolAt(e: &mut phaseAState, prev: i32) -> bool {
    return (((e.pos == 0i64) && ((e.eflags & ExecNotBOL) == 0u32)) || (e.nlMode && (prev == 10i32)));
}

pub fn continuationFlags(re: &mut Regexp, subject: vg::Str, pos: i64, eflags: u32) -> u32 {
    if (pos == 0i64) {
        return eflags;
    }
    if (((re.flags & FlagNewline) != 0u32) && (subject.byte((pos - 1i64)) == 10u8)) {
        return (eflags & !(ExecNotBOL));
    }
    return (eflags | ExecNotBOL);
}

pub fn paRun(mem: &vg::Arena, e: &mut phaseAState, ws: &mut engineWS, re: &mut Regexp) {
    let mut prev: i32 = (2i32).wrapping_neg();
    let zeros: vg::Slice<u32> = ws.zeros.head(e.k);
    loop {
        let si: i64 = (e.ci).wrapping_rem(e.ring);
        let g: u32 = paGen(e.ci);
        ws.slots.update(si, g, |mut _t2, _t1| { _t2.r#gen = _t1; _t2 });
        let mut w: i64 = 0i64;
        {
            let mut r: i64 = 0i64;
            '_b3: while (r < ws.slots.get(si).active.len) {
                '_c3: {
                    let pc: u32 = ws.slots.get(si).active.get(r);
                    if (ws.slots.get(si).stamp.get(((pc) as i64)) == g) {
                        ws.slots.get(si).active.set(w, pc);
                        w += 1i64;
                    }
                }
                r += 1i64;
            }
        }
        ws.slots.update(si, ws.slots.get(si).active.head(w), |mut _t5, _t4| { _t5.active = _t4; _t5 });
        if ((((w == 0i64) && (!e.matched)) && re.prog.scan.enabled) && (e.pos < e.subject.len)) {
            if (!{ let _t6 = prev; bolAt(e, _t6) }) {
                let next: i64 = scanAhead(e, re);
                if (next > e.pos) {
                    e.pos = next;
                    e.ci += 1i64;
                    prev = 120i32;
                    if ((e.pos > 0i64) && (e.subject.byte((e.pos - 1i64)) == 10u8)) {
                        prev = 10i32;
                    }
                    continue;
                }
            }
        }
        let atEnd: bool = (e.pos == e.subject.len);
        e.cur = (2i32).wrapping_neg();
        e.size = 0i64;
        if (!atEnd) {
            {
                let _t7 = decodeRuneAt(e.subject, e.pos);
                e.cur = _t7.0;
                e.size = _t7.1;
            }
        }
        e.bol = { let _t8 = prev; bolAt(e, _t8) };
        e.eol = ((atEnd && ((e.eflags & ExecNotEOL) == 0u32)) || (e.nlMode && (e.cur == 10i32)));
        ws.queue = vg::append_slice(mem, ws.queue.head(0i64), ws.slots.get(si).active);
        if (!e.matched) {
            { let _t9 = si; let _t10 = re.prog.start; let _t11 = ((e.pos) as i32); let _t12 = zeros; paRelax(mem, e, ws, _t9, _t10, _t11, _t12) };
        }
        { let _t13 = si; paClosure(mem, e, ws, re, _t13) };
        if atEnd {
            return;
        }
        { let _t14 = si; paConsume(mem, e, ws, re, _t14) };
        if e.matched {
            let mut pendingWork: bool = false;
            {
                let mut delta: i64 = 1i64;
                '_b15: while (delta < e.ring) {
                    '_c15: {
                        let fi: i64 = ((e.ci + delta)).wrapping_rem(e.ring);
                        if ((ws.slots.get(fi).r#gen == paGen((e.ci + delta))) && (ws.slots.get(fi).active.len > 0i64)) {
                            pendingWork = true;
                            break '_b15;
                        }
                    }
                    delta += 1i64;
                }
            }
            if (!pendingWork) {
                return;
            }
        }
        prev = e.cur;
        e.pos += e.size;
        e.ci += 1i64;
    }
}

pub fn noError() -> Error {
    let mut e: Error = vg::zero();
    e.Pos = (1i64).wrapping_neg();
    return e;
}

pub fn compileError(code: i32, pos: i64) -> Error {
    let mut e: Error = vg::zero();
    e.Code = code;
    e.Pos = pos;
    return e;
}

pub fn ErrorText(code: i32) -> vg::Str {
    {
        let _t1 = code;
        if _t1 == ErrNone {
            return vg::lit(b"success");
        } else if _t1 == ErrNoMatch {
            return vg::lit(b"no match");
        } else if _t1 == ErrBadPat {
            return vg::lit(b"invalid regular expression");
        } else if _t1 == ErrECollate {
            return vg::lit(b"invalid collating element");
        } else if _t1 == ErrECType {
            return vg::lit(b"invalid character class");
        } else if _t1 == ErrEEscape {
            return vg::lit(b"invalid or trailing backslash");
        } else if _t1 == ErrESubReg {
            return vg::lit(b"invalid backreference");
        } else if _t1 == ErrEBrack {
            return vg::lit(b"unbalanced bracket");
        } else if _t1 == ErrEParen {
            return vg::lit(b"unbalanced parenthesis");
        } else if _t1 == ErrEBrace {
            return vg::lit(b"unbalanced brace");
        } else if _t1 == ErrBadBR {
            return vg::lit(b"invalid interval");
        } else if _t1 == ErrERange {
            return vg::lit(b"invalid range endpoint");
        } else if _t1 == ErrESpace {
            return vg::lit(b"capacity limit reached");
        } else if _t1 == ErrBadRpt {
            return vg::lit(b"repetition without an operand");
        } else if _t1 == ErrENoSub {
            return vg::lit(b"offsets requested from a NoSub expression");
        } else {
            return vg::lit(b"unknown error");
        }
    }
    unreachable!()
}

pub fn memoHash(k: memoKey) -> u64 {
    let mut h: u64 = ((((((k.a) as u32)) as u64) * 2654435761u64) ^ (((((k.b) as u32)) as u64) * 2246822519u64));
    h ^= (((((k.c) as u32)) as u64) * 3266489917u64);
    h ^= (((((k.d) as u32)) as u64) * 668265263u64);
    h ^= (h >> 29i64);
    h *= 13787848793156543929u64;
    h ^= (h >> 32i64);
    return h;
}

pub fn memoGet(t: &mut memoTab, k: memoKey) -> (memoVal, bool) {
    let none: memoVal = vg::zero();
    if (t.keys.len == 0i64) {
        return (none, false);
    }
    let mask: u64 = (((t.keys.len - 1i64)) as u64);
    let mut at: u64 = (memoHash(k) & mask);
    while (t.used.get(((at) as i64)) != 0u8) {
        if vego_eq_memoKey(t.keys.get(((at) as i64)), k) {
            return (t.vals.get(((at) as i64)), true);
        }
        at = ((at + 1u64) & mask);
    }
    return (none, false);
}

pub fn memoInsert(t: &mut memoTab, k: memoKey, v: memoVal) {
    let mask: u64 = (((t.keys.len - 1i64)) as u64);
    let mut at: u64 = (memoHash(k) & mask);
    while (t.used.get(((at) as i64)) != 0u8) {
        if vego_eq_memoKey(t.keys.get(((at) as i64)), k) {
            t.vals.set(((at) as i64), v);
            return;
        }
        at = ((at + 1u64) & mask);
    }
    t.used.set(((at) as i64), 1u8);
    t.keys.set(((at) as i64), k);
    t.vals.set(((at) as i64), v);
    t.count += 1i64;
}

pub fn memoGrow(mem: &vg::Arena, t: &mut memoTab) {
    let oldKeys: vg::Slice<memoKey> = t.keys;
    let oldVals: vg::Slice<memoVal> = t.vals;
    let oldUsed: vg::Slice<u8> = t.used;
    let mut size: i64 = 64i64;
    if (oldKeys.len > 0i64) {
        size = (2i64 * oldKeys.len);
    }
    t.keys = vg::make::<memoKey>(mem, size);
    t.vals = vg::make::<memoVal>(mem, size);
    t.used = vg::make::<u8>(mem, size);
    t.count = 0i64;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < oldKeys.len) {
            '_c1: {
                if (oldUsed.get(i) != 0u8) {
                    { let _t2 = oldKeys.get(i); let _t3 = oldVals.get(i); memoInsert(t, _t2, _t3) };
                }
            }
            i += 1i64;
        }
    }
}

pub fn memoPut(mem: &vg::Arena, t: &mut memoTab, k: memoKey, v: memoVal) {
    if ((4i64 * (t.count + 1i64)) >= (3i64 * t.keys.len)) {
        memoGrow(mem, t);
    }
    { let _t1 = k; let _t2 = v; memoInsert(t, _t1, _t2) };
}

pub fn LocalePOSIX() -> Locale {
    let mut l: Locale = vg::zero();
    l.maxSeq = 1i64;
    l.preimagesDefault = 1i64;
    l.preimagesTurkic = 1i64;
    l.posix = true;
    l.valid = true;
    return l;
}

pub fn LocaleValid(l: &mut Locale) -> bool {
    return l.valid;
}

pub fn u32Raw(blob: vg::Str, off: i64) -> u32 {
    return (((((blob.byte(off)) as u32) | (((blob.byte((off + 1i64))) as u32) << 8i64)) | (((blob.byte((off + 2i64))) as u32) << 16i64)) | (((blob.byte((off + 3i64))) as u32) << 24i64));
}

pub fn u16At(l: &mut Locale, sec: i64, index: i64) -> u16 {
    let off: i64 = (l.sec[(sec) as usize].Off + (2i64 * index));
    return (((l.blob.byte(off)) as u16) | (((l.blob.byte((off + 1i64))) as u16) << 8i64));
}

pub fn u32At(l: &mut Locale, sec: i64, index: i64) -> u32 {
    return u32Raw(l.blob, (l.sec[(sec) as usize].Off + (4i64 * index)));
}

pub fn sectionLen(l: &mut Locale, sec: i64) -> i64 {
    return (l.sec[(sec) as usize].End - l.sec[(sec) as usize].Off);
}

pub fn byteString(l: &mut Locale, sec: i64, off: i64) -> vg::Str {
    let start: i64 = (l.sec[(sec) as usize].Off + off);
    let mut end: i64 = start;
    while ((end < l.sec[(sec) as usize].End) && (l.blob.byte(end) != 0u8)) {
        end += 1i64;
    }
    return l.blob.sub(start, end);
}

pub fn localeLoad(l: &mut Locale, blob: vg::Str) -> bool {
    let magic: vg::Str = vg::lit(b"RVLOC001");
    if ((blob.len < magic.len) || (!vg::streq(blob.sub(0i64, magic.len), magic))) {
        return false;
    }
    l.blob = blob;
    let mut cursor: i64 = magic.len;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < numSections) {
            '_c1: {
                if ((cursor + 4i64) > blob.len) {
                    return false;
                }
                let length: i64 = ((u32Raw(blob, cursor)) as i64);
                cursor += 4i64;
                if ((cursor + length) > blob.len) {
                    return false;
                }
                l.sec[(i) as usize].Off = cursor;
                l.sec[(i) as usize].End = (cursor + length);
                cursor += length;
            }
            i += 1i64;
        }
    }
    if (cursor != blob.len) {
        return false;
    }
    if ({ let _t2 = secScalars; sectionLen(l, _t2) } < 4i64) {
        return false;
    }
    l.maxSeq = (({ let _t3 = secScalars; let _t4 = 0i64; u32At(l, _t3, _t4) }) as i64);
    if ((l.maxSeq < 1i64) || (l.maxSeq > maxElemAhead)) {
        return false;
    }
    if (!localeValidate(l)) {
        return false;
    }
    l.preimagesDefault = ({ let _t5 = secInvUpperDefault; maxPairRun(l, _t5) } + { let _t6 = secInvLowerDefault; maxPairRun(l, _t6) });
    l.preimagesTurkic = ({ let _t7 = secInvUpperTurkic; maxPairRun(l, _t7) } + { let _t8 = secInvLowerTurkic; maxPairRun(l, _t8) });
    if ((l.preimagesDefault > maxPreimages) || (l.preimagesTurkic > maxPreimages)) {
        return false;
    }
    return true;
}

pub fn localeValidate(l: &mut Locale) -> bool {
    if ({ let _t1 = secCtypeStage1; sectionLen(l, _t1) } < 8704i64) {
        return false;
    }
    let blocksLen: i64 = { let _t2 = secCtypeBlocks; sectionLen(l, _t2) };
    {
        let mut i: i64 = 0i64;
        '_b3: while (i < ctypeStage1Entries) {
            '_c3: {
                let block: i64 = (({ let _t4 = secCtypeStage1; let _t5 = i; u16At(l, _t4, _t5) }) as i64);
                if ((512i64 * (block + 1i64)) > blocksLen) {
                    return false;
                }
            }
            i += 1i64;
        }
    }
    if ((({ let _t6 = secCaseDefault; sectionLen(l, _t6) }).wrapping_rem(12i64) != 0i64) || (({ let _t7 = secCaseTurkic; sectionLen(l, _t7) }).wrapping_rem(12i64) != 0i64)) {
        return false;
    }
    if ((((({ let _t8 = secInvUpperDefault; sectionLen(l, _t8) }).wrapping_rem(8i64) != 0i64) || (({ let _t9 = secInvLowerDefault; sectionLen(l, _t9) }).wrapping_rem(8i64) != 0i64)) || (({ let _t10 = secInvUpperTurkic; sectionLen(l, _t10) }).wrapping_rem(8i64) != 0i64)) || (({ let _t11 = secInvLowerTurkic; sectionLen(l, _t11) }).wrapping_rem(8i64) != 0i64)) {
        return false;
    }
    if ((({ let _t12 = secSeqCodepoints; sectionLen(l, _t12) }).wrapping_rem(4i64) != 0i64) || (({ let _t13 = secSequences; sectionLen(l, _t13) }).wrapping_rem(8i64) != 0i64)) {
        return false;
    }
    let codepointCount: i64 = ({ let _t14 = secSeqCodepoints; sectionLen(l, _t14) }).wrapping_div(4i64);
    let seqCount: i64 = ({ let _t15 = secSequences; sectionLen(l, _t15) }).wrapping_div(8i64);
    {
        let mut i_2: i64 = 0i64;
        '_b16: while (i_2 < seqCount) {
            '_c16: {
                let off: i64 = (({ let _t17 = secSequences; let _t18 = (2i64 * i_2); u32At(l, _t17, _t18) }) as i64);
                let length: i64 = (({ let _t19 = secSequences; let _t20 = ((2i64 * i_2) + 1i64); u32At(l, _t19, _t20) }) as i64);
                if ((length < 1i64) || ((off + length) > codepointCount)) {
                    return false;
                }
            }
            i_2 += 1i64;
        }
    }
    if (((((({ let _t21 = secRootContractions; sectionLen(l, _t21) }).wrapping_rem(4i64) != 0i64) || (({ let _t22 = secContractionAdds; sectionLen(l, _t22) }).wrapping_rem(4i64) != 0i64)) || (({ let _t23 = secContractionRemoves; sectionLen(l, _t23) }).wrapping_rem(4i64) != 0i64)) || (({ let _t24 = secRootEquivalences; sectionLen(l, _t24) }).wrapping_rem(8i64) != 0i64)) || (({ let _t25 = secCollationOverrides; sectionLen(l, _t25) }).wrapping_rem(8i64) != 0i64)) {
        return false;
    }
    if (!{ let _t26 = secRootContractions; let _t27 = seqCount; contractionIDsValid(l, _t26, _t27) }) {
        return false;
    }
    if (!{ let _t28 = secContractionAdds; let _t29 = seqCount; contractionIDsValid(l, _t28, _t29) }) {
        return false;
    }
    if (({ let _t30 = secCollationProfiles; sectionLen(l, _t30) }).wrapping_rem(24i64) != 0i64) {
        return false;
    }
    let profileCount: i64 = ({ let _t31 = secCollationProfiles; sectionLen(l, _t31) }).wrapping_div(24i64);
    let overrideCount: i64 = ({ let _t32 = secCollationOverrides; sectionLen(l, _t32) }).wrapping_div(8i64);
    let addCount: i64 = ({ let _t33 = secContractionAdds; sectionLen(l, _t33) }).wrapping_div(4i64);
    let removeCount: i64 = ({ let _t34 = secContractionRemoves; sectionLen(l, _t34) }).wrapping_div(4i64);
    {
        let mut i_3: i64 = 0i64;
        '_b35: while (i_3 < profileCount) {
            '_c35: {
                let row: CollProfile = { let _t36 = i_3; collationProfileRow(l, _t36) };
                if ((((row.OverrideFirst + row.OverrideCount) > overrideCount) || ((row.AddFirst + row.AddCount) > addCount)) || ((row.RemoveFirst + row.RemoveCount) > removeCount)) {
                    return false;
                }
            }
            i_3 += 1i64;
        }
    }
    if (({ let _t37 = secTypeNameOffsets; sectionLen(l, _t37) }).wrapping_rem(4i64) != 0i64) {
        return false;
    }
    let typeNameCount: i64 = ({ let _t38 = secTypeNameOffsets; sectionLen(l, _t38) }).wrapping_div(4i64);
    {
        let mut i_4: i64 = 0i64;
        '_b39: while (i_4 < typeNameCount) {
            '_c39: {
                if ((({ let _t40 = secTypeNameOffsets; let _t41 = i_4; u32At(l, _t40, _t41) }) as i64) >= { let _t42 = secTypeNames; sectionLen(l, _t42) }) {
                    return false;
                }
            }
            i_4 += 1i64;
        }
    }
    if (((({ let _t43 = secLocales; sectionLen(l, _t43) }).wrapping_rem(20i64) != 0i64) || (({ let _t44 = secLocaleNameOffsets; sectionLen(l, _t44) }).wrapping_rem(4i64) != 0i64)) || (({ let _t45 = secLocaleTypes; sectionLen(l, _t45) }).wrapping_rem(4i64) != 0i64)) {
        return false;
    }
    let count: i64 = localesCount(l);
    if (({ let _t46 = secLocaleNameOffsets; sectionLen(l, _t46) }).wrapping_div(4i64) != count) {
        return false;
    }
    {
        let mut i_5: i64 = 0i64;
        '_b47: while (i_5 < count) {
            '_c47: {
                if ((({ let _t48 = secLocaleNameOffsets; let _t49 = i_5; u32At(l, _t48, _t49) }) as i64) >= { let _t50 = secLocaleNames; sectionLen(l, _t50) }) {
                    return false;
                }
            }
            i_5 += 1i64;
        }
    }
    let typeRowCount: i64 = ({ let _t51 = secLocaleTypes; sectionLen(l, _t51) }).wrapping_div(4i64);
    {
        let mut i_6: i64 = 0i64;
        '_b52: while (i_6 < count) {
            '_c52: {
                let row_2: LocaleRow = { let _t53 = i_6; localeRowAt(l, _t53) };
                if (((row_2.TypeFirst + row_2.TypeCount) > typeRowCount) || (((row_2.DefaultCollation) as i64) >= profileCount)) {
                    return false;
                }
            }
            i_6 += 1i64;
        }
    }
    {
        let mut i_7: i64 = 0i64;
        '_b54: while (i_7 < typeRowCount) {
            '_c54: {
                if ((({ let _t55 = secLocaleTypes; let _t56 = ((2i64 * i_7) + 1i64); u16At(l, _t55, _t56) }) as i64) >= profileCount) {
                    return false;
                }
            }
            i_7 += 1i64;
        }
    }
    return true;
}

pub fn contractionIDsValid(l: &mut Locale, sec: i64, seqCount: i64) -> bool {
    let count: i64 = ({ let _t1 = sec; sectionLen(l, _t1) }).wrapping_div(4i64);
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < count) {
            '_c2: {
                let id: u32 = { let _t3 = sec; let _t4 = i; u32At(l, _t3, _t4) };
                if ((id >= ((firstSequenceID) as u32)) && ((((id - ((firstSequenceID) as u32))) as i64) >= seqCount)) {
                    return false;
                }
            }
            i += 1i64;
        }
    }
    return true;
}

pub fn localeMaxElementLength(l: &mut Locale) -> i64 {
    return l.maxSeq;
}

pub fn validScalar(r: i32) -> bool {
    return (((r >= 0i32) && (r <= 1114111i32)) && (!((r >= 55296i32) && (r <= 57343i32))));
}

pub fn asciiLower(c: u8) -> u8 {
    if ((c >= 65u8) && (c <= 90u8)) {
        return (c + 32u8);
    }
    return c;
}

pub fn normalizeName(mem: &vg::Arena, input: vg::Str) -> (vg::Str, bool) {
    if (input.len == 0i64) {
        return (vg::lit(b""), false);
    }
    let mut out: vg::Slice<u8> = vg::make_cap::<u8>(mem, 0i64, input.len);
    let mut i: i64 = 0i64;
    while (((i < input.len) && (input.byte(i) != 46u8)) && (input.byte(i) != 64u8)) {
        let mut c: u8 = input.byte(i);
        if ((c >= 128u8) || (out.len == normalizedNameMax)) {
            return (vg::lit(b""), false);
        }
        if (c == 95u8) {
            c = 45u8;
        } else {
            c = asciiLower(c);
        }
        out = vg::append(mem, out, c);
        i += 1i64;
    }
    if ((i < input.len) && (input.byte(i) == 46u8)) {
        i += 1i64;
        let mut codeset: vg::Slice<u8> = vg::make_cap::<u8>(mem, 0i64, 5i64);
        while ((i < input.len) && (input.byte(i) != 64u8)) {
            let c_2: u8 = asciiLower(input.byte(i));
            i += 1i64;
            if ((codeset.len == 5i64) || (c_2 >= 128u8)) {
                return (vg::lit(b""), false);
            }
            codeset = vg::append(mem, codeset, c_2);
        }
        if ((!vg::streq(vg::str_from_bytes(mem, codeset), vg::lit(b"utf8"))) && (!vg::streq(vg::str_from_bytes(mem, codeset), vg::lit(b"utf-8")))) {
            return (vg::lit(b""), false);
        }
    }
    return (vg::str_from_bytes(mem, out), true);
}

pub fn embeddedModifier(name: vg::Str) -> (vg::Str, bool) {
    let at: i64 = indexOfByte(name, 64u8);
    if (at < 0i64) {
        return (vg::lit(b""), false);
    }
    let modifier: vg::Str = name.tail((at + 1i64));
    let keyword: vg::Str = vg::lit(b"collation=");
    if (modifier.len <= keyword.len) {
        return (vg::lit(b""), false);
    }
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < keyword.len) {
            '_c1: {
                if (asciiLower(modifier.byte(i)) != keyword.byte(i)) {
                    return (vg::lit(b""), false);
                }
            }
            i += 1i64;
        }
    }
    return (modifier.tail(keyword.len), true);
}

pub fn longTypeAlias(name: vg::Str) -> vg::Str {
    if vg::streq(name, vg::lit(b"dict")) {
        return vg::lit(b"dictionary");
    }
    if vg::streq(name, vg::lit(b"phonebk")) {
        return vg::lit(b"phonebook");
    }
    if vg::streq(name, vg::lit(b"trad")) {
        return vg::lit(b"traditional");
    }
    return name;
}

pub fn normalizeType(mem: &vg::Arena, input: vg::Str) -> (vg::Str, bool) {
    if (input.len == 0i64) {
        return (vg::lit(b""), true);
    }
    let mut out: vg::Slice<u8> = vg::make_cap::<u8>(mem, 0i64, input.len);
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < input.len) {
            '_c1: {
                let c: u8 = input.byte(i);
                if ((c >= 128u8) || (out.len == normalizedNameMax)) {
                    return (vg::lit(b""), false);
                }
                out = vg::append(mem, out, asciiLower(c));
            }
            i += 1i64;
        }
    }
    return (longTypeAlias(vg::str_from_bytes(mem, out)), true);
}

pub fn findName(l: &mut Locale, name: vg::Str, poolSec: i64, offsetsSec: i64, count: i64) -> i64 {
    let mut low: i64 = 0i64;
    let mut high: i64 = count;
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        let entry: vg::Str = { let _t1 = poolSec; let _t2 = (({ let _t3 = offsetsSec; let _t4 = middle; u32At(l, _t3, _t4) }) as i64); byteString(l, _t1, _t2) };
        if vg::streq(name, entry) {
            return middle;
        }
        if (vg::strcmp3(name, entry) < 0) {
            high = middle;
        } else {
            low = (middle + 1i64);
        }
    }
    return (1i64).wrapping_neg();
}

pub fn localesCount(l: &mut Locale) -> i64 {
    return ({ let _t1 = secLocales; sectionLen(l, _t1) }).wrapping_div(20i64);
}

pub fn localeRowAt(l: &mut Locale, index: i64) -> LocaleRow {
    let base: i64 = (5i64 * index);
    let mut row: LocaleRow = vg::zero();
    row.TypeFirst = (({ let _t1 = secLocales; let _t2 = (base + 1i64); u32At(l, _t1, _t2) }) as i64);
    row.TypeCount = (({ let _t3 = secLocales; let _t4 = (base + 2i64); u32At(l, _t3, _t4) }) as i64);
    row.CaseProfile = (({ let _t5 = secLocales; let _t6 = (base + 3i64); u32At(l, _t5, _t6) }) as u8);
    row.DefaultCollation = (({ let _t7 = secLocales; let _t8 = (base + 4i64); u32At(l, _t7, _t8) }) as u16);
    return row;
}

pub fn normalizeRequest(mem: &vg::Arena, name: vg::Str, collationType: vg::Str) -> (localeRequest, bool) {
    let mut collationType_v: vg::Str = collationType;
    let mut req: localeRequest = vg::zero();
    let _t1 = normalizeName(mem, name);
    let normalized: vg::Str = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        return (req, false);
    }
    let _t2 = embeddedModifier(name);
    let modifier: vg::Str = _t2.0;
    let hasModifier: bool = _t2.1;
    if ((indexOfByte(name, 64u8) >= 0i64) && (!hasModifier)) {
        return (req, false);
    }
    if (hasModifier && (collationType_v.len != 0i64)) {
        return (req, false);
    }
    if hasModifier {
        collationType_v = modifier;
    }
    let _t3 = normalizeType(mem, collationType_v);
    let normalizedType: vg::Str = _t3.0;
    let ok2: bool = _t3.1;
    if (!ok2) {
        return (req, false);
    }
    req.name = normalized;
    req.ctype = normalizedType;
    req.posix = (vg::streq(normalized, vg::lit(b"c")) || vg::streq(normalized, vg::lit(b"posix")));
    return (req, true);
}

pub fn posixSelect(ctype: vg::Str) -> (Locale, bool) {
    if ((ctype.len != 0i64) && (!vg::streq(ctype, vg::lit(b"standard")))) {
        let invalid: Locale = vg::zero();
        return (invalid, false);
    }
    return (LocalePOSIX(), true);
}

pub fn LocaleLoad(blob: vg::Str) -> (Locale, bool) {
    let mut data: Locale = vg::zero();
    if (!{ let _t1 = blob; localeLoad(&mut data, _t1) }) {
        let invalid: Locale = vg::zero();
        return (invalid, false);
    }
    return (data, true);
}

pub fn resolveLocale(data: &mut Locale, req: localeRequest) -> (Locale, bool) {
    let invalid: Locale = vg::zero();
    let mut result: Locale = vg::zero();
    result.blob = data.blob;
    result.sec = data.sec;
    result.maxSeq = data.maxSeq;
    result.preimagesDefault = data.preimagesDefault;
    result.preimagesTurkic = data.preimagesTurkic;
    let index: i64 = { let _t1 = req.name; let _t2 = secLocaleNames; let _t3 = secLocaleNameOffsets; let _t4 = localesCount(&mut result); findName(&mut result, _t1, _t2, _t3, _t4) };
    if (index < 0i64) {
        return (invalid, false);
    }
    let row: LocaleRow = { let _t5 = index; localeRowAt(&mut result, _t5) };
    result.caseProfile = row.CaseProfile;
    result.valid = true;
    if (req.ctype.len == 0i64) {
        result.collationProfile = row.DefaultCollation;
        return (result, true);
    }
    let typeNameCount: i64 = ({ let _t6 = secTypeNameOffsets; sectionLen(&mut result, _t6) }).wrapping_div(4i64);
    let typeID: i64 = { let _t7 = req.ctype; let _t8 = secTypeNames; let _t9 = secTypeNameOffsets; let _t10 = typeNameCount; findName(&mut result, _t7, _t8, _t9, _t10) };
    if (typeID < 0i64) {
        return (invalid, false);
    }
    let mut low: i64 = row.TypeFirst;
    let mut high: i64 = (row.TypeFirst + row.TypeCount);
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        let rowTypeID: i64 = (({ let _t11 = secLocaleTypes; let _t12 = (2i64 * middle); u16At(&mut result, _t11, _t12) }) as i64);
        if (rowTypeID == typeID) {
            result.collationProfile = { let _t13 = secLocaleTypes; let _t14 = ((2i64 * middle) + 1i64); u16At(&mut result, _t13, _t14) };
            return (result, true);
        }
        if (rowTypeID > typeID) {
            high = middle;
        } else {
            low = (middle + 1i64);
        }
    }
    return (invalid, false);
}

pub fn LocaleSelect(mem: &vg::Arena, data: &mut Locale, name: vg::Str, collationType: vg::Str) -> (Locale, bool) {
    let _t1 = normalizeRequest(mem, name, collationType);
    let req: localeRequest = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        let invalid: Locale = vg::zero();
        return (invalid, false);
    }
    if req.posix {
        return posixSelect(req.ctype);
    }
    return { let _t2 = req; resolveLocale(data, _t2) };
}

pub fn LocaleOpen(mem: &vg::Arena, blob: vg::Str, name: vg::Str, collationType: vg::Str) -> (Locale, bool) {
    let invalid: Locale = vg::zero();
    let _t1 = normalizeRequest(mem, name, collationType);
    let req: localeRequest = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        return (invalid, false);
    }
    if req.posix {
        return posixSelect(req.ctype);
    }
    let _t2 = LocaleLoad(blob);
    let mut data: Locale = _t2.0;
    let ok2: bool = _t2.1;
    if (!ok2) {
        return (invalid, false);
    }
    return { let _t3 = req; resolveLocale(&mut data, _t3) };
}

pub fn classByName(name: vg::Str) -> (u8, bool) {
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < numClasses) {
            '_c1: {
                if vg::streq(classNames[(i) as usize], name) {
                    return (((i) as u8), true);
                }
            }
            i += 1i64;
        }
    }
    return (0u8, false);
}

pub fn posixMask(r: i32) -> u16 {
    let upper: bool = ((r >= 65i32) && (r <= 90i32));
    let lower: bool = ((r >= 97i32) && (r <= 122i32));
    let alpha: bool = (upper || lower);
    let digit: bool = ((r >= 48i32) && (r <= 57i32));
    let alnum: bool = (alpha || digit);
    let blank: bool = ((r == 32i32) || (r == 9i32));
    let space: bool = ((((blank || (r == 10i32)) || (r == 11i32)) || (r == 12i32)) || (r == 13i32));
    let cntrl: bool = ((r <= 31i32) || (r == 127i32));
    let print: bool = ((r >= 32i32) && (r <= 126i32));
    let graph: bool = ((r >= 33i32) && (r <= 126i32));
    let punct: bool = (graph && (!alnum));
    let xdigit: bool = ((digit || ((r >= 65i32) && (r <= 70i32))) || ((r >= 97i32) && (r <= 102i32)));
    let mut mask: u16 = vg::zero();
    if alnum {
        mask |= 1u16;
    }
    if alpha {
        mask |= 2u16;
    }
    if blank {
        mask |= 4u16;
    }
    if cntrl {
        mask |= 8u16;
    }
    if digit {
        mask |= 16u16;
    }
    if graph {
        mask |= 32u16;
    }
    if lower {
        mask |= 64u16;
    }
    if print {
        mask |= 128u16;
    }
    if punct {
        mask |= 256u16;
    }
    if space {
        mask |= 512u16;
    }
    if upper {
        mask |= 1024u16;
    }
    if xdigit {
        mask |= 2048u16;
    }
    return mask;
}

pub fn localeClassMask(l: &mut Locale, r: i32) -> u16 {
    if ((!l.valid) || (!validScalar(r))) {
        return 0u16;
    }
    if l.posix {
        return posixMask(r);
    }
    let block: i64 = (({ let _t1 = secCtypeStage1; let _t2 = (((r >> 8i64)) as i64); u16At(l, _t1, _t2) }) as i64);
    return { let _t3 = secCtypeBlocks; let _t4 = ((block * 256i64) + (((r & 255i32)) as i64)); u16At(l, _t3, _t4) };
}

pub fn findCase(l: &mut Locale, sec: i64, r: i32) -> (CasePair, bool) {
    let mut pair: CasePair = vg::zero();
    let count: i64 = ({ let _t1 = sec; sectionLen(l, _t1) }).wrapping_div(12i64);
    let mut low: i64 = 0i64;
    let mut high: i64 = count;
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        let cp: i32 = (({ let _t2 = sec; let _t3 = (3i64 * middle); u32At(l, _t2, _t3) }) as i32);
        if (cp == r) {
            pair.Upper = (({ let _t4 = sec; let _t5 = ((3i64 * middle) + 1i64); u32At(l, _t4, _t5) }) as i32);
            pair.Lower = (({ let _t6 = sec; let _t7 = ((3i64 * middle) + 2i64); u32At(l, _t6, _t7) }) as i32);
            return (pair, true);
        }
        if (cp > r) {
            high = middle;
        } else {
            low = (middle + 1i64);
        }
    }
    return (pair, false);
}

pub fn caseConvert(l: &mut Locale, r: i32, toUpper: bool) -> i32 {
    if ((!l.valid) || (!validScalar(r))) {
        return r;
    }
    if l.posix {
        if ((toUpper && (r >= 97i32)) && (r <= 122i32)) {
            return (r - 32i32);
        }
        if (((!toUpper) && (r >= 65i32)) && (r <= 90i32)) {
            return (r + 32i32);
        }
        return r;
    }
    if (l.caseProfile == 1u8) {
        let _t1 = { let _t2 = secCaseTurkic; let _t3 = r; findCase(l, _t2, _t3) };
        let pair: CasePair = _t1.0;
        let ok: bool = _t1.1;
        if ok {
            if toUpper {
                return pair.Upper;
            }
            return pair.Lower;
        }
    }
    let _t4 = { let _t5 = secCaseDefault; let _t6 = r; findCase(l, _t5, _t6) };
    let pair_2: CasePair = _t4.0;
    let ok_2: bool = _t4.1;
    if (!ok_2) {
        return r;
    }
    if toUpper {
        return pair_2.Upper;
    }
    return pair_2.Lower;
}

pub fn pairSourcesRun(l: &mut Locale, buf: &mut preimageBuf, sec: i64, r: i32) {
    let count: i64 = ({ let _t1 = sec; sectionLen(l, _t1) }).wrapping_div(8i64);
    let mut low: i64 = 0i64;
    let mut high: i64 = count;
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        if ((({ let _t2 = sec; let _t3 = (2i64 * middle); u32At(l, _t2, _t3) }) as i32) < r) {
            low = (middle + 1i64);
        } else {
            high = middle;
        }
    }
    while (low < count) {
        if ((({ let _t4 = sec; let _t5 = (2i64 * low); u32At(l, _t4, _t5) }) as i32) != r) {
            break;
        }
        buf.r[(buf.n) as usize] = (({ let _t6 = sec; let _t7 = ((2i64 * low) + 1i64); u32At(l, _t6, _t7) }) as i32);
        buf.n += 1i64;
        low += 1i64;
    }
}

pub fn maxPairRun(l: &mut Locale, sec: i64) -> i64 {
    let count: i64 = ({ let _t1 = sec; sectionLen(l, _t1) }).wrapping_div(8i64);
    let mut longest: i64 = 0i64;
    let mut run: i64 = 0i64;
    let mut last: u32 = vg::zero();
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < count) {
            '_c2: {
                let target: u32 = { let _t3 = sec; let _t4 = (2i64 * i); u32At(l, _t3, _t4) };
                if ((i > 0i64) && (target == last)) {
                    run += 1i64;
                } else {
                    run = 1i64;
                    last = target;
                }
                longest = std::cmp::max(longest, run);
            }
            i += 1i64;
        }
    }
    return longest;
}

pub fn localeMaxPreimages(l: &mut Locale) -> i64 {
    if (l.caseProfile == 1u8) {
        return l.preimagesTurkic;
    }
    return l.preimagesDefault;
}

pub fn localeCasePreimages(l: &mut Locale, buf: &mut preimageBuf, r: i32) {
    buf.n = 0i64;
    if ((!l.valid) || (!validScalar(r))) {
        return;
    }
    if l.posix {
        if ((r >= 65i32) && (r <= 90i32)) {
            buf.r[(0i64) as usize] = (r + 32i32);
            buf.n = 1i64;
        }
        if ((r >= 97i32) && (r <= 122i32)) {
            buf.r[(0i64) as usize] = (r - 32i32);
            buf.n = 1i64;
        }
        return;
    }
    let mut upperSec: i64 = secInvUpperDefault;
    let mut lowerSec: i64 = secInvLowerDefault;
    if (l.caseProfile == 1u8) {
        upperSec = secInvUpperTurkic;
        lowerSec = secInvLowerTurkic;
    }
    { let _t1 = upperSec; let _t2 = r; pairSourcesRun(l, buf, _t1, _t2) };
    { let _t3 = lowerSec; let _t4 = r; pairSourcesRun(l, buf, _t3, _t4) };
    let mut w: i64 = 0i64;
    {
        let mut i: i64 = 0i64;
        '_b5: while (i < buf.n) {
            '_c5: {
                let candidate: i32 = buf.r[(i) as usize];
                if ((candidate != r) && (!runesContain(vg::arr_slice(&raw mut buf.r, 0i64, w), candidate))) {
                    buf.r[(w) as usize] = candidate;
                    w += 1i64;
                }
            }
            i += 1i64;
        }
    }
    buf.n = w;
}

pub fn localeToUpper(l: &mut Locale, r: i32) -> i32 {
    return { let _t1 = r; let _t2 = true; caseConvert(l, _t1, _t2) };
}

pub fn localeToLower(l: &mut Locale, r: i32) -> i32 {
    return { let _t1 = r; let _t2 = false; caseConvert(l, _t1, _t2) };
}

pub fn compareSequence(l: &mut Locale, seq: vg::Slice<i32>, index: i64) -> i64 {
    let off: i64 = (({ let _t1 = secSequences; let _t2 = (2i64 * index); u32At(l, _t1, _t2) }) as i64);
    let length: i64 = (({ let _t3 = secSequences; let _t4 = ((2i64 * index) + 1i64); u32At(l, _t3, _t4) }) as i64);
    let common: i64 = std::cmp::min(length, seq.len);
    {
        let mut i: i64 = 0i64;
        '_b5: while (i < common) {
            '_c5: {
                let stored: i32 = (({ let _t6 = secSeqCodepoints; let _t7 = (off + i); u32At(l, _t6, _t7) }) as i32);
                if (seq.get(i) < stored) {
                    return (1i64).wrapping_neg();
                }
                if (seq.get(i) > stored) {
                    return 1i64;
                }
            }
            i += 1i64;
        }
    }
    if (seq.len < length) {
        return (1i64).wrapping_neg();
    }
    if (seq.len > length) {
        return 1i64;
    }
    return 0i64;
}

pub fn elementID(l: &mut Locale, seq: vg::Slice<i32>) -> (u32, bool) {
    if (seq.len == 0i64) {
        return (0u32, false);
    }
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < seq.len) {
            '_c1: {
                if (!validScalar(seq.get(i))) {
                    return (0u32, false);
                }
            }
            i += 1i64;
        }
    }
    if (seq.len == 1i64) {
        return (((seq.get(0i64)) as u32), true);
    }
    let count: i64 = ({ let _t2 = secSequences; sectionLen(l, _t2) }).wrapping_div(8i64);
    let mut low: i64 = 0i64;
    let mut high: i64 = count;
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        let comparison: i64 = { let _t3 = seq; let _t4 = middle; compareSequence(l, _t3, _t4) };
        if (comparison == 0i64) {
            return ((((firstSequenceID + middle)) as u32), true);
        }
        if (comparison < 0i64) {
            high = middle;
        } else {
            low = (middle + 1i64);
        }
    }
    return (0u32, false);
}

pub fn u32Contains(l: &mut Locale, sec: i64, first: i64, count: i64, needle: u32) -> bool {
    let mut low: i64 = first;
    let mut high: i64 = (first + count);
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        let value: u32 = { let _t1 = sec; let _t2 = middle; u32At(l, _t1, _t2) };
        if (value == needle) {
            return true;
        }
        if (value > needle) {
            high = middle;
        } else {
            low = (middle + 1i64);
        }
    }
    return false;
}

pub fn collationProfileRow(l: &mut Locale, index: i64) -> CollProfile {
    let base: i64 = (6i64 * index);
    let mut row: CollProfile = vg::zero();
    row.OverrideFirst = (({ let _t1 = secCollationProfiles; let _t2 = base; u32At(l, _t1, _t2) }) as i64);
    row.OverrideCount = (({ let _t3 = secCollationProfiles; let _t4 = (base + 1i64); u32At(l, _t3, _t4) }) as i64);
    row.AddFirst = (({ let _t5 = secCollationProfiles; let _t6 = (base + 2i64); u32At(l, _t5, _t6) }) as i64);
    row.AddCount = (({ let _t7 = secCollationProfiles; let _t8 = (base + 3i64); u32At(l, _t7, _t8) }) as i64);
    row.RemoveFirst = (({ let _t9 = secCollationProfiles; let _t10 = (base + 4i64); u32At(l, _t9, _t10) }) as i64);
    row.RemoveCount = (({ let _t11 = secCollationProfiles; let _t12 = (base + 5i64); u32At(l, _t11, _t12) }) as i64);
    return row;
}

pub fn isContraction(l: &mut Locale, element: u32) -> bool {
    if l.posix {
        return false;
    }
    let row: CollProfile = { let _t1 = ((l.collationProfile) as i64); collationProfileRow(l, _t1) };
    if { let _t2 = secContractionAdds; let _t3 = row.AddFirst; let _t4 = row.AddCount; let _t5 = element; u32Contains(l, _t2, _t3, _t4, _t5) } {
        return true;
    }
    let rootCount: i64 = ({ let _t6 = secRootContractions; sectionLen(l, _t6) }).wrapping_div(4i64);
    if (!{ let _t7 = secRootContractions; let _t8 = 0i64; let _t9 = rootCount; let _t10 = element; u32Contains(l, _t7, _t8, _t9, _t10) }) {
        return false;
    }
    return (!{ let _t11 = secContractionRemoves; let _t12 = row.RemoveFirst; let _t13 = row.RemoveCount; let _t14 = element; u32Contains(l, _t11, _t12, _t13, _t14) });
}

pub fn collatingElementID(l: &mut Locale, seq: vg::Slice<i32>) -> (u32, bool) {
    let _t1 = { let _t2 = seq; elementID(l, _t2) };
    let element: u32 = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        return (0u32, false);
    }
    if ((seq.len > 1i64) && (!{ let _t3 = element; isContraction(l, _t3) })) {
        return (0u32, false);
    }
    return (element, true);
}

pub fn localeIsCollatingElement(l: &mut Locale, seq: vg::Slice<i32>) -> bool {
    if (!l.valid) {
        return false;
    }
    let _t1 = { let _t2 = seq; collatingElementID(l, _t2) };
    let ok: bool = _t1.1;
    return ok;
}

pub fn LocaleCollatingPrefix(l: &mut Locale, seq: vg::Slice<i32>) -> i64 {
    if ((!l.valid) || (seq.len == 0i64)) {
        return 0i64;
    }
    let maximum: i64 = std::cmp::min(seq.len, l.maxSeq);
    {
        let mut candidate: i64 = maximum;
        '_b1: while (candidate >= 2i64) {
            '_c1: {
                if { let _t2 = seq.sub(0i64, candidate); localeIsCollatingElement(l, _t2) } {
                    return candidate;
                }
            }
            candidate -= 1i64;
        }
    }
    if { let _t3 = seq.sub(0i64, 1i64); localeIsCollatingElement(l, _t3) } {
        return 1i64;
    }
    return 0i64;
}

pub fn findPair(l: &mut Locale, sec: i64, first: i64, count: i64, element: u32) -> (u32, bool) {
    let mut low: i64 = first;
    let mut high: i64 = (first + count);
    while (low < high) {
        let middle: i64 = (low + ((high - low)).wrapping_div(2i64));
        let key: u32 = { let _t1 = sec; let _t2 = (2i64 * middle); u32At(l, _t1, _t2) };
        if (key == element) {
            return ({ let _t3 = sec; let _t4 = ((2i64 * middle) + 1i64); u32At(l, _t3, _t4) }, true);
        }
        if (key > element) {
            high = middle;
        } else {
            low = (middle + 1i64);
        }
    }
    return (0u32, false);
}

pub fn primaryToken(l: &mut Locale, element: u32) -> u64 {
    let row: CollProfile = { let _t1 = ((l.collationProfile) as i64); collationProfileRow(l, _t1) };
    let _t2 = { let _t3 = secCollationOverrides; let _t4 = row.OverrideFirst; let _t5 = row.OverrideCount; let _t6 = element; findPair(l, _t3, _t4, _t5, _t6) };
    let mut rep: u32 = _t2.0;
    let mut ok: bool = _t2.1;
    if ok {
        return (8589934592u64 | ((rep) as u64));
    }
    let rootCount: i64 = ({ let _t7 = secRootEquivalences; sectionLen(l, _t7) }).wrapping_div(8i64);
    {
        let _t8 = { let _t9 = secRootEquivalences; let _t10 = 0i64; let _t11 = rootCount; let _t12 = element; findPair(l, _t9, _t10, _t11, _t12) };
        rep = _t8.0;
        ok = _t8.1;
    }
    if ok {
        return (4294967296u64 | ((rep) as u64));
    }
    return ((element) as u64);
}

pub fn localePrimaryEqual(l: &mut Locale, left: vg::Slice<i32>, right: vg::Slice<i32>) -> bool {
    if (!l.valid) {
        return false;
    }
    let _t1 = { let _t2 = left; collatingElementID(l, _t2) };
    let leftElement: u32 = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        return false;
    }
    let _t3 = { let _t4 = right; collatingElementID(l, _t4) };
    let rightElement: u32 = _t3.0;
    let ok2: bool = _t3.1;
    if (!ok2) {
        return false;
    }
    if (leftElement == rightElement) {
        return true;
    }
    if l.posix {
        return false;
    }
    return ({ let _t5 = leftElement; primaryToken(l, _t5) } == { let _t6 = rightElement; primaryToken(l, _t6) });
}

pub fn localeMinEquivLength(l: &mut Locale, seq: vg::Slice<i32>) -> i64 {
    if (!l.valid) {
        return seq.len;
    }
    let _t1 = { let _t2 = seq; collatingElementID(l, _t2) };
    let element: u32 = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        return seq.len;
    }
    if (element < ((firstSequenceID) as u32)) {
        return 1i64;
    }
    if l.posix {
        return seq.len;
    }
    let token: u64 = { let _t3 = element; primaryToken(l, _t3) };
    let mut best: i64 = seq.len;
    let row: CollProfile = { let _t4 = ((l.collationProfile) as i64); collationProfileRow(l, _t4) };
    best = { let _t5 = secCollationOverrides; let _t6 = row.OverrideFirst; let _t7 = row.OverrideCount; let _t8 = token; let _t9 = best; minTokenLength(l, _t5, _t6, _t7, _t8, _t9) };
    if (best == 1i64) {
        return 1i64;
    }
    let rootCount: i64 = ({ let _t10 = secRootEquivalences; sectionLen(l, _t10) }).wrapping_div(8i64);
    return { let _t11 = secRootEquivalences; let _t12 = 0i64; let _t13 = rootCount; let _t14 = token; let _t15 = best; minTokenLength(l, _t11, _t12, _t13, _t14, _t15) };
}

pub fn minTokenLength(l: &mut Locale, sec: i64, first: i64, count: i64, token: u64, best: i64) -> i64 {
    let mut best_v: i64 = best;
    {
        let mut i: i64 = first;
        '_b1: while (i < (first + count)) {
            '_c1: {
                let candidate: u32 = { let _t2 = sec; let _t3 = (2i64 * i); u32At(l, _t2, _t3) };
                if ({ let _t4 = candidate; primaryToken(l, _t4) } != token) {
                    break '_c1;
                }
                if (candidate < ((firstSequenceID) as u32)) {
                    return 1i64;
                }
                if (!{ let _t5 = candidate; isContraction(l, _t5) }) {
                    break '_c1;
                }
                let index: i64 = (((candidate - ((firstSequenceID) as u32))) as i64);
                best_v = std::cmp::min(best_v, (({ let _t6 = secSequences; let _t7 = ((2i64 * index) + 1i64); u32At(l, _t6, _t7) }) as i64));
            }
            i += 1i64;
        }
    }
    return best_v;
}

pub fn localeSupportsRanges(l: &mut Locale) -> bool {
    return (l.valid && l.posix);
}

pub fn LocaleCount(l: &mut Locale) -> i64 {
    return localesCount(l);
}

pub fn LocaleName(l: &mut Locale, index: i64) -> vg::Str {
    if ((index < 0i64) || (index >= localesCount(l))) {
        return vg::lit(b"");
    }
    return { let _t1 = secLocaleNames; let _t2 = (({ let _t3 = secLocaleNameOffsets; let _t4 = index; u32At(l, _t3, _t4) }) as i64); byteString(l, _t1, _t2) };
}

pub fn decodeWindow(mem: &vg::Arena, s: vg::Str, so: i64, eo: i64) -> decoded {
    let mut d: decoded = vg::zero();
    d.runes = vg::make_cap::<i32>(mem, 0i64, (eo - so));
    d.byteAt = vg::make_cap::<i64>(mem, 0i64, ((eo - so) + 1i64));
    d.atSubjectStart = (so == 0i64);
    d.atSubjectEnd = (eo == s.len);
    d.prevIsNewline = ((so > 0i64) && (s.byte((so - 1i64)) == 10u8));
    d.nextIsNewline = ((eo < s.len) && (s.byte(eo) == 10u8));
    let mut i: i64 = so;
    while (i < eo) {
        d.byteAt = vg::append(mem, d.byteAt, i);
        let _t1 = decodeRuneAt(s, i);
        let r: i32 = _t1.0;
        let size: i64 = _t1.1;
        d.runes = vg::append(mem, d.runes, r);
        i += size;
    }
    d.byteAt = vg::append(mem, d.byteAt, eo);
    return d;
}

pub fn indexOfByte(s: vg::Str, c: u8) -> i64 {
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < s.len) {
            '_c1: {
                if (s.byte(i) == c) {
                    return i;
                }
            }
            i += 1i64;
        }
    }
    return (1i64).wrapping_neg();
}

pub fn runesContain(runes: vg::Slice<i32>, r: i32) -> bool {
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < runes.len) {
            '_c1: {
                if (runes.get(i) == r) {
                    return true;
                }
            }
            i += 1i64;
        }
    }
    return false;
}

pub fn charMatches(re: &mut Regexp, ni: i32, c: i32) -> bool {
    if (c < 0i32) {
        return false;
    }
    if ((re.flags & FlagICase) != 0u32) {
        return runesContain(re.nodes.get(((ni) as i64)).fold, c);
    }
    return (c == re.nodes.get(((ni) as i64)).r);
}

pub fn anyMatches(re: &mut Regexp, c: i32) -> bool {
    if (c <= 0i32) {
        return false;
    }
    if (((re.flags & FlagNewline) != 0u32) && (c == 10i32)) {
        return false;
    }
    return true;
}

pub fn atBOL(re: &mut Regexp, d: &mut decoded, i: i64, eflags: u32) -> bool {
    if (i == 0i64) {
        if d.atSubjectStart {
            return ((eflags & ExecNotBOL) == 0u32);
        }
        return (((re.flags & FlagNewline) != 0u32) && d.prevIsNewline);
    }
    return (((re.flags & FlagNewline) != 0u32) && (d.runes.get((i - 1i64)) == 10i32));
}

pub fn atEOL(re: &mut Regexp, d: &mut decoded, i: i64, eflags: u32) -> bool {
    if (i == d.runes.len) {
        if d.atSubjectEnd {
            return ((eflags & ExecNotEOL) == 0u32);
        }
        return (((re.flags & FlagNewline) != 0u32) && d.nextIsNewline);
    }
    return (((re.flags & FlagNewline) != 0u32) && (d.runes.get(i) == 10i32));
}

pub fn fillMatches(re: &mut Regexp, d: &mut decoded, caps: vg::Slice<Match>, pmatch: vg::Slice<Match>) {
    if (((re.flags & FlagNoSub) != 0u32) || (pmatch.len == 0i64)) {
        return;
    }
    {
        let mut idx: i64 = 0i64;
        '_b1: while (idx < pmatch.len) {
            '_c1: {
                if ((idx < caps.len) && (caps.get(idx).So >= 0i64)) {
                    pmatch.update(idx, d.byteAt.get(caps.get(idx).So), |mut _t3, _t2| { _t3.So = _t2; _t3 });
                    pmatch.update(idx, d.byteAt.get(caps.get(idx).Eo), |mut _t5, _t4| { _t5.Eo = _t4; _t5 });
                } else {
                    pmatch.update(idx, (1i64).wrapping_neg(), |mut _t7, _t6| { _t7.So = _t6; _t7 });
                    pmatch.update(idx, (1i64).wrapping_neg(), |mut _t9, _t8| { _t9.Eo = _t8; _t9 });
                }
            }
            idx += 1i64;
        }
    }
}

pub fn onePassAnalyze(mem: &vg::Arena, nodes: vg::Slice<node>, brs: vg::Slice<bracketSet>, ni: i32) -> bool {
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opChar || _t1 == opAny || _t1 == opBOL || _t1 == opEOL {
            return true;
        } else if _t1 == opBracket {
            return (!bracketHasMultiMembers(brs, nodes.get(((ni) as i64)).br));
        } else if _t1 == opGroup {
            return onePassAnalyze(mem, nodes, brs, nodes.get(((ni) as i64)).ch.get(0i64));
        } else if _t1 == opConcat {
            let mut variable: i64 = 0i64;
            {
                let mut i: i64 = 0i64;
                '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c2: {
                        let child: i32 = nodes.get(((ni) as i64)).ch.get(i);
                        if (!onePassAnalyze(mem, nodes, brs, child)) {
                            return false;
                        }
                        if (!fixedLength(nodes, child)) {
                            variable += 1i64;
                        }
                    }
                    i += 1i64;
                }
            }
            return (variable <= 1i64);
        } else if _t1 == opRepeat {
            if (nodes.get(((ni) as i64)).max == 0i64) {
                return true;
            }
            let child_2: i32 = nodes.get(((ni) as i64)).ch.get(0i64);
            return ((fixedLength(nodes, child_2) && (nodes.get(((child_2) as i64)).minL > 0i64)) && onePassAnalyze(mem, nodes, brs, child_2));
        } else if _t1 == opAlt {
            {
                let mut i_2: i64 = 0i64;
                '_b3: while (i_2 < nodes.get(((ni) as i64)).ch.len) {
                    '_c3: {
                        if (!onePassAnalyze(mem, nodes, brs, nodes.get(((ni) as i64)).ch.get(i_2))) {
                            return false;
                        }
                    }
                    i_2 += 1i64;
                }
            }
            if disjointLengths(nodes, ni) {
                return true;
            }
            return disjointFirsts(mem, nodes, ni);
        }
    }
    return false;
}

pub fn fixedLength(nodes: vg::Slice<node>, ni: i32) -> bool {
    return ((nodes.get(((ni) as i64)).minL == nodes.get(((ni) as i64)).maxL) && (nodes.get(((ni) as i64)).maxL < lenInf));
}

pub fn disjointLengths(nodes: vg::Slice<node>, ni: i32) -> bool {
    let count: i64 = nodes.get(((ni) as i64)).ch.len;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < count) {
            '_c1: {
                let a: i32 = nodes.get(((ni) as i64)).ch.get(i);
                {
                    let mut k: i64 = (i + 1i64);
                    '_b2: while (k < count) {
                        '_c2: {
                            let b: i32 = nodes.get(((ni) as i64)).ch.get(k);
                            if ((nodes.get(((a) as i64)).minL <= nodes.get(((b) as i64)).maxL) && (nodes.get(((b) as i64)).minL <= nodes.get(((a) as i64)).maxL)) {
                                return false;
                            }
                        }
                        k += 1i64;
                    }
                }
            }
            i += 1i64;
        }
    }
    return true;
}

pub fn firstSet(mem: &vg::Arena, nodes: vg::Slice<node>, ni: i32) -> (vg::Slice<i32>, bool) {
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opChar {
            let mut out: vg::Slice<i32> = vg::make_cap::<i32>(mem, 0i64, (nodes.get(((ni) as i64)).fold.len + 1i64));
            if (nodes.get(((ni) as i64)).fold.len > 0i64) {
                out = vg::append_slice(mem, out, nodes.get(((ni) as i64)).fold);
                return (out, true);
            }
            out = vg::append(mem, out, nodes.get(((ni) as i64)).r);
            return (out, true);
        } else if _t1 == opBOL || _t1 == opEOL {
            return (vg::zero::<vg::Slice<i32>>(), true);
        } else if _t1 == opGroup {
            return firstSet(mem, nodes, nodes.get(((ni) as i64)).ch.get(0i64));
        } else if _t1 == opRepeat {
            if (nodes.get(((ni) as i64)).max == 0i64) {
                return (vg::zero::<vg::Slice<i32>>(), true);
            }
            return firstSet(mem, nodes, nodes.get(((ni) as i64)).ch.get(0i64));
        } else if _t1 == opAlt {
            let mut out_2: vg::Slice<i32> = vg::zero();
            {
                let mut i: i64 = 0i64;
                '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c2: {
                        let _t3 = firstSet(mem, nodes, nodes.get(((ni) as i64)).ch.get(i));
                        let sub: vg::Slice<i32> = _t3.0;
                        let ok: bool = _t3.1;
                        if (!ok) {
                            return (vg::zero::<vg::Slice<i32>>(), false);
                        }
                        {
                            let mut k: i64 = 0i64;
                            '_b4: while (k < sub.len) {
                                '_c4: {
                                    out_2 = appendUnique(mem, out_2, sub.get(k));
                                }
                                k += 1i64;
                            }
                        }
                    }
                    i += 1i64;
                }
            }
            return (out_2, true);
        } else if _t1 == opConcat {
            let mut out_3: vg::Slice<i32> = vg::zero();
            {
                let mut i_2: i64 = 0i64;
                '_b5: while (i_2 < nodes.get(((ni) as i64)).ch.len) {
                    '_c5: {
                        let child: i32 = nodes.get(((ni) as i64)).ch.get(i_2);
                        let _t6 = firstSet(mem, nodes, child);
                        let sub_2: vg::Slice<i32> = _t6.0;
                        let ok_2: bool = _t6.1;
                        if (!ok_2) {
                            return (vg::zero::<vg::Slice<i32>>(), false);
                        }
                        {
                            let mut k_2: i64 = 0i64;
                            '_b7: while (k_2 < sub_2.len) {
                                '_c7: {
                                    out_3 = appendUnique(mem, out_3, sub_2.get(k_2));
                                }
                                k_2 += 1i64;
                            }
                        }
                        if (nodes.get(((child) as i64)).minL > 0i64) {
                            break '_b5;
                        }
                    }
                    i_2 += 1i64;
                }
            }
            return (out_3, true);
        }
    }
    return (vg::zero::<vg::Slice<i32>>(), false);
}

pub fn disjointFirsts(mem: &vg::Arena, nodes: vg::Slice<node>, ni: i32) -> bool {
    let count: i64 = nodes.get(((ni) as i64)).ch.len;
    let firsts: vg::Slice<vg::Slice<i32>> = vg::make::<vg::Slice<i32>>(mem, count);
    let mut nullable: i64 = 0i64;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < count) {
            '_c1: {
                let branch: i32 = nodes.get(((ni) as i64)).ch.get(i);
                let _t2 = firstSet(mem, nodes, branch);
                let set: vg::Slice<i32> = _t2.0;
                let ok: bool = _t2.1;
                if (!ok) {
                    return false;
                }
                if (nodes.get(((branch) as i64)).minL == 0i64) {
                    nullable += 1i64;
                }
                firsts.set(i, set);
            }
            i += 1i64;
        }
    }
    if (nullable > 1i64) {
        return false;
    }
    {
        let mut i_2: i64 = 0i64;
        '_b3: while (i_2 < count) {
            '_c3: {
                {
                    let mut k: i64 = (i_2 + 1i64);
                    '_b4: while (k < count) {
                        '_c4: {
                            {
                                let mut m: i64 = 0i64;
                                '_b5: while (m < firsts.get(i_2).len) {
                                    '_c5: {
                                        if runesContain(firsts.get(k), firsts.get(i_2).get(m)) {
                                            return false;
                                        }
                                    }
                                    m += 1i64;
                                }
                            }
                        }
                        k += 1i64;
                    }
                }
            }
            i_2 += 1i64;
        }
    }
    nodes.update(((ni) as i64), firsts, |mut _t7, _t6| { _t7.firsts = _t6; _t7 });
    return true;
}

pub fn onePassCaps(re: &mut Regexp, d: &mut decoded, ni: i32, i: i64, j: i64, eflags: u32, caps: vg::Slice<Match>) -> bool {
    let span: i64 = (j - i);
    if ((span < re.nodes.get(((ni) as i64)).minL) || (span > re.nodes.get(((ni) as i64)).maxL)) {
        return false;
    }
    {
        let _t1 = re.nodes.get(((ni) as i64)).op;
        if _t1 == opChar {
            return { let _t2 = ni; let _t3 = d.runes.get(i); charMatches(re, _t2, _t3) };
        } else if _t1 == opAny {
            return { let _t4 = d.runes.get(i); anyMatches(re, _t4) };
        } else if _t1 == opBracket {
            return { let _t5 = re.brackets; let _t6 = re.nodes.get(((ni) as i64)).br; let _t7 = d.runes.get(i); bracketMatchesOne(_t5, _t6, &mut re.loc, _t7) };
        } else if _t1 == opBOL {
            return { let _t8 = i; let _t9 = eflags; atBOL(re, d, _t8, _t9) };
        } else if _t1 == opEOL {
            return { let _t10 = i; let _t11 = eflags; atEOL(re, d, _t10, _t11) };
        } else if _t1 == opGroup {
            let gi: i64 = re.nodes.get(((ni) as i64)).index;
            {
                let mut k: i64 = 0i64;
                '_b12: while (k < re.nested.get(gi).len) {
                    '_c12: {
                        setMatch(caps, ((re.nested.get(gi).get(k)) as i64), (1i64).wrapping_neg(), (1i64).wrapping_neg());
                    }
                    k += 1i64;
                }
            }
            setMatch(caps, gi, i, j);
            return { let _t13 = re.nodes.get(((ni) as i64)).ch.get(0i64); let _t14 = i; let _t15 = j; let _t16 = eflags; let _t17 = caps; onePassCaps(re, d, _t13, _t14, _t15, _t16, _t17) };
        } else if _t1 == opConcat {
            let mut rest: i64 = span;
            {
                let mut k_2: i64 = 0i64;
                '_b18: while (k_2 < re.nodes.get(((ni) as i64)).ch.len) {
                    '_c18: {
                        rest -= re.nodes.get(((re.nodes.get(((ni) as i64)).ch.get(k_2)) as i64)).minL;
                    }
                    k_2 += 1i64;
                }
            }
            let mut at: i64 = i;
            {
                let mut k_3: i64 = 0i64;
                '_b19: while (k_3 < re.nodes.get(((ni) as i64)).ch.len) {
                    '_c19: {
                        let child: i32 = re.nodes.get(((ni) as i64)).ch.get(k_3);
                        let mut length: i64 = re.nodes.get(((child) as i64)).minL;
                        if (!fixedLength(re.nodes, child)) {
                            length += rest;
                        }
                        if ((length < 0i64) || (!{ let _t20 = child; let _t21 = at; let _t22 = (at + length); let _t23 = eflags; let _t24 = caps; onePassCaps(re, d, _t20, _t21, _t22, _t23, _t24) })) {
                            return false;
                        }
                        at += length;
                    }
                    k_3 += 1i64;
                }
            }
            return (at == j);
        } else if _t1 == opRepeat {
            if (span == 0i64) {
                return true;
            }
            let child_2: i32 = re.nodes.get(((ni) as i64)).ch.get(0i64);
            let size: i64 = re.nodes.get(((child_2) as i64)).minL;
            let count: i64 = (span).wrapping_div(size);
            if ((((span).wrapping_rem(size) != 0i64) || (count < re.nodes.get(((ni) as i64)).min)) || ((re.nodes.get(((ni) as i64)).max != infinite) && (count > re.nodes.get(((ni) as i64)).max))) {
                return false;
            }
            let mut at_2: i64 = i;
            {
                let mut k_4: i64 = 0i64;
                '_b25: while (k_4 < count) {
                    '_c25: {
                        if (!{ let _t26 = child_2; let _t27 = at_2; let _t28 = (at_2 + size); let _t29 = eflags; let _t30 = caps; onePassCaps(re, d, _t26, _t27, _t28, _t29, _t30) }) {
                            return false;
                        }
                        at_2 += size;
                    }
                    k_4 += 1i64;
                }
            }
            return true;
        } else if _t1 == opAlt {
            let mut chosen: i64 = (1i64).wrapping_neg();
            {
                let mut idx: i64 = 0i64;
                '_b31: while (idx < re.nodes.get(((ni) as i64)).ch.len) {
                    '_c31: {
                        let branch: i32 = re.nodes.get(((ni) as i64)).ch.get(idx);
                        if ((span < re.nodes.get(((branch) as i64)).minL) || (span > re.nodes.get(((branch) as i64)).maxL)) {
                            break '_c31;
                        }
                        if (chosen >= 0i64) {
                            chosen = (1i64).wrapping_neg();
                            break '_b31;
                        }
                        chosen = idx;
                    }
                    idx += 1i64;
                }
            }
            if ((chosen < 0i64) && (!re.nodes.get(((ni) as i64)).firsts.p.is_null())) {
                if (span == 0i64) {
                    {
                        let mut idx_2: i64 = 0i64;
                        '_b32: while (idx_2 < re.nodes.get(((ni) as i64)).ch.len) {
                            '_c32: {
                                if (re.nodes.get(((re.nodes.get(((ni) as i64)).ch.get(idx_2)) as i64)).minL == 0i64) {
                                    chosen = idx_2;
                                    break '_b32;
                                }
                            }
                            idx_2 += 1i64;
                        }
                    }
                } else {
                    {
                        let mut idx_3: i64 = 0i64;
                        '_b33: while (idx_3 < re.nodes.get(((ni) as i64)).ch.len) {
                            '_c33: {
                                if runesContain(re.nodes.get(((ni) as i64)).firsts.get(idx_3), d.runes.get(i)) {
                                    chosen = idx_3;
                                    break '_b33;
                                }
                            }
                            idx_3 += 1i64;
                        }
                    }
                }
            }
            if (chosen < 0i64) {
                return false;
            }
            return { let _t34 = re.nodes.get(((ni) as i64)).ch.get(chosen); let _t35 = i; let _t36 = j; let _t37 = eflags; let _t38 = caps; onePassCaps(re, d, _t34, _t35, _t36, _t37, _t38) };
        }
    }
    return false;
}

pub fn buildScanFilter(mem: &vg::Arena, pr: &mut program, newlineMode: bool) -> i64 {
    let mut ok: bool = true;
    let mut matchReachable: bool = false;
    let seen: vg::Slice<bool> = vg::make::<bool>(mem, pr.ins.len);
    let mut stack: vg::Slice<u32> = vg::make_cap::<u32>(mem, 0i64, 16i64);
    stack = vg::append(mem, stack, pr.start);
    while (stack.len > 0i64) {
        let pc: u32 = stack.get((stack.len - 1i64));
        stack = stack.head((stack.len - 1i64));
        if seen.get(((pc) as i64)) {
            continue;
        }
        seen.set(((pc) as i64), true);
        {
            let _t1 = pr.ins.get(((pc) as i64)).op;
            if _t1 == iSplit {
                stack = vg::append(mem, vg::append(mem, stack, pr.ins.get(((pc) as i64)).next), pr.ins.get(((pc) as i64)).alt);
            } else if _t1 == iJmp {
                stack = vg::append(mem, stack, pr.ins.get(((pc) as i64)).next);
            } else if _t1 == iBOL {
            } else if _t1 == iEOL {
            } else if _t1 == iMatch {
                matchReachable = true;
            } else if _t1 == iFail {
            } else if _t1 == iRune {
                {
                    let _t2 = vg::array_index(((utf8LeadByte(((pr.ins.get(((pc) as i64)).arg) as i32))) as i64), ((256) as usize));
                    let _t3 = true;
                    pr.scan.stop[_t2] = _t3;
                }
            } else if _t1 == iRuneFold {
                let set: u32 = pr.ins.get(((pc) as i64)).arg;
                {
                    let mut i: i64 = 0i64;
                    '_b4: while (i < pr.foldSets.get(((set) as i64)).len) {
                        '_c4: {
                            {
                                let _t5 = vg::array_index(((utf8LeadByte(pr.foldSets.get(((set) as i64)).get(i))) as i64), ((256) as usize));
                                let _t6 = true;
                                pr.scan.stop[_t5] = _t6;
                            }
                        }
                        i += 1i64;
                    }
                }
            } else {
                ok = false;
            }
        }
    }
    if ((matchReachable || (!ok)) || pr.multi) {
        let none: scanFilter = vg::zero();
        pr.scan = none;
        return 0i64;
    }
    if newlineMode {
        pr.scan.stop[(10i64) as usize] = true;
    }
    pr.scan.enabled = true;
    let mut count: i64 = 0i64;
    {
        let mut b: i64 = 0i64;
        '_b7: while (b < 256i64) {
            '_c7: {
                if pr.scan.stop[(b) as usize] {
                    count += 1i64;
                    pr.scan.b = ((b) as u8);
                }
            }
            b += 1i64;
        }
    }
    pr.scan.single = (count == 1i64);
    return count;
}

pub fn instrEstimate(nodes: vg::Slice<node>, ni: i32) -> i64 {
    let estimateCap: i64 = 4194304i64;
    let mut total: i64 = 0i64;
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opGroup {
            return instrEstimate(nodes, nodes.get(((ni) as i64)).ch.get(0i64));
        } else if _t1 == opConcat {
            {
                let mut i: i64 = 0i64;
                '_b2: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c2: {
                        total += instrEstimate(nodes, nodes.get(((ni) as i64)).ch.get(i));
                        if (total > estimateCap) {
                            return estimateCap;
                        }
                    }
                    i += 1i64;
                }
            }
            return total;
        } else if _t1 == opAlt {
            total = nodes.get(((ni) as i64)).ch.len;
            {
                let mut i_2: i64 = 0i64;
                '_b3: while (i_2 < nodes.get(((ni) as i64)).ch.len) {
                    '_c3: {
                        total += instrEstimate(nodes, nodes.get(((ni) as i64)).ch.get(i_2));
                        if (total > estimateCap) {
                            return estimateCap;
                        }
                    }
                    i_2 += 1i64;
                }
            }
            return total;
        } else if _t1 == opRepeat {
            let mut copies: i64 = nodes.get(((ni) as i64)).max;
            if (nodes.get(((ni) as i64)).max == infinite) {
                copies = (nodes.get(((ni) as i64)).min + 1i64);
            }
            if (copies == 0i64) {
                return 1i64;
            }
            let child: i64 = instrEstimate(nodes, nodes.get(((ni) as i64)).ch.get(0i64));
            total = (((copies * child) + copies) + 2i64);
            if ((total > estimateCap) || (total < 0i64)) {
                return estimateCap;
            }
            return total;
        }
    }
    return 1i64;
}

pub fn compileProgram(mem: &vg::Arena, b: &mut progBuilder, nodes: vg::Slice<node>, root: i32, multi: bool, newlineMode: bool) {
    b.failMin = failMinNone;
    let body: frag = { let _t1 = nodes; let _t2 = root; let _t3 = 0u64; let _t4 = vg::zero::<vg::Slice<u32>>(); emit(mem, b, _t1, _t2, _t3, _t4) };
    if (b.errCode != ErrNone) {
        return;
    }
    let mut m: instr = vg::zero();
    m.op = iMatch;
    let r#match: u32 = { let _t5 = m; addInstr(mem, b, _t5) };
    if ((b.errCode != ErrNone) || b.tooBig) {
        return;
    }
    { let _t6 = body.out; let _t7 = r#match; patch(b, _t6, _t7) };
    b.prog.start = body.start;
    b.prog.multi = multi;
    b.prog.failMin = b.failMin;
    let stops: i64 = { let _t8 = newlineMode; buildScanFilter(mem, &mut b.prog, _t8) };
    b.prog.depth = (1i64).wrapping_neg();
    if (((!newlineMode) && b.prog.scan.enabled) && (stops == 0i64)) {
        b.prog.depth = progDepth(mem, &mut b.prog);
    }
}

pub fn consumingOp(op: u8) -> bool {
    return (op <= iBracket);
}

pub fn progEdge(pr: &mut program, pc: u32, i: i64) -> (u32, bool) {
    let op: u8 = pr.ins.get(((pc) as i64)).op;
    if ((op == iMatch) || (op == iFail)) {
        return (0u32, false);
    }
    if (i == 0i64) {
        return (pr.ins.get(((pc) as i64)).next, true);
    }
    if ((i == 1i64) && (op == iSplit)) {
        return (pr.ins.get(((pc) as i64)).alt, true);
    }
    return (0u32, false);
}

pub fn progDepth(mem: &vg::Arena, pr: &mut program) -> i64 {
    let n: i64 = pr.ins.len;
    let depth: vg::Slice<i32> = vg::make::<i32>(mem, n);
    {
        let mut round: i64 = 0i64;
        '_b1: while (round < 2i64) {
            '_c1: {
                let mut changed: bool = false;
                {
                    let mut pc: i64 = 0i64;
                    '_b2: while (pc < n) {
                        '_c2: {
                            let mut weight: i32 = 0i32;
                            if consumingOp(pr.ins.get(pc).op) {
                                weight = 1i32;
                            }
                            {
                                let mut e: i64 = 0i64;
                                '_b3: while (e < 2i64) {
                                    '_c3: {
                                        let _t4 = { let _t5 = ((pc) as u32); let _t6 = e; progEdge(pr, _t5, _t6) };
                                        let target: u32 = _t4.0;
                                        let has: bool = _t4.1;
                                        if (has && (depth.get(((target) as i64)) < (depth.get(pc) + weight))) {
                                            depth.set(((target) as i64), (depth.get(pc) + weight));
                                            changed = true;
                                        }
                                    }
                                    e += 1i64;
                                }
                            }
                        }
                        pc += 1i64;
                    }
                }
                if (!changed) {
                    let mut best: i32 = 0i32;
                    {
                        let mut pc_2: i64 = 0i64;
                        '_b7: while (pc_2 < n) {
                            '_c7: {
                                best = std::cmp::max(best, depth.get(pc_2));
                            }
                            pc_2 += 1i64;
                        }
                    }
                    return ((best) as i64);
                }
            }
            round += 1i64;
        }
    }
    return (1i64).wrapping_neg();
}

pub fn addInstr(mem: &vg::Arena, b: &mut progBuilder, ins: instr) -> u32 {
    if b.tooBig {
        return 0u32;
    }
    if (b.prog.ins.len >= maxProgram) {
        b.tooBig = true;
        return 0u32;
    }
    b.prog.ins = vg::append(mem, b.prog.ins, ins);
    return (((b.prog.ins.len - 1i64)) as u32);
}

pub fn patch(b: &mut progBuilder, slots: vg::Slice<patchSlot>, target: u32) {
    if b.tooBig {
        return;
    }
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < slots.len) {
            '_c1: {
                if slots.get(i).alt {
                    b.prog.ins.update(((slots.get(i).idx) as i64), target, |mut _t3, _t2| { _t3.alt = _t2; _t3 });
                } else {
                    b.prog.ins.update(((slots.get(i).idx) as i64), target, |mut _t5, _t4| { _t5.next = _t4; _t5 });
                }
            }
            i += 1i64;
        }
    }
}

pub fn singleOut(mem: &vg::Arena, idx: u32, alt: bool) -> vg::Slice<patchSlot> {
    let mut slot: patchSlot = vg::zero();
    slot.idx = idx;
    slot.alt = alt;
    let out: vg::Slice<patchSlot> = vg::make_cap::<patchSlot>(mem, 0i64, 1i64);
    return vg::append(mem, out, slot);
}

pub fn epsilonFrag(mem: &vg::Arena, b: &mut progBuilder) -> frag {
    let mut j: instr = vg::zero();
    j.op = iJmp;
    let idx: u32 = { let _t1 = j; addInstr(mem, b, _t1) };
    let mut f: frag = vg::zero();
    f.start = idx;
    f.out = singleOut(mem, idx, false);
    return f;
}

pub fn copyExtra(mem: &vg::Arena, extra: vg::Slice<u32>) -> vg::Slice<u32> {
    if (extra.len == 0i64) {
        return vg::zero::<vg::Slice<u32>>();
    }
    let dup: vg::Slice<u32> = vg::make::<u32>(mem, extra.len);
    let _ = vg::vcopy(dup, extra);
    return dup;
}

pub fn consumeFrag(mem: &vg::Arena, b: &mut progBuilder, op: u8, arg: u32, mask: u64, extra: vg::Slice<u32>) -> frag {
    let mut ins: instr = vg::zero();
    ins.op = op;
    ins.arg = arg;
    ins.mask = mask;
    ins.extra = copyExtra(mem, extra);
    let idx: u32 = { let _t1 = ins; addInstr(mem, b, _t1) };
    let mut f: frag = vg::zero();
    f.start = idx;
    f.out = singleOut(mem, idx, false);
    return f;
}

pub fn emit(mem: &vg::Arena, b: &mut progBuilder, nodes: vg::Slice<node>, ni: i32, mask: u64, extra: vg::Slice<u32>) -> frag {
    let none: frag = vg::zero();
    if ((b.errCode != ErrNone) || b.tooBig) {
        return none;
    }
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opChar {
            if b.icase {
                let arg: u32 = ((b.prog.foldSets.len) as u32);
                let foldCopy: vg::Slice<i32> = vg::make::<i32>(mem, nodes.get(((ni) as i64)).fold.len);
                let _ = vg::vcopy(foldCopy, nodes.get(((ni) as i64)).fold);
                b.prog.foldSets = vg::append(mem, b.prog.foldSets, foldCopy);
                return { let _t2 = iRuneFold; let _t3 = arg; let _t4 = mask; let _t5 = extra; consumeFrag(mem, b, _t2, _t3, _t4, _t5) };
            }
            return { let _t6 = iRune; let _t7 = ((nodes.get(((ni) as i64)).r) as u32); let _t8 = mask; let _t9 = extra; consumeFrag(mem, b, _t6, _t7, _t8, _t9) };
        } else if _t1 == opAny {
            return { let _t10 = iAny; let _t11 = 0u32; let _t12 = mask; let _t13 = extra; consumeFrag(mem, b, _t10, _t11, _t12, _t13) };
        } else if _t1 == opBracket {
            return { let _t14 = iBracket; let _t15 = ((nodes.get(((ni) as i64)).br) as u32); let _t16 = mask; let _t17 = extra; consumeFrag(mem, b, _t14, _t15, _t16, _t17) };
        } else if _t1 == opBOL {
            return { let _t18 = iBOL; let _t19 = 0u32; let _t20 = 0u64; let _t21 = vg::zero::<vg::Slice<u32>>(); consumeFrag(mem, b, _t18, _t19, _t20, _t21) };
        } else if _t1 == opEOL {
            return { let _t22 = iEOL; let _t23 = 0u32; let _t24 = 0u64; let _t25 = vg::zero::<vg::Slice<u32>>(); consumeFrag(mem, b, _t22, _t23, _t24, _t25) };
        } else if _t1 == opGroup {
            return { let _t26 = nodes; let _t27 = nodes.get(((ni) as i64)).ch.get(0i64); let _t28 = mask; let _t29 = extra; emit(mem, b, _t26, _t27, _t28, _t29) };
        } else if _t1 == opConcat {
            let mut result: frag = { let _t30 = nodes; let _t31 = nodes.get(((ni) as i64)).ch.get(0i64); let _t32 = mask; let _t33 = extra; emit(mem, b, _t30, _t31, _t32, _t33) };
            {
                let mut i: i64 = 1i64;
                '_b34: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c34: {
                        let next: frag = { let _t35 = nodes; let _t36 = nodes.get(((ni) as i64)).ch.get(i); let _t37 = mask; let _t38 = extra; emit(mem, b, _t35, _t36, _t37, _t38) };
                        if (b.errCode != ErrNone) {
                            return none;
                        }
                        { let _t39 = result.out; let _t40 = next.start; patch(b, _t39, _t40) };
                        result.out = next.out;
                    }
                    i += 1i64;
                }
            }
            return result;
        } else if _t1 == opAlt {
            return { let _t41 = nodes; let _t42 = ni; let _t43 = mask; let _t44 = extra; emitAlt(mem, b, _t41, _t42, _t43, _t44) };
        } else if _t1 == opRepeat {
            return { let _t45 = nodes; let _t46 = ni; let _t47 = mask; let _t48 = extra; emitRepeat(mem, b, _t45, _t46, _t47, _t48) };
        }
    }
    b.errCode = ErrBadPat;
    return none;
}

pub fn emitAlt(mem: &vg::Arena, b: &mut progBuilder, nodes: vg::Slice<node>, ni: i32, mask: u64, extra: vg::Slice<u32>) -> frag {
    let none: frag = vg::zero();
    let mut result: frag = vg::zero();
    let mut splits: vg::Slice<u32> = vg::make_cap::<u32>(mem, 0i64, nodes.get(((ni) as i64)).ch.len);
    let count: i64 = nodes.get(((ni) as i64)).ch.len;
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < count) {
            '_c1: {
                if (i < (count - 1i64)) {
                    let mut s: instr = vg::zero();
                    s.op = iSplit;
                    splits = vg::append(mem, splits, { let _t2 = s; addInstr(mem, b, _t2) });
                }
                let sub: frag = { let _t3 = nodes; let _t4 = nodes.get(((ni) as i64)).ch.get(i); let _t5 = mask; let _t6 = extra; emit(mem, b, _t3, _t4, _t5, _t6) };
                if ((b.errCode != ErrNone) || b.tooBig) {
                    return none;
                }
                if (i < (count - 1i64)) {
                    b.prog.ins.update(((splits.get(i)) as i64), sub.start, |mut _t8, _t7| { _t8.next = _t7; _t8 });
                } else {
                    b.prog.ins.update(((splits.get((i - 1i64))) as i64), sub.start, |mut _t10, _t9| { _t10.alt = _t9; _t10 });
                }
                if ((i > 0i64) && (i < (count - 1i64))) {
                    b.prog.ins.update(((splits.get((i - 1i64))) as i64), splits.get(i), |mut _t12, _t11| { _t12.alt = _t11; _t12 });
                }
                result.out = vg::append_slice(mem, result.out, sub.out);
            }
            i += 1i64;
        }
    }
    result.start = splits.get(0i64);
    return result;
}

pub fn fragAppend(b: &mut progBuilder, result: &mut frag, have: bool, f: frag) -> bool {
    if (!have) {
        result.start = f.start;
        result.out = f.out;
        return true;
    }
    { let _t1 = result.out; let _t2 = f.start; patch(b, _t1, _t2) };
    result.out = f.out;
    return true;
}

pub fn emitRepeat(mem: &vg::Arena, b: &mut progBuilder, nodes: vg::Slice<node>, ni: i32, mask: u64, extra: vg::Slice<u32>) -> frag {
    let mut mask_v: u64 = mask;
    let mut extra_v: vg::Slice<u32> = extra;
    let none: frag = vg::zero();
    if (instrEstimate(nodes, ni) > (maxProgram - b.prog.ins.len)) {
        let mut fi: instr = vg::zero();
        fi.op = iFail;
        let idx: u32 = { let _t1 = fi; addInstr(mem, b, _t1) };
        b.failMin = std::cmp::min(b.failMin, nodes.get(((ni) as i64)).minL);
        let mut f: frag = vg::zero();
        f.start = idx;
        return f;
    }
    if nodes.get(((ni) as i64)).minimal {
        let slot: i64 = nodes.get(((ni) as i64)).index;
        if (slot < maskWidth) {
            mask_v |= (1u64 << slot);
        } else {
            let grown: vg::Slice<u32> = vg::make::<u32>(mem, (extra_v.len + 1i64));
            let _ = vg::vcopy(grown, extra_v);
            grown.set(extra_v.len, ((slot) as u32));
            extra_v = grown;
        }
    }
    let child: i32 = nodes.get(((ni) as i64)).ch.get(0i64);
    let lo: i64 = nodes.get(((ni) as i64)).min;
    let hi: i64 = nodes.get(((ni) as i64)).max;
    if ((lo == 0i64) && (hi == 0i64)) {
        return epsilonFrag(mem, b);
    }
    let mut result: frag = vg::zero();
    let mut haveResult: bool = false;
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < lo) {
            '_c2: {
                if ((b.errCode != ErrNone) || b.tooBig) {
                    return none;
                }
                if ((i == (lo - 1i64)) && (hi == infinite)) {
                    let _ = { let _t3 = haveResult; let _t4 = { let _t5 = nodes; let _t6 = child; let _t7 = mask_v; let _t8 = extra_v; emitPlus(mem, b, _t5, _t6, _t7, _t8) }; fragAppend(b, &mut result, _t3, _t4) };
                    return result;
                }
                haveResult = { let _t9 = haveResult; let _t10 = { let _t11 = nodes; let _t12 = child; let _t13 = mask_v; let _t14 = extra_v; emit(mem, b, _t11, _t12, _t13, _t14) }; fragAppend(b, &mut result, _t9, _t10) };
            }
            i += 1i64;
        }
    }
    if (hi == infinite) {
        let _ = { let _t15 = haveResult; let _t16 = { let _t17 = nodes; let _t18 = child; let _t19 = mask_v; let _t20 = extra_v; emitStar(mem, b, _t17, _t18, _t19, _t20) }; fragAppend(b, &mut result, _t15, _t16) };
        return result;
    }
    let mut skips: vg::Slice<patchSlot> = vg::make_cap::<patchSlot>(mem, 0i64, std::cmp::max((hi - lo), 1i64));
    {
        let mut i_2: i64 = lo;
        '_b21: while (i_2 < hi) {
            '_c21: {
                if ((b.errCode != ErrNone) || b.tooBig) {
                    return none;
                }
                let mut s: instr = vg::zero();
                s.op = iSplit;
                let split: u32 = { let _t22 = s; addInstr(mem, b, _t22) };
                let sub: frag = { let _t23 = nodes; let _t24 = child; let _t25 = mask_v; let _t26 = extra_v; emit(mem, b, _t23, _t24, _t25, _t26) };
                if ((b.errCode != ErrNone) || b.tooBig) {
                    return none;
                }
                b.prog.ins.update(((split) as i64), sub.start, |mut _t28, _t27| { _t28.next = _t27; _t28 });
                let mut slot_2: patchSlot = vg::zero();
                slot_2.idx = split;
                slot_2.alt = true;
                skips = vg::append(mem, skips, slot_2);
                let mut piece: frag = vg::zero();
                piece.start = split;
                piece.out = sub.out;
                haveResult = { let _t29 = haveResult; let _t30 = piece; fragAppend(b, &mut result, _t29, _t30) };
            }
            i_2 += 1i64;
        }
    }
    result.out = vg::append_slice(mem, result.out, skips);
    if (!haveResult) {
        return epsilonFrag(mem, b);
    }
    return result;
}

pub fn emitStar(mem: &vg::Arena, b: &mut progBuilder, nodes: vg::Slice<node>, child: i32, mask: u64, extra: vg::Slice<u32>) -> frag {
    let none: frag = vg::zero();
    let mut s: instr = vg::zero();
    s.op = iSplit;
    let split: u32 = { let _t1 = s; addInstr(mem, b, _t1) };
    let body: frag = { let _t2 = nodes; let _t3 = child; let _t4 = mask; let _t5 = extra; emit(mem, b, _t2, _t3, _t4, _t5) };
    if ((b.errCode != ErrNone) || b.tooBig) {
        return none;
    }
    b.prog.ins.update(((split) as i64), body.start, |mut _t7, _t6| { _t7.next = _t6; _t7 });
    { let _t8 = body.out; let _t9 = split; patch(b, _t8, _t9) };
    let mut f: frag = vg::zero();
    f.start = split;
    f.out = singleOut(mem, split, true);
    return f;
}

pub fn emitPlus(mem: &vg::Arena, b: &mut progBuilder, nodes: vg::Slice<node>, child: i32, mask: u64, extra: vg::Slice<u32>) -> frag {
    let none: frag = vg::zero();
    let body: frag = { let _t1 = nodes; let _t2 = child; let _t3 = mask; let _t4 = extra; emit(mem, b, _t1, _t2, _t3, _t4) };
    if ((b.errCode != ErrNone) || b.tooBig) {
        return none;
    }
    let mut s: instr = vg::zero();
    s.op = iSplit;
    let split: u32 = { let _t5 = s; addInstr(mem, b, _t5) };
    b.prog.ins.update(((split) as i64), body.start, |mut _t7, _t6| { _t7.next = _t6; _t7 });
    { let _t8 = body.out; let _t9 = split; patch(b, _t8, _t9) };
    let mut f: frag = vg::zero();
    f.start = body.start;
    f.out = singleOut(mem, split, true);
    return f;
}

pub fn compileScan(re: &mut Regexp, ni: i32) {
    {
        let _t1 = re.nodes.get(((ni) as i64)).op;
        if _t1 == opRepeat {
            if re.nodes.get(((ni) as i64)).minimal {
                re.nodes.update(((ni) as i64), re.minSlots, |mut _t3, _t2| { _t3.index = _t2; _t3 });
                re.minSlots += 1i64;
            }
        } else if _t1 == opBracket {
            if bracketHasMultiMembers(re.brackets, re.nodes.get(((ni) as i64)).br) {
                re.multi = true;
            }
        } else if _t1 == opBOL || _t1 == opEOL {
            re.anchors = true;
        }
    }
    {
        let mut i: i64 = 0i64;
        '_b4: while (i < re.nodes.get(((ni) as i64)).ch.len) {
            '_c4: {
                { let _t5 = re.nodes.get(((ni) as i64)).ch.get(i); compileScan(re, _t5) };
            }
            i += 1i64;
        }
    }
}

pub fn collectNested(mem: &vg::Arena, re: &mut Regexp, ni: i32, stack: &mut groupStack) {
    let isGroup: bool = (re.nodes.get(((ni) as i64)).op == opGroup);
    if isGroup {
        let gi: i32 = ((re.nodes.get(((ni) as i64)).index) as i32);
        {
            let mut k: i64 = 0i64;
            '_b1: while (k < stack.g.len) {
                '_c1: {
                    let outer: i32 = stack.g.get(k);
                    re.nested.set(((outer) as i64), vg::append(mem, re.nested.get(((outer) as i64)), gi));
                }
                k += 1i64;
            }
        }
        stack.g = vg::append(mem, stack.g, gi);
    }
    {
        let mut i: i64 = 0i64;
        '_b2: while (i < re.nodes.get(((ni) as i64)).ch.len) {
            '_c2: {
                { let _t3 = re.nodes.get(((ni) as i64)).ch.get(i); collectNested(mem, re, _t3, stack) };
            }
            i += 1i64;
        }
    }
    if isGroup {
        stack.g = stack.g.head((stack.g.len - 1i64));
    }
}

pub fn minMatchChars(nodes: vg::Slice<node>, brs: vg::Slice<bracketSet>, loc: &mut Locale, ni: i32) -> i64 {
    {
        let _t1 = nodes.get(((ni) as i64)).op;
        if _t1 == opChar || _t1 == opAny {
            return 1i64;
        } else if _t1 == opBracket {
            return { let _t2 = brs; let _t3 = nodes.get(((ni) as i64)).br; bracketMinChars(_t2, _t3, loc) };
        } else if _t1 == opBOL || _t1 == opEOL {
            return 0i64;
        } else if _t1 == opGroup {
            return { let _t4 = nodes; let _t5 = brs; let _t6 = nodes.get(((ni) as i64)).ch.get(0i64); minMatchChars(_t4, _t5, loc, _t6) };
        } else if _t1 == opConcat {
            let mut total: i64 = 0i64;
            {
                let mut i: i64 = 0i64;
                '_b7: while (i < nodes.get(((ni) as i64)).ch.len) {
                    '_c7: {
                        total += { let _t8 = nodes; let _t9 = brs; let _t10 = nodes.get(((ni) as i64)).ch.get(i); minMatchChars(_t8, _t9, loc, _t10) };
                        if (total > lengthCap) {
                            return lengthCap;
                        }
                    }
                    i += 1i64;
                }
            }
            return total;
        } else if _t1 == opAlt {
            let mut best: i64 = lengthCap;
            {
                let mut i_2: i64 = 0i64;
                '_b11: while (i_2 < nodes.get(((ni) as i64)).ch.len) {
                    '_c11: {
                        best = std::cmp::min(best, { let _t12 = nodes; let _t13 = brs; let _t14 = nodes.get(((ni) as i64)).ch.get(i_2); minMatchChars(_t12, _t13, loc, _t14) });
                    }
                    i_2 += 1i64;
                }
            }
            return best;
        } else if _t1 == opRepeat {
            if (nodes.get(((ni) as i64)).min == 0i64) {
                return 0i64;
            }
            let product: i64 = (nodes.get(((ni) as i64)).min * { let _t15 = nodes; let _t16 = brs; let _t17 = nodes.get(((ni) as i64)).ch.get(0i64); minMatchChars(_t15, _t16, loc, _t17) });
            return std::cmp::min(product, lengthCap);
        }
    }
    return 0i64;
}

pub fn Compile(mem: &vg::Arena, pattern: vg::Str, loc: Locale, flags: u32) -> (Regexp, Error) {
    let mut loc_v: Locale = loc;
    let mut re: Regexp = vg::zero();
    if (!LocaleValid(&mut loc_v)) {
        return (re, compileError(ErrBadPat, (1i64).wrapping_neg()));
    }
    let mut p: parser = vg::zero();
    let root: i32 = { let _t1 = pattern; let _t2 = flags; parse(mem, &mut p, &mut loc_v, _t1, _t2) };
    if (root < 0i32) {
        return (re, p.err);
    }
    re.nsub = p.groups;
    re.nodes = p.nodes;
    re.brackets = p.brackets;
    re.root = root;
    re.flags = flags;
    re.loc = loc_v;
    re.nested = vg::make::<vg::Slice<i32>>(mem, (re.nsub + 1i64));
    { let _t3 = root; compileScan(&mut re, _t3) };
    let mut stack: groupStack = vg::zero();
    { let _t4 = root; collectNested(mem, &mut re, _t4, &mut stack) };
    { let _t5 = re.nodes; let _t6 = re.brackets; let _t7 = root; computeLengths(mem, _t5, &mut re.loc, _t6, _t7) };
    re.onePass = onePassAnalyze(mem, re.nodes, re.brackets, root);
    let mut b: progBuilder = vg::zero();
    b.icase = ((flags & FlagICase) != 0u32);
    { let _t8 = re.nodes; let _t9 = root; let _t10 = re.multi; let _t11 = ((flags & FlagNewline) != 0u32); compileProgram(mem, &mut b, _t8, _t9, _t10, _t11) };
    if (b.errCode != ErrNone) {
        return (re, compileError(b.errCode, (1i64).wrapping_neg()));
    }
    if b.tooBig {
        re.minLen = { let _t12 = re.nodes; let _t13 = re.brackets; let _t14 = root; minMatchChars(_t12, _t13, &mut re.loc, _t14) };
    } else {
        re.progOK = true;
        re.prog = b.prog;
    }
    return (re, noError());
}

pub fn NumSub(re: &mut Regexp) -> i64 {
    return re.nsub;
}

pub fn trivialNullMatch(re: &mut Regexp, pmatch: vg::Slice<Match>) -> bool {
    return (((re.nodes.get(((re.root) as i64)).minL == 0i64) && (!re.anchors)) && (((re.flags & FlagNoSub) != 0u32) || (pmatch.len == 0i64)));
}

pub fn Exec(mem: &vg::Arena, re: &mut Regexp, subject: vg::Str, pmatch: vg::Slice<Match>, eflags: u32) -> (bool, Error) {
    if (subject.len > subjectLimit) {
        return (false, compileError(ErrESpace, (1i64).wrapping_neg()));
    }
    if (!re.progOK) {
        if (subject.len < re.minLen) {
            return (false, noError());
        }
        if (runeCount(subject) < re.minLen) {
            return (false, noError());
        }
        if { let _t1 = pmatch; trivialNullMatch(re, _t1) } {
            return (true, noError());
        }
        return (false, compileError(ErrESpace, (1i64).wrapping_neg()));
    }
    if (((re.prog.failMin != failMinNone) && (subject.len >= re.prog.failMin)) && (runeCount(subject) >= re.prog.failMin)) {
        if { let _t2 = pmatch; trivialNullMatch(re, _t2) } {
            return (true, noError());
        }
        if (((re.flags & FlagNoSub) != 0u32) || (pmatch.len == 0i64)) {
            let probe: engineResult = { let _t3 = subject; let _t4 = eflags; runPhaseA(mem, re, _t3, _t4) };
            if probe.matched {
                return (true, noError());
            }
        }
        return (false, compileError(ErrESpace, (1i64).wrapping_neg()));
    }
    let result: engineResult = { let _t5 = subject; let _t6 = eflags; runPhaseA(mem, re, _t5, _t6) };
    if (!result.matched) {
        return (false, noError());
    }
    if (((re.flags & FlagNoSub) != 0u32) || (pmatch.len == 0i64)) {
        return (true, noError());
    }
    setMatch(pmatch, 0i64, result.so, result.eo);
    {
        let mut idx: i64 = 1i64;
        '_b7: while (idx < pmatch.len) {
            '_c7: {
                setMatch(pmatch, idx, (1i64).wrapping_neg(), (1i64).wrapping_neg());
            }
            idx += 1i64;
        }
    }
    if ((pmatch.len == 1i64) || (re.nsub == 0i64)) {
        return (true, noError());
    }
    let mut d: decoded = decodeWindow(mem, subject, result.so, result.eo);
    let caps: vg::Slice<Match> = vg::make::<Match>(mem, (re.nsub + 1i64));
    let serr: Error = { let _t8 = 0i64; let _t9 = d.runes.len; let _t10 = eflags; let _t11 = caps; solveCaptures(mem, re, &mut d, _t8, _t9, _t10, _t11) };
    if (serr.Code != ErrNone) {
        return (false, serr);
    }
    { let _t12 = caps; let _t13 = pmatch; fillMatches(re, &mut d, _t12, _t13) };
    return (true, noError());
}

pub fn MatchIterInit(re: &mut Regexp, limit: i64) -> (MatchIter, Error) {
    let mut it: MatchIter = vg::zero();
    it.lastEnd = (1i64).wrapping_neg();
    it.limit = limit;
    if ((re.flags & FlagNoSub) != 0u32) {
        it.done = true;
        return (it, compileError(ErrENoSub, (1i64).wrapping_neg()));
    }
    if (limit == 0i64) {
        it.done = true;
    }
    return (it, noError());
}

pub fn MatchIterNext(mem: &vg::Arena, re: &mut Regexp, it: &mut MatchIter, subject: vg::Str, eflags: u32, pmatch: vg::Slice<Match>) -> (bool, Error) {
    if it.done {
        return (false, noError());
    }
    if (pmatch.len < (re.nsub + 1i64)) {
        it.done = true;
        return (false, compileError(ErrESpace, (1i64).wrapping_neg()));
    }
    while (it.pos <= subject.len) {
        let flags: u32 = { let _t1 = subject; let _t2 = it.pos; let _t3 = eflags; continuationFlags(re, _t1, _t2, _t3) };
        let _t4 = { let _t5 = subject.tail(it.pos); let _t6 = pmatch; let _t7 = flags; Exec(mem, re, _t5, _t6, _t7) };
        let matched: bool = _t4.0;
        let err: Error = _t4.1;
        if (err.Code != ErrNone) {
            it.done = true;
            return (false, err);
        }
        if (!matched) {
            it.done = true;
            return (false, noError());
        }
        {
            let mut idx: i64 = 0i64;
            '_b8: while (idx < pmatch.len) {
                '_c8: {
                    if (pmatch.get(idx).So >= 0i64) {
                        pmatch.update(idx, it.pos, |mut _t10, _t9| { _t10.So += _t9; _t10 });
                        pmatch.update(idx, it.pos, |mut _t12, _t11| { _t12.Eo += _t11; _t12 });
                    }
                }
                idx += 1i64;
            }
        }
        let so: i64 = pmatch.get(0i64).So;
        let eo: i64 = pmatch.get(0i64).Eo;
        let report: bool = ((so != eo) || (so != it.lastEnd));
        if (eo > so) {
            it.pos = eo;
        } else {
            if (so >= subject.len) {
                it.done = true;
                if (!report) {
                    return (false, noError());
                }
            } else {
                let _t13 = decodeRuneAt(subject, so);
                let size: i64 = _t13.1;
                it.pos = (so + size);
            }
        }
        if report {
            it.lastEnd = eo;
            it.limit -= 1i64;
            if (it.limit == 0i64) {
                it.done = true;
            }
            return (true, noError());
        }
    }
    it.done = true;
    return (false, noError());
}

pub fn parseReplacement(mem: &vg::Arena, replacement: vg::Str, nsub: i64) -> (vg::Slice<replPart>, Error) {
    let mut parts: vg::Slice<replPart> = vg::make_cap::<replPart>(mem, 0i64, 4i64);
    let mut start: i64 = 0i64;
    {
        let mut idx: i64 = 0i64;
        '_b1: while (idx < replacement.len) {
            '_c1: {
                let c: u8 = replacement.byte(idx);
                if ((c != 38u8) && (c != 92u8)) {
                    break '_c1;
                }
                if (start < idx) {
                    let mut lit: replPart = vg::zero();
                    lit.lit = replacement.sub(start, idx);
                    parts = vg::append(mem, parts, lit);
                }
                if (c == 38u8) {
                    let whole: replPart = vg::zero();
                    parts = vg::append(mem, parts, whole);
                    start = (idx + 1i64);
                    break '_c1;
                }
                if ((idx + 1i64) == replacement.len) {
                    return (vg::zero::<vg::Slice<replPart>>(), compileError(ErrEEscape, idx));
                }
                let next: u8 = replacement.byte((idx + 1i64));
                if ((next >= 48u8) && (next <= 57u8)) {
                    let group: i64 = (((next - 48u8)) as i64);
                    if ((group == 0i64) || (group > nsub)) {
                        return (vg::zero::<vg::Slice<replPart>>(), compileError(ErrESubReg, idx));
                    }
                    let mut r#ref: replPart = vg::zero();
                    r#ref.group = group;
                    parts = vg::append(mem, parts, r#ref);
                } else {
                    let mut esc: replPart = vg::zero();
                    esc.lit = replacement.sub((idx + 1i64), (idx + 2i64));
                    parts = vg::append(mem, parts, esc);
                }
                idx += 1i64;
                start = (idx + 1i64);
            }
            idx += 1i64;
        }
    }
    if (start < replacement.len) {
        let mut tail: replPart = vg::zero();
        tail.lit = replacement.tail(start);
        parts = vg::append(mem, parts, tail);
    }
    return (parts, noError());
}

pub fn ReplaceAll(mem: &vg::Arena, re: &mut Regexp, subject: vg::Str, replacement: vg::Str, limit: i64, eflags: u32) -> (vg::Str, Error) {
    if ((re.flags & FlagNoSub) != 0u32) {
        return (vg::lit(b""), compileError(ErrENoSub, (1i64).wrapping_neg()));
    }
    let _t1 = parseReplacement(mem, replacement, re.nsub);
    let parts: vg::Slice<replPart> = _t1.0;
    let perr: Error = _t1.1;
    if (perr.Code != ErrNone) {
        return (vg::lit(b""), perr);
    }
    let _t2 = { let _t3 = limit; MatchIterInit(re, _t3) };
    let mut it: MatchIter = _t2.0;
    let ierr: Error = _t2.1;
    if (ierr.Code != ErrNone) {
        return (vg::lit(b""), ierr);
    }
    let pmatch: vg::Slice<Match> = vg::make::<Match>(mem, (re.nsub + 1i64));
    let mut out: vg::Slice<u8> = vg::zero();
    let mut last: i64 = 0i64;
    let mut any: bool = false;
    loop {
        let _t4 = { let _t5 = subject; let _t6 = eflags; let _t7 = pmatch; MatchIterNext(mem, re, &mut it, _t5, _t6, _t7) };
        let ok: bool = _t4.0;
        let err: Error = _t4.1;
        if (err.Code != ErrNone) {
            return (vg::lit(b""), err);
        }
        if (!ok) {
            break;
        }
        if (!any) {
            out = vg::make_cap::<u8>(mem, 0i64, (subject.len + (subject.len).wrapping_div(8i64)));
            any = true;
        }
        out = vg::append_str(mem, out, subject.sub(last, pmatch.get(0i64).So));
        {
            let mut i: i64 = 0i64;
            '_b8: while (i < parts.len) {
                '_c8: {
                    if (parts.get(i).lit.len != 0i64) {
                        out = vg::append_str(mem, out, parts.get(i).lit);
                        break '_c8;
                    }
                    let r#ref: Match = pmatch.get(parts.get(i).group);
                    if (r#ref.So >= 0i64) {
                        out = vg::append_str(mem, out, subject.sub(r#ref.So, r#ref.Eo));
                    }
                }
                i += 1i64;
            }
        }
        last = pmatch.get(0i64).Eo;
    }
    if (!any) {
        return (subject, noError());
    }
    out = vg::append_str(mem, out, subject.tail(last));
    return (vg::str_from_bytes(mem, out), noError());
}

pub fn fail(p: &mut parser, code: i32, pos: i64) -> i32 {
    if (p.err.Code == ErrNone) {
        p.err = compileError(code, pos);
    }
    return (1i32).wrapping_neg();
}

pub fn addNode(mem: &vg::Arena, p: &mut parser, op: u8) -> i32 {
    let mut n: node = vg::zero();
    n.op = op;
    p.nodes = vg::append(mem, p.nodes, n);
    return (((p.nodes.len - 1i64)) as i32);
}

pub fn eof(p: &mut parser) -> bool {
    return (p.pos >= p.src.len);
}

pub fn peekByte(p: &mut parser) -> u8 {
    if eof(p) {
        return 0u8;
    }
    return p.src.byte(p.pos);
}

pub fn peekByteAt(p: &mut parser, ahead: i64) -> u8 {
    if ((p.pos + ahead) >= p.src.len) {
        return 0u8;
    }
    return p.src.byte((p.pos + ahead));
}

pub fn nextRune(p: &mut parser) -> i32 {
    let _t1 = decodeRuneAt(p.src, p.pos);
    let r: i32 = _t1.0;
    let size: i64 = _t1.1;
    if (r < 0i32) {
        let _ = { let _t2 = ErrBadPat; let _t3 = p.pos; fail(p, _t2, _t3) };
        return invalidRune;
    }
    p.pos += size;
    return r;
}

pub fn parse(mem: &vg::Arena, p: &mut parser, loc: &mut Locale, pattern: vg::Str, flags: u32) -> i32 {
    p.src = pattern;
    p.flags = flags;
    let root: i32 = { let _t1 = false; parseAlt(mem, p, loc, _t1) };
    if (root < 0i32) {
        return (1i32).wrapping_neg();
    }
    if (!eof(p)) {
        return { let _t2 = ErrBadPat; let _t3 = p.pos; fail(p, _t2, _t3) };
    }
    return root;
}

pub fn parseAlt(mem: &vg::Arena, p: &mut parser, loc: &mut Locale, inGroup: bool) -> i32 {
    let first: i32 = { let _t1 = inGroup; parseBranch(mem, p, loc, _t1) };
    if (first < 0i32) {
        return (1i32).wrapping_neg();
    }
    if (peekByte(p) != 124u8) {
        return first;
    }
    let alt: i32 = { let _t2 = opAlt; addNode(mem, p, _t2) };
    p.nodes.update(((alt) as i64), vg::append(mem, p.nodes.get(((alt) as i64)).ch, first), |mut _t4, _t3| { _t4.ch = _t3; _t4 });
    while (peekByte(p) == 124u8) {
        p.pos += 1i64;
        let branch: i32 = { let _t5 = inGroup; parseBranch(mem, p, loc, _t5) };
        if (branch < 0i32) {
            return (1i32).wrapping_neg();
        }
        p.nodes.update(((alt) as i64), vg::append(mem, p.nodes.get(((alt) as i64)).ch, branch), |mut _t7, _t6| { _t7.ch = _t6; _t7 });
    }
    return alt;
}

pub fn parseBranch(mem: &vg::Arena, p: &mut parser, loc: &mut Locale, inGroup: bool) -> i32 {
    let mut exprs: vg::Slice<i32> = vg::make_cap::<i32>(mem, 0i64, 4i64);
    loop {
        if eof(p) {
            break;
        }
        let c: u8 = peekByte(p);
        if (c == 124u8) {
            break;
        }
        if ((c == 41u8) && inGroup) {
            break;
        }
        let expr: i32 = parseExpr(mem, p, loc);
        if (expr < 0i32) {
            return (1i32).wrapping_neg();
        }
        exprs = vg::append(mem, exprs, expr);
    }
    if (exprs.len == 0i64) {
        return { let _t1 = ErrBadPat; let _t2 = p.pos; fail(p, _t1, _t2) };
    }
    if (exprs.len == 1i64) {
        return exprs.get(0i64);
    }
    let cat: i32 = { let _t3 = opConcat; addNode(mem, p, _t3) };
    p.nodes.update(((cat) as i64), exprs, |mut _t5, _t4| { _t5.ch = _t4; _t5 });
    return cat;
}

pub fn isDupByte(c: u8) -> bool {
    return ((((c == 42u8) || (c == 43u8)) || (c == 63u8)) || (c == 123u8));
}

pub fn parseExpr(mem: &vg::Arena, p: &mut parser, loc: &mut Locale) -> i32 {
    let start: i64 = p.pos;
    let c: u8 = peekByte(p);
    if ((c == 94u8) || (c == 36u8)) {
        p.pos += 1i64;
        if isDupByte(peekByte(p)) {
            return { let _t1 = ErrBadRpt; let _t2 = p.pos; fail(p, _t1, _t2) };
        }
        if (c == 94u8) {
            return { let _t3 = opBOL; addNode(mem, p, _t3) };
        }
        return { let _t4 = opEOL; addNode(mem, p, _t4) };
    }
    if isDupByte(c) {
        return { let _t5 = ErrBadRpt; let _t6 = start; fail(p, _t5, _t6) };
    }
    let mut primary: i32 = vg::zero();
    {
        let _t7 = c;
        if _t7 == 40u8 {
            p.pos += 1i64;
            p.groups += 1i64;
            let index: i64 = p.groups;
            let sub: i32 = { let _t8 = true; parseAlt(mem, p, loc, _t8) };
            if (sub < 0i32) {
                return (1i32).wrapping_neg();
            }
            if (peekByte(p) != 41u8) {
                return { let _t9 = ErrEParen; let _t10 = start; fail(p, _t9, _t10) };
            }
            p.pos += 1i64;
            primary = { let _t11 = opGroup; addNode(mem, p, _t11) };
            p.nodes.update(((primary) as i64), vg::append(mem, p.nodes.get(((primary) as i64)).ch, sub), |mut _t13, _t12| { _t13.ch = _t12; _t13 });
            p.nodes.update(((primary) as i64), index, |mut _t15, _t14| { _t15.index = _t14; _t15 });
        } else if _t7 == 91u8 {
            primary = parseBracket(mem, p, loc);
            if (primary < 0i32) {
                return (1i32).wrapping_neg();
            }
        } else if _t7 == 92u8 {
            p.pos += 1i64;
            if eof(p) {
                return { let _t16 = ErrEEscape; let _t17 = start; fail(p, _t16, _t17) };
            }
            let r: i32 = nextRune(p);
            if (r < 0i32) {
                return (1i32).wrapping_neg();
            }
            if ((((((((((((((r == 94i32) || (r == 46i32)) || (r == 91i32)) || (r == 93i32)) || (r == 36i32)) || (r == 40i32)) || (r == 41i32)) || (r == 124i32)) || (r == 42i32)) || (r == 43i32)) || (r == 63i32)) || (r == 123i32)) || (r == 125i32)) || (r == 92i32)) {
                primary = { let _t18 = r; charNode(mem, p, loc, _t18) };
            } else {
                return { let _t19 = ErrBadPat; let _t20 = start; fail(p, _t19, _t20) };
            }
        } else if _t7 == 46u8 {
            p.pos += 1i64;
            primary = { let _t21 = opAny; addNode(mem, p, _t21) };
        } else {
            let r_2: i32 = nextRune(p);
            if (r_2 < 0i32) {
                return (1i32).wrapping_neg();
            }
            primary = { let _t22 = r_2; charNode(mem, p, loc, _t22) };
        }
    }
    return { let _t23 = primary; parseDup(mem, p, _t23) };
}

pub fn charNode(mem: &vg::Arena, p: &mut parser, loc: &mut Locale, r: i32) -> i32 {
    let n: i32 = { let _t1 = opChar; addNode(mem, p, _t1) };
    p.nodes.update(((n) as i64), r, |mut _t3, _t2| { _t3.r = _t2; _t3 });
    if ((p.flags & FlagICase) != 0u32) {
        let mut fold: vg::Slice<i32> = vg::make_cap::<i32>(mem, 0i64, 3i64);
        fold = appendUnique(mem, fold, r);
        fold = appendUnique(mem, fold, { let _t4 = r; localeToUpper(loc, _t4) });
        fold = appendUnique(mem, fold, { let _t5 = r; localeToLower(loc, _t5) });
        p.nodes.update(((n) as i64), fold, |mut _t7, _t6| { _t7.fold = _t6; _t7 });
    }
    return n;
}

pub fn appendUnique(mem: &vg::Arena, runes: vg::Slice<i32>, r: i32) -> vg::Slice<i32> {
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < runes.len) {
            '_c1: {
                if (runes.get(i) == r) {
                    return runes;
                }
            }
            i += 1i64;
        }
    }
    return vg::append(mem, runes, r);
}

pub fn parseDup(mem: &vg::Arena, p: &mut parser, operand: i32) -> i32 {
    let c: u8 = peekByte(p);
    let mut lo: i64 = vg::zero();
    let mut hi: i64 = vg::zero();
    {
        let _t1 = c;
        if _t1 == 42u8 {
            p.pos += 1i64;
            lo = 0i64;
            hi = infinite;
        } else if _t1 == 43u8 {
            p.pos += 1i64;
            lo = 1i64;
            hi = infinite;
        } else if _t1 == 63u8 {
            p.pos += 1i64;
            lo = 0i64;
            hi = 1i64;
        } else if _t1 == 123u8 {
            let _t2 = parseInterval(p);
            let iv: interval = _t2.0;
            let ok: bool = _t2.1;
            if (!ok) {
                return (1i32).wrapping_neg();
            }
            lo = iv.lo;
            hi = iv.hi;
        } else {
            return operand;
        }
    }
    let mut minimal: bool = ((p.flags & FlagMinimal) != 0u32);
    if (peekByte(p) == 63u8) {
        p.pos += 1i64;
        minimal = (!minimal);
    }
    if isDupByte(peekByte(p)) {
        return { let _t3 = ErrBadRpt; let _t4 = p.pos; fail(p, _t3, _t4) };
    }
    let rep: i32 = { let _t5 = opRepeat; addNode(mem, p, _t5) };
    p.nodes.update(((rep) as i64), vg::append(mem, p.nodes.get(((rep) as i64)).ch, operand), |mut _t7, _t6| { _t7.ch = _t6; _t7 });
    p.nodes.update(((rep) as i64), lo, |mut _t9, _t8| { _t9.min = _t8; _t9 });
    p.nodes.update(((rep) as i64), hi, |mut _t11, _t10| { _t11.max = _t10; _t11 });
    p.nodes.update(((rep) as i64), minimal, |mut _t13, _t12| { _t13.minimal = _t12; _t13 });
    return rep;
}

pub fn parseInterval(p: &mut parser) -> (interval, bool) {
    let mut iv: interval = vg::zero();
    let start: i64 = p.pos;
    p.pos += 1i64;
    let _t1 = parseCount(p);
    let lo: i64 = _t1.0;
    let ok: bool = _t1.1;
    if (!ok) {
        { let _t2 = start; intervalError(p, _t2) };
        return (iv, false);
    }
    let c: u8 = peekByte(p);
    if (c == 125u8) {
        p.pos += 1i64;
        iv.lo = lo;
        iv.hi = lo;
        return (iv, true);
    }
    if (c != 44u8) {
        { let _t3 = start; intervalError(p, _t3) };
        return (iv, false);
    }
    p.pos += 1i64;
    if (peekByte(p) == 125u8) {
        p.pos += 1i64;
        iv.lo = lo;
        iv.hi = infinite;
        return (iv, true);
    }
    let _t4 = parseCount(p);
    let hi: i64 = _t4.0;
    let ok2: bool = _t4.1;
    if ((!ok2) || (peekByte(p) != 125u8)) {
        { let _t5 = start; intervalError(p, _t5) };
        return (iv, false);
    }
    p.pos += 1i64;
    if (hi < lo) {
        let _ = { let _t6 = ErrBadBR; let _t7 = start; fail(p, _t6, _t7) };
        return (iv, false);
    }
    iv.lo = lo;
    iv.hi = hi;
    return (iv, true);
}

pub fn intervalError(p: &mut parser, start: i64) {
    if (indexOfByte(p.src.tail(p.pos), 125u8) >= 0i64) {
        let _ = { let _t1 = ErrBadBR; let _t2 = start; fail(p, _t1, _t2) };
        return;
    }
    let _ = { let _t3 = ErrEBrace; let _t4 = start; fail(p, _t3, _t4) };
}

pub fn parseCount(p: &mut parser) -> (i64, bool) {
    if ((eof(p) || (p.src.byte(p.pos) < 48u8)) || (p.src.byte(p.pos) > 57u8)) {
        return (0i64, false);
    }
    let mut value: i64 = 0i64;
    while (((!eof(p)) && (p.src.byte(p.pos) >= 48u8)) && (p.src.byte(p.pos) <= 57u8)) {
        value = ((value * 10i64) + (((p.src.byte(p.pos) - 48u8)) as i64));
        if (value > dupMax) {
            return (0i64, false);
        }
        p.pos += 1i64;
    }
    return (value, true);
}

pub fn satAdd(a: i64, b: i64) -> i64 {
    return std::cmp::min((a + b), lenInf);
}

pub fn satMul(a: i64, b: i64) -> i64 {
    let product: i64 = (a * b);
    if (product > lenInf) {
        return lenInf;
    }
    return product;
}

pub fn computeLengths(mem: &vg::Arena, nodes: vg::Slice<node>, loc: &mut Locale, brackets: vg::Slice<bracketSet>, ni: i32) {
    {
        let mut i: i64 = 0i64;
        '_b1: while (i < nodes.get(((ni) as i64)).ch.len) {
            '_c1: {
                { let _t2 = nodes; let _t3 = brackets; let _t4 = nodes.get(((ni) as i64)).ch.get(i); computeLengths(mem, _t2, loc, _t3, _t4) };
            }
            i += 1i64;
        }
    }
    {
        let _t5 = nodes.get(((ni) as i64)).op;
        if _t5 == opChar || _t5 == opAny {
            nodes.update(((ni) as i64), 1i64, |mut _t7, _t6| { _t7.minL = _t6; _t7 });
            nodes.update(((ni) as i64), 1i64, |mut _t9, _t8| { _t9.maxL = _t8; _t9 });
        } else if _t5 == opBracket {
            nodes.update(((ni) as i64), (({ let _t10 = brackets; let _t11 = nodes.get(((ni) as i64)).br; bracketMinChars(_t10, _t11, loc) }) as i64), |mut _t13, _t12| { _t13.minL = _t12; _t13 });
            nodes.update(((ni) as i64), 1i64, |mut _t15, _t14| { _t15.maxL = _t14; _t15 });
            if bracketHasMultiMembers(brackets, nodes.get(((ni) as i64)).br) {
                nodes.update(((ni) as i64), localeMaxElementLength(loc), |mut _t17, _t16| { _t17.maxL = _t16; _t17 });
            }
        } else if _t5 == opBOL || _t5 == opEOL {
            nodes.update(((ni) as i64), 0i64, |mut _t19, _t18| { _t19.minL = _t18; _t19 });
            nodes.update(((ni) as i64), 0i64, |mut _t21, _t20| { _t21.maxL = _t20; _t21 });
        } else if _t5 == opGroup {
            let child: i32 = nodes.get(((ni) as i64)).ch.get(0i64);
            nodes.update(((ni) as i64), nodes.get(((child) as i64)).minL, |mut _t23, _t22| { _t23.minL = _t22; _t23 });
            nodes.update(((ni) as i64), nodes.get(((child) as i64)).maxL, |mut _t25, _t24| { _t25.maxL = _t24; _t25 });
        } else if _t5 == opConcat {
            let count: i64 = nodes.get(((ni) as i64)).ch.len;
            nodes.update(((ni) as i64), vg::make::<i64>(mem, (count + 1i64)), |mut _t27, _t26| { _t27.sufMin = _t26; _t27 });
            nodes.update(((ni) as i64), vg::make::<i64>(mem, (count + 1i64)), |mut _t29, _t28| { _t29.sufMax = _t28; _t29 });
            {
                let mut k: i64 = (count - 1i64);
                '_b30: while (k >= 0i64) {
                    '_c30: {
                        let child_2: i32 = nodes.get(((ni) as i64)).ch.get(k);
                        nodes.get(((ni) as i64)).sufMin.set(k, satAdd(nodes.get(((ni) as i64)).sufMin.get((k + 1i64)), nodes.get(((child_2) as i64)).minL));
                        nodes.get(((ni) as i64)).sufMax.set(k, satAdd(nodes.get(((ni) as i64)).sufMax.get((k + 1i64)), nodes.get(((child_2) as i64)).maxL));
                    }
                    k -= 1i64;
                }
            }
            nodes.update(((ni) as i64), nodes.get(((ni) as i64)).sufMin.get(0i64), |mut _t32, _t31| { _t32.minL = _t31; _t32 });
            nodes.update(((ni) as i64), nodes.get(((ni) as i64)).sufMax.get(0i64), |mut _t34, _t33| { _t34.maxL = _t33; _t34 });
        } else if _t5 == opAlt {
            nodes.update(((ni) as i64), lenInf, |mut _t36, _t35| { _t36.minL = _t35; _t36 });
            nodes.update(((ni) as i64), 0i64, |mut _t38, _t37| { _t38.maxL = _t37; _t38 });
            {
                let mut i_2: i64 = 0i64;
                '_b39: while (i_2 < nodes.get(((ni) as i64)).ch.len) {
                    '_c39: {
                        let child_3: i32 = nodes.get(((ni) as i64)).ch.get(i_2);
                        nodes.update(((ni) as i64), std::cmp::min(nodes.get(((ni) as i64)).minL, nodes.get(((child_3) as i64)).minL), |mut _t41, _t40| { _t41.minL = _t40; _t41 });
                        nodes.update(((ni) as i64), std::cmp::max(nodes.get(((ni) as i64)).maxL, nodes.get(((child_3) as i64)).maxL), |mut _t43, _t42| { _t43.maxL = _t42; _t43 });
                    }
                    i_2 += 1i64;
                }
            }
        } else if _t5 == opRepeat {
            let child_4: i32 = nodes.get(((ni) as i64)).ch.get(0i64);
            nodes.update(((ni) as i64), satMul(nodes.get(((ni) as i64)).min, nodes.get(((child_4) as i64)).minL), |mut _t45, _t44| { _t45.minL = _t44; _t45 });
            if ((nodes.get(((ni) as i64)).max == 0i64) || (nodes.get(((child_4) as i64)).maxL == 0i64)) {
                nodes.update(((ni) as i64), 0i64, |mut _t47, _t46| { _t47.maxL = _t46; _t47 });
            } else {
                if (nodes.get(((ni) as i64)).max == infinite) {
                    nodes.update(((ni) as i64), lenInf, |mut _t49, _t48| { _t49.maxL = _t48; _t49 });
                } else {
                    nodes.update(((ni) as i64), satMul(nodes.get(((ni) as i64)).max, nodes.get(((child_4) as i64)).maxL), |mut _t51, _t50| { _t51.maxL = _t50; _t51 });
                }
            }
        }
    }
}

pub fn utf8Cont(c: u8) -> bool {
    return ((c >= 128u8) && (c <= 191u8));
}

pub fn decodeRuneAt(s: vg::Str, at: i64) -> (i32, i64) {
    let c0: u8 = s.byte(at);
    if (c0 < 128u8) {
        return (((c0) as i32), 1i64);
    }
    if (c0 < 194u8) {
        return (invalidRune, 1i64);
    }
    if (c0 < 224u8) {
        if (((at + 1i64) >= s.len) || (!utf8Cont(s.byte((at + 1i64))))) {
            return (invalidRune, 1i64);
        }
        return ((((((c0 & 31u8)) as i32) << 6i64) | (((s.byte((at + 1i64)) & 63u8)) as i32)), 2i64);
    }
    if (c0 < 240u8) {
        if (((at + 1i64) >= s.len) || (!utf8Cont(s.byte((at + 1i64))))) {
            return (invalidRune, 1i64);
        }
        if ((c0 == 224u8) && (s.byte((at + 1i64)) < 160u8)) {
            return (invalidRune, 1i64);
        }
        if ((c0 == 237u8) && (s.byte((at + 1i64)) > 159u8)) {
            return (invalidRune, 1i64);
        }
        if (((at + 2i64) >= s.len) || (!utf8Cont(s.byte((at + 2i64))))) {
            return (invalidRune, 1i64);
        }
        return (((((((c0 & 15u8)) as i32) << 12i64) | ((((s.byte((at + 1i64)) & 63u8)) as i32) << 6i64)) | (((s.byte((at + 2i64)) & 63u8)) as i32)), 3i64);
    }
    if (c0 < 245u8) {
        if (((at + 1i64) >= s.len) || (!utf8Cont(s.byte((at + 1i64))))) {
            return (invalidRune, 1i64);
        }
        if ((c0 == 240u8) && (s.byte((at + 1i64)) < 144u8)) {
            return (invalidRune, 1i64);
        }
        if ((c0 == 244u8) && (s.byte((at + 1i64)) > 143u8)) {
            return (invalidRune, 1i64);
        }
        if (((at + 2i64) >= s.len) || (!utf8Cont(s.byte((at + 2i64))))) {
            return (invalidRune, 1i64);
        }
        if (((at + 3i64) >= s.len) || (!utf8Cont(s.byte((at + 3i64))))) {
            return (invalidRune, 1i64);
        }
        return ((((((((c0 & 7u8)) as i32) << 18i64) | ((((s.byte((at + 1i64)) & 63u8)) as i32) << 12i64)) | ((((s.byte((at + 2i64)) & 63u8)) as i32) << 6i64)) | (((s.byte((at + 3i64)) & 63u8)) as i32)), 4i64);
    }
    return (invalidRune, 1i64);
}

pub fn runeCount(s: vg::Str) -> i64 {
    let mut count: i64 = 0i64;
    let mut at: i64 = 0i64;
    while (at < s.len) {
        let _t1 = decodeRuneAt(s, at);
        let size: i64 = _t1.1;
        at += size;
        count += 1i64;
    }
    return count;
}

pub fn utf8LeadByte(r: i32) -> u8 {
    if (r < 128i32) {
        return ((r) as u8);
    }
    if (r < 2048i32) {
        return (((192i32 | (r >> 6i64))) as u8);
    }
    if (r < 65536i32) {
        return (((224i32 | (r >> 12i64))) as u8);
    }
    return (((240i32 | (r >> 18i64))) as u8);
}

