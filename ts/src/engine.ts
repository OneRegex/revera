// Code generated by vegoc from the Vego JSON form. DO NOT EDIT.

import * as vg from "./vg.ts";

const _s1: vg.Str = vg.lit("alnum");
const _s2: vg.Str = vg.lit("alpha");
const _s3: vg.Str = vg.lit("blank");
const _s4: vg.Str = vg.lit("cntrl");
const _s5: vg.Str = vg.lit("digit");
const _s6: vg.Str = vg.lit("graph");
const _s7: vg.Str = vg.lit("lower");
const _s8: vg.Str = vg.lit("print");
const _s9: vg.Str = vg.lit("punct");
const _s10: vg.Str = vg.lit("space");
const _s11: vg.Str = vg.lit("upper");
const _s12: vg.Str = vg.lit("xdigit");
const _s13: vg.Str = vg.lit(".]");
const _s14: vg.Str = vg.lit("=]");
const _s15: vg.Str = vg.lit(":]");
const _s16: vg.Str = vg.lit("success");
const _s17: vg.Str = vg.lit("no match");
const _s18: vg.Str = vg.lit("invalid regular expression");
const _s19: vg.Str = vg.lit("invalid collating element");
const _s20: vg.Str = vg.lit("invalid character class");
const _s21: vg.Str = vg.lit("invalid or trailing backslash");
const _s22: vg.Str = vg.lit("invalid backreference");
const _s23: vg.Str = vg.lit("unbalanced bracket");
const _s24: vg.Str = vg.lit("unbalanced parenthesis");
const _s25: vg.Str = vg.lit("unbalanced brace");
const _s26: vg.Str = vg.lit("invalid interval");
const _s27: vg.Str = vg.lit("invalid range endpoint");
const _s28: vg.Str = vg.lit("capacity limit reached");
const _s29: vg.Str = vg.lit("repetition without an operand");
const _s30: vg.Str = vg.lit("offsets requested from a NoSub expression");
const _s31: vg.Str = vg.lit("unknown error");
const _s32: vg.Str = vg.lit("RVLOC001");
const _s33: vg.Str = vg.lit("utf8");
const _s34: vg.Str = vg.lit("utf-8");
const _s35: vg.Str = vg.lit("collation=");
const _s36: vg.Str = vg.lit("dict");
const _s37: vg.Str = vg.lit("dictionary");
const _s38: vg.Str = vg.lit("phonebk");
const _s39: vg.Str = vg.lit("phonebook");
const _s40: vg.Str = vg.lit("trad");
const _s41: vg.Str = vg.lit("traditional");
const _s42: vg.Str = vg.lit("c");
const _s43: vg.Str = vg.lit("posix");
const _s44: vg.Str = vg.lit("standard");

export const itemChar: number = 0;
export const itemElem: number = 1;
export const itemEquiv: number = 2;
export const itemClass: number = 3;
export const capWorkLimit: number = 50000000;
export const solverArenaLimit: number = 67108864;
export const contractCap: bigint = 4611686018427387904n;
export const frameBytes: number = 256;
export const matcherStackBytes: number = 2048;
export const singleLookupFrames: number = 8;
export const multiLookupFrames: number = 16;
export const ptreeBytes: number = 48;
export const kidBytes: number = 8;
export const mapEntryBytes: number = 128;
export const matchBytes: number = 16;
export const profileRowCost: number = 13;
export const maxElemAhead: number = 8;
export const queueCompactFactor: number = 2;
export const ErrNone: number = 0;
export const ErrNoMatch: number = 1;
export const ErrBadPat: number = 2;
export const ErrECollate: number = 3;
export const ErrECType: number = 4;
export const ErrEEscape: number = 5;
export const ErrESubReg: number = 6;
export const ErrEBrack: number = 7;
export const ErrEParen: number = 8;
export const ErrEBrace: number = 9;
export const ErrBadBR: number = 10;
export const ErrERange: number = 11;
export const ErrESpace: number = 12;
export const ErrBadRpt: number = 13;
export const ErrENoSub: number = 14;
export const FlagICase: number = 1;
export const FlagNewline: number = 2;
export const FlagNoSub: number = 4;
export const FlagMinimal: number = 8;
export const ExecNotBOL: number = 1;
export const ExecNotEOL: number = 2;
export const dupMax: number = 255;
export const classAlnum: number = 0;
export const classAlpha: number = 1;
export const classBlank: number = 2;
export const classCntrl: number = 3;
export const classDigit: number = 4;
export const classGraph: number = 5;
export const classLower: number = 6;
export const classPrint: number = 7;
export const classPunct: number = 8;
export const classSpace: number = 9;
export const classUpper: number = 10;
export const classXdigit: number = 11;
export const numClasses: number = 12;
export const firstSequenceID: number = 1114112;
export const secCtypeStage1: number = 0;
export const secCtypeBlocks: number = 1;
export const secCaseDefault: number = 2;
export const secCaseTurkic: number = 3;
export const secSeqCodepoints: number = 4;
export const secSequences: number = 5;
export const secRootContractions: number = 6;
export const secRootEquivalences: number = 7;
export const secCollationOverrides: number = 8;
export const secContractionAdds: number = 9;
export const secContractionRemoves: number = 10;
export const secCollationProfiles: number = 11;
export const secTypeNames: number = 12;
export const secTypeNameOffsets: number = 13;
export const secLocaleNames: number = 14;
export const secLocaleNameOffsets: number = 15;
export const secLocales: number = 16;
export const secLocaleTypes: number = 17;
export const secInvUpperDefault: number = 18;
export const secInvLowerDefault: number = 19;
export const secInvUpperTurkic: number = 20;
export const secInvLowerTurkic: number = 21;
export const secScalars: number = 22;
export const numSections: number = 23;
export const ctypeStage1Entries: number = 4352;
export const normalizedNameMax: number = 127;
export const maxPreimages: number = 16;
export const iRune: number = 0;
export const iRuneFold: number = 1;
export const iAny: number = 2;
export const iBracket: number = 3;
export const iBOL: number = 4;
export const iEOL: number = 5;
export const iSplit: number = 6;
export const iJmp: number = 7;
export const iMatch: number = 8;
export const iFail: number = 9;
export const failMinNone: number = 4611686018427387904;
export const maxProgram: number = 1048576;
export const maskWidth: number = 64;
export const subjectLimit: number = 2147483647;
export const lengthCap: number = 1099511627776;
export const opChar: number = 0;
export const opAny: number = 1;
export const opBracket: number = 2;
export const opBOL: number = 3;
export const opEOL: number = 4;
export const opConcat: number = 5;
export const opAlt: number = 6;
export const opRepeat: number = 7;
export const opGroup: number = 8;
export const infinite: number = (-1);
export const lenInf: number = 1073741824;
export const invalidRune: number = (-1);

export const classNames: vg.Str[] = [_s1, _s2, _s3, _s4, _s5, _s6, _s7, _s8, _s9, _s10, _s11, _s12];

export class runeRange {
    lo: number;
    hi: number;

    constructor(lo: number = 0, hi: number = 0) {
        this.lo = lo;
        this.hi = hi;
    }

    clone(): runeRange {
        return new runeRange(this.lo, this.hi);
    }

    static eq(a: runeRange, b: runeRange): boolean {
        return a.lo === b.lo && a.hi === b.hi;
    }

    static readonly elem: vg.Elem<runeRange> = vg.structElem(() => new runeRange(), 8);
}

export class bracketSet {
    negated: boolean;
    icase: boolean;
    nlMode: boolean;
    ranges: vg.Slice<runeRange>;
    classMask: number;
    elems: vg.Slice<vg.Slice<number>>;
    equivs: vg.Slice<vg.Slice<number>>;
    multiLens: number;

    constructor(negated: boolean = false, icase: boolean = false, nlMode: boolean = false, ranges: vg.Slice<runeRange> = vg.NIL, classMask: number = 0, elems: vg.Slice<vg.Slice<number>> = vg.NIL, equivs: vg.Slice<vg.Slice<number>> = vg.NIL, multiLens: number = 0) {
        this.negated = negated;
        this.icase = icase;
        this.nlMode = nlMode;
        this.ranges = ranges;
        this.classMask = classMask;
        this.elems = elems;
        this.equivs = equivs;
        this.multiLens = multiLens;
    }

    clone(): bracketSet {
        return new bracketSet(this.negated, this.icase, this.nlMode, this.ranges, this.classMask, this.elems, this.equivs, this.multiLens);
    }

    static readonly elem: vg.Elem<bracketSet> = vg.structElem(() => new bracketSet(), 103);
}

export class bracketItem {
    kind: number;
    r: number;
    seq: vg.Slice<number>;
    class: number;

    constructor(kind: number = 0, r: number = 0, seq: vg.Slice<number> = vg.NIL, class_: number = 0) {
        this.kind = kind;
        this.r = r;
        this.seq = seq;
        this.class = class_;
    }

    clone(): bracketItem {
        return new bracketItem(this.kind, this.r, this.seq, this.class);
    }

    static readonly elem: vg.Elem<bracketItem> = vg.structElem(() => new bracketItem(), 38);
}

export class elemBuf {
    r: Int32Array;

    constructor(r: Int32Array = new Int32Array(8)) {
        this.r = r;
    }

    clone(): elemBuf {
        return new elemBuf(this.r.slice());
    }

    static eq(a: elemBuf, b: elemBuf): boolean {
        return vg.arrEq(a.r, b.r, (x, y) => x === y);
    }

    static readonly elem: vg.Elem<elemBuf> = vg.structElem(() => new elemBuf(), 32);
}

export class ptree {
    n: number;
    i: number;
    j: number;
    branch: number;
    kidsOff: number;
    kidsLen: number;

    constructor(n: number = 0, i: number = 0, j: number = 0, branch: number = 0, kidsOff: number = 0, kidsLen: number = 0) {
        this.n = n;
        this.i = i;
        this.j = j;
        this.branch = branch;
        this.kidsOff = kidsOff;
        this.kidsLen = kidsLen;
    }

    clone(): ptree {
        return new ptree(this.n, this.i, this.j, this.branch, this.kidsOff, this.kidsLen);
    }

    static eq(a: ptree, b: ptree): boolean {
        return a.n === b.n && a.i === b.i && a.j === b.j && a.branch === b.branch && a.kidsOff === b.kidsOff && a.kidsLen === b.kidsLen;
    }

    static readonly elem: vg.Elem<ptree> = vg.structElem(() => new ptree(), 24);
}

export class capSolver {
    eflags: number;
    memo: memoTab;
    cmemo: memoTab;
    rmemo: memoTab;
    ctrA: vg.Slice<number>;
    ctrB: vg.Slice<number>;
    work: number;
    failed: boolean;
    trees: vg.Slice<ptree>;
    kidStore: vg.Slice<number>;

    constructor(eflags: number = 0, memo: memoTab = new memoTab(), cmemo: memoTab = new memoTab(), rmemo: memoTab = new memoTab(), ctrA: vg.Slice<number> = vg.NIL, ctrB: vg.Slice<number> = vg.NIL, work: number = 0, failed: boolean = false, trees: vg.Slice<ptree> = vg.NIL, kidStore: vg.Slice<number> = vg.NIL) {
        this.eflags = eflags;
        this.memo = memo;
        this.cmemo = cmemo;
        this.rmemo = rmemo;
        this.ctrA = ctrA;
        this.ctrB = ctrB;
        this.work = work;
        this.failed = failed;
        this.trees = trees;
        this.kidStore = kidStore;
    }

    clone(): capSolver {
        return new capSolver(this.eflags, this.memo.clone(), this.cmemo.clone(), this.rmemo.clone(), this.ctrA, this.ctrB, this.work, this.failed, this.trees, this.kidStore);
    }

    static readonly elem: vg.Elem<capSolver> = vg.structElem(() => new capSolver(), 453);
}

export class repBest {
    off: number;
    length: number;
    tree: number;
    found: boolean;

    constructor(off: number = 0, length: number = 0, tree: number = 0, found: boolean = false) {
        this.off = off;
        this.length = length;
        this.tree = tree;
        this.found = found;
    }

    clone(): repBest {
        return new repBest(this.off, this.length, this.tree, this.found);
    }

    static eq(a: repBest, b: repBest): boolean {
        return a.off === b.off && a.length === b.length && a.tree === b.tree && a.found === b.found;
    }

    static readonly elem: vg.Elem<repBest> = vg.structElem(() => new repBest(), 21);
}

export class repWin {
    off: number;
    length: number;
    ok: boolean;

    constructor(off: number = 0, length: number = 0, ok: boolean = false) {
        this.off = off;
        this.length = length;
        this.ok = ok;
    }

    clone(): repWin {
        return new repWin(this.off, this.length, this.ok);
    }

    static eq(a: repWin, b: repWin): boolean {
        return a.off === b.off && a.length === b.length && a.ok === b.ok;
    }

    static readonly elem: vg.Elem<repWin> = vg.structElem(() => new repWin(), 17);
}

export class BackendContract {
    HeapBytes: bigint;
    StackBytes: bigint;
    Steps: bigint;

    constructor(HeapBytes: bigint = 0n, StackBytes: bigint = 0n, Steps: bigint = 0n) {
        this.HeapBytes = HeapBytes;
        this.StackBytes = StackBytes;
        this.Steps = Steps;
    }

    clone(): BackendContract {
        return new BackendContract(this.HeapBytes, this.StackBytes, this.Steps);
    }

    static eq(a: BackendContract, b: BackendContract): boolean {
        return a.HeapBytes === b.HeapBytes && a.StackBytes === b.StackBytes && a.Steps === b.Steps;
    }

    static readonly elem: vg.Elem<BackendContract> = vg.structElem(() => new BackendContract(), 24);
}

export class Contract {
    MaxInput: number;
    Matcher: BackendContract;
    OnePass: BackendContract;
    HasOnePass: boolean;
    Solver: BackendContract;
    HasSolver: boolean;

    constructor(MaxInput: number = 0, Matcher: BackendContract = new BackendContract(), OnePass: BackendContract = new BackendContract(), HasOnePass: boolean = false, Solver: BackendContract = new BackendContract(), HasSolver: boolean = false) {
        this.MaxInput = MaxInput;
        this.Matcher = Matcher;
        this.OnePass = OnePass;
        this.HasOnePass = HasOnePass;
        this.Solver = Solver;
        this.HasSolver = HasSolver;
    }

    clone(): Contract {
        return new Contract(this.MaxInput, this.Matcher.clone(), this.OnePass.clone(), this.HasOnePass, this.Solver.clone(), this.HasSolver);
    }

    static eq(a: Contract, b: Contract): boolean {
        return a.MaxInput === b.MaxInput && BackendContract.eq(a.Matcher, b.Matcher) && BackendContract.eq(a.OnePass, b.OnePass) && a.HasOnePass === b.HasOnePass && BackendContract.eq(a.Solver, b.Solver) && a.HasSolver === b.HasSolver;
    }

    static readonly elem: vg.Elem<Contract> = vg.structElem(() => new Contract(), 82);
}

export class lookupCosts {
    sequenceSearch: bigint;
    contraction: bigint;
    primaryToken: bigint;
    classMask: bigint;
    casePreimages: bigint;
    caseConvert: bigint;
    preimages: bigint;

    constructor(sequenceSearch: bigint = 0n, contraction: bigint = 0n, primaryToken: bigint = 0n, classMask: bigint = 0n, casePreimages: bigint = 0n, caseConvert: bigint = 0n, preimages: bigint = 0n) {
        this.sequenceSearch = sequenceSearch;
        this.contraction = contraction;
        this.primaryToken = primaryToken;
        this.classMask = classMask;
        this.casePreimages = casePreimages;
        this.caseConvert = caseConvert;
        this.preimages = preimages;
    }

    clone(): lookupCosts {
        return new lookupCosts(this.sequenceSearch, this.contraction, this.primaryToken, this.classMask, this.casePreimages, this.caseConvert, this.preimages);
    }

    static eq(a: lookupCosts, b: lookupCosts): boolean {
        return a.sequenceSearch === b.sequenceSearch && a.contraction === b.contraction && a.primaryToken === b.primaryToken && a.classMask === b.classMask && a.casePreimages === b.casePreimages && a.caseConvert === b.caseConvert && a.preimages === b.preimages;
    }

    static readonly elem: vg.Elem<lookupCosts> = vg.structElem(() => new lookupCosts(), 56);
}

export class slotTable {
    stamp: vg.Slice<number>;
    starts: vg.Slice<number>;
    ctr: vg.Slice<number>;
    active: vg.Slice<number>;
    gen: number;

    constructor(stamp: vg.Slice<number> = vg.NIL, starts: vg.Slice<number> = vg.NIL, ctr: vg.Slice<number> = vg.NIL, active: vg.Slice<number> = vg.NIL, gen: number = 0) {
        this.stamp = stamp;
        this.starts = starts;
        this.ctr = ctr;
        this.active = active;
        this.gen = gen;
    }

    clone(): slotTable {
        return new slotTable(this.stamp, this.starts, this.ctr, this.active, this.gen);
    }

    static readonly elem: vg.Elem<slotTable> = vg.structElem(() => new slotTable(), 132);
}

export class engineWS {
    slots: vg.Slice<slotTable>;
    queue: vg.Slice<number>;
    onq: vg.Slice<number>;
    bestCtr: vg.Slice<number>;
    ctrBuf: vg.Slice<number>;
    zeros: vg.Slice<number>;
    ahead: vg.Slice<number>;

    constructor(slots: vg.Slice<slotTable> = vg.NIL, queue: vg.Slice<number> = vg.NIL, onq: vg.Slice<number> = vg.NIL, bestCtr: vg.Slice<number> = vg.NIL, ctrBuf: vg.Slice<number> = vg.NIL, zeros: vg.Slice<number> = vg.NIL, ahead: vg.Slice<number> = vg.NIL) {
        this.slots = slots;
        this.queue = queue;
        this.onq = onq;
        this.bestCtr = bestCtr;
        this.ctrBuf = ctrBuf;
        this.zeros = zeros;
        this.ahead = ahead;
    }

    clone(): engineWS {
        return new engineWS(this.slots, this.queue, this.onq, this.bestCtr, this.ctrBuf, this.zeros, this.ahead);
    }

    static readonly elem: vg.Elem<engineWS> = vg.structElem(() => new engineWS(), 224);
}

export class engineResult {
    matched: boolean;
    so: number;
    eo: number;

    constructor(matched: boolean = false, so: number = 0, eo: number = 0) {
        this.matched = matched;
        this.so = so;
        this.eo = eo;
    }

    clone(): engineResult {
        return new engineResult(this.matched, this.so, this.eo);
    }

    static eq(a: engineResult, b: engineResult): boolean {
        return a.matched === b.matched && a.so === b.so && a.eo === b.eo;
    }

    static readonly elem: vg.Elem<engineResult> = vg.structElem(() => new engineResult(), 17);
}

export class phaseAState {
    subject: vg.Str;
    eflags: number;
    k: number;
    ring: number;
    nlMode: boolean;
    ci: number;
    pos: number;
    bol: boolean;
    eol: boolean;
    cur: number;
    size: number;
    matched: boolean;
    so: number;
    eo: number;

    constructor(subject: vg.Str = vg.EMPTY, eflags: number = 0, k: number = 0, ring: number = 0, nlMode: boolean = false, ci: number = 0, pos: number = 0, bol: boolean = false, eol: boolean = false, cur: number = 0, size: number = 0, matched: boolean = false, so: number = 0, eo: number = 0) {
        this.subject = subject;
        this.eflags = eflags;
        this.k = k;
        this.ring = ring;
        this.nlMode = nlMode;
        this.ci = ci;
        this.pos = pos;
        this.bol = bol;
        this.eol = eol;
        this.cur = cur;
        this.size = size;
        this.matched = matched;
        this.so = so;
        this.eo = eo;
    }

    clone(): phaseAState {
        return new phaseAState(this.subject, this.eflags, this.k, this.ring, this.nlMode, this.ci, this.pos, this.bol, this.eol, this.cur, this.size, this.matched, this.so, this.eo);
    }

    static eq(a: phaseAState, b: phaseAState): boolean {
        return vg.streq(a.subject, b.subject) && a.eflags === b.eflags && a.k === b.k && a.ring === b.ring && a.nlMode === b.nlMode && a.ci === b.ci && a.pos === b.pos && a.bol === b.bol && a.eol === b.eol && a.cur === b.cur && a.size === b.size && a.matched === b.matched && a.so === b.so && a.eo === b.eo;
    }

    static readonly elem: vg.Elem<phaseAState> = vg.structElem(() => new phaseAState(), 84);
}

export class Error {
    Code: number;
    Pos: number;

    constructor(Code: number = 0, Pos: number = 0) {
        this.Code = Code;
        this.Pos = Pos;
    }

    clone(): Error {
        return new Error(this.Code, this.Pos);
    }

    static eq(a: Error, b: Error): boolean {
        return a.Code === b.Code && a.Pos === b.Pos;
    }

    static readonly elem: vg.Elem<Error> = vg.structElem(() => new Error(), 12);
}

export class memoKey {
    a: number;
    b: number;
    c: number;
    d: number;

    constructor(a: number = 0, b: number = 0, c: number = 0, d: number = 0) {
        this.a = a;
        this.b = b;
        this.c = c;
        this.d = d;
    }

    clone(): memoKey {
        return new memoKey(this.a, this.b, this.c, this.d);
    }

    static eq(a: memoKey, b: memoKey): boolean {
        return a.a === b.a && a.b === b.b && a.c === b.c && a.d === b.d;
    }

    static readonly elem: vg.Elem<memoKey> = vg.structElem(() => new memoKey(), 16);
}

export class memoVal {
    x: number;
    y: number;
    z: number;

    constructor(x: number = 0, y: number = 0, z: number = 0) {
        this.x = x;
        this.y = y;
        this.z = z;
    }

    clone(): memoVal {
        return new memoVal(this.x, this.y, this.z);
    }

    static eq(a: memoVal, b: memoVal): boolean {
        return a.x === b.x && a.y === b.y && a.z === b.z;
    }

    static readonly elem: vg.Elem<memoVal> = vg.structElem(() => new memoVal(), 12);
}

export class memoTab {
    keys: vg.Slice<memoKey>;
    vals: vg.Slice<memoVal>;
    used: vg.Slice<number>;
    count: number;

    constructor(keys: vg.Slice<memoKey> = vg.NIL, vals: vg.Slice<memoVal> = vg.NIL, used: vg.Slice<number> = vg.NIL, count: number = 0) {
        this.keys = keys;
        this.vals = vals;
        this.used = used;
        this.count = count;
    }

    clone(): memoTab {
        return new memoTab(this.keys, this.vals, this.used, this.count);
    }

    static readonly elem: vg.Elem<memoTab> = vg.structElem(() => new memoTab(), 104);
}

export class SecRange {
    Off: number;
    End: number;

    constructor(Off: number = 0, End: number = 0) {
        this.Off = Off;
        this.End = End;
    }

    clone(): SecRange {
        return new SecRange(this.Off, this.End);
    }

    static eq(a: SecRange, b: SecRange): boolean {
        return a.Off === b.Off && a.End === b.End;
    }

    static readonly elem: vg.Elem<SecRange> = vg.structElem(() => new SecRange(), 16);
}

export class Locale {
    blob: vg.Str;
    sec: SecRange[];
    maxSeq: number;
    preimagesDefault: number;
    preimagesTurkic: number;
    collationProfile: number;
    caseProfile: number;
    posix: boolean;
    valid: boolean;

    constructor(blob: vg.Str = vg.EMPTY, sec: SecRange[] = vg.makeArr(23, () => new SecRange()), maxSeq: number = 0, preimagesDefault: number = 0, preimagesTurkic: number = 0, collationProfile: number = 0, caseProfile: number = 0, posix: boolean = false, valid: boolean = false) {
        this.blob = blob;
        this.sec = sec;
        this.maxSeq = maxSeq;
        this.preimagesDefault = preimagesDefault;
        this.preimagesTurkic = preimagesTurkic;
        this.collationProfile = collationProfile;
        this.caseProfile = caseProfile;
        this.posix = posix;
        this.valid = valid;
    }

    clone(): Locale {
        return new Locale(this.blob, this.sec.map((v) => v.clone()), this.maxSeq, this.preimagesDefault, this.preimagesTurkic, this.collationProfile, this.caseProfile, this.posix, this.valid);
    }

    static eq(a: Locale, b: Locale): boolean {
        return vg.streq(a.blob, b.blob) && vg.arrEq(a.sec, b.sec, (x, y) => SecRange.eq(x, y)) && a.maxSeq === b.maxSeq && a.preimagesDefault === b.preimagesDefault && a.preimagesTurkic === b.preimagesTurkic && a.collationProfile === b.collationProfile && a.caseProfile === b.caseProfile && a.posix === b.posix && a.valid === b.valid;
    }

    static readonly elem: vg.Elem<Locale> = vg.structElem(() => new Locale(), 413);
}

export class LocaleRow {
    TypeFirst: number;
    TypeCount: number;
    CaseProfile: number;
    DefaultCollation: number;

    constructor(TypeFirst: number = 0, TypeCount: number = 0, CaseProfile: number = 0, DefaultCollation: number = 0) {
        this.TypeFirst = TypeFirst;
        this.TypeCount = TypeCount;
        this.CaseProfile = CaseProfile;
        this.DefaultCollation = DefaultCollation;
    }

    clone(): LocaleRow {
        return new LocaleRow(this.TypeFirst, this.TypeCount, this.CaseProfile, this.DefaultCollation);
    }

    static eq(a: LocaleRow, b: LocaleRow): boolean {
        return a.TypeFirst === b.TypeFirst && a.TypeCount === b.TypeCount && a.CaseProfile === b.CaseProfile && a.DefaultCollation === b.DefaultCollation;
    }

    static readonly elem: vg.Elem<LocaleRow> = vg.structElem(() => new LocaleRow(), 19);
}

export class localeRequest {
    name: vg.Str;
    ctype: vg.Str;
    posix: boolean;

    constructor(name: vg.Str = vg.EMPTY, ctype: vg.Str = vg.EMPTY, posix: boolean = false) {
        this.name = name;
        this.ctype = ctype;
        this.posix = posix;
    }

    clone(): localeRequest {
        return new localeRequest(this.name, this.ctype, this.posix);
    }

    static eq(a: localeRequest, b: localeRequest): boolean {
        return vg.streq(a.name, b.name) && vg.streq(a.ctype, b.ctype) && a.posix === b.posix;
    }

    static readonly elem: vg.Elem<localeRequest> = vg.structElem(() => new localeRequest(), 33);
}

export class CasePair {
    Upper: number;
    Lower: number;

    constructor(Upper: number = 0, Lower: number = 0) {
        this.Upper = Upper;
        this.Lower = Lower;
    }

    clone(): CasePair {
        return new CasePair(this.Upper, this.Lower);
    }

    static eq(a: CasePair, b: CasePair): boolean {
        return a.Upper === b.Upper && a.Lower === b.Lower;
    }

    static readonly elem: vg.Elem<CasePair> = vg.structElem(() => new CasePair(), 8);
}

export class preimageBuf {
    r: Int32Array;
    n: number;

    constructor(r: Int32Array = new Int32Array(16), n: number = 0) {
        this.r = r;
        this.n = n;
    }

    clone(): preimageBuf {
        return new preimageBuf(this.r.slice(), this.n);
    }

    static eq(a: preimageBuf, b: preimageBuf): boolean {
        return vg.arrEq(a.r, b.r, (x, y) => x === y) && a.n === b.n;
    }

    static readonly elem: vg.Elem<preimageBuf> = vg.structElem(() => new preimageBuf(), 72);
}

export class CollProfile {
    OverrideFirst: number;
    OverrideCount: number;
    AddFirst: number;
    AddCount: number;
    RemoveFirst: number;
    RemoveCount: number;

    constructor(OverrideFirst: number = 0, OverrideCount: number = 0, AddFirst: number = 0, AddCount: number = 0, RemoveFirst: number = 0, RemoveCount: number = 0) {
        this.OverrideFirst = OverrideFirst;
        this.OverrideCount = OverrideCount;
        this.AddFirst = AddFirst;
        this.AddCount = AddCount;
        this.RemoveFirst = RemoveFirst;
        this.RemoveCount = RemoveCount;
    }

    clone(): CollProfile {
        return new CollProfile(this.OverrideFirst, this.OverrideCount, this.AddFirst, this.AddCount, this.RemoveFirst, this.RemoveCount);
    }

    static eq(a: CollProfile, b: CollProfile): boolean {
        return a.OverrideFirst === b.OverrideFirst && a.OverrideCount === b.OverrideCount && a.AddFirst === b.AddFirst && a.AddCount === b.AddCount && a.RemoveFirst === b.RemoveFirst && a.RemoveCount === b.RemoveCount;
    }

    static readonly elem: vg.Elem<CollProfile> = vg.structElem(() => new CollProfile(), 48);
}

export class Match {
    So: number;
    Eo: number;

    constructor(So: number = 0, Eo: number = 0) {
        this.So = So;
        this.Eo = Eo;
    }

    clone(): Match {
        return new Match(this.So, this.Eo);
    }

    static eq(a: Match, b: Match): boolean {
        return a.So === b.So && a.Eo === b.Eo;
    }

    static readonly elem: vg.Elem<Match> = vg.structElem(() => new Match(), 16);
}

export class decoded {
    runes: vg.Slice<number>;
    byteAt: vg.Slice<number>;
    atSubjectStart: boolean;
    atSubjectEnd: boolean;
    prevIsNewline: boolean;
    nextIsNewline: boolean;

    constructor(runes: vg.Slice<number> = vg.NIL, byteAt: vg.Slice<number> = vg.NIL, atSubjectStart: boolean = false, atSubjectEnd: boolean = false, prevIsNewline: boolean = false, nextIsNewline: boolean = false) {
        this.runes = runes;
        this.byteAt = byteAt;
        this.atSubjectStart = atSubjectStart;
        this.atSubjectEnd = atSubjectEnd;
        this.prevIsNewline = prevIsNewline;
        this.nextIsNewline = nextIsNewline;
    }

    clone(): decoded {
        return new decoded(this.runes, this.byteAt, this.atSubjectStart, this.atSubjectEnd, this.prevIsNewline, this.nextIsNewline);
    }

    static readonly elem: vg.Elem<decoded> = vg.structElem(() => new decoded(), 68);
}

export class instr {
    op: number;
    next: number;
    alt: number;
    arg: number;
    mask: bigint;
    extra: vg.Slice<number>;

    constructor(op: number = 0, next: number = 0, alt: number = 0, arg: number = 0, mask: bigint = 0n, extra: vg.Slice<number> = vg.NIL) {
        this.op = op;
        this.next = next;
        this.alt = alt;
        this.arg = arg;
        this.mask = mask;
        this.extra = extra;
    }

    clone(): instr {
        return new instr(this.op, this.next, this.alt, this.arg, this.mask, this.extra);
    }

    static readonly elem: vg.Elem<instr> = vg.structElem(() => new instr(), 53);
}

export class scanFilter {
    enabled: boolean;
    single: boolean;
    b: number;
    stop: boolean[];

    constructor(enabled: boolean = false, single: boolean = false, b: number = 0, stop: boolean[] = vg.makeArr(256, () => false)) {
        this.enabled = enabled;
        this.single = single;
        this.b = b;
        this.stop = stop;
    }

    clone(): scanFilter {
        return new scanFilter(this.enabled, this.single, this.b, this.stop.slice());
    }

    static eq(a: scanFilter, b: scanFilter): boolean {
        return a.enabled === b.enabled && a.single === b.single && a.b === b.b && vg.arrEq(a.stop, b.stop, (x, y) => x === y);
    }

    static readonly elem: vg.Elem<scanFilter> = vg.structElem(() => new scanFilter(), 259);
}

export class program {
    ins: vg.Slice<instr>;
    start: number;
    foldSets: vg.Slice<vg.Slice<number>>;
    multi: boolean;
    failMin: number;
    scan: scanFilter;
    depth: number;

    constructor(ins: vg.Slice<instr> = vg.NIL, start: number = 0, foldSets: vg.Slice<vg.Slice<number>> = vg.NIL, multi: boolean = false, failMin: number = 0, scan: scanFilter = new scanFilter(), depth: number = 0) {
        this.ins = ins;
        this.start = start;
        this.foldSets = foldSets;
        this.multi = multi;
        this.failMin = failMin;
        this.scan = scan;
        this.depth = depth;
    }

    clone(): program {
        return new program(this.ins, this.start, this.foldSets, this.multi, this.failMin, this.scan.clone(), this.depth);
    }

    static readonly elem: vg.Elem<program> = vg.structElem(() => new program(), 344);
}

export class patchSlot {
    idx: number;
    alt: boolean;

    constructor(idx: number = 0, alt: boolean = false) {
        this.idx = idx;
        this.alt = alt;
    }

    clone(): patchSlot {
        return new patchSlot(this.idx, this.alt);
    }

    static eq(a: patchSlot, b: patchSlot): boolean {
        return a.idx === b.idx && a.alt === b.alt;
    }

    static readonly elem: vg.Elem<patchSlot> = vg.structElem(() => new patchSlot(), 5);
}

export class frag {
    start: number;
    out: vg.Slice<patchSlot>;

    constructor(start: number = 0, out: vg.Slice<patchSlot> = vg.NIL) {
        this.start = start;
        this.out = out;
    }

    clone(): frag {
        return new frag(this.start, this.out);
    }

    static readonly elem: vg.Elem<frag> = vg.structElem(() => new frag(), 36);
}

export class progBuilder {
    prog: program;
    tooBig: boolean;
    failMin: number;
    errCode: number;
    icase: boolean;

    constructor(prog: program = new program(), tooBig: boolean = false, failMin: number = 0, errCode: number = 0, icase: boolean = false) {
        this.prog = prog;
        this.tooBig = tooBig;
        this.failMin = failMin;
        this.errCode = errCode;
        this.icase = icase;
    }

    clone(): progBuilder {
        return new progBuilder(this.prog.clone(), this.tooBig, this.failMin, this.errCode, this.icase);
    }

    static readonly elem: vg.Elem<progBuilder> = vg.structElem(() => new progBuilder(), 358);
}

export class Regexp {
    nodes: vg.Slice<node>;
    brackets: vg.Slice<bracketSet>;
    root: number;
    nsub: number;
    flags: number;
    loc: Locale;
    minSlots: number;
    nested: vg.Slice<vg.Slice<number>>;
    multi: boolean;
    progOK: boolean;
    prog: program;
    minLen: number;
    anchors: boolean;
    onePass: boolean;

    constructor(nodes: vg.Slice<node> = vg.NIL, brackets: vg.Slice<bracketSet> = vg.NIL, root: number = 0, nsub: number = 0, flags: number = 0, loc: Locale = new Locale(), minSlots: number = 0, nested: vg.Slice<vg.Slice<number>> = vg.NIL, multi: boolean = false, progOK: boolean = false, prog: program = new program(), minLen: number = 0, anchors: boolean = false, onePass: boolean = false) {
        this.nodes = nodes;
        this.brackets = brackets;
        this.root = root;
        this.nsub = nsub;
        this.flags = flags;
        this.loc = loc;
        this.minSlots = minSlots;
        this.nested = nested;
        this.multi = multi;
        this.progOK = progOK;
        this.prog = prog;
        this.minLen = minLen;
        this.anchors = anchors;
        this.onePass = onePass;
    }

    clone(): Regexp {
        return new Regexp(this.nodes, this.brackets, this.root, this.nsub, this.flags, this.loc.clone(), this.minSlots, this.nested, this.multi, this.progOK, this.prog.clone(), this.minLen, this.anchors, this.onePass);
    }

    static readonly elem: vg.Elem<Regexp> = vg.structElem(() => new Regexp(), 889);
}

export class groupStack {
    g: vg.Slice<number>;

    constructor(g: vg.Slice<number> = vg.NIL) {
        this.g = g;
    }

    clone(): groupStack {
        return new groupStack(this.g);
    }

    static readonly elem: vg.Elem<groupStack> = vg.structElem(() => new groupStack(), 32);
}

export class MatchIter {
    pos: number;
    lastEnd: number;
    limit: number;
    done: boolean;

    constructor(pos: number = 0, lastEnd: number = 0, limit: number = 0, done: boolean = false) {
        this.pos = pos;
        this.lastEnd = lastEnd;
        this.limit = limit;
        this.done = done;
    }

    clone(): MatchIter {
        return new MatchIter(this.pos, this.lastEnd, this.limit, this.done);
    }

    static eq(a: MatchIter, b: MatchIter): boolean {
        return a.pos === b.pos && a.lastEnd === b.lastEnd && a.limit === b.limit && a.done === b.done;
    }

    static readonly elem: vg.Elem<MatchIter> = vg.structElem(() => new MatchIter(), 25);
}

export class replPart {
    lit: vg.Str;
    group: number;

    constructor(lit: vg.Str = vg.EMPTY, group: number = 0) {
        this.lit = lit;
        this.group = group;
    }

    clone(): replPart {
        return new replPart(this.lit, this.group);
    }

    static eq(a: replPart, b: replPart): boolean {
        return vg.streq(a.lit, b.lit) && a.group === b.group;
    }

    static readonly elem: vg.Elem<replPart> = vg.structElem(() => new replPart(), 24);
}

export class node {
    op: number;
    ch: vg.Slice<number>;
    r: number;
    fold: vg.Slice<number>;
    br: number;
    min: number;
    max: number;
    minimal: boolean;
    index: number;
    minL: number;
    maxL: number;
    sufMin: vg.Slice<number>;
    sufMax: vg.Slice<number>;
    firsts: vg.Slice<vg.Slice<number>>;

    constructor(op: number = 0, ch: vg.Slice<number> = vg.NIL, r: number = 0, fold: vg.Slice<number> = vg.NIL, br: number = 0, min: number = 0, max: number = 0, minimal: boolean = false, index: number = 0, minL: number = 0, maxL: number = 0, sufMin: vg.Slice<number> = vg.NIL, sufMax: vg.Slice<number> = vg.NIL, firsts: vg.Slice<vg.Slice<number>> = vg.NIL) {
        this.op = op;
        this.ch = ch;
        this.r = r;
        this.fold = fold;
        this.br = br;
        this.min = min;
        this.max = max;
        this.minimal = minimal;
        this.index = index;
        this.minL = minL;
        this.maxL = maxL;
        this.sufMin = sufMin;
        this.sufMax = sufMax;
        this.firsts = firsts;
    }

    clone(): node {
        return new node(this.op, this.ch, this.r, this.fold, this.br, this.min, this.max, this.minimal, this.index, this.minL, this.maxL, this.sufMin, this.sufMax, this.firsts);
    }

    static readonly elem: vg.Elem<node> = vg.structElem(() => new node(), 210);
}

export class parser {
    src: vg.Str;
    pos: number;
    flags: number;
    groups: number;
    nodes: vg.Slice<node>;
    brackets: vg.Slice<bracketSet>;
    err: Error;

    constructor(src: vg.Str = vg.EMPTY, pos: number = 0, flags: number = 0, groups: number = 0, nodes: vg.Slice<node> = vg.NIL, brackets: vg.Slice<bracketSet> = vg.NIL, err: Error = new Error()) {
        this.src = src;
        this.pos = pos;
        this.flags = flags;
        this.groups = groups;
        this.nodes = nodes;
        this.brackets = brackets;
        this.err = err;
    }

    clone(): parser {
        return new parser(this.src, this.pos, this.flags, this.groups, this.nodes, this.brackets, this.err.clone());
    }

    static readonly elem: vg.Elem<parser> = vg.structElem(() => new parser(), 112);
}

export class interval {
    lo: number;
    hi: number;

    constructor(lo: number = 0, hi: number = 0) {
        this.lo = lo;
        this.hi = hi;
    }

    clone(): interval {
        return new interval(this.lo, this.hi);
    }

    static eq(a: interval, b: interval): boolean {
        return a.lo === b.lo && a.hi === b.hi;
    }

    static readonly elem: vg.Elem<interval> = vg.structElem(() => new interval(), 16);
}

export function parseBracket(p: parser, loc: Locale): number {
    let _t2!: vg.Slice<node>;
    const start: number = p.pos;
    p.pos = vg.chk(p.pos + 1);
    let b: bracketSet = new bracketSet();
    b.icase = (((p.flags & FlagICase) >>> 0) !== 0);
    b.nlMode = (((p.flags & FlagNewline) >>> 0) !== 0);
    if ((peekByte(p) === 94)) {
        b.negated = true;
        p.pos = vg.chk(p.pos + 1);
    }
    let empty: boolean = true;
    while (true) {
        if (eof(p)) {
            return fail(p, ErrEBrack, start);
        }
        if (((peekByte(p) === 93) && (!empty))) {
            p.pos = vg.chk(p.pos + 1);
            finalizeBracket(b, loc);
            p.brackets = vg.append(bracketSet.elem, p.brackets, b.clone());
            const n: number = addNode(p, opBracket);
            (_t2 = p.nodes, _t2.buf[_t2.off + vg.ix(n, _t2.len)]).br = (vg.chk(p.brackets.len - 1) | 0);
            return n;
        }
        const _t3 = parseBracketItem(p, loc, start);
        const item: bracketItem = _t3[0];
        const ok: boolean = _t3[1];
        if ((!ok)) {
            return ((-1) | 0);
        }
        empty = false;
        if (((((item.kind === itemChar) && (peekByte(p) === 45)) && (peekByteAt(p, 1) !== 93)) && (vg.chk(p.pos + 1) < p.src.length))) {
            const rangeStart: number = p.pos;
            p.pos = vg.chk(p.pos + 1);
            const _t4 = parseBracketItem(p, loc, start);
            const end: bracketItem = _t4[0];
            const ok2: boolean = _t4[1];
            if ((!ok2)) {
                return ((-1) | 0);
            }
            if ((end.kind !== itemChar)) {
                return fail(p, ErrERange, rangeStart);
            }
            if ((!localeSupportsRanges(loc))) {
                return fail(p, ErrERange, rangeStart);
            }
            if ((item.r > end.r)) {
                return fail(p, ErrERange, rangeStart);
            }
            let rr: runeRange = new runeRange();
            rr.lo = item.r;
            rr.hi = end.r;
            b.ranges = vg.append(runeRange.elem, b.ranges, rr.clone());
            if (((peekByte(p) === 45) && (peekByteAt(p, 1) !== 93))) {
                return fail(p, ErrERange, p.pos);
            }
            continue;
        }
        if (((((item.kind !== itemChar) && (peekByte(p) === 45)) && (peekByteAt(p, 1) !== 93)) && (vg.chk(p.pos + 1) < p.src.length))) {
            return fail(p, ErrERange, p.pos);
        }
        switch (item.kind) {
            case itemChar:
            {
                let rr_2: runeRange = new runeRange();
                rr_2.lo = item.r;
                rr_2.hi = item.r;
                b.ranges = vg.append(runeRange.elem, b.ranges, rr_2.clone());
                break;
            }
            case itemElem:
            {
                b.elems = vg.append(vg.SLICE, b.elems, item.seq);
                break;
            }
            case itemEquiv:
            {
                b.equivs = vg.append(vg.SLICE, b.equivs, item.seq);
                break;
            }
            case itemClass:
            {
                b.classMask = (b.classMask | ((1 << item.class) & 0xffff));
                break;
            }
        }
    }
}

export function parseBracketItem(p: parser, loc: Locale, bracketStart: number): [bracketItem, boolean] {
    let item: bracketItem = new bracketItem();
    const c: number = peekByte(p);
    if ((c === 91)) {
        const inner: number = peekByteAt(p, 1);
        if ((inner === 46)) {
            const _t1 = scanInner(p, _s13, ErrECollate);
            const seq: vg.Slice<number> = _t1[0];
            const ok: boolean = _t1[1];
            if ((!ok)) {
                return [item, false];
            }
            if ((!localeIsCollatingElement(loc, seq))) {
                fail(p, ErrECollate, bracketStart);
                return [item, false];
            }
            if ((seq.len === 1)) {
                item.kind = itemChar;
                item.r = seq.buf[seq.off + vg.ix(0, seq.len)];
                return [item, true];
            }
            item.kind = itemElem;
            item.seq = seq;
            return [item, true];
        }
        if ((inner === 61)) {
            const _t2 = scanInner(p, _s14, ErrECollate);
            const seq_2: vg.Slice<number> = _t2[0];
            const ok_2: boolean = _t2[1];
            if ((!ok_2)) {
                return [item, false];
            }
            if ((!localeIsCollatingElement(loc, seq_2))) {
                fail(p, ErrECollate, bracketStart);
                return [item, false];
            }
            item.kind = itemEquiv;
            item.seq = seq_2;
            return [item, true];
        }
        if ((inner === 58)) {
            const _t3 = scanInner(p, _s15, ErrECType);
            const seq_3: vg.Slice<number> = _t3[0];
            const ok_3: boolean = _t3[1];
            if ((!ok_3)) {
                return [item, false];
            }
            const _t4 = classByName(runesToString(seq_3));
            const class_: number = _t4[0];
            const ok2: boolean = _t4[1];
            if ((!ok2)) {
                fail(p, ErrECType, bracketStart);
                return [item, false];
            }
            item.kind = itemClass;
            item.class = class_;
            return [item, true];
        }
    }
    const r: number = nextRune(p);
    if ((r < 0)) {
        return [item, false];
    }
    item.kind = itemChar;
    item.r = r;
    return [item, true];
}

export function runesToString(seq: vg.Slice<number>): vg.Str {
    let out: vg.Slice<number> = vg.make(vg.U8, 0, seq.len);
    for (let i: number = 0; (i < seq.len); i = vg.chk(i + 1)) {
        const r: number = seq.buf[seq.off + vg.ix(i, seq.len)];
        if ((r < 128)) {
            out = vg.append(vg.U8, out, (r & 0xff));
        } else {
            if ((r < 2048)) {
                out = vg.append(vg.U8, vg.append(vg.U8, out, ((192 | (r >> 6)) & 0xff)), ((128 | (r & 63)) & 0xff));
            } else {
                if ((r < 65536)) {
                    out = vg.append(vg.U8, vg.append(vg.U8, vg.append(vg.U8, out, ((224 | (r >> 12)) & 0xff)), ((128 | ((r >> 6) & 63)) & 0xff)), ((128 | (r & 63)) & 0xff));
                } else {
                    out = vg.append(vg.U8, vg.append(vg.U8, vg.append(vg.U8, vg.append(vg.U8, out, ((240 | (r >> 18)) & 0xff)), ((128 | ((r >> 12) & 63)) & 0xff)), ((128 | ((r >> 6) & 63)) & 0xff)), ((128 | (r & 63)) & 0xff));
                }
            }
        }
    }
    return vg.strFromBytes(out);
}

export function scanInner(p: parser, closer: vg.Str, emptyCode: number): [vg.Slice<number>, boolean] {
    let _t2!: vg.Str;
    let _t3!: vg.Str;
    const start: number = p.pos;
    p.pos = vg.chk(p.pos + 2);
    let end: number = (0 - 1);
    for (let i: number = p.pos; (vg.chk(i + 1) < p.src.length); i = vg.chk(i + 1)) {
        if ((((_t2 = p.src, _t2[vg.ix(i, _t2.length)]) === closer[vg.ix(0, closer.length)]) && ((_t3 = p.src, _t3[vg.ix(vg.chk(i + 1), _t3.length)]) === closer[vg.ix(1, closer.length)]))) {
            end = i;
            break;
        }
    }
    if ((end < 0)) {
        fail(p, ErrEBrack, start);
        return [vg.NIL, false];
    }
    if ((end === p.pos)) {
        fail(p, emptyCode, start);
        return [vg.NIL, false];
    }
    let content: vg.Slice<number> = vg.make(vg.I32, 0, vg.chk(end - p.pos));
    let at: number = p.pos;
    while ((at < end)) {
        const _t5 = decodeRuneAt(p.src, at);
        const r: number = _t5[0];
        const size: number = _t5[1];
        if ((r < 0)) {
            fail(p, ErrBadPat, start);
            return [vg.NIL, false];
        }
        content = vg.append(vg.I32, content, r);
        at = vg.chk(at + size);
    }
    p.pos = vg.chk(end + 2);
    return [content, true];
}

export function sortRanges(rr: vg.Slice<runeRange>): void {
    const n: number = rr.len;
    if ((n < 2)) {
        return;
    }
    const tmp: vg.Slice<runeRange> = vg.make(runeRange.elem, n);
    for (let width: number = 1; (width < n); width = vg.chk(width * 2)) {
        for (let lo: number = 0; (lo < n); lo = vg.chk(lo + vg.chk(2 * width))) {
            const mid: number = Math.min(vg.chk(lo + width), n);
            const hi: number = Math.min(vg.chk(lo + vg.chk(2 * width)), n);
            let i: number = lo;
            let j: number = mid;
            let w: number = lo;
            while (((i < mid) && (j < hi))) {
                const less: boolean = ((rr.buf[rr.off + vg.ix(i, rr.len)].lo < rr.buf[rr.off + vg.ix(j, rr.len)].lo) || ((rr.buf[rr.off + vg.ix(i, rr.len)].lo === rr.buf[rr.off + vg.ix(j, rr.len)].lo) && (rr.buf[rr.off + vg.ix(i, rr.len)].hi <= rr.buf[rr.off + vg.ix(j, rr.len)].hi)));
                if (less) {
                    tmp.buf[tmp.off + vg.ix(w, tmp.len)] = rr.buf[rr.off + vg.ix(i, rr.len)].clone();
                    i = vg.chk(i + 1);
                } else {
                    tmp.buf[tmp.off + vg.ix(w, tmp.len)] = rr.buf[rr.off + vg.ix(j, rr.len)].clone();
                    j = vg.chk(j + 1);
                }
                w = vg.chk(w + 1);
            }
            while ((i < mid)) {
                tmp.buf[tmp.off + vg.ix(w, tmp.len)] = rr.buf[rr.off + vg.ix(i, rr.len)].clone();
                i = vg.chk(i + 1);
                w = vg.chk(w + 1);
            }
            while ((j < hi)) {
                tmp.buf[tmp.off + vg.ix(w, tmp.len)] = rr.buf[rr.off + vg.ix(j, rr.len)].clone();
                j = vg.chk(j + 1);
                w = vg.chk(w + 1);
            }
        }
        vg.copy(runeRange.elem, rr, tmp);
    }
}

export function finalizeBracket(b: bracketSet, loc: Locale): void {
    let _t2!: vg.Slice<vg.Slice<number>>;
    let _t5!: vg.Slice<runeRange>;
    let _t6!: vg.Slice<runeRange>;
    let _t7!: vg.Slice<runeRange>;
    let _t8!: vg.Slice<runeRange>;
    let _t9!: vg.Slice<runeRange>;
    let _t10!: vg.Slice<runeRange>;
    let _t11!: vg.Slice<runeRange>;
    let _t12!: vg.Slice<runeRange>;
    let _t13!: number;
    if (((!b.negated) && ((b.elems.len > 0) || (b.equivs.len > 0)))) {
        for (let i: number = 0; (i < b.elems.len); i = vg.chk(i + 1)) {
            b.multiLens = (b.multiLens | ((1 << (_t2 = b.elems, _t2.buf[_t2.off + vg.ix(i, _t2.len)]).len) & 0xffff));
        }
        if ((b.equivs.len > 0)) {
            for (let length: number = 2; (length <= localeMaxElementLength(loc)); length = vg.chk(length + 1)) {
                b.multiLens = (b.multiLens | ((1 << length) & 0xffff));
            }
        }
    }
    if ((b.ranges.len < 2)) {
        return;
    }
    sortRanges(b.ranges);
    let w: number = 0;
    for (let i_2: number = 1; (i_2 < b.ranges.len); i_2 = vg.chk(i_2 + 1)) {
        if (((_t5 = b.ranges, _t5.buf[_t5.off + vg.ix(i_2, _t5.len)]).lo <= (((_t6 = b.ranges, _t6.buf[_t6.off + vg.ix(w, _t6.len)]).hi + 1) | 0))) {
            if (((_t7 = b.ranges, _t7.buf[_t7.off + vg.ix(i_2, _t7.len)]).hi > (_t8 = b.ranges, _t8.buf[_t8.off + vg.ix(w, _t8.len)]).hi)) {
                (_t10 = b.ranges, _t10.buf[_t10.off + vg.ix(w, _t10.len)]).hi = (_t9 = b.ranges, _t9.buf[_t9.off + vg.ix(i_2, _t9.len)]).hi;
            }
        } else {
            w = vg.chk(w + 1);
            (_t12 = b.ranges, _t13 = vg.ix(w, _t12.len), _t12.buf[_t12.off + _t13] = (_t11 = b.ranges, _t11.buf[_t11.off + vg.ix(i_2, _t11.len)]).clone());
        }
    }
    b.ranges = vg.head(b.ranges, vg.chk(w + 1));
}

export function bracketInRanges(brs: vg.Slice<bracketSet>, bi: number, c: number): boolean {
    let _t2!: vg.Slice<runeRange>;
    let _t3!: vg.Slice<runeRange>;
    let low: number = 0;
    let high: number = brs.buf[brs.off + vg.ix(bi, brs.len)].ranges.len;
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        if (((_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].ranges, _t2.buf[_t2.off + vg.ix(middle, _t2.len)]).hi < c)) {
            low = vg.chk(middle + 1);
        } else {
            if (((_t3 = brs.buf[brs.off + vg.ix(bi, brs.len)].ranges, _t3.buf[_t3.off + vg.ix(middle, _t3.len)]).lo > c)) {
                high = middle;
            } else {
                return true;
            }
        }
    }
    return false;
}

export function bracketPositiveSingle(brs: vg.Slice<bracketSet>, bi: number, loc: Locale, c: number): boolean {
    let _t2!: vg.Slice<vg.Slice<number>>;
    if (bracketInRanges(brs, bi, c)) {
        return true;
    }
    if (((brs.buf[brs.off + vg.ix(bi, brs.len)].classMask !== 0) && ((localeClassMask(loc, c) & brs.buf[brs.off + vg.ix(bi, brs.len)].classMask) !== 0))) {
        return true;
    }
    let single: Int32Array = new Int32Array(1);
    single[0] = c;
    for (let i: number = 0; (i < brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len); i = vg.chk(i + 1)) {
        if (localePrimaryEqual(loc, vg.arrSlice(single, 0, 1), (_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].equivs, _t2.buf[_t2.off + vg.ix(i, _t2.len)]))) {
            return true;
        }
    }
    return false;
}

export function bracketMatchesOne(brs: vg.Slice<bracketSet>, bi: number, loc: Locale, c: number): boolean {
    if ((c < 0)) {
        return false;
    }
    if (((brs.buf[brs.off + vg.ix(bi, brs.len)].negated && brs.buf[brs.off + vg.ix(bi, brs.len)].nlMode) && (c === 10))) {
        return false;
    }
    const want: boolean = (!brs.buf[brs.off + vg.ix(bi, brs.len)].negated);
    if ((bracketPositiveSingle(brs, bi, loc, c) === want)) {
        return true;
    }
    if ((!brs.buf[brs.off + vg.ix(bi, brs.len)].icase)) {
        return false;
    }
    let buf: preimageBuf = new preimageBuf();
    localeCasePreimages(loc, buf, c);
    for (let i: number = 0; (i < buf.n); i = vg.chk(i + 1)) {
        if ((bracketPositiveSingle(brs, bi, loc, buf.r[vg.ix(i, 16)]) === want)) {
            return true;
        }
    }
    return false;
}

export function counterpartMatch(brs: vg.Slice<bracketSet>, bi: number, loc: Locale, t: number, e: number): boolean {
    if ((t === e)) {
        return true;
    }
    if ((!brs.buf[brs.off + vg.ix(bi, brs.len)].icase)) {
        return false;
    }
    return ((t === localeToUpper(loc, e)) || (t === localeToLower(loc, e)));
}

export function bracketHasMultiMembers(brs: vg.Slice<bracketSet>, bi: number): boolean {
    return ((!brs.buf[brs.off + vg.ix(bi, brs.len)].negated) && ((brs.buf[brs.off + vg.ix(bi, brs.len)].elems.len > 0) || (brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len > 0)));
}

export function bracketMatchesMulti(brs: vg.Slice<bracketSet>, bi: number, loc: Locale, t: vg.Slice<number>): boolean {
    let _t2!: vg.Slice<vg.Slice<number>>;
    let _t4!: vg.Slice<number>;
    let _t5!: vg.Slice<vg.Slice<number>>;
    if ((t.len > localeMaxElementLength(loc))) {
        return false;
    }
    for (let i: number = 0; (i < brs.buf[brs.off + vg.ix(bi, brs.len)].elems.len); i = vg.chk(i + 1)) {
        if (((_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].elems, _t2.buf[_t2.off + vg.ix(i, _t2.len)]).len !== t.len)) {
            continue;
        }
        let all: boolean = true;
        for (let k: number = 0; (k < t.len); k = vg.chk(k + 1)) {
            if ((!counterpartMatch(brs, bi, loc, t.buf[t.off + vg.ix(k, t.len)], (_t4 = (_t5 = brs.buf[brs.off + vg.ix(bi, brs.len)].elems, _t5.buf[_t5.off + vg.ix(i, _t5.len)]), _t4.buf[_t4.off + vg.ix(k, _t4.len)])))) {
                all = false;
                break;
            }
        }
        if (all) {
            return true;
        }
    }
    if ((brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len === 0)) {
        return false;
    }
    let candidate: elemBuf = new elemBuf();
    return equivCandidate(brs, bi, loc, t, candidate, 0);
}

export function equivCandidate(brs: vg.Slice<bracketSet>, bi: number, loc: Locale, t: vg.Slice<number>, candidate: elemBuf, at: number): boolean {
    let _t2!: vg.Slice<vg.Slice<number>>;
    if ((at === t.len)) {
        if ((!localeIsCollatingElement(loc, vg.arrSlice(candidate.r, 0, t.len)))) {
            return false;
        }
        for (let i: number = 0; (i < brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len); i = vg.chk(i + 1)) {
            if (localePrimaryEqual(loc, vg.arrSlice(candidate.r, 0, t.len), (_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].equivs, _t2.buf[_t2.off + vg.ix(i, _t2.len)]))) {
                return true;
            }
        }
        return false;
    }
    candidate.r[vg.ix(at, 8)] = t.buf[t.off + vg.ix(at, t.len)];
    if (equivCandidate(brs, bi, loc, t, candidate, vg.chk(at + 1))) {
        return true;
    }
    if ((!brs.buf[brs.off + vg.ix(bi, brs.len)].icase)) {
        return false;
    }
    let buf: preimageBuf = new preimageBuf();
    localeCasePreimages(loc, buf, t.buf[t.off + vg.ix(at, t.len)]);
    for (let i_2: number = 0; (i_2 < buf.n); i_2 = vg.chk(i_2 + 1)) {
        candidate.r[vg.ix(at, 8)] = buf.r[vg.ix(i_2, 16)];
        if (equivCandidate(brs, bi, loc, t, candidate, vg.chk(at + 1))) {
            return true;
        }
    }
    return false;
}

export function bracketMatchesSpan(brs: vg.Slice<bracketSet>, bi: number, loc: Locale, runes: vg.Slice<number>, i: number, j: number): boolean {
    const k: number = vg.chk(j - i);
    if ((k < 1)) {
        return false;
    }
    if ((k === 1)) {
        return bracketMatchesOne(brs, bi, loc, runes.buf[runes.off + vg.ix(i, runes.len)]);
    }
    if ((!bracketHasMultiMembers(brs, bi))) {
        return false;
    }
    return bracketMatchesMulti(brs, bi, loc, vg.sub(runes, i, j));
}

export function bracketMinChars(brs: vg.Slice<bracketSet>, bi: number, loc: Locale): number {
    let _t2!: vg.Slice<vg.Slice<number>>;
    let _t4!: vg.Slice<vg.Slice<number>>;
    if ((((brs.buf[brs.off + vg.ix(bi, brs.len)].negated || (brs.buf[brs.off + vg.ix(bi, brs.len)].ranges.len > 0)) || (brs.buf[brs.off + vg.ix(bi, brs.len)].classMask !== 0)) || ((brs.buf[brs.off + vg.ix(bi, brs.len)].elems.len === 0) && (brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len === 0)))) {
        return 1;
    }
    let best: number = lenInf;
    for (let i: number = 0; (i < brs.buf[brs.off + vg.ix(bi, brs.len)].elems.len); i = vg.chk(i + 1)) {
        best = Math.min(best, (_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].elems, _t2.buf[_t2.off + vg.ix(i, _t2.len)]).len);
    }
    for (let i_2: number = 0; (i_2 < brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len); i_2 = vg.chk(i_2 + 1)) {
        best = Math.min(best, localeMinEquivLength(loc, (_t4 = brs.buf[brs.off + vg.ix(bi, brs.len)].equivs, _t4.buf[_t4.off + vg.ix(i_2, _t4.len)])));
    }
    return best;
}

export function capStep(s: capSolver): boolean {
    s.work = vg.chk(s.work + 1);
    if ((s.work > capWorkLimit)) {
        s.failed = true;
    }
    return (!s.failed);
}

export function seedArenas(s: capSolver): void {
    const scratch: ptree = new ptree();
    s.trees = vg.append(ptree.elem, s.trees, scratch.clone());
    s.kidStore = vg.append(vg.I32, s.kidStore, ((-1) | 0));
}

export function newTree(s: capSolver, t: ptree): number {
    let _t1!: vg.Slice<ptree>;
    let _t2!: number;
    if ((s.failed || (s.trees.len >= solverArenaLimit))) {
        s.failed = true;
        (_t1 = s.trees, _t2 = vg.ix(0, _t1.len), _t1.buf[_t1.off + _t2] = t.clone());
        return 0;
    }
    s.trees = vg.append(ptree.elem, s.trees, t.clone());
    return (vg.chk(s.trees.len - 1) | 0);
}

export function kidAlloc(s: capSolver, length: number): number {
    if ((s.failed || (vg.chk(s.kidStore.len + length) > solverArenaLimit))) {
        s.failed = true;
        while ((s.kidStore.len < length)) {
            s.kidStore = vg.append(vg.I32, s.kidStore, ((-1) | 0));
        }
        return 0;
    }
    const off: number = s.kidStore.len;
    if ((vg.chk(off + length) <= s.kidStore.cap)) {
        s.kidStore = vg.head(s.kidStore, vg.chk(off + length));
        return off;
    }
    for (let i: number = 0; (i < length); i = vg.chk(i + 1)) {
        s.kidStore = vg.append(vg.I32, s.kidStore, ((-1) | 0));
    }
    return off;
}

export function kidAlloc1(s: capSolver, t: number): number {
    let _t1!: vg.Slice<number>;
    let _t2!: number;
    const off: number = kidAlloc(s, 1);
    (_t1 = s.kidStore, _t2 = vg.ix(off, _t1.len), _t1.buf[_t1.off + _t2] = t);
    return off;
}

export function kidPrepend(s: capSolver, head: number, tailOff: number, tailLen: number): number {
    let _t1!: vg.Slice<number>;
    let _t2!: number;
    const off: number = kidAlloc(s, vg.chk(tailLen + 1));
    (_t1 = s.kidStore, _t2 = vg.ix(off, _t1.len), _t1.buf[_t1.off + _t2] = head);
    vg.copy(vg.I32, vg.sub(s.kidStore, vg.chk(off + 1), vg.chk(vg.chk(off + 1) + tailLen)), vg.sub(s.kidStore, tailOff, vg.chk(tailOff + tailLen)));
    return off;
}

export function setMatch(caps: vg.Slice<Match>, idx: number, so: number, eo: number): void {
    caps.buf[caps.off + vg.ix(idx, caps.len)].So = so;
    caps.buf[caps.off + vg.ix(idx, caps.len)].Eo = eo;
}

export function addCounters(s: capSolver, re: Regexp, t: number, out: vg.Slice<number>): void {
    let _t1!: vg.Slice<ptree>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t4!: vg.Slice<node>;
    let _t5!: vg.Slice<ptree>;
    let _t6!: vg.Slice<ptree>;
    let _t7!: vg.Slice<ptree>;
    let _t8!: vg.Slice<ptree>;
    let _t10!: vg.Slice<number>;
    const ni: number = (_t1 = s.trees, _t1.buf[_t1.off + vg.ix(t, _t1.len)]).n;
    if ((((_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).op === opRepeat) && (_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).minimal)) {
        out.buf[out.off + vg.ix((_t4 = re.nodes, _t4.buf[_t4.off + vg.ix(ni, _t4.len)]).index, out.len)] = vg.chk(out.buf[out.off + vg.ix((_t4 = re.nodes, _t4.buf[_t4.off + vg.ix(ni, _t4.len)]).index, out.len)] + (((_t5 = s.trees, _t5.buf[_t5.off + vg.ix(t, _t5.len)]).j - (_t6 = s.trees, _t6.buf[_t6.off + vg.ix(t, _t6.len)]).i) | 0));
    }
    const off: number = (_t7 = s.trees, _t7.buf[_t7.off + vg.ix(t, _t7.len)]).kidsOff;
    const count: number = (_t8 = s.trees, _t8.buf[_t8.off + vg.ix(t, _t8.len)]).kidsLen;
    for (let k: number = 0; (k < count); k = vg.chk(k + 1)) {
        addCounters(s, re, (_t10 = s.kidStore, _t10.buf[_t10.off + vg.ix(vg.chk(off + k), _t10.len)]), out);
    }
}

export function structCmp(s: capSolver, re: Regexp, a: number, b: number): number {
    let _t1!: vg.Slice<ptree>;
    let _t2!: vg.Slice<ptree>;
    let _t3!: vg.Slice<ptree>;
    let _t4!: vg.Slice<ptree>;
    let _t5!: vg.Slice<ptree>;
    let _t6!: vg.Slice<node>;
    let _t7!: vg.Slice<node>;
    let _t8!: vg.Slice<ptree>;
    let _t9!: vg.Slice<ptree>;
    let _t10!: vg.Slice<ptree>;
    let _t11!: vg.Slice<ptree>;
    let _t12!: vg.Slice<node>;
    let _t13!: vg.Slice<ptree>;
    let _t14!: vg.Slice<ptree>;
    let _t15!: vg.Slice<ptree>;
    let _t16!: vg.Slice<ptree>;
    let _t17!: vg.Slice<number>;
    let _t18!: vg.Slice<number>;
    let _t20!: vg.Slice<number>;
    let _t21!: vg.Slice<number>;
    let _t23!: vg.Slice<number>;
    let _t24!: vg.Slice<number>;
    let _t25!: vg.Slice<ptree>;
    let _t26!: vg.Slice<ptree>;
    let _t27!: vg.Slice<ptree>;
    let _t28!: vg.Slice<ptree>;
    let _t29!: vg.Slice<node>;
    let _t31!: vg.Slice<number>;
    let _t32!: vg.Slice<number>;
    const spanA: number = (((_t1 = s.trees, _t1.buf[_t1.off + vg.ix(a, _t1.len)]).j - (_t2 = s.trees, _t2.buf[_t2.off + vg.ix(a, _t2.len)]).i) | 0);
    const spanB: number = (((_t3 = s.trees, _t3.buf[_t3.off + vg.ix(b, _t3.len)]).j - (_t4 = s.trees, _t4.buf[_t4.off + vg.ix(b, _t4.len)]).i) | 0);
    const ni: number = (_t5 = s.trees, _t5.buf[_t5.off + vg.ix(a, _t5.len)]).n;
    if ((spanA !== spanB)) {
        if ((((_t6 = re.nodes, _t6.buf[_t6.off + vg.ix(ni, _t6.len)]).op === opRepeat) && (_t7 = re.nodes, _t7.buf[_t7.off + vg.ix(ni, _t7.len)]).minimal)) {
            return vg.chk(spanA - spanB);
        }
        return vg.chk(spanB - spanA);
    }
    const offA: number = (_t8 = s.trees, _t8.buf[_t8.off + vg.ix(a, _t8.len)]).kidsOff;
    const offB: number = (_t9 = s.trees, _t9.buf[_t9.off + vg.ix(b, _t9.len)]).kidsOff;
    const lenA: number = (_t10 = s.trees, _t10.buf[_t10.off + vg.ix(a, _t10.len)]).kidsLen;
    const lenB: number = (_t11 = s.trees, _t11.buf[_t11.off + vg.ix(b, _t11.len)]).kidsLen;
    switch ((_t12 = re.nodes, _t12.buf[_t12.off + vg.ix(ni, _t12.len)]).op) {
        case opAlt:
        {
            if (((_t13 = s.trees, _t13.buf[_t13.off + vg.ix(a, _t13.len)]).branch !== (_t14 = s.trees, _t14.buf[_t14.off + vg.ix(b, _t14.len)]).branch)) {
                return (((_t15 = s.trees, _t15.buf[_t15.off + vg.ix(a, _t15.len)]).branch - (_t16 = s.trees, _t16.buf[_t16.off + vg.ix(b, _t16.len)]).branch) | 0);
            }
            return structCmp(s, re, (_t17 = s.kidStore, _t17.buf[_t17.off + vg.ix(offA, _t17.len)]), (_t18 = s.kidStore, _t18.buf[_t18.off + vg.ix(offB, _t18.len)]));
            break;
        }
        case opConcat:
        case opGroup:
        {
            for (let idx: number = 0; (idx < lenA); idx = vg.chk(idx + 1)) {
                const c: number = structCmp(s, re, (_t20 = s.kidStore, _t20.buf[_t20.off + vg.ix(vg.chk(offA + idx), _t20.len)]), (_t21 = s.kidStore, _t21.buf[_t21.off + vg.ix(vg.chk(offB + idx), _t21.len)]));
                if ((c !== 0)) {
                    return c;
                }
            }
            break;
        }
        case opRepeat:
        {
            const limit: number = Math.min(lenA, lenB);
            for (let idx_2: number = 0; (idx_2 < limit); idx_2 = vg.chk(idx_2 + 1)) {
                const ka: number = (_t23 = s.kidStore, _t23.buf[_t23.off + vg.ix(vg.chk(offA + idx_2), _t23.len)]);
                const kb: number = (_t24 = s.kidStore, _t24.buf[_t24.off + vg.ix(vg.chk(offB + idx_2), _t24.len)]);
                const sa: number = (((_t25 = s.trees, _t25.buf[_t25.off + vg.ix(ka, _t25.len)]).j - (_t26 = s.trees, _t26.buf[_t26.off + vg.ix(ka, _t26.len)]).i) | 0);
                const sb: number = (((_t27 = s.trees, _t27.buf[_t27.off + vg.ix(kb, _t27.len)]).j - (_t28 = s.trees, _t28.buf[_t28.off + vg.ix(kb, _t28.len)]).i) | 0);
                if ((sa !== sb)) {
                    if ((_t29 = re.nodes, _t29.buf[_t29.off + vg.ix(ni, _t29.len)]).minimal) {
                        return vg.chk(sa - sb);
                    }
                    return vg.chk(sb - sa);
                }
            }
            if ((lenA !== lenB)) {
                return vg.chk(lenA - lenB);
            }
            for (let idx_3: number = 0; (idx_3 < lenA); idx_3 = vg.chk(idx_3 + 1)) {
                const c_2: number = structCmp(s, re, (_t31 = s.kidStore, _t31.buf[_t31.off + vg.ix(vg.chk(offA + idx_3), _t31.len)]), (_t32 = s.kidStore, _t32.buf[_t32.off + vg.ix(vg.chk(offB + idx_3), _t32.len)]));
                if ((c_2 !== 0)) {
                    return c_2;
                }
            }
            break;
        }
    }
    return 0;
}

export function cmpCand(s: capSolver, re: Regexp, a: number, b: number): number {
    let _t2!: vg.Slice<number>;
    let _t3!: number;
    let _t4!: vg.Slice<number>;
    let _t5!: number;
    let _t7!: vg.Slice<number>;
    let _t8!: vg.Slice<number>;
    let _t9!: vg.Slice<number>;
    let _t10!: vg.Slice<number>;
    if (s.failed) {
        return 1;
    }
    if ((re.minSlots > 0)) {
        for (let idx: number = 0; (idx < re.minSlots); idx = vg.chk(idx + 1)) {
            (_t2 = s.ctrA, _t3 = vg.ix(idx, _t2.len), _t2.buf[_t2.off + _t3] = 0);
            (_t4 = s.ctrB, _t5 = vg.ix(idx, _t4.len), _t4.buf[_t4.off + _t5] = 0);
        }
        addCounters(s, re, a, s.ctrA);
        addCounters(s, re, b, s.ctrB);
        for (let idx_2: number = 0; (idx_2 < re.minSlots); idx_2 = vg.chk(idx_2 + 1)) {
            if (((_t7 = s.ctrA, _t7.buf[_t7.off + vg.ix(idx_2, _t7.len)]) !== (_t8 = s.ctrB, _t8.buf[_t8.off + vg.ix(idx_2, _t8.len)]))) {
                return vg.chk((_t9 = s.ctrA, _t9.buf[_t9.off + vg.ix(idx_2, _t9.len)]) - (_t10 = s.ctrB, _t10.buf[_t10.off + vg.ix(idx_2, _t10.len)]));
            }
        }
    }
    return structCmp(s, re, a, b);
}

export function bestParse(s: capSolver, re: Regexp, d: decoded, ni: number, i: number, j: number): number {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t5!: vg.Slice<node>;
    let _t6!: vg.Slice<number>;
    let _t7!: vg.Slice<number>;
    let _t8!: vg.Slice<node>;
    let _t9!: vg.Slice<number>;
    let _t10!: vg.Slice<node>;
    let _t11!: vg.Slice<node>;
    let _t13!: vg.Slice<number>;
    let _t14!: vg.Slice<node>;
    let _t16!: vg.Slice<node>;
    let _t17!: vg.Slice<node>;
    let _t18!: vg.Slice<number>;
    let _t19!: vg.Slice<node>;
    const span: number = vg.chk(j - i);
    if (((span < (_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).minL) || (((_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).maxL < lenInf) && (span > (_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).maxL)))) {
        return ((-1) | 0);
    }
    let key: memoKey = new memoKey();
    key.a = ni;
    key.b = (i | 0);
    key.c = (j | 0);
    const _t4 = memoGet(s.memo, key);
    const cached: memoVal = _t4[0];
    const hit: boolean = _t4[1];
    if (hit) {
        return cached.x;
    }
    if ((!capStep(s))) {
        return ((-1) | 0);
    }
    let best: number = (-1);
    switch ((_t5 = re.nodes, _t5.buf[_t5.off + vg.ix(ni, _t5.len)]).op) {
        case opChar:
        {
            if (((j === vg.chk(i + 1)) && charMatches(re, ni, (_t6 = d.runes, _t6.buf[_t6.off + vg.ix(i, _t6.len)])))) {
                best = newTree(s, new ptree(ni, (i | 0), (j | 0)));
            }
            break;
        }
        case opAny:
        {
            if (((j === vg.chk(i + 1)) && anyMatches(re, (_t7 = d.runes, _t7.buf[_t7.off + vg.ix(i, _t7.len)])))) {
                best = newTree(s, new ptree(ni, (i | 0), (j | 0)));
            }
            break;
        }
        case opBracket:
        {
            if (bracketMatchesSpan(re.brackets, (_t8 = re.nodes, _t8.buf[_t8.off + vg.ix(ni, _t8.len)]).br, re.loc, d.runes, i, j)) {
                best = newTree(s, new ptree(ni, (i | 0), (j | 0)));
            }
            break;
        }
        case opBOL:
        {
            if (((j === i) && atBOL(re, d, i, s.eflags))) {
                best = newTree(s, new ptree(ni, (i | 0), (j | 0)));
            }
            break;
        }
        case opEOL:
        {
            if (((j === i) && atEOL(re, d, i, s.eflags))) {
                best = newTree(s, new ptree(ni, (i | 0), (j | 0)));
            }
            break;
        }
        case opGroup:
        {
            const sub: number = bestParse(s, re, d, (_t9 = (_t10 = re.nodes, _t10.buf[_t10.off + vg.ix(ni, _t10.len)]).ch, _t9.buf[_t9.off + vg.ix(0, _t9.len)]), i, j);
            if ((sub >= 0)) {
                const off: number = kidAlloc1(s, sub);
                best = newTree(s, new ptree(ni, (i | 0), (j | 0), 0, (off | 0), 1));
            }
            break;
        }
        case opAlt:
        {
            for (let bi: number = 0; (bi < (_t11 = re.nodes, _t11.buf[_t11.off + vg.ix(ni, _t11.len)]).ch.len); bi = vg.chk(bi + 1)) {
                const sub_2: number = bestParse(s, re, d, (_t13 = (_t14 = re.nodes, _t14.buf[_t14.off + vg.ix(ni, _t14.len)]).ch, _t13.buf[_t13.off + vg.ix(bi, _t13.len)]), i, j);
                if ((sub_2 < 0)) {
                    continue;
                }
                const off_2: number = kidAlloc1(s, sub_2);
                const candidate: number = newTree(s, new ptree(ni, (i | 0), (j | 0), (bi | 0), (off_2 | 0), 1));
                if (((best < 0) || (cmpCand(s, re, candidate, best) < 0))) {
                    best = candidate;
                }
            }
            break;
        }
        case opConcat:
        {
            const _t15 = bestConcat(s, re, d, ni, 0, i, j);
            const off_3: number = _t15[0];
            const count: number = _t15[1];
            if ((off_3 >= 0)) {
                best = newTree(s, new ptree(ni, (i | 0), (j | 0), 0, (off_3 | 0), (count | 0)));
            }
            break;
        }
        case opRepeat:
        {
            if (((i === j) && ((_t16 = re.nodes, _t16.buf[_t16.off + vg.ix(ni, _t16.len)]).min === 0))) {
                let sub_3: number = (-1);
                if (((_t17 = re.nodes, _t17.buf[_t17.off + vg.ix(ni, _t17.len)]).max !== 0)) {
                    sub_3 = bestParse(s, re, d, (_t18 = (_t19 = re.nodes, _t19.buf[_t19.off + vg.ix(ni, _t19.len)]).ch, _t18.buf[_t18.off + vg.ix(0, _t18.len)]), i, i);
                }
                if ((sub_3 >= 0)) {
                    const off_4: number = kidAlloc1(s, sub_3);
                    best = newTree(s, new ptree(ni, (i | 0), (j | 0), 0, (off_4 | 0), 1));
                } else {
                    best = newTree(s, new ptree(ni, (i | 0), (j | 0)));
                }
            } else {
                const win: repWin = bestRep(s, re, d, ni, i, j, 0, false);
                if (win.ok) {
                    best = newTree(s, new ptree(ni, (i | 0), (j | 0), 0, (win.off | 0), (win.length | 0)));
                }
            }
            break;
        }
    }
    let val: memoVal = new memoVal();
    val.x = best;
    memoPut(s.memo, key, val);
    return best;
}

export function bestConcat(s: capSolver, re: Regexp, d: decoded, ni: number, idx: number, i: number, j: number): [number, number] {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<node>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<node>;
    let _t7!: vg.Slice<node>;
    let _t8!: vg.Slice<number>;
    let _t9!: vg.Slice<node>;
    let _t10!: vg.Slice<number>;
    let _t11!: vg.Slice<node>;
    let _t12!: vg.Slice<number>;
    let _t13!: vg.Slice<node>;
    let _t14!: vg.Slice<node>;
    let _t15!: vg.Slice<node>;
    if ((idx === vg.chk((_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).ch.len - 1))) {
        const sub: number = bestParse(s, re, d, (_t2 = (_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).ch, _t2.buf[_t2.off + vg.ix(idx, _t2.len)]), i, j);
        if ((sub < 0)) {
            return [(0 - 1), 0];
        }
        return [kidAlloc1(s, sub), 1];
    }
    let key: memoKey = new memoKey();
    key.a = ni;
    key.b = (idx | 0);
    key.c = (i | 0);
    key.d = (j | 0);
    const _t4 = memoGet(s.cmemo, key);
    const cached: memoVal = _t4[0];
    const hit: boolean = _t4[1];
    if (hit) {
        return [cached.x, cached.y];
    }
    if ((!capStep(s))) {
        return [(0 - 1), 0];
    }
    let bestOff: number = (0 - 1);
    let bestLen: number = 0;
    let bestTree: number = (-1);
    const head0: number = (_t5 = (_t6 = re.nodes, _t6.buf[_t6.off + vg.ix(ni, _t6.len)]).ch, _t5.buf[_t5.off + vg.ix(idx, _t5.len)]);
    let lo: number = vg.chk(i + (_t7 = re.nodes, _t7.buf[_t7.off + vg.ix(head0, _t7.len)]).minL);
    if (((_t8 = (_t9 = re.nodes, _t9.buf[_t9.off + vg.ix(ni, _t9.len)]).sufMax, _t8.buf[_t8.off + vg.ix(vg.chk(idx + 1), _t8.len)]) < lenInf)) {
        lo = Math.max(lo, vg.chk(j - (_t10 = (_t11 = re.nodes, _t11.buf[_t11.off + vg.ix(ni, _t11.len)]).sufMax, _t10.buf[_t10.off + vg.ix(vg.chk(idx + 1), _t10.len)])));
    }
    let hi: number = vg.chk(j - (_t12 = (_t13 = re.nodes, _t13.buf[_t13.off + vg.ix(ni, _t13.len)]).sufMin, _t12.buf[_t12.off + vg.ix(vg.chk(idx + 1), _t12.len)]));
    if (((_t14 = re.nodes, _t14.buf[_t14.off + vg.ix(head0, _t14.len)]).maxL < lenInf)) {
        hi = Math.min(hi, vg.chk(i + (_t15 = re.nodes, _t15.buf[_t15.off + vg.ix(head0, _t15.len)]).maxL));
    }
    for (let m: number = lo; (m <= hi); m = vg.chk(m + 1)) {
        if ((!capStep(s))) {
            return [(0 - 1), 0];
        }
        const head: number = bestParse(s, re, d, head0, i, m);
        if ((head < 0)) {
            continue;
        }
        const _t17 = bestConcat(s, re, d, ni, vg.chk(idx + 1), m, j);
        const tailOff: number = _t17[0];
        const tailLen: number = _t17[1];
        if ((tailOff < 0)) {
            continue;
        }
        const off: number = kidPrepend(s, head, tailOff, tailLen);
        const candidate: number = newTree(s, new ptree(ni, (i | 0), (j | 0), 0, (off | 0), (vg.chk(tailLen + 1) | 0)));
        if (((bestTree < 0) || (cmpCand(s, re, candidate, bestTree) < 0))) {
            bestTree = candidate;
            bestOff = off;
            bestLen = vg.chk(tailLen + 1);
        }
    }
    let val: memoVal = new memoVal();
    val.x = (bestOff | 0);
    val.y = (bestLen | 0);
    memoPut(s.cmemo, key, val);
    return [bestOff, bestLen];
}

export function repTry(s: capSolver, re: Regexp, ni: number, i: number, j: number, off: number, length: number, best: repBest): void {
    const candidate: number = newTree(s, new ptree(ni, (i | 0), (j | 0), 0, (off | 0), (length | 0)));
    if (((!best.found) || (cmpCand(s, re, candidate, best.tree) < 0))) {
        best.off = off;
        best.length = length;
        best.tree = candidate;
        best.found = true;
    }
}

export function bestRep(s: capSolver, re: Regexp, d: decoded, ni: number, i: number, j: number, done: number, hasEmpty: boolean): repWin {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t5!: vg.Slice<node>;
    let _t6!: vg.Slice<node>;
    let _t7!: vg.Slice<node>;
    let _t8!: vg.Slice<node>;
    let _t9!: vg.Slice<node>;
    let _t10!: vg.Slice<number>;
    let _t11!: vg.Slice<node>;
    let _t12!: vg.Slice<node>;
    let _t13!: vg.Slice<node>;
    let _t14!: vg.Slice<node>;
    let _t15!: vg.Slice<node>;
    let _t16!: vg.Slice<node>;
    let _t17!: vg.Slice<node>;
    let _t18!: vg.Slice<node>;
    let _t19!: vg.Slice<node>;
    let _t20!: vg.Slice<node>;
    let _t22!: vg.Slice<node>;
    let done_v: number = done;
    if ((((_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).max === infinite) && (done_v > (_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).min))) {
        done_v = (_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).min;
    }
    let key: memoKey = new memoKey();
    key.a = ni;
    key.b = (i | 0);
    key.c = (j | 0);
    key.d = (vg.shl64(done_v, 1) | 0);
    if (hasEmpty) {
        key.d = (key.d | 1);
    }
    let win: repWin = new repWin();
    const _t4 = memoGet(s.rmemo, key);
    const cached: memoVal = _t4[0];
    const hit: boolean = _t4[1];
    if (hit) {
        win.off = cached.x;
        win.length = cached.y;
        win.ok = (cached.z !== 0);
        return win;
    }
    if ((!capStep(s))) {
        win.off = (0 - 1);
        return win;
    }
    let best: repBest = new repBest();
    if ((((i === j) && (done_v >= (_t5 = re.nodes, _t5.buf[_t5.off + vg.ix(ni, _t5.len)]).min)) && ((!hasEmpty) || (done_v === (_t6 = re.nodes, _t6.buf[_t6.off + vg.ix(ni, _t6.len)]).min)))) {
        repTry(s, re, ni, i, j, 0, 0, best);
    }
    const canTake: boolean = (((_t7 = re.nodes, _t7.buf[_t7.off + vg.ix(ni, _t7.len)]).max === infinite) || (done_v < (_t8 = re.nodes, _t8.buf[_t8.off + vg.ix(ni, _t8.len)]).max));
    if ((canTake && (!(hasEmpty && (done_v >= (_t9 = re.nodes, _t9.buf[_t9.off + vg.ix(ni, _t9.len)]).min))))) {
        const child: number = (_t10 = (_t11 = re.nodes, _t11.buf[_t11.off + vg.ix(ni, _t11.len)]).ch, _t10.buf[_t10.off + vg.ix(0, _t10.len)]);
        const tailMin: number = satMul(Math.max(vg.chk(vg.chk((_t12 = re.nodes, _t12.buf[_t12.off + vg.ix(ni, _t12.len)]).min - done_v) - 1), 0), (_t13 = re.nodes, _t13.buf[_t13.off + vg.ix(child, _t13.len)]).minL);
        let tailMax: number = lenInf;
        if (((_t14 = re.nodes, _t14.buf[_t14.off + vg.ix(ni, _t14.len)]).max !== infinite)) {
            tailMax = satMul(vg.chk(vg.chk((_t15 = re.nodes, _t15.buf[_t15.off + vg.ix(ni, _t15.len)]).max - done_v) - 1), (_t16 = re.nodes, _t16.buf[_t16.off + vg.ix(child, _t16.len)]).maxL);
        } else {
            if (((_t17 = re.nodes, _t17.buf[_t17.off + vg.ix(child, _t17.len)]).maxL === 0)) {
                tailMax = 0;
            }
        }
        let lo: number = vg.chk(i + (_t18 = re.nodes, _t18.buf[_t18.off + vg.ix(child, _t18.len)]).minL);
        if ((tailMax < lenInf)) {
            lo = Math.max(lo, vg.chk(j - tailMax));
        }
        let hi: number = vg.chk(j - tailMin);
        if (((_t19 = re.nodes, _t19.buf[_t19.off + vg.ix(child, _t19.len)]).maxL < lenInf)) {
            hi = Math.min(hi, vg.chk(i + (_t20 = re.nodes, _t20.buf[_t20.off + vg.ix(child, _t20.len)]).maxL));
        }
        for (let m: number = lo; (m <= hi); m = vg.chk(m + 1)) {
            if (((m === i) && (done_v >= (_t22 = re.nodes, _t22.buf[_t22.off + vg.ix(ni, _t22.len)]).min))) {
                continue;
            }
            if ((!capStep(s))) {
                win.off = (0 - 1);
                return win;
            }
            const head: number = bestParse(s, re, d, child, i, m);
            if ((head < 0)) {
                continue;
            }
            const tail: repWin = bestRep(s, re, d, ni, m, j, vg.chk(done_v + 1), (hasEmpty || (m === i)));
            if ((!tail.ok)) {
                continue;
            }
            const off: number = kidPrepend(s, head, tail.off, tail.length);
            repTry(s, re, ni, i, j, off, vg.chk(tail.length + 1), best);
        }
    }
    let val: memoVal = new memoVal();
    val.x = (best.off | 0);
    val.y = (best.length | 0);
    if (best.found) {
        val.z = 1;
    } else {
        val.x = ((-1) | 0);
    }
    memoPut(s.rmemo, key, val);
    win.off = val.x;
    win.length = val.y;
    win.ok = best.found;
    return win;
}

export function solveCaptures(re: Regexp, d: decoded, so: number, eo: number, eflags: number, caps: vg.Slice<Match>): Error {
    if (re.onePass) {
        for (let idx: number = 0; (idx < caps.len); idx = vg.chk(idx + 1)) {
            setMatch(caps, idx, (0 - 1), (0 - 1));
        }
        setMatch(caps, 0, so, eo);
        if (onePassCaps(re, d, re.root, so, eo, eflags, caps)) {
            return noError();
        }
        return compileError(ErrESpace, (0 - 1));
    }
    let s: capSolver = new capSolver();
    s.eflags = eflags;
    s.ctrA = vg.make(vg.INT, re.minSlots);
    s.ctrB = vg.make(vg.INT, re.minSlots);
    seedArenas(s);
    const best: number = bestParse(s, re, d, re.root, so, eo);
    if (s.failed) {
        return compileError(ErrESpace, (0 - 1));
    }
    if ((best < 0)) {
        return compileError(ErrESpace, (0 - 1));
    }
    for (let idx_2: number = 0; (idx_2 < caps.len); idx_2 = vg.chk(idx_2 + 1)) {
        setMatch(caps, idx_2, (0 - 1), (0 - 1));
    }
    setMatch(caps, 0, so, eo);
    assignCaps(re, s, best, caps);
    return noError();
}

export function assignCaps(re: Regexp, s: capSolver, t: number, caps: vg.Slice<Match>): void {
    let _t1!: vg.Slice<ptree>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t4!: vg.Slice<vg.Slice<number>>;
    let _t6!: vg.Slice<number>;
    let _t7!: vg.Slice<vg.Slice<number>>;
    let _t8!: vg.Slice<ptree>;
    let _t9!: vg.Slice<ptree>;
    let _t10!: vg.Slice<ptree>;
    let _t11!: vg.Slice<ptree>;
    let _t13!: vg.Slice<number>;
    const ni: number = (_t1 = s.trees, _t1.buf[_t1.off + vg.ix(t, _t1.len)]).n;
    if (((_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).op === opGroup)) {
        const gi: number = (_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).index;
        for (let k: number = 0; (k < (_t4 = re.nested, _t4.buf[_t4.off + vg.ix(gi, _t4.len)]).len); k = vg.chk(k + 1)) {
            setMatch(caps, (_t6 = (_t7 = re.nested, _t7.buf[_t7.off + vg.ix(gi, _t7.len)]), _t6.buf[_t6.off + vg.ix(k, _t6.len)]), (0 - 1), (0 - 1));
        }
        setMatch(caps, gi, (_t8 = s.trees, _t8.buf[_t8.off + vg.ix(t, _t8.len)]).i, (_t9 = s.trees, _t9.buf[_t9.off + vg.ix(t, _t9.len)]).j);
    }
    const off: number = (_t10 = s.trees, _t10.buf[_t10.off + vg.ix(t, _t10.len)]).kidsOff;
    const count: number = (_t11 = s.trees, _t11.buf[_t11.off + vg.ix(t, _t11.len)]).kidsLen;
    for (let k_2: number = 0; (k_2 < count); k_2 = vg.chk(k_2 + 1)) {
        assignCaps(re, s, (_t13 = s.kidStore, _t13.buf[_t13.off + vg.ix(vg.chk(off + k_2), _t13.len)]), caps);
    }
}

export function cAdd(a: bigint, b: bigint): bigint {
    if ((a > BigInt.asIntN(64, 4611686018427387904n - b))) {
        return 4611686018427387904n;
    }
    return BigInt.asIntN(64, a + b);
}

export function cMul(a: bigint, b: bigint): bigint {
    if (((a === 0n) || (b === 0n))) {
        return 0n;
    }
    if ((a > BigInt.asIntN(64, vg.divBig(4611686018427387904n, b)))) {
        return 4611686018427387904n;
    }
    return BigInt.asIntN(64, a * b);
}

export function ContractHeapBytes(c: Contract): bigint {
    let capture: bigint = 0n;
    if (c.HasOnePass) {
        capture = c.OnePass.HeapBytes;
    }
    if (c.HasSolver) {
        capture = vg.maxBig(capture, c.Solver.HeapBytes);
    }
    return cAdd(c.Matcher.HeapBytes, capture);
}

export function ContractStackBytes(c: Contract): bigint {
    let deepest: bigint = c.Matcher.StackBytes;
    if (c.HasOnePass) {
        deepest = vg.maxBig(deepest, c.OnePass.StackBytes);
    }
    if (c.HasSolver) {
        deepest = vg.maxBig(deepest, c.Solver.StackBytes);
    }
    return deepest;
}

export function ContractSteps(c: Contract): bigint {
    let steps: bigint = c.Matcher.Steps;
    if (c.HasOnePass) {
        steps = cAdd(steps, c.OnePass.Steps);
    }
    if (c.HasSolver) {
        steps = cAdd(steps, c.Solver.Steps);
    }
    return steps;
}

export function ContractFor(re: Regexp, maxInput: number): Contract {
    const length: bigint = BigInt(Math.min(Math.max(maxInput, 0), subjectLimit));
    const atom: bigint = atomCost(re);
    let c: Contract = new Contract();
    c.MaxInput = vg.intOf(length);
    c.Matcher = matcherContract(re, length, atom);
    if (((re.progOK && (re.nsub > 0)) && (((re.flags & FlagNoSub) >>> 0) === 0))) {
        if (re.onePass) {
            c.OnePass = onePassContract(re, length, atom);
            c.HasOnePass = true;
        } else {
            c.Solver = solverContract(re, length, atom);
            c.HasSolver = true;
        }
    }
    return c;
}

export function matcherContract(re: Regexp, length: bigint, atom: bigint): BackendContract {
    let b: BackendContract = new BackendContract();
    if ((!re.progOK)) {
        b.StackBytes = 2048n;
        b.Steps = cAdd(cMul(2n, length), 2n);
        return b;
    }
    const n: bigint = BigInt(re.prog.ins.len);
    const k: bigint = BigInt(re.minSlots);
    let ring: bigint = 2n;
    if (re.prog.multi) {
        ring = BigInt.asIntN(64, 8n + 1n);
    }
    const heap: bigint = workspaceHeapBound(n, k, ring);
    const weight: bigint = cAdd(cMul(4n, k), 22n);
    const perTest: bigint = cAdd(cAdd(atom, 2n), cMul(BigInt.asIntN(64, ring - 1n), cAdd(cMul(4n, k), 12n)));
    let perBoundary: bigint = cMul(weight, cMul(BigInt.asIntN(64, n + 1n), BigInt.asIntN(64, n + 1n)));
    perBoundary = cAdd(perBoundary, cMul(n, perTest));
    perBoundary = cAdd(perBoundary, cAdd(cAdd(n, cMul(2n, k)), cAdd(cMul(4n, ring), 38n)));
    let boundaries: bigint = cAdd(length, 1n);
    if ((re.prog.depth >= 0)) {
        boundaries = vg.minBig(boundaries, BigInt.asIntN(64, BigInt(re.prog.depth) + 3n));
    }
    const steps: bigint = cAdd(cAdd(BigInt.asIntN(64, 24n + ring), length), cMul(boundaries, perBoundary));
    const stack: bigint = BigInt.asIntN(64, 2048n + BigInt.asIntN(64, 16n * 256n));
    b.HeapBytes = heap;
    b.StackBytes = stack;
    b.Steps = steps;
    return b;
}

export function captureHeap(re: Regexp, length: bigint): bigint {
    const window: bigint = cAdd(cMul(4n, length), cMul(8n, cAdd(length, 1n)));
    const spans: bigint = cMul(16n, cAdd(BigInt(re.nsub), 1n));
    const payload: bigint = cAdd(window, spans);
    const allowance: bigint = 24576n;
    return cAdd(cAdd(payload, allowance), 64n);
}

export function onePassContract(re: Regexp, length: bigint, atom: bigint): BackendContract {
    let b: BackendContract = new BackendContract();
    const perVisit: bigint = cAdd(atom, BigInt.asIntN(64, BigInt(re.nsub) + 1n));
    b.HeapBytes = captureHeap(re, length);
    b.StackBytes = cMul(cAdd(astHeight(re.nodes, re.root), BigInt.asIntN(64, 2n + 8n)), 256n);
    b.Steps = cMul(cMul(astSize(re.nodes, re.root), cAdd(length, 2n)), perVisit);
    return b;
}

export function solverContract(re: Regexp, length: bigint, atom: bigint): BackendContract {
    let b: BackendContract = new BackendContract();
    const depth: bigint = solverDepth(re.nodes, re.root, length);
    const structural: bigint = vg.minBig(solverSteps(re.nodes, re.root, length), cAdd(50000000n, depth));
    const tree: bigint = treeNodes(re.nodes, re.root, length);
    const perStep: bigint = cAdd(atom, cAdd(cMul(2n, tree), BigInt.asIntN(64, cMul(2n, BigInt(re.minSlots)) + 4n)));
    const steps: bigint = cAdd(cMul(structural, perStep), cMul(tree, BigInt.asIntN(64, BigInt(re.nsub) + 2n)));
    const perAlloc: bigint = cAdd(BigInt.asIntN(64, BigInt.asIntN(64, 2n * 48n) + 128n), cMul(8n, cAdd(solverFanout(re.nodes, re.root, length), 1n)));
    let heap: bigint = cAdd(cMul(structural, perAlloc), cMul(16n, BigInt(re.minSlots)));
    heap = cAdd(heap, 4096n);
    heap = cAdd(heap, captureHeap(re, length));
    const stack: bigint = cMul(cAdd(depth, BigInt.asIntN(64, 3n + 16n)), 256n);
    b.HeapBytes = heap;
    b.StackBytes = stack;
    b.Steps = steps;
    return b;
}

export function astSize(nodes: vg.Slice<node>, ni: number): bigint {
    let _t2!: vg.Slice<number>;
    let total: bigint = 1n;
    for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
        total = cAdd(total, astSize(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)])));
    }
    return total;
}

export function astHeight(nodes: vg.Slice<node>, ni: number): bigint {
    let _t2!: vg.Slice<number>;
    let deepest: bigint = 0n;
    for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
        deepest = vg.maxBig(deepest, astHeight(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)])));
    }
    return BigInt.asIntN(64, deepest + 1n);
}

export function atomCost(re: Regexp): bigint {
    let lc: lookupCosts = localeLookupCosts(re.loc);
    return atomCostNode(re.nodes, re.brackets, lc, re.root);
}

export function atomCostNode(nodes: vg.Slice<node>, brs: vg.Slice<bracketSet>, lc: lookupCosts, ni: number): bigint {
    let _t2!: vg.Slice<number>;
    let cost: bigint = 1n;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opChar:
        {
            cost = BigInt.asIntN(64, BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].fold.len) + 1n);
            break;
        }
        case opBracket:
        {
            cost = bracketAtomCost(brs, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].br, lc);
            break;
        }
    }
    for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
        cost = vg.maxBig(cost, atomCostNode(nodes, brs, lc, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)])));
    }
    return cost;
}

export function searchSteps(count: number): bigint {
    let count_v: number = count;
    let steps: bigint = 0n;
    while ((count_v > 0)) {
        count_v = vg.div(count_v, 2);
        steps = BigInt.asIntN(64, steps + 1n);
    }
    return steps;
}

export function u32ContainsCost(count: number): bigint {
    return BigInt.asIntN(64, 1n + BigInt.asIntN(64, 3n * searchSteps(count)));
}

export function findPairCost(count: number): bigint {
    return BigInt.asIntN(64, 3n + BigInt.asIntN(64, 3n * searchSteps(count)));
}

export function findCaseCost(count: number): bigint {
    return BigInt.asIntN(64, 6n + BigInt.asIntN(64, 3n * searchSteps(count)));
}

export function pairSourcesRunCost(count: number, preimages: bigint): bigint {
    return BigInt.asIntN(64, BigInt.asIntN(64, 5n + BigInt.asIntN(64, 3n * searchSteps(count))) + BigInt.asIntN(64, 5n * preimages));
}

export function compareSequenceCost(length: bigint): bigint {
    return BigInt.asIntN(64, 5n + BigInt.asIntN(64, 3n * length));
}

export function localeLookupCosts(l: Locale): lookupCosts {
    let lc: lookupCosts = new lookupCosts();
    if (l.posix) {
        lc.contraction = 1n;
        lc.classMask = 3n;
        lc.casePreimages = 2n;
        lc.caseConvert = 2n;
        lc.preimages = 1n;
        return lc;
    }
    lc.sequenceSearch = searchSteps(vg.div(sectionLen(l, secSequences), 8));
    const row: CollProfile = collationProfileRow(l, l.collationProfile);
    lc.contraction = BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, 1n + 13n) + u32ContainsCost(row.AddCount)) + 1n) + u32ContainsCost(vg.div(sectionLen(l, secRootContractions), 4))) + u32ContainsCost(row.RemoveCount));
    lc.primaryToken = BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, 1n + 13n) + findPairCost(row.OverrideCount)) + 1n) + findPairCost(vg.div(sectionLen(l, secRootEquivalences), 8)));
    lc.classMask = 4n;
    lc.preimages = BigInt(localeMaxPreimages(l));
    lc.caseConvert = BigInt.asIntN(64, 2n + findCaseCost(vg.div(sectionLen(l, secCaseDefault), 12)));
    let upper: number = secInvUpperDefault;
    let lower: number = secInvLowerDefault;
    if ((l.caseProfile === 1)) {
        lc.caseConvert = BigInt.asIntN(64, lc.caseConvert + findCaseCost(vg.div(sectionLen(l, secCaseTurkic), 12)));
        upper = secInvUpperTurkic;
        lower = secInvLowerTurkic;
    }
    lc.casePreimages = BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, 2n + pairSourcesRunCost(vg.div(sectionLen(l, upper), 8), lc.preimages)) + pairSourcesRunCost(vg.div(sectionLen(l, lower), 8), lc.preimages)) + BigInt.asIntN(64, lc.preimages * BigInt.asIntN(64, 2n + lc.preimages)));
    return lc;
}

export function elementIDCost(lc: lookupCosts, length: bigint): bigint {
    if ((length === 1n)) {
        return 3n;
    }
    return BigInt.asIntN(64, BigInt.asIntN(64, 2n + BigInt.asIntN(64, 2n * length)) + BigInt.asIntN(64, lc.sequenceSearch * BigInt.asIntN(64, 1n + compareSequenceCost(length))));
}

export function collatingElementIDCost(lc: lookupCosts, length: bigint): bigint {
    let cost: bigint = BigInt.asIntN(64, 1n + elementIDCost(lc, length));
    if ((length > 1n)) {
        cost = BigInt.asIntN(64, cost + lc.contraction);
    }
    return cost;
}

export function primaryEqualCost(lc: lookupCosts, left: bigint, right: bigint): bigint {
    return BigInt.asIntN(64, BigInt.asIntN(64, BigInt.asIntN(64, 1n + collatingElementIDCost(lc, left)) + collatingElementIDCost(lc, right)) + BigInt.asIntN(64, 2n * lc.primaryToken));
}

export function equivsCost(brs: vg.Slice<bracketSet>, bi: number, lc: lookupCosts, length: bigint): bigint {
    let _t2!: vg.Slice<vg.Slice<number>>;
    let cost: bigint = 0n;
    for (let i: number = 0; (i < brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len); i = vg.chk(i + 1)) {
        cost = cAdd(cost, BigInt.asIntN(64, 1n + primaryEqualCost(lc, length, BigInt((_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].equivs, _t2.buf[_t2.off + vg.ix(i, _t2.len)]).len))));
    }
    return cost;
}

export function positiveSingleCost(brs: vg.Slice<bracketSet>, bi: number, lc: lookupCosts): bigint {
    let cost: bigint = BigInt.asIntN(64, 2n + searchSteps(brs.buf[brs.off + vg.ix(bi, brs.len)].ranges.len));
    if ((brs.buf[brs.off + vg.ix(bi, brs.len)].classMask !== 0)) {
        cost = BigInt.asIntN(64, cost + lc.classMask);
    }
    return cAdd(cost, equivsCost(brs, bi, lc, 1n));
}

export function matchesOneCost(brs: vg.Slice<bracketSet>, bi: number, lc: lookupCosts): bigint {
    const positive: bigint = positiveSingleCost(brs, bi, lc);
    let cost: bigint = cAdd(1n, positive);
    if (brs.buf[brs.off + vg.ix(bi, brs.len)].icase) {
        cost = cAdd(cost, cAdd(lc.casePreimages, cMul(lc.preimages, cAdd(1n, positive))));
    }
    return cost;
}

export function candidateLeafCost(brs: vg.Slice<bracketSet>, bi: number, lc: lookupCosts, length: bigint): bigint {
    return cAdd(BigInt.asIntN(64, 1n + collatingElementIDCost(lc, length)), equivsCost(brs, bi, lc, length));
}

export function probeCost(brs: vg.Slice<bracketSet>, bi: number, lc: lookupCosts, length: bigint): bigint {
    let _t2!: vg.Slice<vg.Slice<number>>;
    let cost: bigint = cAdd(2n, BigInt(brs.buf[brs.off + vg.ix(bi, brs.len)].elems.len));
    let counterpart: bigint = 1n;
    if (brs.buf[brs.off + vg.ix(bi, brs.len)].icase) {
        counterpart = BigInt.asIntN(64, 1n + BigInt.asIntN(64, 2n * lc.caseConvert));
    }
    for (let i: number = 0; (i < brs.buf[brs.off + vg.ix(bi, brs.len)].elems.len); i = vg.chk(i + 1)) {
        if ((BigInt((_t2 = brs.buf[brs.off + vg.ix(bi, brs.len)].elems, _t2.buf[_t2.off + vg.ix(i, _t2.len)]).len) === length)) {
            cost = cAdd(cost, cMul(length, counterpart));
        }
    }
    if ((brs.buf[brs.off + vg.ix(bi, brs.len)].equivs.len === 0)) {
        return cost;
    }
    const leaf: bigint = candidateLeafCost(brs, bi, lc, length);
    if ((!brs.buf[brs.off + vg.ix(bi, brs.len)].icase)) {
        return cAdd(cost, cAdd(BigInt.asIntN(64, length + 1n), leaf));
    }
    let candidates: bigint = 1n;
    for (let i_2: bigint = 0n; (i_2 < length); i_2 = BigInt.asIntN(64, i_2 + 1n)) {
        candidates = cMul(candidates, BigInt.asIntN(64, lc.preimages + 1n));
    }
    return cAdd(cost, cMul(candidates, cAdd(BigInt.asIntN(64, 2n + lc.preimages), cAdd(lc.casePreimages, leaf))));
}

export function bracketAtomCost(brs: vg.Slice<bracketSet>, bi: number, lc: lookupCosts): bigint {
    let cost: bigint = matchesOneCost(brs, bi, lc);
    if ((brs.buf[brs.off + vg.ix(bi, brs.len)].multiLens === 0)) {
        return cost;
    }
    cost = cAdd(cost, BigInt.asIntN(64, 8n - 1n));
    for (let length: number = 2; (length <= maxElemAhead); length = vg.chk(length + 1)) {
        if (((brs.buf[brs.off + vg.ix(bi, brs.len)].multiLens & ((1 << length) & 0xffff)) !== 0)) {
            cost = cAdd(cost, probeCost(brs, bi, lc, BigInt(length)));
        }
    }
    return cost;
}

export function solverSteps(nodes: vg.Slice<node>, ni: number, length: bigint): bigint {
    let _t2!: vg.Slice<number>;
    const spans: bigint = BigInt.asIntN(64, vg.divBig(cMul(cAdd(length, 1n), cAdd(length, 2n)), 2n));
    let total: bigint = spans;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opConcat:
        {
            total = cAdd(total, cMul(BigInt(vg.chk(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len - 1)), cMul(spans, cAdd(length, 2n))));
            break;
        }
        case opAlt:
        {
            total = cAdd(total, cMul(BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len), spans));
            break;
        }
        case opRepeat:
        {
            let states: bigint = BigInt.asIntN(64, BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min) + 1n);
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max !== infinite)) {
                states = BigInt.asIntN(64, BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max) + 1n);
            }
            total = cAdd(total, cMul(cMul(2n, states), cMul(spans, cAdd(length, 2n))));
            break;
        }
    }
    for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
        total = cAdd(total, solverSteps(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]), length));
    }
    return total;
}

export function repInstances(nodes: vg.Slice<node>, ni: number, length: bigint): bigint {
    let instances: bigint = cAdd(length, BigInt.asIntN(64, BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min) + 1n));
    if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max !== infinite)) {
        instances = vg.minBig(instances, BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max));
    }
    return instances;
}

export function solverDepth(nodes: vg.Slice<node>, ni: number, length: bigint): bigint {
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opGroup:
        case opAlt:
        case opConcat:
        {
            let deepest: bigint = 0n;
            for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                deepest = vg.maxBig(deepest, solverDepth(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]), length));
            }
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op === opConcat)) {
                return cAdd(deepest, BigInt.asIntN(64, BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len) + 1n));
            }
            return BigInt.asIntN(64, deepest + 1n);
            break;
        }
        case opRepeat:
        {
            return cAdd(BigInt.asIntN(64, repInstances(nodes, ni, length) + 1n), solverDepth(nodes, (_t3 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t3.buf[_t3.off + vg.ix(0, _t3.len)]), length));
            break;
        }
    }
    return 1n;
}

export function treeNodes(nodes: vg.Slice<node>, ni: number, length: bigint): bigint {
    let _t2!: vg.Slice<number>;
    let _t4!: vg.Slice<number>;
    let _t5!: vg.Slice<number>;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opGroup:
        case opAlt:
        {
            let widest: bigint = 0n;
            for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                widest = vg.maxBig(widest, treeNodes(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]), length));
            }
            return cAdd(widest, 1n);
            break;
        }
        case opConcat:
        {
            let total: bigint = 1n;
            for (let i_2: number = 0; (i_2 < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i_2 = vg.chk(i_2 + 1)) {
                total = cAdd(total, treeNodes(nodes, (_t4 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t4.buf[_t4.off + vg.ix(i_2, _t4.len)]), length));
            }
            return total;
            break;
        }
        case opRepeat:
        {
            return cAdd(1n, cMul(repInstances(nodes, ni, length), treeNodes(nodes, (_t5 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t5.buf[_t5.off + vg.ix(0, _t5.len)]), length)));
            break;
        }
    }
    return 1n;
}

export function solverFanout(nodes: vg.Slice<node>, ni: number, length: bigint): bigint {
    let _t2!: vg.Slice<number>;
    let widest: bigint = 1n;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opConcat:
        {
            widest = BigInt(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len);
            break;
        }
        case opRepeat:
        {
            widest = cAdd(repInstances(nodes, ni, length), 1n);
            break;
        }
    }
    for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
        widest = vg.maxBig(widest, solverFanout(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]), length));
    }
    return widest;
}

export function prepare(ws: engineWS, n: number, k: number, ring: number): void {
    let _t2!: vg.Slice<slotTable>;
    let _t3!: vg.Slice<slotTable>;
    let _t4!: vg.Slice<slotTable>;
    ws.slots = vg.make(slotTable.elem, ring);
    for (let i: number = 0; (i < ring); i = vg.chk(i + 1)) {
        (_t2 = ws.slots, _t2.buf[_t2.off + vg.ix(i, _t2.len)]).stamp = vg.make(vg.U32, n);
        (_t3 = ws.slots, _t3.buf[_t3.off + vg.ix(i, _t3.len)]).starts = vg.make(vg.I32, n);
        if ((k > 0)) {
            (_t4 = ws.slots, _t4.buf[_t4.off + vg.ix(i, _t4.len)]).ctr = vg.make(vg.U32, vg.chk(n * k));
        }
    }
    ws.onq = vg.make(vg.U8, n);
    if ((k > 0)) {
        ws.bestCtr = vg.make(vg.U32, k);
        ws.ctrBuf = vg.make(vg.U32, k);
        ws.zeros = vg.make(vg.U32, k);
    }
    ws.queue = vg.make(vg.U32, 0, 16);
}

export function workspaceHeapBound(n: bigint, k: bigint, ring: bigint): bigint {
    let perSlot: bigint = cMul(8n, n);
    if ((k > 0n)) {
        perSlot = cAdd(perSlot, cMul(4n, cMul(n, k)));
    }
    let heap: bigint = cAdd(cMul(ring, 104n), cMul(ring, perSlot));
    heap = cAdd(heap, n);
    if ((k > 0n)) {
        heap = cAdd(heap, BigInt.asIntN(64, 12n * k));
    }
    heap = cAdd(heap, 64n);
    heap = cAdd(heap, cMul(ring, cAdd(cMul(16n, n), 64n)));
    heap = cAdd(heap, cAdd(cMul(32n, n), 272n));
    return heap;
}

export function ctrLess(a: vg.Slice<number>, b: vg.Slice<number>): boolean {
    for (let i: number = 0; (i < a.len); i = vg.chk(i + 1)) {
        if ((a.buf[a.off + vg.ix(i, a.len)] !== b.buf[b.off + vg.ix(i, b.len)])) {
            return (a.buf[a.off + vg.ix(i, a.len)] < b.buf[b.off + vg.ix(i, b.len)]);
        }
    }
    return false;
}

export function trailingZeros64(x: bigint): number {
    let x_v: bigint = x;
    let n: number = 0;
    if (((x_v & 4294967295n) === 0n)) {
        n = vg.chk(n + 32);
        x_v = (x_v >> 32n);
    }
    if (((x_v & 65535n) === 0n)) {
        n = vg.chk(n + 16);
        x_v = (x_v >> 16n);
    }
    if (((x_v & 255n) === 0n)) {
        n = vg.chk(n + 8);
        x_v = (x_v >> 8n);
    }
    if (((x_v & 15n) === 0n)) {
        n = vg.chk(n + 4);
        x_v = (x_v >> 4n);
    }
    if (((x_v & 3n) === 0n)) {
        n = vg.chk(n + 2);
        x_v = (x_v >> 2n);
    }
    if (((x_v & 1n) === 0n)) {
        n = vg.chk(n + 1);
    }
    return n;
}

export function runPhaseA(re: Regexp, subject: vg.Str, eflags: number): engineResult {
    let ws: engineWS = new engineWS();
    let e: phaseAState = new phaseAState();
    e.subject = subject;
    e.eflags = eflags;
    e.k = re.minSlots;
    e.ring = 2;
    e.nlMode = (((re.flags & FlagNewline) >>> 0) !== 0);
    if (re.prog.multi) {
        e.ring = vg.chk(maxElemAhead + 1);
    }
    prepare(ws, re.prog.ins.len, e.k, e.ring);
    paRun(e, ws, re);
    let result: engineResult = new engineResult();
    result.matched = e.matched;
    result.so = e.so;
    result.eo = e.eo;
    return result;
}

export function paGen(boundary: number): number {
    return (((boundary >>> 0) + 1) >>> 0);
}

export function paConsider(e: phaseAState, ws: engineWS, start: number, ctr: vg.Slice<number>, end: number): void {
    if (((!e.matched) || (start < e.so))) {
        e.matched = true;
        e.so = start;
        e.eo = end;
        vg.copy(vg.U32, vg.head(ws.bestCtr, e.k), ctr);
        return;
    }
    if ((start > e.so)) {
        return;
    }
    if ((e.k > 0)) {
        if (ctrLess(ctr, vg.head(ws.bestCtr, e.k))) {
            e.eo = end;
            vg.copy(vg.U32, vg.head(ws.bestCtr, e.k), ctr);
            return;
        }
        if (ctrLess(vg.head(ws.bestCtr, e.k), ctr)) {
            return;
        }
    }
    if ((end > e.eo)) {
        e.eo = end;
    }
}

export function paPrune(e: phaseAState, ws: engineWS, start: number, ctr: vg.Slice<number>): boolean {
    if ((!e.matched)) {
        return false;
    }
    if ((start > e.so)) {
        return true;
    }
    if (((start < e.so) || (e.k === 0))) {
        return false;
    }
    return ctrLess(vg.head(ws.bestCtr, e.k), ctr);
}

export function paStore(e: phaseAState, ws: engineWS, si: number, pc: number, start: number, ctr: vg.Slice<number>): boolean {
    let _t1!: vg.Slice<number>;
    let _t2!: vg.Slice<slotTable>;
    let _t3!: vg.Slice<slotTable>;
    let _t4!: vg.Slice<number>;
    let _t5!: vg.Slice<slotTable>;
    let _t6!: vg.Slice<slotTable>;
    let _t7!: vg.Slice<slotTable>;
    let _t8!: vg.Slice<number>;
    let _t9!: number;
    let _t10!: vg.Slice<slotTable>;
    let _t11!: vg.Slice<slotTable>;
    let _t12!: vg.Slice<slotTable>;
    let _t13!: vg.Slice<number>;
    let _t14!: number;
    let _t15!: vg.Slice<slotTable>;
    let _t16!: vg.Slice<slotTable>;
    if (paPrune(e, ws, start, ctr)) {
        return false;
    }
    if (((_t1 = (_t2 = ws.slots, _t2.buf[_t2.off + vg.ix(si, _t2.len)]).stamp, _t1.buf[_t1.off + vg.ix(pc, _t1.len)]) === (_t3 = ws.slots, _t3.buf[_t3.off + vg.ix(si, _t3.len)]).gen)) {
        const oldStart: number = (_t4 = (_t5 = ws.slots, _t5.buf[_t5.off + vg.ix(si, _t5.len)]).starts, _t4.buf[_t4.off + vg.ix(pc, _t4.len)]);
        if ((oldStart < start)) {
            return false;
        }
        if ((oldStart === start)) {
            if ((e.k === 0)) {
                return false;
            }
            const base: number = vg.chk(pc * e.k);
            if ((!ctrLess(ctr, vg.sub((_t6 = ws.slots, _t6.buf[_t6.off + vg.ix(si, _t6.len)]).ctr, base, vg.chk(base + e.k))))) {
                return false;
            }
        }
    } else {
        (_t8 = (_t10 = ws.slots, _t10.buf[_t10.off + vg.ix(si, _t10.len)]).stamp, _t9 = vg.ix(pc, _t8.len), _t8.buf[_t8.off + _t9] = (_t7 = ws.slots, _t7.buf[_t7.off + vg.ix(si, _t7.len)]).gen);
        (_t12 = ws.slots, _t12.buf[_t12.off + vg.ix(si, _t12.len)]).active = vg.append(vg.U32, (_t11 = ws.slots, _t11.buf[_t11.off + vg.ix(si, _t11.len)]).active, pc);
    }
    (_t13 = (_t15 = ws.slots, _t15.buf[_t15.off + vg.ix(si, _t15.len)]).starts, _t14 = vg.ix(pc, _t13.len), _t13.buf[_t13.off + _t14] = start);
    if ((e.k > 0)) {
        const base_2: number = vg.chk(pc * e.k);
        vg.copy(vg.U32, vg.sub((_t16 = ws.slots, _t16.buf[_t16.off + vg.ix(si, _t16.len)]).ctr, base_2, vg.chk(base_2 + e.k)), ctr);
    }
    return true;
}

export function paRelax(e: phaseAState, ws: engineWS, si: number, pc: number, start: number, ctr: vg.Slice<number>): void {
    if (paStore(e, ws, si, pc, start, ctr)) {
        ws.queue = vg.append(vg.U32, ws.queue, pc);
    }
}

export function compactQueue(ws: engineWS): void {
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    let _t4!: vg.Slice<number>;
    let _t5!: number;
    let _t6!: vg.Slice<number>;
    let _t7!: number;
    let _t9!: vg.Slice<number>;
    let _t10!: number;
    let _t11!: vg.Slice<number>;
    let w: number = 0;
    for (let r: number = 0; (r < ws.queue.len); r = vg.chk(r + 1)) {
        const pc: number = (_t2 = ws.queue, _t2.buf[_t2.off + vg.ix(r, _t2.len)]);
        if (((_t3 = ws.onq, _t3.buf[_t3.off + vg.ix(pc, _t3.len)]) === 0)) {
            (_t4 = ws.onq, _t5 = vg.ix(pc, _t4.len), _t4.buf[_t4.off + _t5] = 1);
            (_t6 = ws.queue, _t7 = vg.ix(w, _t6.len), _t6.buf[_t6.off + _t7] = pc);
            w = vg.chk(w + 1);
        }
    }
    ws.queue = vg.head(ws.queue, w);
    for (let i: number = 0; (i < ws.queue.len); i = vg.chk(i + 1)) {
        (_t9 = ws.onq, _t10 = vg.ix((_t11 = ws.queue, _t11.buf[_t11.off + vg.ix(i, _t11.len)]), _t9.len), _t9.buf[_t9.off + _t10] = 0);
    }
}

export function paClosure(e: phaseAState, ws: engineWS, re: Regexp, si: number): void {
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    let _t4!: vg.Slice<slotTable>;
    let _t5!: vg.Slice<slotTable>;
    let _t6!: vg.Slice<number>;
    let _t7!: vg.Slice<slotTable>;
    let _t8!: vg.Slice<slotTable>;
    let _t9!: vg.Slice<instr>;
    let _t10!: vg.Slice<instr>;
    let _t11!: vg.Slice<instr>;
    let _t12!: vg.Slice<instr>;
    let _t13!: vg.Slice<instr>;
    let _t14!: vg.Slice<instr>;
    const limit: number = vg.chk(queueCompactFactor * re.prog.ins.len);
    while ((ws.queue.len > 0)) {
        if ((ws.queue.len > limit)) {
            compactQueue(ws);
        }
        const pc: number = (_t2 = ws.queue, _t2.buf[_t2.off + vg.ix(vg.chk(ws.queue.len - 1), _t2.len)]);
        ws.queue = vg.head(ws.queue, vg.chk(ws.queue.len - 1));
        if (((_t3 = (_t4 = ws.slots, _t4.buf[_t4.off + vg.ix(si, _t4.len)]).stamp, _t3.buf[_t3.off + vg.ix(pc, _t3.len)]) !== (_t5 = ws.slots, _t5.buf[_t5.off + vg.ix(si, _t5.len)]).gen)) {
            continue;
        }
        const start: number = (_t6 = (_t7 = ws.slots, _t7.buf[_t7.off + vg.ix(si, _t7.len)]).starts, _t6.buf[_t6.off + vg.ix(pc, _t6.len)]);
        const base: number = vg.chk(pc * e.k);
        const ctr: vg.Slice<number> = vg.sub((_t8 = ws.slots, _t8.buf[_t8.off + vg.ix(si, _t8.len)]).ctr, base, vg.chk(base + e.k));
        switch ((_t9 = re.prog.ins, _t9.buf[_t9.off + vg.ix(pc, _t9.len)]).op) {
            case iSplit:
            {
                paRelax(e, ws, si, (_t10 = re.prog.ins, _t10.buf[_t10.off + vg.ix(pc, _t10.len)]).next, start, ctr);
                paRelax(e, ws, si, (_t11 = re.prog.ins, _t11.buf[_t11.off + vg.ix(pc, _t11.len)]).alt, start, ctr);
                break;
            }
            case iJmp:
            {
                paRelax(e, ws, si, (_t12 = re.prog.ins, _t12.buf[_t12.off + vg.ix(pc, _t12.len)]).next, start, ctr);
                break;
            }
            case iBOL:
            {
                if (e.bol) {
                    paRelax(e, ws, si, (_t13 = re.prog.ins, _t13.buf[_t13.off + vg.ix(pc, _t13.len)]).next, start, ctr);
                }
                break;
            }
            case iEOL:
            {
                if (e.eol) {
                    paRelax(e, ws, si, (_t14 = re.prog.ins, _t14.buf[_t14.off + vg.ix(pc, _t14.len)]).next, start, ctr);
                }
                break;
            }
            case iMatch:
            {
                paConsider(e, ws, start, ctr, e.pos);
                break;
            }
        }
    }
}

export function paArrive(e: phaseAState, ws: engineWS, re: Regexp, pc: number, delta: number, start: number, ctr: vg.Slice<number>): void {
    let _t1!: vg.Slice<slotTable>;
    let _t2!: vg.Slice<slotTable>;
    let _t3!: vg.Slice<slotTable>;
    let _t4!: vg.Slice<slotTable>;
    let _t5!: vg.Slice<instr>;
    let _t6!: vg.Slice<instr>;
    let _t8!: vg.Slice<number>;
    let _t9!: number;
    let _t10!: vg.Slice<instr>;
    let _t12!: vg.Slice<number>;
    let _t13!: vg.Slice<instr>;
    let _t14!: vg.Slice<instr>;
    const fi: number = vg.rem(vg.chk(e.ci + delta), e.ring);
    const g: number = paGen(vg.chk(e.ci + delta));
    if (((_t1 = ws.slots, _t1.buf[_t1.off + vg.ix(fi, _t1.len)]).gen !== g)) {
        (_t2 = ws.slots, _t2.buf[_t2.off + vg.ix(fi, _t2.len)]).gen = g;
        (_t4 = ws.slots, _t4.buf[_t4.off + vg.ix(fi, _t4.len)]).active = vg.head((_t3 = ws.slots, _t3.buf[_t3.off + vg.ix(fi, _t3.len)]).active, 0);
    }
    let newCtr: vg.Slice<number> = ctr;
    const mask: bigint = (_t5 = re.prog.ins, _t5.buf[_t5.off + vg.ix(pc, _t5.len)]).mask;
    const hasExtra: boolean = ((_t6 = re.prog.ins, _t6.buf[_t6.off + vg.ix(pc, _t6.len)]).extra.len > 0);
    if (((e.k > 0) && ((mask !== 0n) || hasExtra))) {
        const buffer: vg.Slice<number> = vg.head(ws.ctrBuf, e.k);
        vg.copy(vg.U32, buffer, ctr);
        for (let m: bigint = mask; (m !== 0n); m = (m & BigInt.asUintN(64, m - 1n))) {
            (_t8 = buffer, _t9 = vg.ix(trailingZeros64(m), _t8.len), _t8.buf[_t8.off + _t9] = ((_t8.buf[_t8.off + _t9] + (delta >>> 0)) >>> 0));
        }
        for (let i: number = 0; (i < (_t10 = re.prog.ins, _t10.buf[_t10.off + vg.ix(pc, _t10.len)]).extra.len); i = vg.chk(i + 1)) {
            buffer.buf[buffer.off + vg.ix((_t12 = (_t13 = re.prog.ins, _t13.buf[_t13.off + vg.ix(pc, _t13.len)]).extra, _t12.buf[_t12.off + vg.ix(i, _t12.len)]), buffer.len)] = ((buffer.buf[buffer.off + vg.ix((_t12 = (_t13 = re.prog.ins, _t13.buf[_t13.off + vg.ix(pc, _t13.len)]).extra, _t12.buf[_t12.off + vg.ix(i, _t12.len)]), buffer.len)] + (delta >>> 0)) >>> 0);
        }
        newCtr = buffer;
    }
    paStore(e, ws, fi, (_t14 = re.prog.ins, _t14.buf[_t14.off + vg.ix(pc, _t14.len)]).next, start, newCtr);
}

export function paConsume(e: phaseAState, ws: engineWS, re: Regexp, si: number): void {
    let _t1!: vg.Slice<slotTable>;
    let _t3!: vg.Slice<number>;
    let _t4!: vg.Slice<slotTable>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<slotTable>;
    let _t7!: vg.Slice<slotTable>;
    let _t8!: vg.Slice<number>;
    let _t9!: vg.Slice<slotTable>;
    let _t10!: vg.Slice<slotTable>;
    let _t11!: vg.Slice<instr>;
    let _t12!: vg.Slice<instr>;
    let _t13!: vg.Slice<vg.Slice<number>>;
    let _t14!: vg.Slice<instr>;
    let _t15!: vg.Slice<instr>;
    let _t16!: vg.Slice<bracketSet>;
    let _t18!: vg.Slice<bracketSet>;
    let aheadReady: boolean = false;
    for (let ai: number = 0; (ai < (_t1 = ws.slots, _t1.buf[_t1.off + vg.ix(si, _t1.len)]).active.len); ai = vg.chk(ai + 1)) {
        const pc: number = (_t3 = (_t4 = ws.slots, _t4.buf[_t4.off + vg.ix(si, _t4.len)]).active, _t3.buf[_t3.off + vg.ix(ai, _t3.len)]);
        if (((_t5 = (_t6 = ws.slots, _t6.buf[_t6.off + vg.ix(si, _t6.len)]).stamp, _t5.buf[_t5.off + vg.ix(pc, _t5.len)]) !== (_t7 = ws.slots, _t7.buf[_t7.off + vg.ix(si, _t7.len)]).gen)) {
            continue;
        }
        const start: number = (_t8 = (_t9 = ws.slots, _t9.buf[_t9.off + vg.ix(si, _t9.len)]).starts, _t8.buf[_t8.off + vg.ix(pc, _t8.len)]);
        const base: number = vg.chk(pc * e.k);
        const ctr: vg.Slice<number> = vg.sub((_t10 = ws.slots, _t10.buf[_t10.off + vg.ix(si, _t10.len)]).ctr, base, vg.chk(base + e.k));
        switch ((_t11 = re.prog.ins, _t11.buf[_t11.off + vg.ix(pc, _t11.len)]).op) {
            case iRune:
            {
                if ((e.cur === ((_t12 = re.prog.ins, _t12.buf[_t12.off + vg.ix(pc, _t12.len)]).arg | 0))) {
                    paArrive(e, ws, re, pc, 1, start, ctr);
                }
                break;
            }
            case iRuneFold:
            {
                if (runesContain((_t13 = re.prog.foldSets, _t13.buf[_t13.off + vg.ix((_t14 = re.prog.ins, _t14.buf[_t14.off + vg.ix(pc, _t14.len)]).arg, _t13.len)]), e.cur)) {
                    paArrive(e, ws, re, pc, 1, start, ctr);
                }
                break;
            }
            case iAny:
            {
                if (anyMatches(re, e.cur)) {
                    paArrive(e, ws, re, pc, 1, start, ctr);
                }
                break;
            }
            case iBracket:
            {
                const bi: number = ((_t15 = re.prog.ins, _t15.buf[_t15.off + vg.ix(pc, _t15.len)]).arg | 0);
                if (bracketMatchesOne(re.brackets, bi, re.loc, e.cur)) {
                    paArrive(e, ws, re, pc, 1, start, ctr);
                }
                if (((_t16 = re.brackets, _t16.buf[_t16.off + vg.ix(bi, _t16.len)]).multiLens === 0)) {
                    continue;
                }
                if ((!aheadReady)) {
                    decodeAhead(e, ws);
                    aheadReady = true;
                }
                for (let length: number = 2; (length <= ws.ahead.len); length = vg.chk(length + 1)) {
                    if ((((_t18 = re.brackets, _t18.buf[_t18.off + vg.ix(bi, _t18.len)]).multiLens & ((1 << length) & 0xffff)) === 0)) {
                        continue;
                    }
                    if (bracketMatchesMulti(re.brackets, bi, re.loc, vg.head(ws.ahead, length))) {
                        paArrive(e, ws, re, pc, length, start, ctr);
                    }
                }
                break;
            }
        }
    }
}

export function decodeAhead(e: phaseAState, ws: engineWS): void {
    ws.ahead = vg.head(ws.ahead, 0);
    let at: number = e.pos;
    while (((ws.ahead.len < maxElemAhead) && (at < e.subject.length))) {
        const _t2 = decodeRuneAt(e.subject, at);
        const r: number = _t2[0];
        const size: number = _t2[1];
        ws.ahead = vg.append(vg.I32, ws.ahead, r);
        at = vg.chk(at + size);
    }
}

export function scanAhead(e: phaseAState, re: Regexp): number {
    let _t2!: vg.Str;
    if (re.prog.scan.single) {
        const idx: number = indexOfByte(vg.strTail(e.subject, e.pos), re.prog.scan.b);
        if ((idx < 0)) {
            return e.subject.length;
        }
        return vg.chk(e.pos + idx);
    }
    for (let i: number = e.pos; (i < e.subject.length); i = vg.chk(i + 1)) {
        if (re.prog.scan.stop[vg.ix((_t2 = e.subject, _t2[vg.ix(i, _t2.length)]), 256)]) {
            return i;
        }
    }
    return e.subject.length;
}

export function bolAt(e: phaseAState, prev: number): boolean {
    return (((e.pos === 0) && (((e.eflags & ExecNotBOL) >>> 0) === 0)) || (e.nlMode && (prev === 10)));
}

export function continuationFlags(re: Regexp, subject: vg.Str, pos: number, eflags: number): number {
    if ((pos === 0)) {
        return eflags;
    }
    if (((((re.flags & FlagNewline) >>> 0) !== 0) && (subject[vg.ix(vg.chk(pos - 1), subject.length)] === 10))) {
        return ((eflags & ~ExecNotBOL) >>> 0);
    }
    return ((eflags | ExecNotBOL) >>> 0);
}

export function paRun(e: phaseAState, ws: engineWS, re: Regexp): void {
    let _t2!: vg.Slice<slotTable>;
    let _t3!: vg.Slice<slotTable>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<slotTable>;
    let _t7!: vg.Slice<number>;
    let _t8!: vg.Slice<slotTable>;
    let _t9!: vg.Slice<number>;
    let _t10!: number;
    let _t11!: vg.Slice<slotTable>;
    let _t12!: vg.Slice<slotTable>;
    let _t13!: vg.Slice<slotTable>;
    let _t14!: vg.Str;
    let _t16!: vg.Slice<slotTable>;
    let _t18!: vg.Slice<slotTable>;
    let _t19!: vg.Slice<slotTable>;
    let prev: number = ((-2) | 0);
    const zeros: vg.Slice<number> = vg.head(ws.zeros, e.k);
    while (true) {
        const si: number = vg.rem(e.ci, e.ring);
        const g: number = paGen(e.ci);
        (_t2 = ws.slots, _t2.buf[_t2.off + vg.ix(si, _t2.len)]).gen = g;
        let w: number = 0;
        for (let r: number = 0; (r < (_t3 = ws.slots, _t3.buf[_t3.off + vg.ix(si, _t3.len)]).active.len); r = vg.chk(r + 1)) {
            const pc: number = (_t5 = (_t6 = ws.slots, _t6.buf[_t6.off + vg.ix(si, _t6.len)]).active, _t5.buf[_t5.off + vg.ix(r, _t5.len)]);
            if (((_t7 = (_t8 = ws.slots, _t8.buf[_t8.off + vg.ix(si, _t8.len)]).stamp, _t7.buf[_t7.off + vg.ix(pc, _t7.len)]) === g)) {
                (_t9 = (_t11 = ws.slots, _t11.buf[_t11.off + vg.ix(si, _t11.len)]).active, _t10 = vg.ix(w, _t9.len), _t9.buf[_t9.off + _t10] = pc);
                w = vg.chk(w + 1);
            }
        }
        (_t13 = ws.slots, _t13.buf[_t13.off + vg.ix(si, _t13.len)]).active = vg.head((_t12 = ws.slots, _t12.buf[_t12.off + vg.ix(si, _t12.len)]).active, w);
        if (((((w === 0) && (!e.matched)) && re.prog.scan.enabled) && (e.pos < e.subject.length))) {
            if ((!bolAt(e, prev))) {
                const next: number = scanAhead(e, re);
                if ((next > e.pos)) {
                    e.pos = next;
                    e.ci = vg.chk(e.ci + 1);
                    prev = 120;
                    if (((e.pos > 0) && ((_t14 = e.subject, _t14[vg.ix(vg.chk(e.pos - 1), _t14.length)]) === 10))) {
                        prev = 10;
                    }
                    continue;
                }
            }
        }
        const atEnd: boolean = (e.pos === e.subject.length);
        e.cur = ((-2) | 0);
        e.size = 0;
        if ((!atEnd)) {
            {
                const _t15 = decodeRuneAt(e.subject, e.pos);
                e.cur = _t15[0];
                e.size = _t15[1];
            }
        }
        e.bol = bolAt(e, prev);
        e.eol = ((atEnd && (((e.eflags & ExecNotEOL) >>> 0) === 0)) || (e.nlMode && (e.cur === 10)));
        ws.queue = vg.appendSlice(vg.U32, vg.head(ws.queue, 0), (_t16 = ws.slots, _t16.buf[_t16.off + vg.ix(si, _t16.len)]).active);
        if ((!e.matched)) {
            paRelax(e, ws, si, re.prog.start, (e.pos | 0), zeros);
        }
        paClosure(e, ws, re, si);
        if (atEnd) {
            return;
        }
        paConsume(e, ws, re, si);
        if (e.matched) {
            let pendingWork: boolean = false;
            for (let delta: number = 1; (delta < e.ring); delta = vg.chk(delta + 1)) {
                const fi: number = vg.rem(vg.chk(e.ci + delta), e.ring);
                if ((((_t18 = ws.slots, _t18.buf[_t18.off + vg.ix(fi, _t18.len)]).gen === paGen(vg.chk(e.ci + delta))) && ((_t19 = ws.slots, _t19.buf[_t19.off + vg.ix(fi, _t19.len)]).active.len > 0))) {
                    pendingWork = true;
                    break;
                }
            }
            if ((!pendingWork)) {
                return;
            }
        }
        prev = e.cur;
        e.pos = vg.chk(e.pos + e.size);
        e.ci = vg.chk(e.ci + 1);
    }
}

export function noError(): Error {
    let e: Error = new Error();
    e.Pos = (0 - 1);
    return e;
}

export function compileError(code: number, pos: number): Error {
    let e: Error = new Error();
    e.Code = code;
    e.Pos = pos;
    return e;
}

export function ErrorText(code: number): vg.Str {
    switch (code) {
        case ErrNone:
        {
            return _s16;
            break;
        }
        case ErrNoMatch:
        {
            return _s17;
            break;
        }
        case ErrBadPat:
        {
            return _s18;
            break;
        }
        case ErrECollate:
        {
            return _s19;
            break;
        }
        case ErrECType:
        {
            return _s20;
            break;
        }
        case ErrEEscape:
        {
            return _s21;
            break;
        }
        case ErrESubReg:
        {
            return _s22;
            break;
        }
        case ErrEBrack:
        {
            return _s23;
            break;
        }
        case ErrEParen:
        {
            return _s24;
            break;
        }
        case ErrEBrace:
        {
            return _s25;
            break;
        }
        case ErrBadBR:
        {
            return _s26;
            break;
        }
        case ErrERange:
        {
            return _s27;
            break;
        }
        case ErrESpace:
        {
            return _s28;
            break;
        }
        case ErrBadRpt:
        {
            return _s29;
            break;
        }
        case ErrENoSub:
        {
            return _s30;
            break;
        }
        default:
        {
            return _s31;
            break;
        }
    }
}

export function memoHash(k: memoKey): bigint {
    let h: bigint = (BigInt.asUintN(64, BigInt((k.a >>> 0)) * 2654435761n) ^ BigInt.asUintN(64, BigInt((k.b >>> 0)) * 2246822519n));
    h = (h ^ BigInt.asUintN(64, BigInt((k.c >>> 0)) * 3266489917n));
    h = (h ^ BigInt.asUintN(64, BigInt((k.d >>> 0)) * 668265263n));
    h = (h ^ (h >> 29n));
    h = BigInt.asUintN(64, h * 13787848793156543929n);
    h = (h ^ (h >> 32n));
    return h;
}

export function memoGet(t: memoTab, k: memoKey): [memoVal, boolean] {
    let _t1!: vg.Slice<number>;
    let _t3!: vg.Slice<memoKey>;
    let _t4!: vg.Slice<memoVal>;
    const none: memoVal = new memoVal();
    if ((t.keys.len === 0)) {
        return [none, false];
    }
    const mask: bigint = BigInt.asUintN(64, BigInt(vg.chk(t.keys.len - 1)));
    let at: bigint = (memoHash(k) & mask);
    while (((_t1 = t.used, _t1.buf[_t1.off + vg.ix(vg.intOf(at), _t1.len)]) !== 0)) {
        if (memoKey.eq((_t3 = t.keys, _t3.buf[_t3.off + vg.ix(vg.intOf(at), _t3.len)]), k)) {
            return [(_t4 = t.vals, _t4.buf[_t4.off + vg.ix(vg.intOf(at), _t4.len)]).clone(), true];
        }
        at = (BigInt.asUintN(64, at + 1n) & mask);
    }
    return [none, false];
}

export function memoInsert(t: memoTab, k: memoKey, v: memoVal): void {
    let _t1!: vg.Slice<number>;
    let _t3!: vg.Slice<memoKey>;
    let _t4!: vg.Slice<memoVal>;
    let _t5!: number;
    let _t6!: vg.Slice<number>;
    let _t7!: number;
    let _t8!: vg.Slice<memoKey>;
    let _t9!: number;
    let _t10!: vg.Slice<memoVal>;
    let _t11!: number;
    const mask: bigint = BigInt.asUintN(64, BigInt(vg.chk(t.keys.len - 1)));
    let at: bigint = (memoHash(k) & mask);
    while (((_t1 = t.used, _t1.buf[_t1.off + vg.ix(vg.intOf(at), _t1.len)]) !== 0)) {
        if (memoKey.eq((_t3 = t.keys, _t3.buf[_t3.off + vg.ix(vg.intOf(at), _t3.len)]), k)) {
            (_t4 = t.vals, _t5 = vg.ix(vg.intOf(at), _t4.len), _t4.buf[_t4.off + _t5] = v.clone());
            return;
        }
        at = (BigInt.asUintN(64, at + 1n) & mask);
    }
    (_t6 = t.used, _t7 = vg.ix(vg.intOf(at), _t6.len), _t6.buf[_t6.off + _t7] = 1);
    (_t8 = t.keys, _t9 = vg.ix(vg.intOf(at), _t8.len), _t8.buf[_t8.off + _t9] = k.clone());
    (_t10 = t.vals, _t11 = vg.ix(vg.intOf(at), _t10.len), _t10.buf[_t10.off + _t11] = v.clone());
    t.count = vg.chk(t.count + 1);
}

export function memoGrow(t: memoTab): void {
    const oldKeys: vg.Slice<memoKey> = t.keys;
    const oldVals: vg.Slice<memoVal> = t.vals;
    const oldUsed: vg.Slice<number> = t.used;
    let size: number = 64;
    if ((oldKeys.len > 0)) {
        size = vg.chk(2 * oldKeys.len);
    }
    t.keys = vg.make(memoKey.elem, size);
    t.vals = vg.make(memoVal.elem, size);
    t.used = vg.make(vg.U8, size);
    t.count = 0;
    for (let i: number = 0; (i < oldKeys.len); i = vg.chk(i + 1)) {
        if ((oldUsed.buf[oldUsed.off + vg.ix(i, oldUsed.len)] !== 0)) {
            memoInsert(t, oldKeys.buf[oldKeys.off + vg.ix(i, oldKeys.len)], oldVals.buf[oldVals.off + vg.ix(i, oldVals.len)]);
        }
    }
}

export function memoPut(t: memoTab, k: memoKey, v: memoVal): void {
    if ((vg.chk(4 * vg.chk(t.count + 1)) >= vg.chk(3 * t.keys.len))) {
        memoGrow(t);
    }
    memoInsert(t, k, v);
}

export function LocalePOSIX(): Locale {
    let l: Locale = new Locale();
    l.maxSeq = 1;
    l.preimagesDefault = 1;
    l.preimagesTurkic = 1;
    l.posix = true;
    l.valid = true;
    return l;
}

export function LocaleValid(l: Locale): boolean {
    return l.valid;
}

export function u32Raw(blob: vg.Str, off: number): number {
    return ((((((blob[vg.ix(off, blob.length)] | ((blob[vg.ix(vg.chk(off + 1), blob.length)] << 8) >>> 0)) >>> 0) | ((blob[vg.ix(vg.chk(off + 2), blob.length)] << 16) >>> 0)) >>> 0) | ((blob[vg.ix(vg.chk(off + 3), blob.length)] << 24) >>> 0)) >>> 0);
}

export function u16At(l: Locale, sec: number, index: number): number {
    let _t1!: vg.Str;
    let _t2!: vg.Str;
    const off: number = vg.chk(l.sec[vg.ix(sec, 23)].Off + vg.chk(2 * index));
    return ((_t1 = l.blob, _t1[vg.ix(off, _t1.length)]) | (((_t2 = l.blob, _t2[vg.ix(vg.chk(off + 1), _t2.length)]) << 8) & 0xffff));
}

export function u32At(l: Locale, sec: number, index: number): number {
    return u32Raw(l.blob, vg.chk(l.sec[vg.ix(sec, 23)].Off + vg.chk(4 * index)));
}

export function sectionLen(l: Locale, sec: number): number {
    return vg.chk(l.sec[vg.ix(sec, 23)].End - l.sec[vg.ix(sec, 23)].Off);
}

export function byteString(l: Locale, sec: number, off: number): vg.Str {
    let _t1!: vg.Str;
    const start: number = vg.chk(l.sec[vg.ix(sec, 23)].Off + off);
    let end: number = start;
    while (((end < l.sec[vg.ix(sec, 23)].End) && ((_t1 = l.blob, _t1[vg.ix(end, _t1.length)]) !== 0))) {
        end = vg.chk(end + 1);
    }
    return vg.strSub(l.blob, start, end);
}

export function localeLoad(l: Locale, blob: vg.Str): boolean {
    const magic: vg.Str = _s32;
    if (((blob.length < magic.length) || (!vg.streq(vg.strSub(blob, 0, magic.length), magic)))) {
        return false;
    }
    l.blob = blob;
    let cursor: number = magic.length;
    for (let i: number = 0; (i < numSections); i = vg.chk(i + 1)) {
        if ((vg.chk(cursor + 4) > blob.length)) {
            return false;
        }
        const length: number = u32Raw(blob, cursor);
        cursor = vg.chk(cursor + 4);
        if ((vg.chk(cursor + length) > blob.length)) {
            return false;
        }
        l.sec[vg.ix(i, 23)].Off = cursor;
        l.sec[vg.ix(i, 23)].End = vg.chk(cursor + length);
        cursor = vg.chk(cursor + length);
    }
    if ((cursor !== blob.length)) {
        return false;
    }
    if ((sectionLen(l, secScalars) < 4)) {
        return false;
    }
    l.maxSeq = u32At(l, secScalars, 0);
    if (((l.maxSeq < 1) || (l.maxSeq > maxElemAhead))) {
        return false;
    }
    if ((!localeValidate(l))) {
        return false;
    }
    l.preimagesDefault = vg.chk(maxPairRun(l, secInvUpperDefault) + maxPairRun(l, secInvLowerDefault));
    l.preimagesTurkic = vg.chk(maxPairRun(l, secInvUpperTurkic) + maxPairRun(l, secInvLowerTurkic));
    if (((l.preimagesDefault > maxPreimages) || (l.preimagesTurkic > maxPreimages))) {
        return false;
    }
    return true;
}

export function localeValidate(l: Locale): boolean {
    if ((sectionLen(l, secCtypeStage1) < vg.chk(2 * ctypeStage1Entries))) {
        return false;
    }
    const blocksLen: number = sectionLen(l, secCtypeBlocks);
    for (let i: number = 0; (i < ctypeStage1Entries); i = vg.chk(i + 1)) {
        const block: number = u16At(l, secCtypeStage1, i);
        if ((vg.chk(vg.chk(2 * 256) * vg.chk(block + 1)) > blocksLen)) {
            return false;
        }
    }
    if (((vg.rem(sectionLen(l, secCaseDefault), 12) !== 0) || (vg.rem(sectionLen(l, secCaseTurkic), 12) !== 0))) {
        return false;
    }
    if (((((vg.rem(sectionLen(l, secInvUpperDefault), 8) !== 0) || (vg.rem(sectionLen(l, secInvLowerDefault), 8) !== 0)) || (vg.rem(sectionLen(l, secInvUpperTurkic), 8) !== 0)) || (vg.rem(sectionLen(l, secInvLowerTurkic), 8) !== 0))) {
        return false;
    }
    if (((vg.rem(sectionLen(l, secSeqCodepoints), 4) !== 0) || (vg.rem(sectionLen(l, secSequences), 8) !== 0))) {
        return false;
    }
    const codepointCount: number = vg.div(sectionLen(l, secSeqCodepoints), 4);
    const seqCount: number = vg.div(sectionLen(l, secSequences), 8);
    for (let i_2: number = 0; (i_2 < seqCount); i_2 = vg.chk(i_2 + 1)) {
        const off: number = u32At(l, secSequences, vg.chk(2 * i_2));
        const length: number = u32At(l, secSequences, vg.chk(vg.chk(2 * i_2) + 1));
        if (((length < 1) || (vg.chk(off + length) > codepointCount))) {
            return false;
        }
    }
    if ((((((vg.rem(sectionLen(l, secRootContractions), 4) !== 0) || (vg.rem(sectionLen(l, secContractionAdds), 4) !== 0)) || (vg.rem(sectionLen(l, secContractionRemoves), 4) !== 0)) || (vg.rem(sectionLen(l, secRootEquivalences), 8) !== 0)) || (vg.rem(sectionLen(l, secCollationOverrides), 8) !== 0))) {
        return false;
    }
    if ((!contractionIDsValid(l, secRootContractions, seqCount))) {
        return false;
    }
    if ((!contractionIDsValid(l, secContractionAdds, seqCount))) {
        return false;
    }
    if ((vg.rem(sectionLen(l, secCollationProfiles), 24) !== 0)) {
        return false;
    }
    const profileCount: number = vg.div(sectionLen(l, secCollationProfiles), 24);
    const overrideCount: number = vg.div(sectionLen(l, secCollationOverrides), 8);
    const addCount: number = vg.div(sectionLen(l, secContractionAdds), 4);
    const removeCount: number = vg.div(sectionLen(l, secContractionRemoves), 4);
    for (let i_3: number = 0; (i_3 < profileCount); i_3 = vg.chk(i_3 + 1)) {
        const row: CollProfile = collationProfileRow(l, i_3);
        if ((((vg.chk(row.OverrideFirst + row.OverrideCount) > overrideCount) || (vg.chk(row.AddFirst + row.AddCount) > addCount)) || (vg.chk(row.RemoveFirst + row.RemoveCount) > removeCount))) {
            return false;
        }
    }
    if ((vg.rem(sectionLen(l, secTypeNameOffsets), 4) !== 0)) {
        return false;
    }
    const typeNameCount: number = vg.div(sectionLen(l, secTypeNameOffsets), 4);
    for (let i_4: number = 0; (i_4 < typeNameCount); i_4 = vg.chk(i_4 + 1)) {
        if ((u32At(l, secTypeNameOffsets, i_4) >= sectionLen(l, secTypeNames))) {
            return false;
        }
    }
    if ((((vg.rem(sectionLen(l, secLocales), 20) !== 0) || (vg.rem(sectionLen(l, secLocaleNameOffsets), 4) !== 0)) || (vg.rem(sectionLen(l, secLocaleTypes), 4) !== 0))) {
        return false;
    }
    const count: number = localesCount(l);
    if ((vg.div(sectionLen(l, secLocaleNameOffsets), 4) !== count)) {
        return false;
    }
    for (let i_5: number = 0; (i_5 < count); i_5 = vg.chk(i_5 + 1)) {
        if ((u32At(l, secLocaleNameOffsets, i_5) >= sectionLen(l, secLocaleNames))) {
            return false;
        }
    }
    const typeRowCount: number = vg.div(sectionLen(l, secLocaleTypes), 4);
    for (let i_6: number = 0; (i_6 < count); i_6 = vg.chk(i_6 + 1)) {
        const row_2: LocaleRow = localeRowAt(l, i_6);
        if (((vg.chk(row_2.TypeFirst + row_2.TypeCount) > typeRowCount) || (row_2.DefaultCollation >= profileCount))) {
            return false;
        }
    }
    for (let i_7: number = 0; (i_7 < typeRowCount); i_7 = vg.chk(i_7 + 1)) {
        if ((u16At(l, secLocaleTypes, vg.chk(vg.chk(2 * i_7) + 1)) >= profileCount)) {
            return false;
        }
    }
    return true;
}

export function contractionIDsValid(l: Locale, sec: number, seqCount: number): boolean {
    const count: number = vg.div(sectionLen(l, sec), 4);
    for (let i: number = 0; (i < count); i = vg.chk(i + 1)) {
        const id: number = u32At(l, sec, i);
        if (((id >= firstSequenceID) && (((id - firstSequenceID) >>> 0) >= seqCount))) {
            return false;
        }
    }
    return true;
}

export function localeMaxElementLength(l: Locale): number {
    return l.maxSeq;
}

export function validScalar(r: number): boolean {
    return (((r >= 0) && (r <= 1114111)) && (!((r >= 55296) && (r <= 57343))));
}

export function asciiLower(c: number): number {
    if (((c >= 65) && (c <= 90))) {
        return ((c + ((97 - 65) & 0xff)) & 0xff);
    }
    return c;
}

export function normalizeName(input: vg.Str): [vg.Str, boolean] {
    if ((input.length === 0)) {
        return [vg.EMPTY, false];
    }
    let out: vg.Slice<number> = vg.make(vg.U8, 0, input.length);
    let i: number = 0;
    while ((((i < input.length) && (input[vg.ix(i, input.length)] !== 46)) && (input[vg.ix(i, input.length)] !== 64))) {
        let c: number = input[vg.ix(i, input.length)];
        if (((c >= 128) || (out.len === normalizedNameMax))) {
            return [vg.EMPTY, false];
        }
        if ((c === 95)) {
            c = 45;
        } else {
            c = asciiLower(c);
        }
        out = vg.append(vg.U8, out, c);
        i = vg.chk(i + 1);
    }
    if (((i < input.length) && (input[vg.ix(i, input.length)] === 46))) {
        i = vg.chk(i + 1);
        let codeset: vg.Slice<number> = vg.make(vg.U8, 0, 5);
        while (((i < input.length) && (input[vg.ix(i, input.length)] !== 64))) {
            const c_2: number = asciiLower(input[vg.ix(i, input.length)]);
            i = vg.chk(i + 1);
            if (((codeset.len === 5) || (c_2 >= 128))) {
                return [vg.EMPTY, false];
            }
            codeset = vg.append(vg.U8, codeset, c_2);
        }
        if (((!vg.streq(vg.strFromBytes(codeset), _s33)) && (!vg.streq(vg.strFromBytes(codeset), _s34)))) {
            return [vg.EMPTY, false];
        }
    }
    return [vg.strFromBytes(out), true];
}

export function embeddedModifier(name: vg.Str): [vg.Str, boolean] {
    const at: number = indexOfByte(name, 64);
    if ((at < 0)) {
        return [vg.EMPTY, false];
    }
    const modifier: vg.Str = vg.strTail(name, vg.chk(at + 1));
    const keyword: vg.Str = _s35;
    if ((modifier.length <= keyword.length)) {
        return [vg.EMPTY, false];
    }
    for (let i: number = 0; (i < keyword.length); i = vg.chk(i + 1)) {
        if ((asciiLower(modifier[vg.ix(i, modifier.length)]) !== keyword[vg.ix(i, keyword.length)])) {
            return [vg.EMPTY, false];
        }
    }
    return [vg.strTail(modifier, keyword.length), true];
}

export function longTypeAlias(name: vg.Str): vg.Str {
    if (vg.streq(name, _s36)) {
        return _s37;
    }
    if (vg.streq(name, _s38)) {
        return _s39;
    }
    if (vg.streq(name, _s40)) {
        return _s41;
    }
    return name;
}

export function normalizeType(input: vg.Str): [vg.Str, boolean] {
    if ((input.length === 0)) {
        return [vg.EMPTY, true];
    }
    let out: vg.Slice<number> = vg.make(vg.U8, 0, input.length);
    for (let i: number = 0; (i < input.length); i = vg.chk(i + 1)) {
        const c: number = input[vg.ix(i, input.length)];
        if (((c >= 128) || (out.len === normalizedNameMax))) {
            return [vg.EMPTY, false];
        }
        out = vg.append(vg.U8, out, asciiLower(c));
    }
    return [longTypeAlias(vg.strFromBytes(out)), true];
}

export function findName(l: Locale, name: vg.Str, poolSec: number, offsetsSec: number, count: number): number {
    let low: number = 0;
    let high: number = count;
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        const entry: vg.Str = byteString(l, poolSec, u32At(l, offsetsSec, middle));
        if (vg.streq(name, entry)) {
            return middle;
        }
        if ((vg.strcmp3(name, entry) < 0)) {
            high = middle;
        } else {
            low = vg.chk(middle + 1);
        }
    }
    return (0 - 1);
}

export function localesCount(l: Locale): number {
    return vg.div(sectionLen(l, secLocales), 20);
}

export function localeRowAt(l: Locale, index: number): LocaleRow {
    const base: number = vg.chk(5 * index);
    let row: LocaleRow = new LocaleRow();
    row.TypeFirst = u32At(l, secLocales, vg.chk(base + 1));
    row.TypeCount = u32At(l, secLocales, vg.chk(base + 2));
    row.CaseProfile = (u32At(l, secLocales, vg.chk(base + 3)) & 0xff);
    row.DefaultCollation = (u32At(l, secLocales, vg.chk(base + 4)) & 0xffff);
    return row;
}

export function normalizeRequest(name: vg.Str, collationType: vg.Str): [localeRequest, boolean] {
    let collationType_v: vg.Str = collationType;
    let req: localeRequest = new localeRequest();
    const _t1 = normalizeName(name);
    const normalized: vg.Str = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        return [req, false];
    }
    const _t2 = embeddedModifier(name);
    const modifier: vg.Str = _t2[0];
    const hasModifier: boolean = _t2[1];
    if (((indexOfByte(name, 64) >= 0) && (!hasModifier))) {
        return [req, false];
    }
    if ((hasModifier && (collationType_v.length !== 0))) {
        return [req, false];
    }
    if (hasModifier) {
        collationType_v = modifier;
    }
    const _t3 = normalizeType(collationType_v);
    const normalizedType: vg.Str = _t3[0];
    const ok2: boolean = _t3[1];
    if ((!ok2)) {
        return [req, false];
    }
    req.name = normalized;
    req.ctype = normalizedType;
    req.posix = (vg.streq(normalized, _s42) || vg.streq(normalized, _s43));
    return [req, true];
}

export function posixSelect(ctype: vg.Str): [Locale, boolean] {
    if (((ctype.length !== 0) && (!vg.streq(ctype, _s44)))) {
        const invalid: Locale = new Locale();
        return [invalid, false];
    }
    return [LocalePOSIX(), true];
}

export function LocaleLoad(blob: vg.Str): [Locale, boolean] {
    let data: Locale = new Locale();
    if ((!localeLoad(data, blob))) {
        const invalid: Locale = new Locale();
        return [invalid, false];
    }
    return [data, true];
}

export function resolveLocale(data: Locale, req: localeRequest): [Locale, boolean] {
    const invalid: Locale = new Locale();
    let result: Locale = new Locale();
    result.blob = data.blob;
    result.sec = data.sec.map((v) => v.clone());
    result.maxSeq = data.maxSeq;
    result.preimagesDefault = data.preimagesDefault;
    result.preimagesTurkic = data.preimagesTurkic;
    const index: number = findName(result, req.name, secLocaleNames, secLocaleNameOffsets, localesCount(result));
    if ((index < 0)) {
        return [invalid, false];
    }
    const row: LocaleRow = localeRowAt(result, index);
    result.caseProfile = row.CaseProfile;
    result.valid = true;
    if ((req.ctype.length === 0)) {
        result.collationProfile = row.DefaultCollation;
        return [result, true];
    }
    const typeNameCount: number = vg.div(sectionLen(result, secTypeNameOffsets), 4);
    const typeID: number = findName(result, req.ctype, secTypeNames, secTypeNameOffsets, typeNameCount);
    if ((typeID < 0)) {
        return [invalid, false];
    }
    let low: number = row.TypeFirst;
    let high: number = vg.chk(row.TypeFirst + row.TypeCount);
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        const rowTypeID: number = u16At(result, secLocaleTypes, vg.chk(2 * middle));
        if ((rowTypeID === typeID)) {
            result.collationProfile = u16At(result, secLocaleTypes, vg.chk(vg.chk(2 * middle) + 1));
            return [result, true];
        }
        if ((rowTypeID > typeID)) {
            high = middle;
        } else {
            low = vg.chk(middle + 1);
        }
    }
    return [invalid, false];
}

export function LocaleSelect(data: Locale, name: vg.Str, collationType: vg.Str): [Locale, boolean] {
    const _t1 = normalizeRequest(name, collationType);
    const req: localeRequest = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        const invalid: Locale = new Locale();
        return [invalid, false];
    }
    if (req.posix) {
        return posixSelect(req.ctype);
    }
    return resolveLocale(data, req);
}

export function LocaleOpen(blob: vg.Str, name: vg.Str, collationType: vg.Str): [Locale, boolean] {
    const invalid: Locale = new Locale();
    const _t1 = normalizeRequest(name, collationType);
    const req: localeRequest = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        return [invalid, false];
    }
    if (req.posix) {
        return posixSelect(req.ctype);
    }
    const _t2 = LocaleLoad(blob);
    let data: Locale = _t2[0];
    const ok2: boolean = _t2[1];
    if ((!ok2)) {
        return [invalid, false];
    }
    return resolveLocale(data, req);
}

export function classByName(name: vg.Str): [number, boolean] {
    for (let i: number = 0; (i < numClasses); i = vg.chk(i + 1)) {
        if (vg.streq(classNames[vg.ix(i, 12)], name)) {
            return [(i & 0xff), true];
        }
    }
    return [0, false];
}

export function posixMask(r: number): number {
    const upper: boolean = ((r >= 65) && (r <= 90));
    const lower: boolean = ((r >= 97) && (r <= 122));
    const alpha: boolean = (upper || lower);
    const digit: boolean = ((r >= 48) && (r <= 57));
    const alnum: boolean = (alpha || digit);
    const blank: boolean = ((r === 32) || (r === 9));
    const space: boolean = ((((blank || (r === 10)) || (r === 11)) || (r === 12)) || (r === 13));
    const cntrl: boolean = ((r <= 31) || (r === 127));
    const print: boolean = ((r >= 32) && (r <= 126));
    const graph: boolean = ((r >= 33) && (r <= 126));
    const punct: boolean = (graph && (!alnum));
    const xdigit: boolean = ((digit || ((r >= 65) && (r <= 70))) || ((r >= 97) && (r <= 102)));
    let mask: number = 0;
    if (alnum) {
        mask = (mask | ((1 << classAlnum) & 0xffff));
    }
    if (alpha) {
        mask = (mask | ((1 << classAlpha) & 0xffff));
    }
    if (blank) {
        mask = (mask | ((1 << classBlank) & 0xffff));
    }
    if (cntrl) {
        mask = (mask | ((1 << classCntrl) & 0xffff));
    }
    if (digit) {
        mask = (mask | ((1 << classDigit) & 0xffff));
    }
    if (graph) {
        mask = (mask | ((1 << classGraph) & 0xffff));
    }
    if (lower) {
        mask = (mask | ((1 << classLower) & 0xffff));
    }
    if (print) {
        mask = (mask | ((1 << classPrint) & 0xffff));
    }
    if (punct) {
        mask = (mask | ((1 << classPunct) & 0xffff));
    }
    if (space) {
        mask = (mask | ((1 << classSpace) & 0xffff));
    }
    if (upper) {
        mask = (mask | ((1 << classUpper) & 0xffff));
    }
    if (xdigit) {
        mask = (mask | ((1 << classXdigit) & 0xffff));
    }
    return mask;
}

export function localeClassMask(l: Locale, r: number): number {
    if (((!l.valid) || (!validScalar(r)))) {
        return 0;
    }
    if (l.posix) {
        return posixMask(r);
    }
    const block: number = u16At(l, secCtypeStage1, (r >> 8));
    return u16At(l, secCtypeBlocks, vg.chk(vg.chk(block * 256) + (r & 255)));
}

export function findCase(l: Locale, sec: number, r: number): [CasePair, boolean] {
    let pair: CasePair = new CasePair();
    const count: number = vg.div(sectionLen(l, sec), 12);
    let low: number = 0;
    let high: number = count;
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        const cp: number = (u32At(l, sec, vg.chk(3 * middle)) | 0);
        if ((cp === r)) {
            pair.Upper = (u32At(l, sec, vg.chk(vg.chk(3 * middle) + 1)) | 0);
            pair.Lower = (u32At(l, sec, vg.chk(vg.chk(3 * middle) + 2)) | 0);
            return [pair, true];
        }
        if ((cp > r)) {
            high = middle;
        } else {
            low = vg.chk(middle + 1);
        }
    }
    return [pair, false];
}

export function caseConvert(l: Locale, r: number, toUpper: boolean): number {
    if (((!l.valid) || (!validScalar(r)))) {
        return r;
    }
    if (l.posix) {
        if (((toUpper && (r >= 97)) && (r <= 122))) {
            return ((r - 32) | 0);
        }
        if ((((!toUpper) && (r >= 65)) && (r <= 90))) {
            return ((r + 32) | 0);
        }
        return r;
    }
    if ((l.caseProfile === 1)) {
        const _t1 = findCase(l, secCaseTurkic, r);
        const pair: CasePair = _t1[0];
        const ok: boolean = _t1[1];
        if (ok) {
            if (toUpper) {
                return pair.Upper;
            }
            return pair.Lower;
        }
    }
    const _t2 = findCase(l, secCaseDefault, r);
    const pair_2: CasePair = _t2[0];
    const ok_2: boolean = _t2[1];
    if ((!ok_2)) {
        return r;
    }
    if (toUpper) {
        return pair_2.Upper;
    }
    return pair_2.Lower;
}

export function pairSourcesRun(l: Locale, buf: preimageBuf, sec: number, r: number): void {
    let _t3!: Int32Array;
    let _t4!: number;
    const count: number = vg.div(sectionLen(l, sec), 8);
    let low: number = 0;
    let high: number = count;
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        if (((u32At(l, sec, vg.chk(2 * middle)) | 0) < r)) {
            low = vg.chk(middle + 1);
        } else {
            high = middle;
        }
    }
    while ((low < count)) {
        if (((u32At(l, sec, vg.chk(2 * low)) | 0) !== r)) {
            break;
        }
        (_t3 = buf.r, _t4 = vg.ix(buf.n, 16), _t3[_t4] = (u32At(l, sec, vg.chk(vg.chk(2 * low) + 1)) | 0));
        buf.n = vg.chk(buf.n + 1);
        low = vg.chk(low + 1);
    }
}

export function maxPairRun(l: Locale, sec: number): number {
    const count: number = vg.div(sectionLen(l, sec), 8);
    let longest: number = 0;
    let run: number = 0;
    let last: number = 0;
    for (let i: number = 0; (i < count); i = vg.chk(i + 1)) {
        const target: number = u32At(l, sec, vg.chk(2 * i));
        if (((i > 0) && (target === last))) {
            run = vg.chk(run + 1);
        } else {
            run = 1;
            last = target;
        }
        longest = Math.max(longest, run);
    }
    return longest;
}

export function localeMaxPreimages(l: Locale): number {
    if ((l.caseProfile === 1)) {
        return l.preimagesTurkic;
    }
    return l.preimagesDefault;
}

export function localeCasePreimages(l: Locale, buf: preimageBuf, r: number): void {
    buf.n = 0;
    if (((!l.valid) || (!validScalar(r)))) {
        return;
    }
    if (l.posix) {
        if (((r >= 65) && (r <= 90))) {
            buf.r[0] = ((r + 32) | 0);
            buf.n = 1;
        }
        if (((r >= 97) && (r <= 122))) {
            buf.r[0] = ((r - 32) | 0);
            buf.n = 1;
        }
        return;
    }
    let upperSec: number = secInvUpperDefault;
    let lowerSec: number = secInvLowerDefault;
    if ((l.caseProfile === 1)) {
        upperSec = secInvUpperTurkic;
        lowerSec = secInvLowerTurkic;
    }
    pairSourcesRun(l, buf, upperSec, r);
    pairSourcesRun(l, buf, lowerSec, r);
    let w: number = 0;
    for (let i: number = 0; (i < buf.n); i = vg.chk(i + 1)) {
        const candidate: number = buf.r[vg.ix(i, 16)];
        if (((candidate !== r) && (!runesContain(vg.arrSlice(buf.r, 0, w), candidate)))) {
            buf.r[vg.ix(w, 16)] = candidate;
            w = vg.chk(w + 1);
        }
    }
    buf.n = w;
}

export function localeToUpper(l: Locale, r: number): number {
    return caseConvert(l, r, true);
}

export function localeToLower(l: Locale, r: number): number {
    return caseConvert(l, r, false);
}

export function compareSequence(l: Locale, seq: vg.Slice<number>, index: number): number {
    const off: number = u32At(l, secSequences, vg.chk(2 * index));
    const length: number = u32At(l, secSequences, vg.chk(vg.chk(2 * index) + 1));
    const common: number = Math.min(length, seq.len);
    for (let i: number = 0; (i < common); i = vg.chk(i + 1)) {
        const stored: number = (u32At(l, secSeqCodepoints, vg.chk(off + i)) | 0);
        if ((seq.buf[seq.off + vg.ix(i, seq.len)] < stored)) {
            return (0 - 1);
        }
        if ((seq.buf[seq.off + vg.ix(i, seq.len)] > stored)) {
            return 1;
        }
    }
    if ((seq.len < length)) {
        return (0 - 1);
    }
    if ((seq.len > length)) {
        return 1;
    }
    return 0;
}

export function elementID(l: Locale, seq: vg.Slice<number>): [number, boolean] {
    if ((seq.len === 0)) {
        return [0, false];
    }
    for (let i: number = 0; (i < seq.len); i = vg.chk(i + 1)) {
        if ((!validScalar(seq.buf[seq.off + vg.ix(i, seq.len)]))) {
            return [0, false];
        }
    }
    if ((seq.len === 1)) {
        return [(seq.buf[seq.off + vg.ix(0, seq.len)] >>> 0), true];
    }
    const count: number = vg.div(sectionLen(l, secSequences), 8);
    let low: number = 0;
    let high: number = count;
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        const comparison: number = compareSequence(l, seq, middle);
        if ((comparison === 0)) {
            return [(vg.chk(firstSequenceID + middle) >>> 0), true];
        }
        if ((comparison < 0)) {
            high = middle;
        } else {
            low = vg.chk(middle + 1);
        }
    }
    return [0, false];
}

export function u32Contains(l: Locale, sec: number, first: number, count: number, needle: number): boolean {
    let low: number = first;
    let high: number = vg.chk(first + count);
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        const value: number = u32At(l, sec, middle);
        if ((value === needle)) {
            return true;
        }
        if ((value > needle)) {
            high = middle;
        } else {
            low = vg.chk(middle + 1);
        }
    }
    return false;
}

export function collationProfileRow(l: Locale, index: number): CollProfile {
    const base: number = vg.chk(6 * index);
    let row: CollProfile = new CollProfile();
    row.OverrideFirst = u32At(l, secCollationProfiles, base);
    row.OverrideCount = u32At(l, secCollationProfiles, vg.chk(base + 1));
    row.AddFirst = u32At(l, secCollationProfiles, vg.chk(base + 2));
    row.AddCount = u32At(l, secCollationProfiles, vg.chk(base + 3));
    row.RemoveFirst = u32At(l, secCollationProfiles, vg.chk(base + 4));
    row.RemoveCount = u32At(l, secCollationProfiles, vg.chk(base + 5));
    return row;
}

export function isContraction(l: Locale, element: number): boolean {
    if (l.posix) {
        return false;
    }
    const row: CollProfile = collationProfileRow(l, l.collationProfile);
    if (u32Contains(l, secContractionAdds, row.AddFirst, row.AddCount, element)) {
        return true;
    }
    const rootCount: number = vg.div(sectionLen(l, secRootContractions), 4);
    if ((!u32Contains(l, secRootContractions, 0, rootCount, element))) {
        return false;
    }
    return (!u32Contains(l, secContractionRemoves, row.RemoveFirst, row.RemoveCount, element));
}

export function collatingElementID(l: Locale, seq: vg.Slice<number>): [number, boolean] {
    const _t1 = elementID(l, seq);
    const element: number = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        return [0, false];
    }
    if (((seq.len > 1) && (!isContraction(l, element)))) {
        return [0, false];
    }
    return [element, true];
}

export function localeIsCollatingElement(l: Locale, seq: vg.Slice<number>): boolean {
    if ((!l.valid)) {
        return false;
    }
    const _t1 = collatingElementID(l, seq);
    const ok: boolean = _t1[1];
    return ok;
}

export function LocaleCollatingPrefix(l: Locale, seq: vg.Slice<number>): number {
    if (((!l.valid) || (seq.len === 0))) {
        return 0;
    }
    const maximum: number = Math.min(seq.len, l.maxSeq);
    for (let candidate: number = maximum; (candidate >= 2); candidate = vg.chk(candidate - 1)) {
        if (localeIsCollatingElement(l, vg.sub(seq, 0, candidate))) {
            return candidate;
        }
    }
    if (localeIsCollatingElement(l, vg.sub(seq, 0, 1))) {
        return 1;
    }
    return 0;
}

export function findPair(l: Locale, sec: number, first: number, count: number, element: number): [number, boolean] {
    let low: number = first;
    let high: number = vg.chk(first + count);
    while ((low < high)) {
        const middle: number = vg.chk(low + vg.div(vg.chk(high - low), 2));
        const key: number = u32At(l, sec, vg.chk(2 * middle));
        if ((key === element)) {
            return [u32At(l, sec, vg.chk(vg.chk(2 * middle) + 1)), true];
        }
        if ((key > element)) {
            high = middle;
        } else {
            low = vg.chk(middle + 1);
        }
    }
    return [0, false];
}

export function primaryToken(l: Locale, element: number): bigint {
    const row: CollProfile = collationProfileRow(l, l.collationProfile);
    const _t1 = findPair(l, secCollationOverrides, row.OverrideFirst, row.OverrideCount, element);
    let rep: number = _t1[0];
    let ok: boolean = _t1[1];
    if (ok) {
        return (8589934592n | BigInt(rep));
    }
    const rootCount: number = vg.div(sectionLen(l, secRootEquivalences), 8);
    {
        const _t2 = findPair(l, secRootEquivalences, 0, rootCount, element);
        rep = _t2[0];
        ok = _t2[1];
    }
    if (ok) {
        return (4294967296n | BigInt(rep));
    }
    return BigInt(element);
}

export function localePrimaryEqual(l: Locale, left: vg.Slice<number>, right: vg.Slice<number>): boolean {
    if ((!l.valid)) {
        return false;
    }
    const _t1 = collatingElementID(l, left);
    const leftElement: number = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        return false;
    }
    const _t2 = collatingElementID(l, right);
    const rightElement: number = _t2[0];
    const ok2: boolean = _t2[1];
    if ((!ok2)) {
        return false;
    }
    if ((leftElement === rightElement)) {
        return true;
    }
    if (l.posix) {
        return false;
    }
    return (primaryToken(l, leftElement) === primaryToken(l, rightElement));
}

export function localeMinEquivLength(l: Locale, seq: vg.Slice<number>): number {
    if ((!l.valid)) {
        return seq.len;
    }
    const _t1 = collatingElementID(l, seq);
    const element: number = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        return seq.len;
    }
    if ((element < firstSequenceID)) {
        return 1;
    }
    if (l.posix) {
        return seq.len;
    }
    const token: bigint = primaryToken(l, element);
    let best: number = seq.len;
    const row: CollProfile = collationProfileRow(l, l.collationProfile);
    best = minTokenLength(l, secCollationOverrides, row.OverrideFirst, row.OverrideCount, token, best);
    if ((best === 1)) {
        return 1;
    }
    const rootCount: number = vg.div(sectionLen(l, secRootEquivalences), 8);
    return minTokenLength(l, secRootEquivalences, 0, rootCount, token, best);
}

export function minTokenLength(l: Locale, sec: number, first: number, count: number, token: bigint, best: number): number {
    let best_v: number = best;
    for (let i: number = first; (i < vg.chk(first + count)); i = vg.chk(i + 1)) {
        const candidate: number = u32At(l, sec, vg.chk(2 * i));
        if ((primaryToken(l, candidate) !== token)) {
            continue;
        }
        if ((candidate < firstSequenceID)) {
            return 1;
        }
        if ((!isContraction(l, candidate))) {
            continue;
        }
        const index: number = ((candidate - firstSequenceID) >>> 0);
        best_v = Math.min(best_v, u32At(l, secSequences, vg.chk(vg.chk(2 * index) + 1)));
    }
    return best_v;
}

export function localeSupportsRanges(l: Locale): boolean {
    return (l.valid && l.posix);
}

export function LocaleCount(l: Locale): number {
    return localesCount(l);
}

export function LocaleName(l: Locale, index: number): vg.Str {
    if (((index < 0) || (index >= localesCount(l)))) {
        return vg.EMPTY;
    }
    return byteString(l, secLocaleNames, u32At(l, secLocaleNameOffsets, index));
}

export function decodeWindow(s: vg.Str, so: number, eo: number): decoded {
    let d: decoded = new decoded();
    d.runes = vg.make(vg.I32, 0, vg.chk(eo - so));
    d.byteAt = vg.make(vg.INT, 0, vg.chk(vg.chk(eo - so) + 1));
    d.atSubjectStart = (so === 0);
    d.atSubjectEnd = (eo === s.length);
    d.prevIsNewline = ((so > 0) && (s[vg.ix(vg.chk(so - 1), s.length)] === 10));
    d.nextIsNewline = ((eo < s.length) && (s[vg.ix(eo, s.length)] === 10));
    let i: number = so;
    while ((i < eo)) {
        d.byteAt = vg.append(vg.INT, d.byteAt, i);
        const _t2 = decodeRuneAt(s, i);
        const r: number = _t2[0];
        const size: number = _t2[1];
        d.runes = vg.append(vg.I32, d.runes, r);
        i = vg.chk(i + size);
    }
    d.byteAt = vg.append(vg.INT, d.byteAt, eo);
    return d;
}

export function indexOfByte(s: vg.Str, c: number): number {
    for (let i: number = 0; (i < s.length); i = vg.chk(i + 1)) {
        if ((s[vg.ix(i, s.length)] === c)) {
            return i;
        }
    }
    return (0 - 1);
}

export function runesContain(runes: vg.Slice<number>, r: number): boolean {
    for (let i: number = 0; (i < runes.len); i = vg.chk(i + 1)) {
        if ((runes.buf[runes.off + vg.ix(i, runes.len)] === r)) {
            return true;
        }
    }
    return false;
}

export function charMatches(re: Regexp, ni: number, c: number): boolean {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    if ((c < 0)) {
        return false;
    }
    if ((((re.flags & FlagICase) >>> 0) !== 0)) {
        return runesContain((_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).fold, c);
    }
    return (c === (_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).r);
}

export function anyMatches(re: Regexp, c: number): boolean {
    if ((c <= 0)) {
        return false;
    }
    if (((((re.flags & FlagNewline) >>> 0) !== 0) && (c === 10))) {
        return false;
    }
    return true;
}

export function atBOL(re: Regexp, d: decoded, i: number, eflags: number): boolean {
    let _t1!: vg.Slice<number>;
    if ((i === 0)) {
        if (d.atSubjectStart) {
            return (((eflags & ExecNotBOL) >>> 0) === 0);
        }
        return ((((re.flags & FlagNewline) >>> 0) !== 0) && d.prevIsNewline);
    }
    return ((((re.flags & FlagNewline) >>> 0) !== 0) && ((_t1 = d.runes, _t1.buf[_t1.off + vg.ix(vg.chk(i - 1), _t1.len)]) === 10));
}

export function atEOL(re: Regexp, d: decoded, i: number, eflags: number): boolean {
    let _t1!: vg.Slice<number>;
    if ((i === d.runes.len)) {
        if (d.atSubjectEnd) {
            return (((eflags & ExecNotEOL) >>> 0) === 0);
        }
        return ((((re.flags & FlagNewline) >>> 0) !== 0) && d.nextIsNewline);
    }
    return ((((re.flags & FlagNewline) >>> 0) !== 0) && ((_t1 = d.runes, _t1.buf[_t1.off + vg.ix(i, _t1.len)]) === 10));
}

export function fillMatches(re: Regexp, d: decoded, caps: vg.Slice<Match>, pmatch: vg.Slice<Match>): void {
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    if (((((re.flags & FlagNoSub) >>> 0) !== 0) || (pmatch.len === 0))) {
        return;
    }
    for (let idx: number = 0; (idx < pmatch.len); idx = vg.chk(idx + 1)) {
        if (((idx < caps.len) && (caps.buf[caps.off + vg.ix(idx, caps.len)].So >= 0))) {
            pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].So = (_t2 = d.byteAt, _t2.buf[_t2.off + vg.ix(caps.buf[caps.off + vg.ix(idx, caps.len)].So, _t2.len)]);
            pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].Eo = (_t3 = d.byteAt, _t3.buf[_t3.off + vg.ix(caps.buf[caps.off + vg.ix(idx, caps.len)].Eo, _t3.len)]);
        } else {
            pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].So = (0 - 1);
            pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].Eo = (0 - 1);
        }
    }
}

export function onePassAnalyze(nodes: vg.Slice<node>, brs: vg.Slice<bracketSet>, ni: number): boolean {
    let _t1!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    let _t4!: vg.Slice<number>;
    let _t6!: vg.Slice<number>;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opChar:
        case opAny:
        case opBOL:
        case opEOL:
        {
            return true;
            break;
        }
        case opBracket:
        {
            return (!bracketHasMultiMembers(brs, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].br));
            break;
        }
        case opGroup:
        {
            return onePassAnalyze(nodes, brs, (_t1 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t1.buf[_t1.off + vg.ix(0, _t1.len)]));
            break;
        }
        case opConcat:
        {
            let variable: number = 0;
            for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                const child: number = (_t3 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t3.buf[_t3.off + vg.ix(i, _t3.len)]);
                if ((!onePassAnalyze(nodes, brs, child))) {
                    return false;
                }
                if ((!fixedLength(nodes, child))) {
                    variable = vg.chk(variable + 1);
                }
            }
            return (variable <= 1);
            break;
        }
        case opRepeat:
        {
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max === 0)) {
                return true;
            }
            const child_2: number = (_t4 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t4.buf[_t4.off + vg.ix(0, _t4.len)]);
            return ((fixedLength(nodes, child_2) && (nodes.buf[nodes.off + vg.ix(child_2, nodes.len)].minL > 0)) && onePassAnalyze(nodes, brs, child_2));
            break;
        }
        case opAlt:
        {
            for (let i_2: number = 0; (i_2 < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i_2 = vg.chk(i_2 + 1)) {
                if ((!onePassAnalyze(nodes, brs, (_t6 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t6.buf[_t6.off + vg.ix(i_2, _t6.len)])))) {
                    return false;
                }
            }
            if (disjointLengths(nodes, ni)) {
                return true;
            }
            return disjointFirsts(nodes, ni);
            break;
        }
    }
    return false;
}

export function fixedLength(nodes: vg.Slice<node>, ni: number): boolean {
    return ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL === nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL) && (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL < lenInf));
}

export function disjointLengths(nodes: vg.Slice<node>, ni: number): boolean {
    let _t2!: vg.Slice<number>;
    let _t4!: vg.Slice<number>;
    const count: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len;
    for (let i: number = 0; (i < count); i = vg.chk(i + 1)) {
        const a: number = (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]);
        for (let k: number = vg.chk(i + 1); (k < count); k = vg.chk(k + 1)) {
            const b: number = (_t4 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t4.buf[_t4.off + vg.ix(k, _t4.len)]);
            if (((nodes.buf[nodes.off + vg.ix(a, nodes.len)].minL <= nodes.buf[nodes.off + vg.ix(b, nodes.len)].maxL) && (nodes.buf[nodes.off + vg.ix(b, nodes.len)].minL <= nodes.buf[nodes.off + vg.ix(a, nodes.len)].maxL))) {
                return false;
            }
        }
    }
    return true;
}

export function firstSet(nodes: vg.Slice<node>, ni: number): [vg.Slice<number>, boolean] {
    let _t1!: vg.Slice<number>;
    let _t2!: vg.Slice<number>;
    let _t5!: vg.Slice<number>;
    let _t8!: vg.Slice<number>;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opChar:
        {
            let out: vg.Slice<number> = vg.make(vg.I32, 0, vg.chk(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].fold.len + 1));
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].fold.len > 0)) {
                out = vg.appendSlice(vg.I32, out, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].fold);
                return [out, true];
            }
            out = vg.append(vg.I32, out, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].r);
            return [out, true];
            break;
        }
        case opBOL:
        case opEOL:
        {
            return [vg.NIL, true];
            break;
        }
        case opGroup:
        {
            return firstSet(nodes, (_t1 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t1.buf[_t1.off + vg.ix(0, _t1.len)]));
            break;
        }
        case opRepeat:
        {
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max === 0)) {
                return [vg.NIL, true];
            }
            return firstSet(nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(0, _t2.len)]));
            break;
        }
        case opAlt:
        {
            let out_2: vg.Slice<number> = vg.NIL;
            for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                const _t4 = firstSet(nodes, (_t5 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t5.buf[_t5.off + vg.ix(i, _t5.len)]));
                const sub: vg.Slice<number> = _t4[0];
                const ok: boolean = _t4[1];
                if ((!ok)) {
                    return [vg.NIL, false];
                }
                for (let k: number = 0; (k < sub.len); k = vg.chk(k + 1)) {
                    out_2 = appendUnique(out_2, sub.buf[sub.off + vg.ix(k, sub.len)]);
                }
            }
            return [out_2, true];
            break;
        }
        case opConcat:
        {
            let out_3: vg.Slice<number> = vg.NIL;
            for (let i_2: number = 0; (i_2 < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i_2 = vg.chk(i_2 + 1)) {
                const child: number = (_t8 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t8.buf[_t8.off + vg.ix(i_2, _t8.len)]);
                const _t9 = firstSet(nodes, child);
                const sub_2: vg.Slice<number> = _t9[0];
                const ok_2: boolean = _t9[1];
                if ((!ok_2)) {
                    return [vg.NIL, false];
                }
                for (let k_2: number = 0; (k_2 < sub_2.len); k_2 = vg.chk(k_2 + 1)) {
                    out_3 = appendUnique(out_3, sub_2.buf[sub_2.off + vg.ix(k_2, sub_2.len)]);
                }
                if ((nodes.buf[nodes.off + vg.ix(child, nodes.len)].minL > 0)) {
                    break;
                }
            }
            return [out_3, true];
            break;
        }
    }
    return [vg.NIL, false];
}

export function disjointFirsts(nodes: vg.Slice<node>, ni: number): boolean {
    let _t2!: vg.Slice<number>;
    let _t7!: vg.Slice<number>;
    const count: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len;
    const firsts: vg.Slice<vg.Slice<number>> = vg.make(vg.SLICE, count);
    let nullable: number = 0;
    for (let i: number = 0; (i < count); i = vg.chk(i + 1)) {
        const branch: number = (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]);
        const _t3 = firstSet(nodes, branch);
        const set_: vg.Slice<number> = _t3[0];
        const ok: boolean = _t3[1];
        if ((!ok)) {
            return false;
        }
        if ((nodes.buf[nodes.off + vg.ix(branch, nodes.len)].minL === 0)) {
            nullable = vg.chk(nullable + 1);
        }
        firsts.buf[firsts.off + vg.ix(i, firsts.len)] = set_;
    }
    if ((nullable > 1)) {
        return false;
    }
    for (let i_2: number = 0; (i_2 < count); i_2 = vg.chk(i_2 + 1)) {
        for (let k: number = vg.chk(i_2 + 1); (k < count); k = vg.chk(k + 1)) {
            for (let m: number = 0; (m < firsts.buf[firsts.off + vg.ix(i_2, firsts.len)].len); m = vg.chk(m + 1)) {
                if (runesContain(firsts.buf[firsts.off + vg.ix(k, firsts.len)], (_t7 = firsts.buf[firsts.off + vg.ix(i_2, firsts.len)], _t7.buf[_t7.off + vg.ix(m, _t7.len)]))) {
                    return false;
                }
            }
        }
    }
    nodes.buf[nodes.off + vg.ix(ni, nodes.len)].firsts = firsts;
    return true;
}

export function onePassCaps(re: Regexp, d: decoded, ni: number, i: number, j: number, eflags: number, caps: vg.Slice<Match>): boolean {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t4!: vg.Slice<number>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<node>;
    let _t7!: vg.Slice<number>;
    let _t8!: vg.Slice<node>;
    let _t9!: vg.Slice<vg.Slice<number>>;
    let _t11!: vg.Slice<number>;
    let _t12!: vg.Slice<vg.Slice<number>>;
    let _t13!: vg.Slice<number>;
    let _t14!: vg.Slice<node>;
    let _t15!: vg.Slice<node>;
    let _t17!: vg.Slice<node>;
    let _t18!: vg.Slice<number>;
    let _t19!: vg.Slice<node>;
    let _t20!: vg.Slice<node>;
    let _t22!: vg.Slice<number>;
    let _t23!: vg.Slice<node>;
    let _t24!: vg.Slice<node>;
    let _t25!: vg.Slice<number>;
    let _t26!: vg.Slice<node>;
    let _t27!: vg.Slice<node>;
    let _t28!: vg.Slice<node>;
    let _t29!: vg.Slice<node>;
    let _t30!: vg.Slice<node>;
    let _t32!: vg.Slice<node>;
    let _t34!: vg.Slice<number>;
    let _t35!: vg.Slice<node>;
    let _t36!: vg.Slice<node>;
    let _t37!: vg.Slice<node>;
    let _t38!: vg.Slice<node>;
    let _t39!: vg.Slice<node>;
    let _t41!: vg.Slice<node>;
    let _t42!: vg.Slice<number>;
    let _t43!: vg.Slice<node>;
    let _t44!: vg.Slice<node>;
    let _t46!: vg.Slice<vg.Slice<number>>;
    let _t47!: vg.Slice<node>;
    let _t48!: vg.Slice<number>;
    let _t49!: vg.Slice<number>;
    let _t50!: vg.Slice<node>;
    const span: number = vg.chk(j - i);
    if (((span < (_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).minL) || (span > (_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).maxL))) {
        return false;
    }
    switch ((_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).op) {
        case opChar:
        {
            return charMatches(re, ni, (_t4 = d.runes, _t4.buf[_t4.off + vg.ix(i, _t4.len)]));
            break;
        }
        case opAny:
        {
            return anyMatches(re, (_t5 = d.runes, _t5.buf[_t5.off + vg.ix(i, _t5.len)]));
            break;
        }
        case opBracket:
        {
            return bracketMatchesOne(re.brackets, (_t6 = re.nodes, _t6.buf[_t6.off + vg.ix(ni, _t6.len)]).br, re.loc, (_t7 = d.runes, _t7.buf[_t7.off + vg.ix(i, _t7.len)]));
            break;
        }
        case opBOL:
        {
            return atBOL(re, d, i, eflags);
            break;
        }
        case opEOL:
        {
            return atEOL(re, d, i, eflags);
            break;
        }
        case opGroup:
        {
            const gi: number = (_t8 = re.nodes, _t8.buf[_t8.off + vg.ix(ni, _t8.len)]).index;
            for (let k: number = 0; (k < (_t9 = re.nested, _t9.buf[_t9.off + vg.ix(gi, _t9.len)]).len); k = vg.chk(k + 1)) {
                setMatch(caps, (_t11 = (_t12 = re.nested, _t12.buf[_t12.off + vg.ix(gi, _t12.len)]), _t11.buf[_t11.off + vg.ix(k, _t11.len)]), (0 - 1), (0 - 1));
            }
            setMatch(caps, gi, i, j);
            return onePassCaps(re, d, (_t13 = (_t14 = re.nodes, _t14.buf[_t14.off + vg.ix(ni, _t14.len)]).ch, _t13.buf[_t13.off + vg.ix(0, _t13.len)]), i, j, eflags, caps);
            break;
        }
        case opConcat:
        {
            let rest: number = span;
            for (let k_2: number = 0; (k_2 < (_t15 = re.nodes, _t15.buf[_t15.off + vg.ix(ni, _t15.len)]).ch.len); k_2 = vg.chk(k_2 + 1)) {
                rest = vg.chk(rest - (_t17 = re.nodes, _t17.buf[_t17.off + vg.ix((_t18 = (_t19 = re.nodes, _t19.buf[_t19.off + vg.ix(ni, _t19.len)]).ch, _t18.buf[_t18.off + vg.ix(k_2, _t18.len)]), _t17.len)]).minL);
            }
            let at: number = i;
            for (let k_3: number = 0; (k_3 < (_t20 = re.nodes, _t20.buf[_t20.off + vg.ix(ni, _t20.len)]).ch.len); k_3 = vg.chk(k_3 + 1)) {
                const child: number = (_t22 = (_t23 = re.nodes, _t23.buf[_t23.off + vg.ix(ni, _t23.len)]).ch, _t22.buf[_t22.off + vg.ix(k_3, _t22.len)]);
                let length: number = (_t24 = re.nodes, _t24.buf[_t24.off + vg.ix(child, _t24.len)]).minL;
                if ((!fixedLength(re.nodes, child))) {
                    length = vg.chk(length + rest);
                }
                if (((length < 0) || (!onePassCaps(re, d, child, at, vg.chk(at + length), eflags, caps)))) {
                    return false;
                }
                at = vg.chk(at + length);
            }
            return (at === j);
            break;
        }
        case opRepeat:
        {
            if ((span === 0)) {
                return true;
            }
            const child_2: number = (_t25 = (_t26 = re.nodes, _t26.buf[_t26.off + vg.ix(ni, _t26.len)]).ch, _t25.buf[_t25.off + vg.ix(0, _t25.len)]);
            const size: number = (_t27 = re.nodes, _t27.buf[_t27.off + vg.ix(child_2, _t27.len)]).minL;
            const count: number = vg.div(span, size);
            if ((((vg.rem(span, size) !== 0) || (count < (_t28 = re.nodes, _t28.buf[_t28.off + vg.ix(ni, _t28.len)]).min)) || (((_t29 = re.nodes, _t29.buf[_t29.off + vg.ix(ni, _t29.len)]).max !== infinite) && (count > (_t30 = re.nodes, _t30.buf[_t30.off + vg.ix(ni, _t30.len)]).max)))) {
                return false;
            }
            let at_2: number = i;
            for (let k_4: number = 0; (k_4 < count); k_4 = vg.chk(k_4 + 1)) {
                if ((!onePassCaps(re, d, child_2, at_2, vg.chk(at_2 + size), eflags, caps))) {
                    return false;
                }
                at_2 = vg.chk(at_2 + size);
            }
            return true;
            break;
        }
        case opAlt:
        {
            let chosen: number = (0 - 1);
            for (let idx: number = 0; (idx < (_t32 = re.nodes, _t32.buf[_t32.off + vg.ix(ni, _t32.len)]).ch.len); idx = vg.chk(idx + 1)) {
                const branch: number = (_t34 = (_t35 = re.nodes, _t35.buf[_t35.off + vg.ix(ni, _t35.len)]).ch, _t34.buf[_t34.off + vg.ix(idx, _t34.len)]);
                if (((span < (_t36 = re.nodes, _t36.buf[_t36.off + vg.ix(branch, _t36.len)]).minL) || (span > (_t37 = re.nodes, _t37.buf[_t37.off + vg.ix(branch, _t37.len)]).maxL))) {
                    continue;
                }
                if ((chosen >= 0)) {
                    chosen = (0 - 1);
                    break;
                }
                chosen = idx;
            }
            if (((chosen < 0) && ((_t38 = re.nodes, _t38.buf[_t38.off + vg.ix(ni, _t38.len)]).firsts.buf !== null))) {
                if ((span === 0)) {
                    for (let idx_2: number = 0; (idx_2 < (_t39 = re.nodes, _t39.buf[_t39.off + vg.ix(ni, _t39.len)]).ch.len); idx_2 = vg.chk(idx_2 + 1)) {
                        if (((_t41 = re.nodes, _t41.buf[_t41.off + vg.ix((_t42 = (_t43 = re.nodes, _t43.buf[_t43.off + vg.ix(ni, _t43.len)]).ch, _t42.buf[_t42.off + vg.ix(idx_2, _t42.len)]), _t41.len)]).minL === 0)) {
                            chosen = idx_2;
                            break;
                        }
                    }
                } else {
                    for (let idx_3: number = 0; (idx_3 < (_t44 = re.nodes, _t44.buf[_t44.off + vg.ix(ni, _t44.len)]).ch.len); idx_3 = vg.chk(idx_3 + 1)) {
                        if (runesContain((_t46 = (_t47 = re.nodes, _t47.buf[_t47.off + vg.ix(ni, _t47.len)]).firsts, _t46.buf[_t46.off + vg.ix(idx_3, _t46.len)]), (_t48 = d.runes, _t48.buf[_t48.off + vg.ix(i, _t48.len)]))) {
                            chosen = idx_3;
                            break;
                        }
                    }
                }
            }
            if ((chosen < 0)) {
                return false;
            }
            return onePassCaps(re, d, (_t49 = (_t50 = re.nodes, _t50.buf[_t50.off + vg.ix(ni, _t50.len)]).ch, _t49.buf[_t49.off + vg.ix(chosen, _t49.len)]), i, j, eflags, caps);
            break;
        }
    }
    return false;
}

export function buildScanFilter(pr: program, newlineMode: boolean): number {
    let _t2!: vg.Slice<instr>;
    let _t3!: vg.Slice<instr>;
    let _t4!: vg.Slice<instr>;
    let _t5!: vg.Slice<instr>;
    let _t6!: boolean[];
    let _t7!: number;
    let _t8!: vg.Slice<instr>;
    let _t9!: vg.Slice<instr>;
    let _t10!: vg.Slice<vg.Slice<number>>;
    let _t12!: boolean[];
    let _t13!: number;
    let _t14!: vg.Slice<number>;
    let _t15!: vg.Slice<vg.Slice<number>>;
    let ok: boolean = true;
    let matchReachable: boolean = false;
    const seen: vg.Slice<boolean> = vg.make(vg.BOOL, pr.ins.len);
    let stack: vg.Slice<number> = vg.make(vg.U32, 0, 16);
    stack = vg.append(vg.U32, stack, pr.start);
    while ((stack.len > 0)) {
        const pc: number = stack.buf[stack.off + vg.ix(vg.chk(stack.len - 1), stack.len)];
        stack = vg.head(stack, vg.chk(stack.len - 1));
        if (seen.buf[seen.off + vg.ix(pc, seen.len)]) {
            continue;
        }
        seen.buf[seen.off + vg.ix(pc, seen.len)] = true;
        switch ((_t2 = pr.ins, _t2.buf[_t2.off + vg.ix(pc, _t2.len)]).op) {
            case iSplit:
            {
                stack = vg.append(vg.U32, vg.append(vg.U32, stack, (_t3 = pr.ins, _t3.buf[_t3.off + vg.ix(pc, _t3.len)]).next), (_t4 = pr.ins, _t4.buf[_t4.off + vg.ix(pc, _t4.len)]).alt);
                break;
            }
            case iJmp:
            {
                stack = vg.append(vg.U32, stack, (_t5 = pr.ins, _t5.buf[_t5.off + vg.ix(pc, _t5.len)]).next);
                break;
            }
            case iBOL:
            {
                break;
            }
            case iEOL:
            {
                break;
            }
            case iMatch:
            {
                matchReachable = true;
                break;
            }
            case iFail:
            {
                break;
            }
            case iRune:
            {
                (_t6 = pr.scan.stop, _t7 = vg.ix(utf8LeadByte(((_t8 = pr.ins, _t8.buf[_t8.off + vg.ix(pc, _t8.len)]).arg | 0)), 256), _t6[_t7] = true);
                break;
            }
            case iRuneFold:
            {
                const set_: number = (_t9 = pr.ins, _t9.buf[_t9.off + vg.ix(pc, _t9.len)]).arg;
                for (let i: number = 0; (i < (_t10 = pr.foldSets, _t10.buf[_t10.off + vg.ix(set_, _t10.len)]).len); i = vg.chk(i + 1)) {
                    (_t12 = pr.scan.stop, _t13 = vg.ix(utf8LeadByte((_t14 = (_t15 = pr.foldSets, _t15.buf[_t15.off + vg.ix(set_, _t15.len)]), _t14.buf[_t14.off + vg.ix(i, _t14.len)])), 256), _t12[_t13] = true);
                }
                break;
            }
            default:
            {
                ok = false;
                break;
            }
        }
    }
    if (((matchReachable || (!ok)) || pr.multi)) {
        const none: scanFilter = new scanFilter();
        pr.scan = none.clone();
        return 0;
    }
    if (newlineMode) {
        pr.scan.stop[10] = true;
    }
    pr.scan.enabled = true;
    let count: number = 0;
    for (let b: number = 0; (b < 256); b = vg.chk(b + 1)) {
        if (pr.scan.stop[vg.ix(b, 256)]) {
            count = vg.chk(count + 1);
            pr.scan.b = (b & 0xff);
        }
    }
    pr.scan.single = (count === 1);
    return count;
}

export function instrEstimate(nodes: vg.Slice<node>, ni: number): number {
    let _t1!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<number>;
    const estimateCap: number = vg.chk(1048576 * 4);
    let total: number = 0;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opGroup:
        {
            return instrEstimate(nodes, (_t1 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t1.buf[_t1.off + vg.ix(0, _t1.len)]));
            break;
        }
        case opConcat:
        {
            for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                total = vg.chk(total + instrEstimate(nodes, (_t3 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t3.buf[_t3.off + vg.ix(i, _t3.len)])));
                if ((total > estimateCap)) {
                    return estimateCap;
                }
            }
            return total;
            break;
        }
        case opAlt:
        {
            total = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len;
            for (let i_2: number = 0; (i_2 < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i_2 = vg.chk(i_2 + 1)) {
                total = vg.chk(total + instrEstimate(nodes, (_t5 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t5.buf[_t5.off + vg.ix(i_2, _t5.len)])));
                if ((total > estimateCap)) {
                    return estimateCap;
                }
            }
            return total;
            break;
        }
        case opRepeat:
        {
            let copies: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max;
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max === infinite)) {
                copies = vg.chk(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min + 1);
            }
            if ((copies === 0)) {
                return 1;
            }
            const child: number = instrEstimate(nodes, (_t6 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t6.buf[_t6.off + vg.ix(0, _t6.len)]));
            total = vg.chk(vg.chk(vg.chk(copies * child) + copies) + 2);
            if (((total > estimateCap) || (total < 0))) {
                return estimateCap;
            }
            return total;
            break;
        }
    }
    return 1;
}

export function compileProgram(b: progBuilder, nodes: vg.Slice<node>, root: number, multi: boolean, newlineMode: boolean): void {
    b.failMin = failMinNone;
    const body: frag = emit(b, nodes, root, 0n, vg.NIL);
    if ((b.errCode !== ErrNone)) {
        return;
    }
    let m: instr = new instr();
    m.op = iMatch;
    const match: number = addInstr(b, m);
    if (((b.errCode !== ErrNone) || b.tooBig)) {
        return;
    }
    patch(b, body.out, match);
    b.prog.start = body.start;
    b.prog.multi = multi;
    b.prog.failMin = b.failMin;
    const stops: number = buildScanFilter(b.prog, newlineMode);
    b.prog.depth = (0 - 1);
    if ((((!newlineMode) && b.prog.scan.enabled) && (stops === 0))) {
        b.prog.depth = progDepth(b.prog);
    }
}

export function consumingOp(op: number): boolean {
    return (op <= iBracket);
}

export function progEdge(pr: program, pc: number, i: number): [number, boolean] {
    let _t1!: vg.Slice<instr>;
    let _t2!: vg.Slice<instr>;
    let _t3!: vg.Slice<instr>;
    const op: number = (_t1 = pr.ins, _t1.buf[_t1.off + vg.ix(pc, _t1.len)]).op;
    if (((op === iMatch) || (op === iFail))) {
        return [0, false];
    }
    if ((i === 0)) {
        return [(_t2 = pr.ins, _t2.buf[_t2.off + vg.ix(pc, _t2.len)]).next, true];
    }
    if (((i === 1) && (op === iSplit))) {
        return [(_t3 = pr.ins, _t3.buf[_t3.off + vg.ix(pc, _t3.len)]).alt, true];
    }
    return [0, false];
}

export function progDepth(pr: program): number {
    let _t3!: vg.Slice<instr>;
    const n: number = pr.ins.len;
    const depth: vg.Slice<number> = vg.make(vg.I32, n);
    for (let round: number = 0; (round < 2); round = vg.chk(round + 1)) {
        let changed: boolean = false;
        for (let pc: number = 0; (pc < n); pc = vg.chk(pc + 1)) {
            let weight: number = 0;
            if (consumingOp((_t3 = pr.ins, _t3.buf[_t3.off + vg.ix(pc, _t3.len)]).op)) {
                weight = 1;
            }
            for (let e: number = 0; (e < 2); e = vg.chk(e + 1)) {
                const _t5 = progEdge(pr, (pc >>> 0), e);
                const target: number = _t5[0];
                const has: boolean = _t5[1];
                if ((has && (depth.buf[depth.off + vg.ix(target, depth.len)] < ((depth.buf[depth.off + vg.ix(pc, depth.len)] + weight) | 0)))) {
                    depth.buf[depth.off + vg.ix(target, depth.len)] = ((depth.buf[depth.off + vg.ix(pc, depth.len)] + weight) | 0);
                    changed = true;
                }
            }
        }
        if ((!changed)) {
            let best: number = 0;
            for (let pc_2: number = 0; (pc_2 < n); pc_2 = vg.chk(pc_2 + 1)) {
                best = Math.max(best, depth.buf[depth.off + vg.ix(pc_2, depth.len)]);
            }
            return best;
        }
    }
    return (0 - 1);
}

export function addInstr(b: progBuilder, ins: instr): number {
    if (b.tooBig) {
        return 0;
    }
    if ((b.prog.ins.len >= maxProgram)) {
        b.tooBig = true;
        return 0;
    }
    b.prog.ins = vg.append(instr.elem, b.prog.ins, ins.clone());
    return (vg.chk(b.prog.ins.len - 1) >>> 0);
}

export function patch(b: progBuilder, slots: vg.Slice<patchSlot>, target: number): void {
    let _t2!: vg.Slice<instr>;
    let _t3!: vg.Slice<instr>;
    if (b.tooBig) {
        return;
    }
    for (let i: number = 0; (i < slots.len); i = vg.chk(i + 1)) {
        if (slots.buf[slots.off + vg.ix(i, slots.len)].alt) {
            (_t2 = b.prog.ins, _t2.buf[_t2.off + vg.ix(slots.buf[slots.off + vg.ix(i, slots.len)].idx, _t2.len)]).alt = target;
        } else {
            (_t3 = b.prog.ins, _t3.buf[_t3.off + vg.ix(slots.buf[slots.off + vg.ix(i, slots.len)].idx, _t3.len)]).next = target;
        }
    }
}

export function singleOut(idx: number, alt: boolean): vg.Slice<patchSlot> {
    let slot: patchSlot = new patchSlot();
    slot.idx = idx;
    slot.alt = alt;
    const out: vg.Slice<patchSlot> = vg.make(patchSlot.elem, 0, 1);
    return vg.append(patchSlot.elem, out, slot.clone());
}

export function epsilonFrag(b: progBuilder): frag {
    let j: instr = new instr();
    j.op = iJmp;
    const idx: number = addInstr(b, j);
    let f: frag = new frag();
    f.start = idx;
    f.out = singleOut(idx, false);
    return f;
}

export function copyExtra(extra: vg.Slice<number>): vg.Slice<number> {
    if ((extra.len === 0)) {
        return vg.NIL;
    }
    const dup: vg.Slice<number> = vg.make(vg.U32, extra.len);
    vg.copy(vg.U32, dup, extra);
    return dup;
}

export function consumeFrag(b: progBuilder, op: number, arg: number, mask: bigint, extra: vg.Slice<number>): frag {
    let ins: instr = new instr();
    ins.op = op;
    ins.arg = arg;
    ins.mask = mask;
    ins.extra = copyExtra(extra);
    const idx: number = addInstr(b, ins);
    let f: frag = new frag();
    f.start = idx;
    f.out = singleOut(idx, false);
    return f;
}

export function emit(b: progBuilder, nodes: vg.Slice<node>, ni: number, mask: bigint, extra: vg.Slice<number>): frag {
    let _t1!: vg.Slice<number>;
    let _t2!: vg.Slice<number>;
    let _t4!: vg.Slice<number>;
    const none: frag = new frag();
    if (((b.errCode !== ErrNone) || b.tooBig)) {
        return none;
    }
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opChar:
        {
            if (b.icase) {
                const arg: number = (b.prog.foldSets.len >>> 0);
                const foldCopy: vg.Slice<number> = vg.make(vg.I32, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].fold.len);
                vg.copy(vg.I32, foldCopy, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].fold);
                b.prog.foldSets = vg.append(vg.SLICE, b.prog.foldSets, foldCopy);
                return consumeFrag(b, iRuneFold, arg, mask, extra);
            }
            return consumeFrag(b, iRune, (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].r >>> 0), mask, extra);
            break;
        }
        case opAny:
        {
            return consumeFrag(b, iAny, 0, mask, extra);
            break;
        }
        case opBracket:
        {
            return consumeFrag(b, iBracket, (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].br >>> 0), mask, extra);
            break;
        }
        case opBOL:
        {
            return consumeFrag(b, iBOL, 0, 0n, vg.NIL);
            break;
        }
        case opEOL:
        {
            return consumeFrag(b, iEOL, 0, 0n, vg.NIL);
            break;
        }
        case opGroup:
        {
            return emit(b, nodes, (_t1 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t1.buf[_t1.off + vg.ix(0, _t1.len)]), mask, extra);
            break;
        }
        case opConcat:
        {
            let result: frag = emit(b, nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(0, _t2.len)]), mask, extra);
            for (let i: number = 1; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                const next: frag = emit(b, nodes, (_t4 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t4.buf[_t4.off + vg.ix(i, _t4.len)]), mask, extra);
                if ((b.errCode !== ErrNone)) {
                    return none;
                }
                patch(b, result.out, next.start);
                result.out = next.out;
            }
            return result;
            break;
        }
        case opAlt:
        {
            return emitAlt(b, nodes, ni, mask, extra);
            break;
        }
        case opRepeat:
        {
            return emitRepeat(b, nodes, ni, mask, extra);
            break;
        }
    }
    b.errCode = ErrBadPat;
    return none;
}

export function emitAlt(b: progBuilder, nodes: vg.Slice<node>, ni: number, mask: bigint, extra: vg.Slice<number>): frag {
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<instr>;
    let _t4!: vg.Slice<instr>;
    let _t5!: vg.Slice<instr>;
    const none: frag = new frag();
    let result: frag = new frag();
    let splits: vg.Slice<number> = vg.make(vg.U32, 0, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len);
    const count: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len;
    for (let i: number = 0; (i < count); i = vg.chk(i + 1)) {
        if ((i < vg.chk(count - 1))) {
            let s: instr = new instr();
            s.op = iSplit;
            splits = vg.append(vg.U32, splits, addInstr(b, s));
        }
        const sub: frag = emit(b, nodes, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]), mask, extra);
        if (((b.errCode !== ErrNone) || b.tooBig)) {
            return none;
        }
        if ((i < vg.chk(count - 1))) {
            (_t3 = b.prog.ins, _t3.buf[_t3.off + vg.ix(splits.buf[splits.off + vg.ix(i, splits.len)], _t3.len)]).next = sub.start;
        } else {
            (_t4 = b.prog.ins, _t4.buf[_t4.off + vg.ix(splits.buf[splits.off + vg.ix(vg.chk(i - 1), splits.len)], _t4.len)]).alt = sub.start;
        }
        if (((i > 0) && (i < vg.chk(count - 1)))) {
            (_t5 = b.prog.ins, _t5.buf[_t5.off + vg.ix(splits.buf[splits.off + vg.ix(vg.chk(i - 1), splits.len)], _t5.len)]).alt = splits.buf[splits.off + vg.ix(i, splits.len)];
        }
        result.out = vg.appendSlice(patchSlot.elem, result.out, sub.out);
    }
    result.start = splits.buf[splits.off + vg.ix(0, splits.len)];
    return result;
}

export function fragAppend(b: progBuilder, result: frag, have: boolean, f: frag): boolean {
    if ((!have)) {
        result.start = f.start;
        result.out = f.out;
        return true;
    }
    patch(b, result.out, f.start);
    result.out = f.out;
    return true;
}

export function emitRepeat(b: progBuilder, nodes: vg.Slice<node>, ni: number, mask: bigint, extra: vg.Slice<number>): frag {
    let _t1!: vg.Slice<number>;
    let _t4!: vg.Slice<instr>;
    let mask_v: bigint = mask;
    let extra_v: vg.Slice<number> = extra;
    const none: frag = new frag();
    if ((instrEstimate(nodes, ni) > vg.chk(maxProgram - b.prog.ins.len))) {
        let fi: instr = new instr();
        fi.op = iFail;
        const idx: number = addInstr(b, fi);
        b.failMin = Math.min(b.failMin, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL);
        let f: frag = new frag();
        f.start = idx;
        return f;
    }
    if (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minimal) {
        const slot: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].index;
        if ((slot < maskWidth)) {
            mask_v = (mask_v | BigInt.asUintN(64, 1n << vg.shiftCount(slot)));
        } else {
            const grown: vg.Slice<number> = vg.make(vg.U32, vg.chk(extra_v.len + 1));
            vg.copy(vg.U32, grown, extra_v);
            grown.buf[grown.off + vg.ix(extra_v.len, grown.len)] = (slot >>> 0);
            extra_v = grown;
        }
    }
    const child: number = (_t1 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t1.buf[_t1.off + vg.ix(0, _t1.len)]);
    const lo: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min;
    const hi: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max;
    if (((lo === 0) && (hi === 0))) {
        return epsilonFrag(b);
    }
    let result: frag = new frag();
    let haveResult: boolean = false;
    for (let i: number = 0; (i < lo); i = vg.chk(i + 1)) {
        if (((b.errCode !== ErrNone) || b.tooBig)) {
            return none;
        }
        if (((i === vg.chk(lo - 1)) && (hi === infinite))) {
            fragAppend(b, result, haveResult, emitPlus(b, nodes, child, mask_v, extra_v));
            return result;
        }
        haveResult = fragAppend(b, result, haveResult, emit(b, nodes, child, mask_v, extra_v));
    }
    if ((hi === infinite)) {
        fragAppend(b, result, haveResult, emitStar(b, nodes, child, mask_v, extra_v));
        return result;
    }
    let skips: vg.Slice<patchSlot> = vg.make(patchSlot.elem, 0, Math.max(vg.chk(hi - lo), 1));
    for (let i_2: number = lo; (i_2 < hi); i_2 = vg.chk(i_2 + 1)) {
        if (((b.errCode !== ErrNone) || b.tooBig)) {
            return none;
        }
        let s: instr = new instr();
        s.op = iSplit;
        const split: number = addInstr(b, s);
        const sub: frag = emit(b, nodes, child, mask_v, extra_v);
        if (((b.errCode !== ErrNone) || b.tooBig)) {
            return none;
        }
        (_t4 = b.prog.ins, _t4.buf[_t4.off + vg.ix(split, _t4.len)]).next = sub.start;
        let slot_2: patchSlot = new patchSlot();
        slot_2.idx = split;
        slot_2.alt = true;
        skips = vg.append(patchSlot.elem, skips, slot_2.clone());
        let piece: frag = new frag();
        piece.start = split;
        piece.out = sub.out;
        haveResult = fragAppend(b, result, haveResult, piece);
    }
    result.out = vg.appendSlice(patchSlot.elem, result.out, skips);
    if ((!haveResult)) {
        return epsilonFrag(b);
    }
    return result;
}

export function emitStar(b: progBuilder, nodes: vg.Slice<node>, child: number, mask: bigint, extra: vg.Slice<number>): frag {
    let _t1!: vg.Slice<instr>;
    const none: frag = new frag();
    let s: instr = new instr();
    s.op = iSplit;
    const split: number = addInstr(b, s);
    const body: frag = emit(b, nodes, child, mask, extra);
    if (((b.errCode !== ErrNone) || b.tooBig)) {
        return none;
    }
    (_t1 = b.prog.ins, _t1.buf[_t1.off + vg.ix(split, _t1.len)]).next = body.start;
    patch(b, body.out, split);
    let f: frag = new frag();
    f.start = split;
    f.out = singleOut(split, true);
    return f;
}

export function emitPlus(b: progBuilder, nodes: vg.Slice<node>, child: number, mask: bigint, extra: vg.Slice<number>): frag {
    let _t1!: vg.Slice<instr>;
    const none: frag = new frag();
    const body: frag = emit(b, nodes, child, mask, extra);
    if (((b.errCode !== ErrNone) || b.tooBig)) {
        return none;
    }
    let s: instr = new instr();
    s.op = iSplit;
    const split: number = addInstr(b, s);
    (_t1 = b.prog.ins, _t1.buf[_t1.off + vg.ix(split, _t1.len)]).next = body.start;
    patch(b, body.out, split);
    let f: frag = new frag();
    f.start = body.start;
    f.out = singleOut(split, true);
    return f;
}

export function compileScan(re: Regexp, ni: number): void {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t4!: vg.Slice<node>;
    let _t5!: vg.Slice<node>;
    let _t7!: vg.Slice<number>;
    let _t8!: vg.Slice<node>;
    switch ((_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).op) {
        case opRepeat:
        {
            if ((_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).minimal) {
                (_t3 = re.nodes, _t3.buf[_t3.off + vg.ix(ni, _t3.len)]).index = re.minSlots;
                re.minSlots = vg.chk(re.minSlots + 1);
            }
            break;
        }
        case opBracket:
        {
            if (bracketHasMultiMembers(re.brackets, (_t4 = re.nodes, _t4.buf[_t4.off + vg.ix(ni, _t4.len)]).br)) {
                re.multi = true;
            }
            break;
        }
        case opBOL:
        case opEOL:
        {
            re.anchors = true;
            break;
        }
    }
    for (let i: number = 0; (i < (_t5 = re.nodes, _t5.buf[_t5.off + vg.ix(ni, _t5.len)]).ch.len); i = vg.chk(i + 1)) {
        compileScan(re, (_t7 = (_t8 = re.nodes, _t8.buf[_t8.off + vg.ix(ni, _t8.len)]).ch, _t7.buf[_t7.off + vg.ix(i, _t7.len)]));
    }
}

export function collectNested(re: Regexp, ni: number, stack: groupStack): void {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t4!: vg.Slice<number>;
    let _t5!: vg.Slice<vg.Slice<number>>;
    let _t6!: vg.Slice<vg.Slice<number>>;
    let _t7!: number;
    let _t8!: vg.Slice<node>;
    let _t10!: vg.Slice<number>;
    let _t11!: vg.Slice<node>;
    const isGroup: boolean = ((_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(ni, _t1.len)]).op === opGroup);
    if (isGroup) {
        const gi: number = ((_t2 = re.nodes, _t2.buf[_t2.off + vg.ix(ni, _t2.len)]).index | 0);
        for (let k: number = 0; (k < stack.g.len); k = vg.chk(k + 1)) {
            const outer: number = (_t4 = stack.g, _t4.buf[_t4.off + vg.ix(k, _t4.len)]);
            (_t6 = re.nested, _t7 = vg.ix(outer, _t6.len), _t6.buf[_t6.off + _t7] = vg.append(vg.I32, (_t5 = re.nested, _t5.buf[_t5.off + vg.ix(outer, _t5.len)]), gi));
        }
        stack.g = vg.append(vg.I32, stack.g, gi);
    }
    for (let i: number = 0; (i < (_t8 = re.nodes, _t8.buf[_t8.off + vg.ix(ni, _t8.len)]).ch.len); i = vg.chk(i + 1)) {
        collectNested(re, (_t10 = (_t11 = re.nodes, _t11.buf[_t11.off + vg.ix(ni, _t11.len)]).ch, _t10.buf[_t10.off + vg.ix(i, _t10.len)]), stack);
    }
    if (isGroup) {
        stack.g = vg.head(stack.g, vg.chk(stack.g.len - 1));
    }
}

export function minMatchChars(nodes: vg.Slice<node>, brs: vg.Slice<bracketSet>, loc: Locale, ni: number): number {
    let _t1!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<number>;
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opChar:
        case opAny:
        {
            return 1;
            break;
        }
        case opBracket:
        {
            return bracketMinChars(brs, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].br, loc);
            break;
        }
        case opBOL:
        case opEOL:
        {
            return 0;
            break;
        }
        case opGroup:
        {
            return minMatchChars(nodes, brs, loc, (_t1 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t1.buf[_t1.off + vg.ix(0, _t1.len)]));
            break;
        }
        case opConcat:
        {
            let total: number = 0;
            for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
                total = vg.chk(total + minMatchChars(nodes, brs, loc, (_t3 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t3.buf[_t3.off + vg.ix(i, _t3.len)])));
                if ((total > lengthCap)) {
                    return lengthCap;
                }
            }
            return total;
            break;
        }
        case opAlt:
        {
            let best: number = lengthCap;
            for (let i_2: number = 0; (i_2 < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i_2 = vg.chk(i_2 + 1)) {
                best = Math.min(best, minMatchChars(nodes, brs, loc, (_t5 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t5.buf[_t5.off + vg.ix(i_2, _t5.len)])));
            }
            return best;
            break;
        }
        case opRepeat:
        {
            if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min === 0)) {
                return 0;
            }
            const product: number = vg.chk(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min * minMatchChars(nodes, brs, loc, (_t6 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t6.buf[_t6.off + vg.ix(0, _t6.len)])));
            return Math.min(product, lengthCap);
            break;
        }
    }
    return 0;
}

export function Compile(pattern: vg.Str, loc: Locale, flags: number): [Regexp, Error] {
    let loc_v: Locale = loc.clone();
    let re: Regexp = new Regexp();
    if ((!LocaleValid(loc_v))) {
        return [re, compileError(ErrBadPat, (0 - 1))];
    }
    let p: parser = new parser();
    const root: number = parse(p, loc_v, pattern, flags);
    if ((root < 0)) {
        return [re, p.err.clone()];
    }
    re.nsub = p.groups;
    re.nodes = p.nodes;
    re.brackets = p.brackets;
    re.root = root;
    re.flags = flags;
    re.loc = loc_v.clone();
    re.nested = vg.make(vg.SLICE, vg.chk(re.nsub + 1));
    compileScan(re, root);
    let stack: groupStack = new groupStack();
    collectNested(re, root, stack);
    computeLengths(re.nodes, re.loc, re.brackets, root);
    re.onePass = onePassAnalyze(re.nodes, re.brackets, root);
    let b: progBuilder = new progBuilder();
    b.icase = (((flags & FlagICase) >>> 0) !== 0);
    compileProgram(b, re.nodes, root, re.multi, (((flags & FlagNewline) >>> 0) !== 0));
    if ((b.errCode !== ErrNone)) {
        return [re, compileError(b.errCode, (0 - 1))];
    }
    if (b.tooBig) {
        re.minLen = minMatchChars(re.nodes, re.brackets, re.loc, root);
    } else {
        re.progOK = true;
        re.prog = b.prog.clone();
    }
    return [re, noError()];
}

export function NumSub(re: Regexp): number {
    return re.nsub;
}

export function trivialNullMatch(re: Regexp, pmatch: vg.Slice<Match>): boolean {
    let _t1!: vg.Slice<node>;
    return ((((_t1 = re.nodes, _t1.buf[_t1.off + vg.ix(re.root, _t1.len)]).minL === 0) && (!re.anchors)) && ((((re.flags & FlagNoSub) >>> 0) !== 0) || (pmatch.len === 0)));
}

export function Exec(re: Regexp, subject: vg.Str, pmatch: vg.Slice<Match>, eflags: number): [boolean, Error] {
    if ((subject.length > subjectLimit)) {
        return [false, compileError(ErrESpace, (0 - 1))];
    }
    if ((!re.progOK)) {
        if ((subject.length < re.minLen)) {
            return [false, noError()];
        }
        if ((runeCount(subject) < re.minLen)) {
            return [false, noError()];
        }
        if (trivialNullMatch(re, pmatch)) {
            return [true, noError()];
        }
        return [false, compileError(ErrESpace, (0 - 1))];
    }
    if ((((re.prog.failMin !== failMinNone) && (subject.length >= re.prog.failMin)) && (runeCount(subject) >= re.prog.failMin))) {
        if (trivialNullMatch(re, pmatch)) {
            return [true, noError()];
        }
        if (((((re.flags & FlagNoSub) >>> 0) !== 0) || (pmatch.len === 0))) {
            const probe: engineResult = runPhaseA(re, subject, eflags);
            if (probe.matched) {
                return [true, noError()];
            }
        }
        return [false, compileError(ErrESpace, (0 - 1))];
    }
    const result: engineResult = runPhaseA(re, subject, eflags);
    if ((!result.matched)) {
        return [false, noError()];
    }
    if (((((re.flags & FlagNoSub) >>> 0) !== 0) || (pmatch.len === 0))) {
        return [true, noError()];
    }
    setMatch(pmatch, 0, result.so, result.eo);
    for (let idx: number = 1; (idx < pmatch.len); idx = vg.chk(idx + 1)) {
        setMatch(pmatch, idx, (0 - 1), (0 - 1));
    }
    if (((pmatch.len === 1) || (re.nsub === 0))) {
        return [true, noError()];
    }
    let d: decoded = decodeWindow(subject, result.so, result.eo);
    const caps: vg.Slice<Match> = vg.make(Match.elem, vg.chk(re.nsub + 1));
    const serr: Error = solveCaptures(re, d, 0, d.runes.len, eflags, caps);
    if ((serr.Code !== ErrNone)) {
        return [false, serr];
    }
    fillMatches(re, d, caps, pmatch);
    return [true, noError()];
}

export function MatchIterInit(re: Regexp, limit: number): [MatchIter, Error] {
    let it: MatchIter = new MatchIter();
    it.lastEnd = (0 - 1);
    it.limit = limit;
    if ((((re.flags & FlagNoSub) >>> 0) !== 0)) {
        it.done = true;
        return [it, compileError(ErrENoSub, (0 - 1))];
    }
    if ((limit === 0)) {
        it.done = true;
    }
    return [it, noError()];
}

export function MatchIterNext(re: Regexp, it: MatchIter, subject: vg.Str, eflags: number, pmatch: vg.Slice<Match>): [boolean, Error] {
    if (it.done) {
        return [false, noError()];
    }
    if ((pmatch.len < vg.chk(re.nsub + 1))) {
        it.done = true;
        return [false, compileError(ErrESpace, (0 - 1))];
    }
    while ((it.pos <= subject.length)) {
        const flags: number = continuationFlags(re, subject, it.pos, eflags);
        const _t2 = Exec(re, vg.strTail(subject, it.pos), pmatch, flags);
        const matched: boolean = _t2[0];
        const err: Error = _t2[1];
        if ((err.Code !== ErrNone)) {
            it.done = true;
            return [false, err];
        }
        if ((!matched)) {
            it.done = true;
            return [false, noError()];
        }
        for (let idx: number = 0; (idx < pmatch.len); idx = vg.chk(idx + 1)) {
            if ((pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].So >= 0)) {
                pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].So = vg.chk(pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].So + it.pos);
                pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].Eo = vg.chk(pmatch.buf[pmatch.off + vg.ix(idx, pmatch.len)].Eo + it.pos);
            }
        }
        const so: number = pmatch.buf[pmatch.off + vg.ix(0, pmatch.len)].So;
        const eo: number = pmatch.buf[pmatch.off + vg.ix(0, pmatch.len)].Eo;
        const report: boolean = ((so !== eo) || (so !== it.lastEnd));
        if ((eo > so)) {
            it.pos = eo;
        } else {
            if ((so >= subject.length)) {
                it.done = true;
                if ((!report)) {
                    return [false, noError()];
                }
            } else {
                const _t4 = decodeRuneAt(subject, so);
                const size: number = _t4[1];
                it.pos = vg.chk(so + size);
            }
        }
        if (report) {
            it.lastEnd = eo;
            it.limit = vg.chk(it.limit - 1);
            if ((it.limit === 0)) {
                it.done = true;
            }
            return [true, noError()];
        }
    }
    it.done = true;
    return [false, noError()];
}

export function parseReplacement(replacement: vg.Str, nsub: number): [vg.Slice<replPart>, Error] {
    let parts: vg.Slice<replPart> = vg.make(replPart.elem, 0, 4);
    let start: number = 0;
    for (let idx: number = 0; (idx < replacement.length); idx = vg.chk(idx + 1)) {
        const c: number = replacement[vg.ix(idx, replacement.length)];
        if (((c !== 38) && (c !== 92))) {
            continue;
        }
        if ((start < idx)) {
            let lit: replPart = new replPart();
            lit.lit = vg.strSub(replacement, start, idx);
            parts = vg.append(replPart.elem, parts, lit.clone());
        }
        if ((c === 38)) {
            const whole: replPart = new replPart();
            parts = vg.append(replPart.elem, parts, whole.clone());
            start = vg.chk(idx + 1);
            continue;
        }
        if ((vg.chk(idx + 1) === replacement.length)) {
            return [vg.NIL, compileError(ErrEEscape, idx)];
        }
        const next: number = replacement[vg.ix(vg.chk(idx + 1), replacement.length)];
        if (((next >= 48) && (next <= 57))) {
            const group: number = ((next - 48) & 0xff);
            if (((group === 0) || (group > nsub))) {
                return [vg.NIL, compileError(ErrESubReg, idx)];
            }
            let ref: replPart = new replPart();
            ref.group = group;
            parts = vg.append(replPart.elem, parts, ref.clone());
        } else {
            let esc: replPart = new replPart();
            esc.lit = vg.strSub(replacement, vg.chk(idx + 1), vg.chk(idx + 2));
            parts = vg.append(replPart.elem, parts, esc.clone());
        }
        idx = vg.chk(idx + 1);
        start = vg.chk(idx + 1);
    }
    if ((start < replacement.length)) {
        let tail: replPart = new replPart();
        tail.lit = vg.strTail(replacement, start);
        parts = vg.append(replPart.elem, parts, tail.clone());
    }
    return [parts, noError()];
}

export function ReplaceAll(re: Regexp, subject: vg.Str, replacement: vg.Str, limit: number, eflags: number): [vg.Str, Error] {
    if ((((re.flags & FlagNoSub) >>> 0) !== 0)) {
        return [vg.EMPTY, compileError(ErrENoSub, (0 - 1))];
    }
    const _t1 = parseReplacement(replacement, re.nsub);
    const parts: vg.Slice<replPart> = _t1[0];
    const perr: Error = _t1[1];
    if ((perr.Code !== ErrNone)) {
        return [vg.EMPTY, perr];
    }
    const _t2 = MatchIterInit(re, limit);
    let it: MatchIter = _t2[0];
    const ierr: Error = _t2[1];
    if ((ierr.Code !== ErrNone)) {
        return [vg.EMPTY, ierr];
    }
    const pmatch: vg.Slice<Match> = vg.make(Match.elem, vg.chk(re.nsub + 1));
    let out: vg.Slice<number> = vg.NIL;
    let last: number = 0;
    let any_: boolean = false;
    while (true) {
        const _t4 = MatchIterNext(re, it, subject, eflags, pmatch);
        const ok: boolean = _t4[0];
        const err: Error = _t4[1];
        if ((err.Code !== ErrNone)) {
            return [vg.EMPTY, err];
        }
        if ((!ok)) {
            break;
        }
        if ((!any_)) {
            out = vg.make(vg.U8, 0, vg.chk(subject.length + vg.div(subject.length, 8)));
            any_ = true;
        }
        out = vg.appendStr(out, vg.strSub(subject, last, pmatch.buf[pmatch.off + vg.ix(0, pmatch.len)].So));
        for (let i: number = 0; (i < parts.len); i = vg.chk(i + 1)) {
            if ((parts.buf[parts.off + vg.ix(i, parts.len)].lit.length !== 0)) {
                out = vg.appendStr(out, parts.buf[parts.off + vg.ix(i, parts.len)].lit);
                continue;
            }
            const ref: Match = pmatch.buf[pmatch.off + vg.ix(parts.buf[parts.off + vg.ix(i, parts.len)].group, pmatch.len)].clone();
            if ((ref.So >= 0)) {
                out = vg.appendStr(out, vg.strSub(subject, ref.So, ref.Eo));
            }
        }
        last = pmatch.buf[pmatch.off + vg.ix(0, pmatch.len)].Eo;
    }
    if ((!any_)) {
        return [subject, noError()];
    }
    out = vg.appendStr(out, vg.strTail(subject, last));
    return [vg.strFromBytes(out), noError()];
}

export function fail(p: parser, code: number, pos: number): number {
    if ((p.err.Code === ErrNone)) {
        p.err = compileError(code, pos);
    }
    return ((-1) | 0);
}

export function addNode(p: parser, op: number): number {
    let n: node = new node();
    n.op = op;
    p.nodes = vg.append(node.elem, p.nodes, n.clone());
    return (vg.chk(p.nodes.len - 1) | 0);
}

export function eof(p: parser): boolean {
    return (p.pos >= p.src.length);
}

export function peekByte(p: parser): number {
    let _t1!: vg.Str;
    if (eof(p)) {
        return 0;
    }
    return (_t1 = p.src, _t1[vg.ix(p.pos, _t1.length)]);
}

export function peekByteAt(p: parser, ahead: number): number {
    let _t1!: vg.Str;
    if ((vg.chk(p.pos + ahead) >= p.src.length)) {
        return 0;
    }
    return (_t1 = p.src, _t1[vg.ix(vg.chk(p.pos + ahead), _t1.length)]);
}

export function nextRune(p: parser): number {
    const _t1 = decodeRuneAt(p.src, p.pos);
    const r: number = _t1[0];
    const size: number = _t1[1];
    if ((r < 0)) {
        fail(p, ErrBadPat, p.pos);
        return invalidRune;
    }
    p.pos = vg.chk(p.pos + size);
    return r;
}

export function parse(p: parser, loc: Locale, pattern: vg.Str, flags: number): number {
    p.src = pattern;
    p.flags = flags;
    const root: number = parseAlt(p, loc, false);
    if ((root < 0)) {
        return ((-1) | 0);
    }
    if ((!eof(p))) {
        return fail(p, ErrBadPat, p.pos);
    }
    return root;
}

export function parseAlt(p: parser, loc: Locale, inGroup: boolean): number {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t4!: vg.Slice<node>;
    let _t5!: vg.Slice<node>;
    const first: number = parseBranch(p, loc, inGroup);
    if ((first < 0)) {
        return ((-1) | 0);
    }
    if ((peekByte(p) !== 124)) {
        return first;
    }
    const alt: number = addNode(p, opAlt);
    (_t2 = p.nodes, _t2.buf[_t2.off + vg.ix(alt, _t2.len)]).ch = vg.append(vg.I32, (_t1 = p.nodes, _t1.buf[_t1.off + vg.ix(alt, _t1.len)]).ch, first);
    while ((peekByte(p) === 124)) {
        p.pos = vg.chk(p.pos + 1);
        const branch: number = parseBranch(p, loc, inGroup);
        if ((branch < 0)) {
            return ((-1) | 0);
        }
        (_t5 = p.nodes, _t5.buf[_t5.off + vg.ix(alt, _t5.len)]).ch = vg.append(vg.I32, (_t4 = p.nodes, _t4.buf[_t4.off + vg.ix(alt, _t4.len)]).ch, branch);
    }
    return alt;
}

export function parseBranch(p: parser, loc: Locale, inGroup: boolean): number {
    let _t2!: vg.Slice<node>;
    let exprs: vg.Slice<number> = vg.make(vg.I32, 0, 4);
    while (true) {
        if (eof(p)) {
            break;
        }
        const c: number = peekByte(p);
        if ((c === 124)) {
            break;
        }
        if (((c === 41) && inGroup)) {
            break;
        }
        const expr: number = parseExpr(p, loc);
        if ((expr < 0)) {
            return ((-1) | 0);
        }
        exprs = vg.append(vg.I32, exprs, expr);
    }
    if ((exprs.len === 0)) {
        return fail(p, ErrBadPat, p.pos);
    }
    if ((exprs.len === 1)) {
        return exprs.buf[exprs.off + vg.ix(0, exprs.len)];
    }
    const cat: number = addNode(p, opConcat);
    (_t2 = p.nodes, _t2.buf[_t2.off + vg.ix(cat, _t2.len)]).ch = exprs;
    return cat;
}

export function isDupByte(c: number): boolean {
    return ((((c === 42) || (c === 43)) || (c === 63)) || (c === 123));
}

export function parseExpr(p: parser, loc: Locale): number {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    const start: number = p.pos;
    const c: number = peekByte(p);
    if (((c === 94) || (c === 36))) {
        p.pos = vg.chk(p.pos + 1);
        if (isDupByte(peekByte(p))) {
            return fail(p, ErrBadRpt, p.pos);
        }
        if ((c === 94)) {
            return addNode(p, opBOL);
        }
        return addNode(p, opEOL);
    }
    if (isDupByte(c)) {
        return fail(p, ErrBadRpt, start);
    }
    let primary: number = 0;
    switch (c) {
        case 40:
        {
            p.pos = vg.chk(p.pos + 1);
            p.groups = vg.chk(p.groups + 1);
            const index: number = p.groups;
            const sub: number = parseAlt(p, loc, true);
            if ((sub < 0)) {
                return ((-1) | 0);
            }
            if ((peekByte(p) !== 41)) {
                return fail(p, ErrEParen, start);
            }
            p.pos = vg.chk(p.pos + 1);
            primary = addNode(p, opGroup);
            (_t2 = p.nodes, _t2.buf[_t2.off + vg.ix(primary, _t2.len)]).ch = vg.append(vg.I32, (_t1 = p.nodes, _t1.buf[_t1.off + vg.ix(primary, _t1.len)]).ch, sub);
            (_t3 = p.nodes, _t3.buf[_t3.off + vg.ix(primary, _t3.len)]).index = index;
            break;
        }
        case 91:
        {
            primary = parseBracket(p, loc);
            if ((primary < 0)) {
                return ((-1) | 0);
            }
            break;
        }
        case 92:
        {
            p.pos = vg.chk(p.pos + 1);
            if (eof(p)) {
                return fail(p, ErrEEscape, start);
            }
            const r: number = nextRune(p);
            if ((r < 0)) {
                return ((-1) | 0);
            }
            if (((((((((((((((r === 94) || (r === 46)) || (r === 91)) || (r === 93)) || (r === 36)) || (r === 40)) || (r === 41)) || (r === 124)) || (r === 42)) || (r === 43)) || (r === 63)) || (r === 123)) || (r === 125)) || (r === 92))) {
                primary = charNode(p, loc, r);
            } else {
                return fail(p, ErrBadPat, start);
            }
            break;
        }
        case 46:
        {
            p.pos = vg.chk(p.pos + 1);
            primary = addNode(p, opAny);
            break;
        }
        default:
        {
            const r_2: number = nextRune(p);
            if ((r_2 < 0)) {
                return ((-1) | 0);
            }
            primary = charNode(p, loc, r_2);
            break;
        }
    }
    return parseDup(p, primary);
}

export function charNode(p: parser, loc: Locale, r: number): number {
    let _t1!: vg.Slice<node>;
    let _t2!: vg.Slice<node>;
    const n: number = addNode(p, opChar);
    (_t1 = p.nodes, _t1.buf[_t1.off + vg.ix(n, _t1.len)]).r = r;
    if ((((p.flags & FlagICase) >>> 0) !== 0)) {
        let fold: vg.Slice<number> = vg.make(vg.I32, 0, 3);
        fold = appendUnique(fold, r);
        fold = appendUnique(fold, localeToUpper(loc, r));
        fold = appendUnique(fold, localeToLower(loc, r));
        (_t2 = p.nodes, _t2.buf[_t2.off + vg.ix(n, _t2.len)]).fold = fold;
    }
    return n;
}

export function appendUnique(runes: vg.Slice<number>, r: number): vg.Slice<number> {
    for (let i: number = 0; (i < runes.len); i = vg.chk(i + 1)) {
        if ((runes.buf[runes.off + vg.ix(i, runes.len)] === r)) {
            return runes;
        }
    }
    return vg.append(vg.I32, runes, r);
}

export function parseDup(p: parser, operand: number): number {
    let _t2!: vg.Slice<node>;
    let _t3!: vg.Slice<node>;
    let _t4!: vg.Slice<node>;
    let _t5!: vg.Slice<node>;
    let _t6!: vg.Slice<node>;
    const c: number = peekByte(p);
    let lo: number = 0;
    let hi: number = 0;
    switch (c) {
        case 42:
        {
            p.pos = vg.chk(p.pos + 1);
            lo = 0;
            hi = infinite;
            break;
        }
        case 43:
        {
            p.pos = vg.chk(p.pos + 1);
            lo = 1;
            hi = infinite;
            break;
        }
        case 63:
        {
            p.pos = vg.chk(p.pos + 1);
            lo = 0;
            hi = 1;
            break;
        }
        case 123:
        {
            const _t1 = parseInterval(p);
            const iv: interval = _t1[0];
            const ok: boolean = _t1[1];
            if ((!ok)) {
                return ((-1) | 0);
            }
            lo = iv.lo;
            hi = iv.hi;
            break;
        }
        default:
        {
            return operand;
            break;
        }
    }
    let minimal: boolean = (((p.flags & FlagMinimal) >>> 0) !== 0);
    if ((peekByte(p) === 63)) {
        p.pos = vg.chk(p.pos + 1);
        minimal = (!minimal);
    }
    if (isDupByte(peekByte(p))) {
        return fail(p, ErrBadRpt, p.pos);
    }
    const rep: number = addNode(p, opRepeat);
    (_t3 = p.nodes, _t3.buf[_t3.off + vg.ix(rep, _t3.len)]).ch = vg.append(vg.I32, (_t2 = p.nodes, _t2.buf[_t2.off + vg.ix(rep, _t2.len)]).ch, operand);
    (_t4 = p.nodes, _t4.buf[_t4.off + vg.ix(rep, _t4.len)]).min = lo;
    (_t5 = p.nodes, _t5.buf[_t5.off + vg.ix(rep, _t5.len)]).max = hi;
    (_t6 = p.nodes, _t6.buf[_t6.off + vg.ix(rep, _t6.len)]).minimal = minimal;
    return rep;
}

export function parseInterval(p: parser): [interval, boolean] {
    let iv: interval = new interval();
    const start: number = p.pos;
    p.pos = vg.chk(p.pos + 1);
    const _t1 = parseCount(p);
    const lo: number = _t1[0];
    const ok: boolean = _t1[1];
    if ((!ok)) {
        intervalError(p, start);
        return [iv, false];
    }
    const c: number = peekByte(p);
    if ((c === 125)) {
        p.pos = vg.chk(p.pos + 1);
        iv.lo = lo;
        iv.hi = lo;
        return [iv, true];
    }
    if ((c !== 44)) {
        intervalError(p, start);
        return [iv, false];
    }
    p.pos = vg.chk(p.pos + 1);
    if ((peekByte(p) === 125)) {
        p.pos = vg.chk(p.pos + 1);
        iv.lo = lo;
        iv.hi = infinite;
        return [iv, true];
    }
    const _t2 = parseCount(p);
    const hi: number = _t2[0];
    const ok2: boolean = _t2[1];
    if (((!ok2) || (peekByte(p) !== 125))) {
        intervalError(p, start);
        return [iv, false];
    }
    p.pos = vg.chk(p.pos + 1);
    if ((hi < lo)) {
        fail(p, ErrBadBR, start);
        return [iv, false];
    }
    iv.lo = lo;
    iv.hi = hi;
    return [iv, true];
}

export function intervalError(p: parser, start: number): void {
    if ((indexOfByte(vg.strTail(p.src, p.pos), 125) >= 0)) {
        fail(p, ErrBadBR, start);
        return;
    }
    fail(p, ErrEBrace, start);
}

export function parseCount(p: parser): [number, boolean] {
    let _t1!: vg.Str;
    let _t2!: vg.Str;
    let _t3!: vg.Str;
    let _t4!: vg.Str;
    let _t6!: vg.Str;
    if (((eof(p) || ((_t1 = p.src, _t1[vg.ix(p.pos, _t1.length)]) < 48)) || ((_t2 = p.src, _t2[vg.ix(p.pos, _t2.length)]) > 57))) {
        return [0, false];
    }
    let value: number = 0;
    while ((((!eof(p)) && ((_t3 = p.src, _t3[vg.ix(p.pos, _t3.length)]) >= 48)) && ((_t4 = p.src, _t4[vg.ix(p.pos, _t4.length)]) <= 57))) {
        value = vg.chk(vg.chk(value * 10) + (((_t6 = p.src, _t6[vg.ix(p.pos, _t6.length)]) - 48) & 0xff));
        if ((value > dupMax)) {
            return [0, false];
        }
        p.pos = vg.chk(p.pos + 1);
    }
    return [value, true];
}

export function satAdd(a: number, b: number): number {
    return Math.min(vg.chk(a + b), lenInf);
}

export function satMul(a: number, b: number): number {
    const product: number = vg.chk(a * b);
    if ((product > lenInf)) {
        return lenInf;
    }
    return product;
}

export function computeLengths(nodes: vg.Slice<node>, loc: Locale, brackets: vg.Slice<bracketSet>, ni: number): void {
    let _t2!: vg.Slice<number>;
    let _t3!: vg.Slice<number>;
    let _t5!: vg.Slice<number>;
    let _t6!: vg.Slice<number>;
    let _t7!: vg.Slice<number>;
    let _t8!: number;
    let _t9!: vg.Slice<number>;
    let _t10!: vg.Slice<number>;
    let _t11!: number;
    let _t12!: vg.Slice<number>;
    let _t13!: vg.Slice<number>;
    let _t15!: vg.Slice<number>;
    let _t16!: vg.Slice<number>;
    for (let i: number = 0; (i < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i = vg.chk(i + 1)) {
        computeLengths(nodes, loc, brackets, (_t2 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t2.buf[_t2.off + vg.ix(i, _t2.len)]));
    }
    switch (nodes.buf[nodes.off + vg.ix(ni, nodes.len)].op) {
        case opChar:
        case opAny:
        {
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = 1;
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = 1;
            break;
        }
        case opBracket:
        {
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = bracketMinChars(brackets, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].br, loc);
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = 1;
            if (bracketHasMultiMembers(brackets, nodes.buf[nodes.off + vg.ix(ni, nodes.len)].br)) {
                nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = localeMaxElementLength(loc);
            }
            break;
        }
        case opBOL:
        case opEOL:
        {
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = 0;
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = 0;
            break;
        }
        case opGroup:
        {
            const child: number = (_t3 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t3.buf[_t3.off + vg.ix(0, _t3.len)]);
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = nodes.buf[nodes.off + vg.ix(child, nodes.len)].minL;
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = nodes.buf[nodes.off + vg.ix(child, nodes.len)].maxL;
            break;
        }
        case opConcat:
        {
            const count: number = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len;
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMin = vg.make(vg.INT, vg.chk(count + 1));
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMax = vg.make(vg.INT, vg.chk(count + 1));
            for (let k: number = vg.chk(count - 1); (k >= 0); k = vg.chk(k - 1)) {
                const child_2: number = (_t5 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t5.buf[_t5.off + vg.ix(k, _t5.len)]);
                (_t7 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMin, _t8 = vg.ix(k, _t7.len), _t7.buf[_t7.off + _t8] = satAdd((_t6 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMin, _t6.buf[_t6.off + vg.ix(vg.chk(k + 1), _t6.len)]), nodes.buf[nodes.off + vg.ix(child_2, nodes.len)].minL));
                (_t10 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMax, _t11 = vg.ix(k, _t10.len), _t10.buf[_t10.off + _t11] = satAdd((_t9 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMax, _t9.buf[_t9.off + vg.ix(vg.chk(k + 1), _t9.len)]), nodes.buf[nodes.off + vg.ix(child_2, nodes.len)].maxL));
            }
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = (_t12 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMin, _t12.buf[_t12.off + vg.ix(0, _t12.len)]);
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = (_t13 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].sufMax, _t13.buf[_t13.off + vg.ix(0, _t13.len)]);
            break;
        }
        case opAlt:
        {
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = lenInf;
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = 0;
            for (let i_2: number = 0; (i_2 < nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch.len); i_2 = vg.chk(i_2 + 1)) {
                const child_3: number = (_t15 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t15.buf[_t15.off + vg.ix(i_2, _t15.len)]);
                nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = Math.min(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL, nodes.buf[nodes.off + vg.ix(child_3, nodes.len)].minL);
                nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = Math.max(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL, nodes.buf[nodes.off + vg.ix(child_3, nodes.len)].maxL);
            }
            break;
        }
        case opRepeat:
        {
            const child_4: number = (_t16 = nodes.buf[nodes.off + vg.ix(ni, nodes.len)].ch, _t16.buf[_t16.off + vg.ix(0, _t16.len)]);
            nodes.buf[nodes.off + vg.ix(ni, nodes.len)].minL = satMul(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].min, nodes.buf[nodes.off + vg.ix(child_4, nodes.len)].minL);
            if (((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max === 0) || (nodes.buf[nodes.off + vg.ix(child_4, nodes.len)].maxL === 0))) {
                nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = 0;
            } else {
                if ((nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max === infinite)) {
                    nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = lenInf;
                } else {
                    nodes.buf[nodes.off + vg.ix(ni, nodes.len)].maxL = satMul(nodes.buf[nodes.off + vg.ix(ni, nodes.len)].max, nodes.buf[nodes.off + vg.ix(child_4, nodes.len)].maxL);
                }
            }
            break;
        }
    }
}

export function utf8Cont(c: number): boolean {
    return ((c >= 128) && (c <= 191));
}

export function decodeRuneAt(s: vg.Str, at: number): [number, number] {
    const c0: number = s[vg.ix(at, s.length)];
    if ((c0 < 128)) {
        return [c0, 1];
    }
    if ((c0 < 194)) {
        return [invalidRune, 1];
    }
    if ((c0 < 224)) {
        if (((vg.chk(at + 1) >= s.length) || (!utf8Cont(s[vg.ix(vg.chk(at + 1), s.length)])))) {
            return [invalidRune, 1];
        }
        return [(((c0 & 31) << 6) | (s[vg.ix(vg.chk(at + 1), s.length)] & 63)), 2];
    }
    if ((c0 < 240)) {
        if (((vg.chk(at + 1) >= s.length) || (!utf8Cont(s[vg.ix(vg.chk(at + 1), s.length)])))) {
            return [invalidRune, 1];
        }
        if (((c0 === 224) && (s[vg.ix(vg.chk(at + 1), s.length)] < 160))) {
            return [invalidRune, 1];
        }
        if (((c0 === 237) && (s[vg.ix(vg.chk(at + 1), s.length)] > 159))) {
            return [invalidRune, 1];
        }
        if (((vg.chk(at + 2) >= s.length) || (!utf8Cont(s[vg.ix(vg.chk(at + 2), s.length)])))) {
            return [invalidRune, 1];
        }
        return [((((c0 & 15) << 12) | ((s[vg.ix(vg.chk(at + 1), s.length)] & 63) << 6)) | (s[vg.ix(vg.chk(at + 2), s.length)] & 63)), 3];
    }
    if ((c0 < 245)) {
        if (((vg.chk(at + 1) >= s.length) || (!utf8Cont(s[vg.ix(vg.chk(at + 1), s.length)])))) {
            return [invalidRune, 1];
        }
        if (((c0 === 240) && (s[vg.ix(vg.chk(at + 1), s.length)] < 144))) {
            return [invalidRune, 1];
        }
        if (((c0 === 244) && (s[vg.ix(vg.chk(at + 1), s.length)] > 143))) {
            return [invalidRune, 1];
        }
        if (((vg.chk(at + 2) >= s.length) || (!utf8Cont(s[vg.ix(vg.chk(at + 2), s.length)])))) {
            return [invalidRune, 1];
        }
        if (((vg.chk(at + 3) >= s.length) || (!utf8Cont(s[vg.ix(vg.chk(at + 3), s.length)])))) {
            return [invalidRune, 1];
        }
        return [(((((c0 & 7) << 18) | ((s[vg.ix(vg.chk(at + 1), s.length)] & 63) << 12)) | ((s[vg.ix(vg.chk(at + 2), s.length)] & 63) << 6)) | (s[vg.ix(vg.chk(at + 3), s.length)] & 63)), 4];
    }
    return [invalidRune, 1];
}

export function runeCount(s: vg.Str): number {
    let count: number = 0;
    let at: number = 0;
    while ((at < s.length)) {
        const _t2 = decodeRuneAt(s, at);
        const size: number = _t2[1];
        at = vg.chk(at + size);
        count = vg.chk(count + 1);
    }
    return count;
}

export function utf8LeadByte(r: number): number {
    if ((r < 128)) {
        return (r & 0xff);
    }
    if ((r < 2048)) {
        return ((192 | (r >> 6)) & 0xff);
    }
    if ((r < 65536)) {
        return ((224 | (r >> 12)) & 0xff);
    }
    return ((240 | (r >> 18)) & 0xff);
}

