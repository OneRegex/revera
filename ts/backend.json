{
  "name": "ts",
  "generated": ["src/engine.ts", "src/probe_engine.ts"],
  "toolchain": ["node", "--version"],
  "release": {
    "build": [["make", "all"]],
    "driver": "driver",
    "probe": "probe",
    "bench": "bench",
    "fuzzcase": "fuzzcase"
  },
  "checked": [
    {
      "name": "typecheck",
      "requires": ["node", "-e", "require.resolve('typescript/package.json'); require.resolve('@types/node/package.json')"],
      "build": [["make", "check"]],
      "driver": "driver",
      "probe": "probe",
      "fuzzcase": "fuzzcase"
    }
  ]
}
